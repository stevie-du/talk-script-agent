[pytest]
# 测试目录
testpaths = tests
python_files = test_*.py
# 默认只收 test_* —— 三个测试文件同时保留 main() 以便不装 pytest 也能直接跑
python_functions = test_*
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
