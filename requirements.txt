# 运行依赖
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
pydantic>=2.7,<3.0
pyyaml>=6.0,<7.0
# 文件上传（/api/packs/import 收技能包 zip）—— FastAPI 的 UploadFile 没有它直接 500
python-multipart>=0.0.9,<1.0

# 开发/测试依赖（pip install -r requirements.txt pytest）
# 测试：.venv\Scripts\python -m pytest
# 界面回归：node _verify/verify.js
pytest>=8.0
