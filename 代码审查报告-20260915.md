# TalkScript 代码结构审查 · 2026-09-15

> ## 修复状态（就地更新）
>
> | # | 级别 | 问题 | 状态 |
> |---|---|---|---|
> | 1 | P0-2 | `temperature: 0.0` 被静默改写成 0.7 | ✅ 已修（`config.py` 加 `_num()`；断言 `test_temperature_zero_roundtrip`；变异检验通过） |
> | 2 | P0-1 | 历史索引生成期间 100% 失效 | ✅ 已修（`store.py` 判据改为「含 result.json/job.json 的目录」；重建 5→0 次，115.2→11.7 ms；断言 `test_index_cache_survives_inflight_job`；变异检验通过） |
> | 3 | P0-3 | 分步作业卡「待确认」永久占满并发额度 | ✅ 已修（`jobs.py` 加 `BUSY_STATES` + `PAUSED_KEEP` 上界；断言 `test_paused_jobs_do_not_block_new_generations`；变异检验复现 409） |
> | 4 | P1-1 | `rate_by_style: 0` → `ZeroDivisionError` | ✅ 已修（`knowledge.py` 出口夹下限 + `DEFAULT_RATE` 收敛为单一来源；断言 `tests/test_rate_lower_bound.py` 13 条；变异检验报红 7 条） |
> | 5 | P1-2 | `config.yaml` 读坏时静默退回全默认 | ✅ 已修（抽出 `read_config_file()` 两处一致 + WARNING + `config_error` 透出 `/api/config` + 设置页警示；后端断言 48 条、UI 3 条；6 组变异全部报红） |
> | 6 | P1-3 | 打包产物不含 Python 运行时 | ✅ 已修（**内嵌 Python 3.13.12 运行时**：`desktop/scripts/build-python-runtime.mjs` 构建时下载 embeddable zip + 校验 sha256 + 装 5 个依赖 + 导入自检，产物 38.7 MB；`resolveEngine` 拆到 `engine-path.js` 并把出厂路径排在 `engine.exe` 之前；`beforePack` 钩子保证打包前必建。**已真跑 `npm run dist` 并用打出来的产物端到端验过**：安装包 96 MB、app 启动命中出厂运行时、`/api/health` 回 200。断言：pytest 10 条 + `node --test` 11 条 + 端到端冒烟 12 条；变异 M1~M8，其中 M8 由产物级断言兜住） |
> | 7 | P1-4 | 并发额度 check-then-act | ✅ 已修（`JobRegistry.add_if_room()` 检查与插入同锁；顺序改为先验包后占额度；断言 8 条；变异 F/G 均报红） |
> | 8 | P2-1 | 前后端 temperature 区间不一致 | ✅ 已修（后端提 `NUMERIC_BOUNDS` 常量、前端四项收成同一张表并放开到 0~2；断言 10 条 + UI 2 条；变异 H/I/J 报红） |
> | 9 | P2-2 | `bindSettings`/`bindOverlays` 缺幂等守卫 | ✅ 已修（三种写法（`fn._bound` / `el._bound` / 裸的）统一到 `util.bindOnce`，五个绑定函数全包上；`__ts.rebind()` 作为验证入口，UI 断言「再绑一遍监听器一个不多」；变异 M1/M2/M4 报红，M3 说明为何抓不到 `bindOverlays`） |
> | 10 | P2-3 | `agent-skills/` 与 `packs/elevator` 已漂移 | ✅ 已修（逐文件比对确认 21 个共同文件**逐字节相同**、3 个独有文件是导出器模板产物，删除安全；已删本地副本，导出功能实测正常） |
> | 11 | P2-4 | 磁盘卫生 / `.gitignore` 漏 `_verify/_*.py` | ✅ 已修（`.gitignore` 从「按扩展名逐条列举」改成 `_verify/_*` 一把覆盖；清掉 36 个临时截图/日志 + 11M 临时 Chrome profile；**14 个 `dist*` 删不掉是环境对 `.asar` 的保护性拦截**，已查清并说明，见该节） |
> | 12 | P2-5 | 渲染层缺少 CSP | ✅ 已修（引擎中间件下发 CSP，**不带 `'unsafe-inline'`**——5 处内联 `style=` 已改成 class；实测 5 种注入向量全被拦、CSSOM 不被拦；断言 6 条 + UI 5 条；变异 M1/M2/M3/M4 报红 1/1+2/1/3） |
> | 13 | P2-6 | 工作区两处安全修复未提交 | ⬜ 待修 |
> | 14 | P1-5 | `banwords.yaml` 读坏 → **合规校验静默全过** | ✅ 已修（与 P1-6 同根因一起修；`PackBrokenError` → 409；断言 23 条；变异 M1/M2/M3 报红 14/3/1） |
> | 15 | P1-6 | `pack.yaml` 读坏 → 参数条空掉、包名退成 slug、**`param_audit` 一起哑掉** | ✅ 已修（`PackInfo.pack_error` 下发 + 界面「（损坏）」标记 + warn 色说明；UI 断言 5 条；变异 M4/U1/U2 报红 6/1/1） |
> | 16 | P2-7 | `list_packs` 吞 `Exception` 且不记日志（包静默消失） | ✅ 已修（`pack_info` 改为全函数 + `list_packs` 收窄为 `OSError` 且记日志；实测「YAML 语法对但结构不对」的包原先会**整包消失** + `Pack()` 冒 500，现为 409；断言 14 条；变异 M7/M8/M9 报红 12/1/5） |
> | 17 | P2-8 | `quota_degraded` **只写不读**（降级信号没人消费） | ✅ 已修（**实测比报告写的严重**：不是「只写不读」而是「算了但没写出去」——`_finalize` 的白名单把它滤掉了，`result.json` 里一个字都没有；现补契约字段 + 落盘 + 界面横幅 + 脚本.md 提示，并新增 `check_contract_complete` 双向守卫；断言 18 条 + UI 5 条；变异 M1~M7 报红 5/7/3/13/2/1/1） |
>
> 状态取值：⬜ 待修 · 🟡 进行中 · ✅ 已修（附验证证据） · ⏸️ 暂缓（附理由）

> 范围：代码结构 / 模块划分 / 依赖关系，以及由此暴露的性能、安全、规范一致性问题。
> 优先级 P0（必修）> P1（应修）> P2（可修）。
>
> **证据分级**：标「✅ 实测」的条目在本机跑出了可复现输出；标「📖 读码」的是静态推断，
> 未构造运行时复现。上一份《深度审查-20260914》因为「凭代码推断下结论」误判了 4 项，
> 所以这里把两者分开标，读的时候请区别对待。

---

## 摘要

架构本身是健康的：**引擎（`app/`，Python + FastAPI）与外壳（`desktop/`，Electron）严格分离，
行业知识全部外置到 `packs/`，引擎不认识任何具体行业**。这层抽象守得很干净 ——
`knowledge.py` 只认 `pack.yaml` 的 schema，`prompts.py` 只认 `skill.yaml` 的占位符，
新增行业不需要动一行引擎代码。模块拆分也有明确理由（`jobs` 拆出状态机、`store` 拆出落盘、
`security` 拆出访问控制），每个文件顶部都写清了「为什么拆」。

本次审查发现 **5 项已实测复现的缺陷**（3 项 P0、2 项 P1）与 **6 项一致性问题**。
其中两项 P0 值得特别注意，因为它们**与本项目自己确立的原则直接冲突**：

1. **历史索引缓存实际处于「生成期间 100% 失效」状态** —— 每 3 秒全量重建一次，
   400 条记录时单次 115 ms。索引这个优化目前是负收益。
2. **`temperature: 0.0` 存进去读回来是 0.7** —— 恰好是本轮未提交改动刚放开的那个边界值，
   属于项目反复整治的「静默降级」同一类。

另外必须点名：**工作区里有两处未提交的真实安全修复**（`get_pack` 漏用 `_safe_name`、
`temperature` 越界），内容是对的，但悬在工作区意味着切分支/`git checkout` 就会丢。

---

## 方法与覆盖

| 做了什么 | 结果 |
|---|---|
| 通读 `app/` 全部 16 个模块 | 已完成 |
| 通读 `desktop/renderer/js/` 全部 12 个模块 + `main.js` + `preload.js` | 已完成 |
| `pytest` 全量 | ✅ **65 passed**（基线健康） |
| 针对性运行时探针（已跑后删除） | 复现 P0-1 / P0-2 / P0-3 / P1-1 / P1-2 |
| 前端 `innerHTML` 注入点全量排查 | 12 个模块，**未发现未转义的动态值** |

**未覆盖**：`_verify/verify.js`（界面回归，未跑，避免与可能在跑的桌面应用抢端口）；
打包链路（未实际执行 `npm run dist`，P1-3 为读码结论）；`desktop/dist*/` 内历史产物内容。

---

## P0 —— 必修

### P0-1 历史索引在「有作业在跑」时 100% 重建，每 3 秒一次全量解析 ✅ 实测

**位置**：`app/store.py:194-219`（`_read_index_file` / `_count_on_disk`）

**根因**：缓存失效判据写成了「索引条数 == 磁盘上的作业**目录**数」，但这两者的口径不一致：

- `job_dir()` 在 `start_generate` 时就 `mkdir(parents=True)` —— 目录**一开工就存在**；
- 索引只收录**终态**作业 —— `write_result`（done）与 `write_job`（仅 failed/cancelled）才 `_upsert`。

于是只要有一个作业在跑，`len(index) != _count_on_disk()` **恒成立**，每次 `history()` 都走
`_rebuild()`：全量 `glob` + 解析所有 `result.json`/`job.json` + **重写** `index.json`。

**实测**（探针已删除，复现方式见下）：

```
稳定态第 1 次 history() -> 重建次数 = 0     ← 缓存本来是对的
稳定态第 2 次 history() -> 重建次数 = 0
有 1 个作业在跑时，5 次 history() -> 重建 5 次，平均  16.4 ms/次   ← 100% 失效
400 条记录 + 1 个在跑时          -> 重建 5 次，平均 115.2 ms/次
```

**触发路径**：`GET /api/history` ← `sessions.js:loadSessions()`；而 `loadSessions` 在
「存在未结束会话」时会每 3 秒自轮询（`sessions.js:62-64`）。也就是说，**整个生成期间
每 3 秒一次 O(N) 解析 + 一次磁盘写**，随历史条数线性增长。

**影响**：生成期间引擎持续做无谓的重 IO。FastAPI 同步端点跑在线程池里，这些工作都在
GIL 下与生成线程抢锁；`_rebuild` 还会拿 `self._lock`，与 `write_result` 的落盘锁竞争。
索引这个优化目前是**负收益**。

**复现**：构造 40 条 `write_result` 后 monkeypatch `ArtifactStore._rebuild` 计数，
再 `job_dir()` 建一个空作业目录，循环调 `history()` 即可看到重建次数 == 调用次数。

**建议**：把计数口径对齐到「**已落盘的记录**」而不是「目录」——

```python
def _count_on_disk(self) -> int:
    if not self.gen.exists():
        return 0
    return sum(1 for p in self.gen.glob("*/*")
               if (p / "result.json").exists() or (p / "job.json").exists())
```

这样「在跑的作业」不再污染判据，每完成一个作业最多触发一次重建（`write_result` 写盘
与 `_upsert` 之间的瞬时窗口），而不是每 3 秒一次。若仍嫌抖动，可再叠一个「最短重建间隔」。

> ### ✅ 已修（2026-09-15）
>
> 按上述口径改了 `_count_on_disk()` —— 判据现在与 `_rebuild()` 的收录条件**完全一致**
> （`_rebuild` 就是 glob `*/job.json` 与 `*/result.json`），所以 `count == len(index)`
> 是一条真正成立的不变量，而不只是「碰巧对上」。
>
> 实测（400 条记录 + 1 个在跑作业，5 次 `history()`）：
>
> ```
> 修复前：重建 5 次 / 5 次调用，115.2 ms/次
> 修复后：重建 0 次 / 5 次调用， 11.7 ms/次   ← 单次耗时降到约 1/10
> ```
>
> 断言：`tests/test_server_hardening.py::test_index_cache_survives_inflight_job`
> —— ① 稳定态 0 重建；② `job_dir()` 建出空目录后连调 5 次 `history()`，**必须 0 重建**；
> ③ 落一个 `job.json` 后允许重建一次（磁盘上确实多了记录）并**必须重新收敛**到 0；
> ④ 功能面不退步：删掉该记录后仍要能自愈。
>
> **变异检验**：把判据换回 `if p.is_dir()` →
> `AssertionError: 有作业在跑时空转重建了 5 次（5 次调用）` / `assert [1,1,1,1,1] == []` ❌ 报红 ✓
>
> 回归 **66 → 67 passed**。
>
> **过程教训（值得记）**：第一次改 `_count_on_disk` 时 Edit 只换掉了 docstring、
> `return` 行没落盘，而单跑测试却显示通过 —— 我据此以为修好了，直到全量跑才暴露。
> **凡是「单跑过、全量挂」或「改了却没生效」，第一件事是 `sed -n` 核对文件真实内容，
> 不要相信 Edit 的返回值。** 后续每次改动都补了一步落盘核对。

---

### P0-2 `temperature: 0.0` 被静默改写成 0.7 ✅ 实测

**位置**：`app/config.py:180`

```python
temperature=float(llm.get("temperature", 0.7) or 0.7),
```

`0.0` 是 falsy → `or 0.7` 生效。`retries` 那一行用的是 `or 0`（正确），
`timeout` / `max_tokens` 也踩同样的 `or 默认值`，只是被接口层的区间校验挡住了 —— **只有 temperature 的 0 是合法值**。

**实测**：

```
写盘内容: llm: |   api_key: k |   retries: 0 |   temperature: 0.0
temperature = 0.7   <-- 期望 0.0
retries     = 0     <-- 正确
```

**为什么这项是 P0**：它**恰好抵消了本轮未提交的修复**。
`app/server.py:469-470` 刚把 temperature 的合法区间定成 `0.0 ~ 2.0`，
而 `0.0` 正是唯一存不进去的值。用户把温度调到 0 求确定性输出 → 界面提示「已保存，下次生成即生效」
→ 实际每次生成都用 0.7。这正是本项目在 `param_audit` / `dropped_short` 上反复整治的
「静默降级」：**不报错、不可见、与用户意图相反**。

**建议**：改成显式 None 判断，只在**键缺失**时取默认：

```python
def _pick(d, key, default, cast):
    v = d.get(key)
    return default if v is None or v == "" else cast(v)

temperature=_pick(llm, "temperature", 0.7, float),
timeout=_pick(llm, "timeout", 180.0, float),
max_tokens=_pick(llm, "max_tokens", DEFAULT_CONFIG["llm"]["max_tokens"], int),
```

`retries` 现写法已正确，可一并统一到 `_pick` 以免将来又被改回去。
另外建议补一条断言：`temperature=0.0` 写盘后 `load_config` 必须读回 `0.0`
（现有 `tests/` 里没有覆盖这个边界）。

> ### ✅ 已修（2026-09-15）
>
> `app/config.py` 新增 `_num(data, key, default, cast)`，四处数值项
> （temperature / retries / timeout / max_tokens）统一走它。语义是
> **只有键缺失或为空才取默认**；值非法时退回默认并 `log.warning`
> （旧写法 `float("快")` 会把 ValueError 冒到 `create_app`，引擎直接起不来）。
> 同时给 `config.py` 补了 `logging`（P1-2 也要用）。
>
> 实测：
>
> ```
> temperature=0.0 retries=0    -> 0.0 / 0 / 5.0 / 256     ✓ 0.0 不再被吃掉
> temperature=0.0 其余缺失      -> 0.0 / 2 / 180.0 / 16000
> 全部缺失                     -> 0.7 / 2 / 180.0 / 16000  ✓ 仍会取默认
> temperature='快'             -> 0.7 + WARNING 日志        ✓ 不再崩
> ```
>
> 断言：`tests/test_server_hardening.py::test_temperature_zero_roundtrip`
> —— 分三段守：① 走接口存 0.0 既不被 `v not in ("", None)` 过滤掉、也不被区间校验拒；
> ② 直接写盘时 `load_config` 读回 0.0；③ 键真缺失时**仍要**取默认 0.7
> （防止把修复改成「永不取默认」）。
>
> **变异检验**：把 `_num(...)` 换回 `float(llm.get("temperature", 0.7) or 0.7)` →
> `AssertionError: temperature 被静默改写成了 0.7（期望 0.0）` ❌ 报红 ✓，随后已恢复。
>
> 回归 **65 → 66 passed**。顺带修正了 `test_numeric_settings_editable_and_bounded`
> 里那段已过时的注释（它原写着「load_config 里是 `or 默认值`，0 会被换掉」，
> 现在这句话不再成立；但该测试的区间校验仍要保留 —— 它还负责挡负数与超上限）。

---

### P0-3 分步模式作业卡在「待确认」会永久占满并发额度 ✅ 实测

**位置**：`app/jobs.py:234-246`（`running_count` / `prune`）、`app/pipeline.py:126`

`running_count()` 把 `paused_awaiting_confirmation` 也算作「运行中」：

```python
return sum(1 for j in self._jobs.values() if j.state not in TERMINAL_STATES)
```

而 `prune()` 只回收**终态**作业，非终态永不出栈；作业也没有任何超时。

**实测**：

```
4 个停在待确认的作业 -> running_count = 4 （上限 MAX_CONCURRENT_JOBS = 4）
prune() 之后 running_count = 4  <-- 仍占满
```

**影响**：连开 4 条「分步确认」模式生成、都不点「继续」，之后**所有** `/api/generate`
一律 409「同时进行的生成已达上限（4 个）」。用户看到的是一句与自己操作无关的错误，
唯一出路是重启应用。这不是理论风险 —— 「分步确认」就是给人慢慢编辑选题卡用的，
放着不管几分钟是常态。

**建议**（二选一，推荐前者）：

1. `running_count()` 只统计真正占用模型资源的态 ——
   `queued/selecting/writing/checking/rewriting`，把 `paused_awaiting_confirmation` 排除在配额之外。
   它不烧 token、不占线程，本来就不该算并发。
2. 给「待确认」加超时（如 30 分钟无操作 → `transition("cancelled")` + `prune()`）。

> ### ✅ 已修（2026-09-15）
>
> 取方案 1，并**补上一道它带来的缺口**：待确认作业被排除出额度后，数量就不再被
> `MAX_CONCURRENT_JOBS` 兜住了，所以另外加了 `PAUSED_KEEP = 20` 的内存上界
> （`prune()` 顺带把超出部分的待确认作业按最旧丢弃）。两个常量都写在 `jobs.py` 顶部，
> 各自带一段「为什么是这个口径」的说明。
>
> `prune()` 顺手重构成 `_trim(pred, keep)` 两行调用 —— 原来只有终态一类，
> 现在终态 / 待确认两类走同一段逻辑，避免复制粘贴。
>
> 实测（`tests/test_server_hardening.py::test_paused_jobs_do_not_block_new_generations`）：
>
> ```
> 4 条分步生成停在「待确认」→ 第 5 次 /api/generate       200 + 正常 done ✓
> 4 条真在跑（writing）    → 第 5 次 /api/generate       409「已达上限」✓（上限没被放开）
> 25 条待确认 + prune()    → 剩 PAUSED_KEEP=20 条          ✓（内存有上界）
> ```
>
> **变异检验**：把 `running_count()` 换回 `state not in TERMINAL_STATES` →
> 断言如实报出用户可见症状：
> `AssertionError: 待确认作业占满了并发额度：409 {"detail":"同时进行的生成已达上限（4 个）…"}` ❌ 报红 ✓
>
> 回归 **67 → 68 passed**。这条断言的价值在于它走的是**完整 HTTP 链路**
> （`/api/generate` → 409），而不只是单元级断言 `running_count() == 0` ——
> 后者即使实现错了也可能因为口径巧合而通过。

---

## P1 —— 应修

### P1-1 `rate_by_style` 配 0 时崩溃，而 checker 有兜底 —— 两处口径不一致 ✅ 实测

**位置**：`app/pipeline.py:342-351`（`_compute_timings` 的 `n / rate`）
对照 `app/checker.py:64-77`（`estimate_seconds` 显式处理 `r <= 0` → 退回 `DEFAULT_RATE`）

`Pack.rate_for_style()` 是 `float(rates.get(style or "", 4.5))`，值为 `0` 时**不报错**，
直接返回 `0.0`。于是同一份包配置，`checker` 兜到 4.5，`pipeline` 直接除零。

**实测**：

```
rate_for_style('快') = 0.0
归一化后 rate = 0.0  quota = {'body': 0, 'cta': 0, 'hook': 0, 'total': 0}
_compute_timings 抛异常 -> ZeroDivisionError division by zero
而 checker.estimate_seconds 有兜底 -> 0.8888888888888888
```

**影响**：包作者写了个 `0`，生成会在**烧完 token 之后**的组装阶段失败，
错误文案是「float division by zero」；更早的症状是配额全 0（模型被要求写 0 字）
→ 校验必然不通过 → 白跑 3 轮回炉。`_normalize` 里 `if not target` 的降级分支也救不了，
因为 `{total:0,...}` 是非空 dict，`quota_degraded` 保持 `False`。

**建议**：在**出口**统一夹下限，而不是在每个调用点夹（现在有两个调用点）：

```python
# knowledge.py
def rate_for_style(self, style: str | None) -> float:
    rates = self.data.get("rate_by_style", {}) or {}
    try:
        return max(float(rates.get(style or "", 4.5)), 0.5)
    except (TypeError, ValueError):
        return 4.5
```

顺带：`checker.DEFAULT_RATE` 与 `rate_for_style` 的 4.5 是两处硬编码的同一个魔数，
建议提取为共享常量，避免将来只改一处。

#### ✅ 已修（2026-09-15）

**改动 1 · 下限收在出口**（`app/knowledge.py:258-279`）

```python
rates = self.data.get("rate_by_style", {}) or {}
try:
    v = float(rates.get(style or "", DEFAULT_RATE))
except (TypeError, ValueError):
    return DEFAULT_RATE
return v if v > 0 else DEFAULT_RATE
```

比原建议多修了一处：原建议写 `max(float(...), 0.5)`，硬编码了第二个魔数 `0.5`。
这里改成退回 `DEFAULT_RATE` —— 理由是「0.5 字/秒」不是一个有意义的语速，
退回兜底比夹到一个没人定义过的下限更可解释；且与 `estimate_seconds` 的既有行为
（`r <= 0 → DEFAULT_RATE`）**逐字一致**，两处口径彻底合并。

**改动 2 · `DEFAULT_RATE` 收敛为单一来源**

- `app/checker.py:39` 的注释改为「**这是全局唯一的一份** —— `knowledge.rate_for_style`
  也 import 它」，并写明修复前分叉的现场（`estimate_seconds` 有兜底、`_compute_timings` 没有）。
- `app/knowledge.py:19` 加 `from .checker import DEFAULT_RATE`，替换掉原先写死的 `4.5`。

**新增断言** `tests/test_rate_lower_bound.py`（13 条，四个方向）：

| 方向 | 断言 | 说明 |
|---|---|---|
| 出口恒正 | `test_zero_rate_is_clamped_at_exit` | 0 → `DEFAULT_RATE` |
| 非法值全覆盖 | `test_all_illegal_rates_fall_back`（6 参数化） | `0 / 0.0 / -3.0 / "abc" / None / True` |
| 不误伤正常值 | `test_valid_rate_is_preserved` | `6.0` 原样保留 |
| 组装不炸 | `test_compute_timings_survives_zero_rate` | 且断言时间轴**单调递增**（防空壳） |
| **反空转守卫** | `test_raw_zero_still_crashes` | 直接喂 `0.0` 必须仍抛 `ZeroDivisionError` |
| 单一来源 | `test_fallback_value_is_single_source` | 「没配过」与「配了 0」必须同值，且 `== checker.DEFAULT_RATE` |
| 口径一致 | `test_estimate_seconds_agrees_with_exit` | 校验侧与组装侧同语速 |
| 整表缺失 | `test_missing_table_entirely` | `rate_by_style` 整个没写 |

`test_raw_zero_still_crashes` 是刻意加的：如果将来有人给 `_compute_timings` 内部加兜底，
这条会变红，提醒把兜底收回出口 —— 防止「修好了但口径又变成两处」。

**实测**：

```
$ pytest tests/test_rate_lower_bound.py -q
.............            [100%]        # 13 passed
```

**变异检验**：把 `return v if v > 0 else DEFAULT_RATE` 改回 `return v`，**7 条报红**，
且报错内容正是原缺陷现场：

```
AssertionError: assert 4.5 == 0.0          # test_fallback_value_is_single_source
ZeroDivisionError: division by zero         # test_estimate_seconds_agrees_with_exit
FAILED test_zero_rate_is_clamped_at_exit
FAILED test_all_illegal_rates_fall_back[0] / [0.0] / [-3.0]
FAILED test_compute_timings_survives_zero_rate
FAILED test_fallback_value_is_single_source
FAILED test_estimate_seconds_agrees_with_exit
```

**全量回归**：68 → **81 通过**（新增 13 条），零失败。

---

### P1-2 `config.yaml` 语法坏掉时静默退回全默认，且无任何日志 ✅ 实测

**位置**：`app/config.py:160-170`（`load_config`）与 `210-219`（`save_config`）

```python
except (OSError, yaml.YAMLError):
    # 配置读坏不该让引擎起不来……
    data = {}
```

设计意图（引擎要能起来）是对的，但**代价被藏起来了**：不 log、不返回错误、
`/api/config` 照样返回一份看起来正常的默认值。

**实测**：把 `api_key` 那行多缩进一个空格后 ——

```
正常读取 -> base_url = https://my-own-endpoint.example/v1 | model = my-model
缩进坏掉 -> base_url = https://open.bigmodel.cn/api/paas/v4 | model = glm-4.7 | api_key 长度 = 0
```

用户手改 `config.yaml` 打错一个缩进，设置页会显示「已配置 Key · 模型 glm-4.7」，
而他填的自定义端点与 Key **全部失效且毫无提示**。这与本项目自己在
`param_audit`（"静默降级比报错更危险"）和 `dropped_short`（"保留可见性"）上确立的原则相矛盾。

**建议**：
1. 至少 `log.warning("配置读取失败，已退回默认值：%s —— %s", p, e)`；
2. 更好：`load_config` 顺带返回一个 `config_error: str | None`，
   由 `/api/config` 下发，设置页在顶部摊一条「配置文件读取失败，当前显示的是默认值」。

#### ✅ 已修（2026-09-15）

两条建议**都做了**（1 是 2 的前提，缺了 2 用户还是看不见）。

**改动 1 · 抽出 `read_config_file(p) -> (data, error)`**（`app/config.py:235-266`）

原来 `load_config` 与 `save_config` **各自抄了一份**同样的 try/except，且**两处都静默**——
修的时候极易只修一处。抽成一个函数后两处自动一致。

```python
except yaml.YAMLError as e:
    brief = _yaml_error_brief(e)
    log.warning("config.yaml 解析失败（%s），本次改用内置默认值：%s", p, brief)
    return {}, f"config.yaml 语法有误（{brief}）"
except OSError as e:
    log.warning("config.yaml 读取失败（%s）：%s", p, e)
    return {}, f"config.yaml 读取失败（{e}）"
```

另外补了原报告没提的第三种坏法：**文件能解析但顶层不是映射**
（`- a\n- b\n` → list；`just a bare string` → str）。原代码 `if not isinstance(data, dict): data = {}`
同样是静默的，现在也报 `顶层应为键值映射，实际是 list`。

**改动 2 · `config_error` 一路透出**

- `AppConfig` 加字段 `config_error: str = ""`（`app/config.py:59-66`）
- `/api/config` 下发（`app/server.py:448-460`）
- `settings.js` 新增 `renderConfigError()`，把提示渲染到新增的 `#st-cfg-err`
- `index.html` 加 `<p class="hint cfg-err hidden" id="st-cfg-err">`，
  `styles.css` 加 `.hint.cfg-err`（用 `--warn` 而非 `--bad`：配置读坏不是错误状态，
  引擎照常起来、重存一次就好；红字反而让人以为数据丢了。要传达的是「现在显示的这份不是你的」）

提示文案把两件事都写清：

> ⚠ config.yaml 语法有误（mapping values are not allowed here，第 2 行第 44 列）。
> 上面显示的是内置默认值，不是你保存过的配置 —— 重新填一次并点「保存」即可覆盖修复。

因为 `_cfg()` 是**每次现读**（不缓存），用户重存一次提示就自动消失——
文案里承诺的「重新保存即可覆盖修复」是被断言守住的。

**顺带更正两处过时注释**：`server.py` 里 `set_config` / `reset_config` 的注释还在引用
「`load_config` 那里是 `or 默认值`，0 会悄悄变回默认」——P0-2 之后这句话已经不成立
（`_num` 只在键缺失或值为空时取默认）。边界校验本身仍然必需，但理由换了：
现在是「越界值会被**照单全收**」，而不是「会被静默改回默认」。

**安全考量 · 错误说明不取 `str(e)`**

`_yaml_error_brief()` 只取 `context` / `problem` / `problem_mark` 的行列号，理由要记准（实测）：

| 输入方式 | `str(e)` 是否带出错行原文 |
|---|---|
| 字符串 `yaml.safe_load(text)` | **是** —— `… sk-…: extra` + 插入符 |
| 文件对象 `yaml.safe_load(open(p))` | 否 —— PyYAML 流式 Reader 不保留 buffer，`get_snippet()` 返回 None |

也就是说**当前这条读路径本来就不泄漏**，这不是「修一个已发生的泄漏」。
保留它的理由是**不让安全性质取决于「用了哪种读法」**：
`yaml.safe_load(p.read_text())` 更短更顺手，而 `knowledge.read_yaml_cached` 用的正是它——
这次改动只差一行。附带收益是消息形态与输入方式无关，位置统一成中文「第 N 行第 M 列」。

**新增断言**（后端 48 条 + UI 3 条）

`tests/test_config_corruption.py`：

| 组 | 断言 |
|---|---|
| 不崩 + 报错 | `test_broken_config_does_not_raise_and_is_reported`（8 种坏法参数化） |
| 不误报 | `test_good_config_reports_nothing` / `test_missing_file_is_not_an_error` / `test_empty_file_is_not_an_error` |
| 日志 | `test_broken_config_logs_a_warning`（8 种）/ `test_good_config_logs_nothing` |
| 位置可用 | `test_message_points_at_line_and_column` / `test_top_level_not_mapping_is_described` |
| **脱敏** | `test_yaml_error_never_carries_source_snippet`（3 种真带正文的形态）/ `test_brief_still_says_what_is_wrong` |
| **前提守卫** | `test_file_stream_path_does_not_leak_today` —— 钉住「文件流不泄漏」这个实测事实 |
| 出口 | `test_error_message_never_leaks_secret` / `test_log_record_never_leaks_secret` |
| 透出 | `test_api_config_exposes_error` / `test_api_config_error_clears_after_save` |

`_verify/verify.js` 新增 3 条（桩引擎加 `&cfgerr=1` 开关）：
正常时警示隐藏且无文案 / 读坏时给出警示 / 警示用 warn 色且不顶掉原状态行。

**变异检验**（6 组，全部报红）

| 变异 | 报红 |
|---|---|
| A `read_config_file` 退回静默 `return {}, ""` | **18 条** |
| B1 `_yaml_error_brief` 函数体退回 `return str(e)` | **9 条**（含 3 条脱敏断言） |
| B2 调用点退回 `str(e)` | 5 条（中文位置断言） |
| C `/api/config` 不下发 `config_error` | 2 条 |
| D `settings.js` 去掉 `renderConfigError()` 调用 | UI 1 条 |
| E 去掉「空文案就隐藏」守卫 | UI 1 条（方向相反） |

> **一次自我纠错值得记**：第一版 `test_raw_str_would_have_leaked_but_brief_does_not`
> 用**字符串**输入构造「会泄漏」的场景，然后据此断言「脱敏有效」——
> 而生产路径走的是文件流。变异 B（调用点改 `str(e)`）跑完**泄漏断言全绿**，
> 才暴露出这条断言是**空转**的。改成在字符串输入（真带正文）上直接测
> `_yaml_error_brief` 后，B1 才能报红。
> 这正是项目里那条老教训的又一次应验：**过滤/构造出来的场景必须验证前提**，
> 否则「断言通过」只说明场景没造对。

**实测**：

```
$ pytest tests/test_config_corruption.py -q
................................................          # 48 passed
$ pytest -q                                                # 全量
129 passed                                                 # 81 → 129
$ node _verify/verify.js
114/114 通过                                                # 111 → 114
```

---

### P1-3 打包产物不含 Python 运行时，安装包装完不可用 ✅ 已修（2026-09-15）

**位置**：`desktop/main.js:39-64`（`resolveEngine`）+ `desktop/package.json` 的 `build`

`resolveEngine` 的第一优先级是 `resources/engine/engine.exe`，但 `extraResources`
只搬了 `../app`、`../packs`、`renderer` —— **没有任何构建步骤产出 `engine.exe`**。
打包态 `rootDir = resources/engine`，那里也没有 `.venv`。于是只剩 PATH 里的 `python`，
而它未必装了 `fastapi / uvicorn / httpx / pydantic / pyyaml`。

**影响**：`npm run dist` 出来的安装包，在没装过 Python 的机器上启动即弹
「引擎启动失败」，提示让用户 `pip install -r requirements.txt` —— 对一个 NSIS 安装包来说
这是不可接受的交付形态。`desktop/dist2`…`dist15` 共 14 个历史产物目录也侧面说明
这条链路被反复试过。

**建议**：二选一并写进 README ——
(a) build 流程加 PyInstaller 产出 `engine.exe` 并 `extraResources` 带上；
(b) 明确声明「需预装 Python 3.11+ 与依赖」，并把检测与安装指引做成首启引导页。

#### 方案验证（2026-09-15 实测，待决策）

原建议漏了第三条路，而它对这个项目最合适：**内嵌 Python 运行时**。
先把前提摸清 —— 这个引擎的依赖面**极小**：

```
第三方：fastapi / pydantic / httpx / pyyaml / uvicorn（仅 main() 用）
编译扩展：只有 pydantic_core（Rust）。无 sqlite3 / multiprocessing / ctypes / numpy
uvicorn 走 uvicorn.run(app, ...)，无 --reload → watchfiles 用不上；httptools 可选
```

于是三条路的实际对比：

| | 内嵌 Python（**推荐**） | PyInstaller `--onedir` | 声明需预装 |
|---|---|---|---|
| 产物 | `resources/engine/py/` 一个目录 | `engine.exe` + `_internal/` | 无 |
| 体积 | **38 MB**（实测 `py/` 合计） | 约 35~45 MB | 0 |
| 构建 | 解压 + `pip --target` + 写 `._pth`，约 1 分钟 | 要调 spec / hiddenimports | 无 |
| 调试 | **与开发态完全一致**（就是官方 python.exe） | bootloader 包一层，traceback 变形 | — |
| 杀毒误报 | 低 | **较高**（PyInstaller 引导器是常见误报源，叠加 NSIS 更甚） | 无 |
| 用户机器要求 | 无 | 无 | **需装 Python + 5 个包** |

PyInstaller 不是不可行 —— 6.22.3 的 classifier 已含 `Python :: 3.14` / `3.15`，
版本不是障碍。但**这个项目的核心取向是「让不可见的失效变得可见」**：
内嵌运行时出错时的 traceback 与开发态一字不差，而 bootloader 层的失败
（缺 hiddenimport、DLL 找不到）恰恰是最难查的一类。

**实测过程**（在 `/tmp` 里按真实布局搭了一遍，`app/` + `packs/` + `renderer/` + `py/`）：

```
1) 下载 python-3.14.3-embed-amd64.zip（12 MB）→ 解压
2) 建 py/Lib/site-packages，用宿主 pip 装 5 个运行依赖 → 16 MB
3) 写 py/python314._pth（见下）
4) ./py/python.exe -m app.server --root . --port 18772 --token t2
   → 引擎正常启动
5) curl /api/health  → {"ok":true,"version":"0.0.0-dev","mock":false}
6) curl /api/meta    → 行业包与参数完整下发
7) TALKSCRIPT_MOCK=1 跑一次真生成
   → state: "done"，quota {total:261,...}，steps 齐全
```

**踩到的坑（写进构建脚本，别踩第二次）**：内嵌发行版默认**禁用 `site`**、
且 `._pth` 存在时进入 isolated 模式 —— **cwd 不进 `sys.path`**。于是
`python -m app.server` 直接报 `No module named 'app'`：

```
$ ./py/python.exe -m app.server --root .
...: Error while finding module specification for 'app.server'
     (ModuleNotFoundError: No module named 'app')
```

`._pth` 必须是这四行（`..` 是相对 `py/` 的 rootDir，即 `resources/engine`）：

```
python314.zip
.
Lib\site-packages
..
import site
```

改完后 `sys.path` = `py/python314.zip` · `py/` · `py/Lib/site-packages` · `engine/`，
`app.server` 正常导入。

**顺带能简化的一处**：`requirements.txt` 用的是 `uvicorn[standard]`，但引擎没有
`--reload`，`httptools` / `watchfiles` / `websockets` 全用不上，`uvicorn.run`
会自动退回纯 Python 的 `h11` 解析器。打包时改用 `uvicorn`（无 extras）
可去掉两个编译扩展、少几 MB，也少两个「wheel 缺失」的可能。

**许可**：Python 是 PSF License，允许随产品分发（`py/LICENSE.txt` 已在包里，
保留即可）。

**剩下要定的三件小事**（都有默认值，不阻塞）：
① 固定 Python 版本 → **3.13.x**（比 3.14 更保守，依赖 wheel 更全）；
② 内嵌 zip 是构建时下载（不入库）还是 vendor 进仓库 → **构建时下载 + 校验 sha256**；
③ `py/` 放 `resources/engine/py/` 还是 `resources/py/` → **`resources/engine/py/`**
（与 `app/` / `packs/` / `renderer/` 同级，`._pth` 里的 `..` 正好指向 `app/` 那一层）。

#### 落地（2026-09-15）

三条路里选了「内嵌 Python 运行时」。新增/改动：

| 文件 | 作用 |
|---|---|
| `desktop/scripts/build-python-runtime.mjs` | **构建脚本**（零依赖）：下载 → 校验 sha256 → 解压 → 写 `._pth` → 装依赖 → 自检 |
| `desktop/scripts/before-pack.js` | electron-builder 的 `beforePack` 钩子，保证打包前运行时一定是新的 |
| `desktop/engine-path.js` | `resolveEngine` 从 `main.js` 拆出来（纯逻辑，可脱离 Electron 单测） |
| `desktop/engine-path.test.js` | 降级链的顺序断言（`node --test`，11 条） |
| `requirements-runtime.txt` | 装进发行包的那 5 个依赖（**不带 pytest、不带 `[uvicorn standard]`**） |
| `_verify/runtime-smoke.js` | 端到端冒烟：按打包布局真起引擎 + 真跑一次生成 |
| `desktop/package.json` | `extraResources` 带 `vendor/py`；`files` 补 `engine-path.js`；挂 `beforePack` |

**降级链**（`desktop/engine-path.js`，顺序 = 我们期望用户跑哪个）：

```
TALKSCRIPT_PYTHON → engine/py/python.exe（出厂自带）→ engine.exe → 项目 .venv → PATH
```

**关键：出厂路径排在 `engine.exe` 之前。** 顺序错了的表现是「装完启动还是找不到
解释器」，而读码很容易看漏 —— 所以把纯逻辑从 Electron 壳里拆出来，
用 `node --test` 把每条路径与先后都钉住。日志里也会打出命中哪一条
（`engine (engine/py（出厂运行时）): …`），引擎起不来时第一件要问的就是这个。

#### 构建脚本里两个必须记住的点

**① `pip install` 必须带 `--python-version`。**

宿主 Python 的版本与内嵌运行时未必相同（本机开发环境是 3.14，运行时钉 3.13）。
不钉版本的话 pip 按**宿主**的 ABI 挑 wheel，装进去的是
`pydantic_core-…-cp314-…whl`，而 3.13 的解释器加载不了它：

```
ModuleNotFoundError: No module named 'pydantic_core._pydantic_core'
```

**这个坑第一次构建时就踩到了**：脚本报「完成：38.7 MB」，目录看着完全正常，
只有手工 import 才发现。所以加了下面那道自检。

**② 必须有一道「用运行时自己的解释器 import 一遍」的自检。**

sha256 只保证 zip 没被换过、版本自检只保证解释器本身能跑 —— 都管不了
「依赖装错 ABI / 少装一个 / `._pth` 那几行没生效」。自检要按**打包后的真实布局**跑
（`._pth` 里的 `..` 是相对 `py/` 的，指向 `app/` 所在的那一层），所以在构建树里
临时铺一份 `app/`，跑完就删 —— 而不是靠 `PYTHONPATH` 作弊。

**③ `--python-version` 只管 wheel 的 ABI 标签，不管字节码版本。**

打包完解包核对时发现的：安装包里有 **404 个 `.pyc` 全是 `cpython-314`**，
而运行时是 3.13 —— **一个都用不上**，全是死重。

> **⚠ 数字更正**：最初这里写的是「1.8 MB」，那是早期口径不全的量法。
> 按同一口径复测（把同一份 `site-packages` 分别交给 3.14 和 3.13 编译）：
> 3.14 → **404 个 / 6.76 MB**，3.13 → **404 个 / 5.77 MB**。
> 所以死重是 **约 6.8 MB**，换成对的版本后**净减约 1 MB**（与实测「小了 1 MB」吻合）。
根因是 pip 编译 `.pyc` 用的是**正在跑 pip 的那个解释器**（开发机 3.14），
`--python-version` 不改变这一点。

修法两步、必须成对：`pip install --no-compile`（不让 pip 编）
\+ 用运行时自己的解释器 `python.exe -m compileall Lib/site-packages`。

> **⚠ 这里要纠正一个我自己先得出的错误结论。**
> 我最初量到「无缓存 1.15s vs 有缓存 0.67s」，差点写成「每次启动省 0.5 秒」。
> 换成 6 次取最小值的稳定测法后，差异只有 **~0.03s**，在噪声内 ——
> 那个 0.5s 是重建之后磁盘冷缓存的假象。
> **所以这条修的真实价值是「交付物里不该有 6.8 MB 永远用不上的内容」
> \+ 一条哨兵，不是性能。** 差点把一个测不准的数字写进结论。
>
> （顺带也说明：**性能数字要按「多次取最小值」测，体积数字要按「同口径」测** ——
> 前者差点让我多报 0.5 秒的收益，后者差点让我少报 4 倍的死重。）

顺手加了两道守卫：

| 守卫 | 位置 | 挡住什么 |
|---|---|---|
| `assertBytecodeMatchesRuntime()` | 构建脚本 | `.pyc` 版本标签与运行时不一致 → **构建直接失败** |
| 产物级断言 | `_verify/runtime-smoke.js` | 对着打出来的包扫 `.pyc` 标签，错配就报红 |

#### 验收

```
pytest                 203 → 212（新增 tests/test_runtime_requirements.py 9 条）
node --test desktop/   新增 11/11（降级链顺序）
node _verify/runtime-smoke.js   新增 10/10（端到端）
node _verify/verify.js 133/133（不变）
```

`_verify/runtime-smoke.js` 的输出（**这才是「装完能用」的证据**）：

```
✅  sys.path 含 stdlib zip（python313.zip）
✅  sys.path 含 Lib\site-packages（第三方依赖）
✅  sys.path 含 py/ 的上一级（app/ 在那儿）
✅  引擎能起来（/api/health 通）  — {"ok":true,"version":"0.0.0-smoke","mock":true}
✅  /api/meta 鉴权通过（带令牌）  — HTTP 200
✅  /api/meta 能读到行业包（证明 ._pth 的 `..` 生效）  — packs=elevator
✅  /api/meta 下发参数条与审计
✅  提交生成作业被接受  — HTTP 200 {"job_id":"20260915-214218-286e41"}
✅  生成跑到 done（全链路可用）  — state=done
✅  产物有分段文案与字数配额  — sections=5 quota=261
10/10 通过
```

跑之前会**清掉 `PYTHONPATH` / `PYTHONHOME` / `VIRTUAL_ENV`** ——
否则「自带运行时」可能只是借了宿主环境的光，换台机器就废。

产物体积 **38.7 MB**（zip 10.4 MB），与方案预估一致。

#### 变异检验

| 变异 | 改回什么 | 结果 |
|---|---|---|
| M1 | 把出厂运行时那块挪到 `engine.exe` 之后 | engine-path 测试 **1 红**（正是顺序那条） |
| M2 | 去掉 `--python-version` | **构建直接失败**（退出码 1），日志里能看到装的是 `cp314` |
| M3 | 去掉 `beforePack` 钩子 | pytest **1 红** |
| M4 | 从 `files` 去掉 `engine-path.js` | pytest **1 红** |
| M5 | 从 `extraResources` 去掉 `vendor/py` | pytest **1 红** |
| M6 | 改一处运行时的版本区间 | pytest **1 红** |
| M7 | 去掉 `--no-compile`（让 pip 用它自己的解释器编译） | **构建失败**（退出码 1），诊断精确到「404 个版本对不上」 |
| M8 | 把编译**和**校验一起去掉（模拟「有人嫌麻烦删掉整块」） | 构建**「成功」**（退出码 0，42.1 MB）—— 但产物级冒烟 **11/12，404 个错配** 兜住了 |

M2 是这一组里最有价值的：它证明**自检能挡住静默失败** ——
没有自检的话，构建会「成功」，问题只在用户装完之后才出现。

M7 / M8 是这一组的第二层，两个放在一起才有意思：

- **M7 只拆一半**（去掉 `--no-compile`，但留下校验）→ 构建**直接失败**。
  诊断信息精确（`404 个版本对不上`），因为校验就在那儿等着。
- **M8 整块拆掉**（编译和校验一起删）→ **构建「成功」了**，退出码 0、
  产物 42.1 MB —— 一切看起来都正常。**没有任何构建期信号。**

**M8 才是这条修法真正的风险模型**：将来有人觉得「这两步碍事」而删掉时，
构建不会拦他。兜住它的是**产物级断言**（`_verify/runtime-smoke.js` 里那两条
`.pyc` 标签检查）—— 它验的是**交付物里实际装了什么**，不是「构建脚本长什么样」。

两层各守一件事：构建期校验**早**、但可以被一并删掉；产物级断言**晚**、但删不掉
（要让它失效，得同时改构建脚本和冒烟工具）。**单一防线挡不住「嫌麻烦的人」，
两层错开位置才挡得住。**

> 产物级断言本身也做了正对照：往打好的包里塞一个 `cpython-314.pyc`，
> 冒烟立刻 **11/12** 并报出具体文件名 —— 它不是一条永远为真的装饰。

#### 真跑了一次 `npm run dist`（2026-09-15）

上面那些断言都是静态的 + 构建树里的冒烟。「electron-builder 真的会调那个钩子吗」
「打出来的产物里真的有解释器吗」**没验过就还是未知**。所以真跑了一次：

```
$ npm run dist          # 1m46s，退出码 0
[py-runtime] 已是最新（3.13.12，38.7 MB），跳过。--force 可强制重建   ← beforePack 被调用了

desktop/dist/
  TalkScript Setup 0.2.0.exe    96 MB   ← NSIS 安装包
  TalkScript 0.2.0.exe          96 MB   ← portable
  win-unpacked/resources/engine/{app,packs,py,renderer}
```

三件事一次确认：**钩子确实被调用**（那行 `[py-runtime]` 就是证据，而且它走了幂等分支）、
**`vendor/py` 确实被搬进 `resources/engine/py`**、**`._pth` 是脚本写的那四行**。

接着用**打出来的产物本身**跑冒烟（给 `_verify/runtime-smoke.js` 加了 `--root`）：

```
$ node _verify/runtime-smoke.js --root desktop/dist/win-unpacked/resources/engine
目标：desktop\dist\win-unpacked\resources\engine（已成型产物，只读）
✅ sys.path 含 stdlib zip / Lib\site-packages / py 的上一级
✅ 引擎能起来   ✅ /api/meta 读到 elevator 包
✅ 提交生成作业 ✅ 生成跑到 done ✅ sections=5 quota=261
10/10 通过
```

最后**真启动了那个 app**，抓主进程日志 —— 这是「命中哪条降级路径」的最终证据：

```
[talkscript] engine (engine/py（出厂运行时）):
  E:\…\resources\engine\py\python.exe -m app.server
  --port 57909 --root E:\…\resources\engine
  --data-dir C:\Users\78470\AppData\Roaming\TalkScript
  --token 7I1ly919nm4Y1ePxtu-MUmJRm8rOKhYZ --version 0.2.0
```

再探它起着的引擎：

```
GET http://127.0.0.1:57909/api/health → 200 {"ok":true,"version":"0.2.0","mock":false}
```

**安装包在没装过 Python 的机器上能跑，这件事现在是实测过的了。**

> 附注：本机直接双击 exe 会崩在 GPU（`GPU process isn't usable. Goodbye.`），
> 加 `--no-sandbox` 正常 —— 这是本环境的已知问题，与 P1-3 无关，
> 开发态本来就是用 `npm run start:no-sandbox` 绕过的。

#### 带着字节码修正重新打包（2026-09-15 22:52）

上面的 96 MB 包是**修正之前**打的。修完之后重新跑了一次 `npm run dist`（1m24s，退出码 0），
并拿**新产物本身**验收（不再只看构建树）：

```
$ node _verify/runtime-smoke.js --root desktop/dist/win-unpacked/resources/engine
✅  site-packages 里的 .pyc 全部匹配 cpython-313（没有别的版本的死重）  — 404 个
✅  确实存在可用的字节码缓存（不是零个）                              — 404 个
✅  引擎能起来 / /api/meta 读到 elevator 包 / 生成跑到 done …
12/12 通过
```

`dist/` 下的两个安装包都换成了 22:52 的新包（`TalkScript Setup 0.2.0.exe` 96 MB、
portable 96 MB），解包核对 `py/Lib/site-packages` 下 **404 个 `.pyc` 全是 `cpython-313`**。

回归基线随之上移：

```
.venv/Scripts/python.exe -m pytest -q                212 → 213（+1 条编译口径断言）
node --test desktop/engine-path.test.js              11/11（不变）
node _verify/runtime-smoke.js                        10 → 12（+2 条字节码断言）
```

> 顺手纠正了一处**留下来的错误注释**：构建脚本里 `compileBytecode()` 的注释
> 仍写着「差 0.5 秒 / 用户每次启动都要付」，而报告里已经自己证伪了这个数。
> **同一个结论抄在两处，就一定会有一处先过期** —— 已改成与实测一致的口径，
> 并写明「别再改回去」。

#### 还没做的

- **没验过 NSIS 安装流程本身**（真装一次、从「开始菜单」启动、卸载后是否残留）。
  这需要在干净机器或虚拟机里做，本环境不方便。
  但「安装包里的运行时能用」已经由上面三步证明。
- `TALKSCRIPT_PYTHON` 的语义没变：显式覆盖仍然优先，方便用户自带解释器。
- 打包产物 `desktop/dist/`（约 800 MB，含两个 96 MB 安装包）已 gitignore，留在本地供试用。

#### 横向参考：本机已装的 4 个 agent 工具，运行时是怎么解决的

上面三条路是凭工程经验推的。为免「拍脑袋」，把本机实际装着的四款 agent 工具
（含用户点名的 Codex / TRAE Work / WorkBuddy）拆了一遍 —— 看它们**到底把解释器
放在哪、什么时候放、谁来放**。全部为磁盘实测，非文档推测。

| | 技术形态 | 运行时策略 | 安装包代价 |
|---|---|---|---|
| **WorkBuddy** | Electron 壳 + Python/Node 工具链 | **zip 随安装包发**，首启解压到**用户目录** | `resources/vendor/` 三个 zip 共 **161 MB**（python 18.7 + node 86.4 + PortableGit 56.4），总安装 **1.3 GB** |
| **TRAE Work（SOLO CN）** | Electron 壳 + **原生编译模块** | **不携带任何解释器**，全部编成 Rust/C++ 二进制 | 无独立运行时（但 `ai_agent.dll` 278 MB + `harness.dll` 197 MB） |
| **Codex** | Rust CLI + 插件 | **按需下载**插件运行时 | 基础包极小，`plugins/cache/` 15 MB + 6.1 MB |
| **opencode** | 纯 Electron | **无自带运行时**，靠用户环境 | `app.asar` 而已 |

**WorkBuddy 是唯一与本项目同构的样本**（Electron + Python 引擎），它的做法：

```
安装目录 E:/Apps/Vibe/Tencent/WorkBuddyAI/
  resources/vendor/python.zip      18,755,291 B   ← 随安装包发（install-manifest.json 里有条目）
  resources/vendor/node.zip        86,429,813 B
  resources/vendor/PortableGit.zip 56,373,885 B
  resources/install-manifest.json   fileCount:116，含上述 3 个 zip（安装器校验用）

用户目录 ~/.workbuddy-ai/binaries/
  python/versions/3.13.12/   53 MB —— python.exe + python313.dll + Lib + DLLs
                             + include + libs + Scripts，**含 pip 26.1.2**
  node/versions/22.22.2-2/
  .cache/registry.json       同时记两套来源：source:"managed"（托管解压）
                             与 systemDetectionCache（探测到的系统 Python/Node），
                             附 verified / size / mtimeMs
```

三点值得直接抄：

1. **zip 随包，不联网下载**。WorkBuddy 的 zip 在 `install-manifest.json` 的
   `fileCount` 清单里 → 由安装器随包落地，首启只是解压。Codex 那种「按需下载」
   适合插件（可选能力），**核心引擎的运行时不能依赖首启联网**。
2. **解压到用户目录，不放安装目录**。Program Files 只读；而且用户目录便于
   「修复安装」时单独重灌运行时。
3. **`registry.json` 记双来源**：托管解压的 + 探测到系统已有的。这跟本项目
   `resolveEngine()` 的降级链（`TALKSCRIPT_PYTHON` → `engine.exe` →
   `.venv` → PATH）是同一个思路，但它把**「当前用的是哪一个」持久化并可见**，
   而不是每次静默重猜。

**但有一点不要抄**：WorkBuddy 的 `python.zip` 解压后是**完整 python.org 发行树**
（`DLLs/` `include/` `libs/` `Scripts/` 齐全，53 MB，含 pip）。它必须这么做 ——
它的 agent 要**在运行时 `pip install` 用户点名的包**，所以 site-packages 必须可写。
**TalkScript 的依赖是固定的 5 个包**，用户永远不会触发一次 `pip install`，
因此：**不需要 pip、不需要可写 site-packages、不需要解压到用户目录** ——
embeddable 的 38 MB 版本完全够用，且可以**直接躺在 `resources/engine/py/` 里不解压**
（NSIS 本来就会压缩载荷，再套一层 zip 只是多一次首启解压）。

**TRAE Work 这条路本项目走不了**：它把整个 agent 编成了原生二进制
（`resources/app/modules/ai-agent/ai-agent.exe`，dev 模式下是 `cargo run`），
`libckg.dll` 39 MB、`trae-sandbox.exe`、`browser-bridge.exe`、`agent-tool-host.exe`
88 MB 全是 Rust/C++ 产物 —— 换来的是**零解释器依赖**，代价是 278 MB 的
`ai_agent.dll`。除非把 `app/` 整个重写成 Rust，否则不适用。
（顺带一个可借鉴的小点：它的 ripgrep 走 npm optional dependency 分发预编译
二进制 —— `@byted-fe/ripgrep-win32-x64` / `@vscode/ripgrep`，这跟本项目
「按平台带 pydantic_core 的 wheel」是同一招。）

**收敛后的结论**：原推荐（**内嵌 embeddable Python，`resources/engine/py/`，
不解压到用户目录**）与 WorkBuddy 的做法**同源但更省** —— 差在两点：
① 用 embeddable（38 MB）而非完整发行版（53 MB），因为不需要 pip；
② 运行时留在安装目录，因为不需要写。

对应的 `resolveEngine` 改动很小，就是给现有的降级链补一环
（放在 `engine.exe` **之前**，理由见下）：

```js
const pyDir = path.join(resourcesDir, 'engine', 'py');
const bundledPy = process.platform === 'win32'
  ? path.join(pyDir, 'python.exe') : path.join(pyDir, 'bin', 'python3');
if (fs.existsSync(bundledPy)) return { cmd: bundledPy, args };
```

优先级建议为：`TALKSCRIPT_PYTHON`（调试逃生口）→ `engine/py/python.exe`（**出厂路径**）
→ `engine.exe`（PyInstaller 若将来改走那条路）→ `.venv`（开发态）→ PATH。
把出厂路径提到 `engine.exe` 之前，是因为**降级链的顺序应该反映「我们期望用户跑哪个」**，
而不是「哪条路先被写出来」。

---

### P1-4 并发额度检查是 check-then-act，可被并发请求绕过 📖 读码

**位置**：`app/pipeline.py:126-133`

```python
if self.registry.running_count() >= MAX_CONCURRENT_JOBS:   # ← 检查
    raise StateConflict(...)
...
self.registry.add(job)                                     # ← 置位（不同锁）
```

`/api/generate` 是同步 `def` → FastAPI 丢线程池执行 → 真并发。
两个请求可同时读到 `running_count()==3` 双双通过，上限 4 被超到 5、6。

**注意**：本项目的 `jobs.py` 已经把「检查与置位在同一把锁内」这条不变量
在状态机上做对了（`transition()`），唯独这里漏了 —— 属于同一类问题的遗漏点，
而不是设计缺陷。

**建议**：把「判断 + 插入」收进 `JobRegistry` 的原子方法：

```python
def add_if_room(self, job: Job, limit: int) -> bool:
    with self._lock:
        if sum(1 for j in self._jobs.values() if j.state not in TERMINAL_STATES) >= limit:
            return False
        self._jobs[job.id] = job
        return True
```

#### ✅ 已修（2026-09-15）

**按建议做了，但口径改了一处**：判据用 `j.state in BUSY_STATES`，
**不是**报告里写的 `not in TERMINAL_STATES`。因为 P0-3 已经确立
「待确认（`paused_awaiting_confirmation`）不占并发额度」—— 照抄建议会把 P0-3 改回去。
`add_if_room` 必须与 `running_count()` 用**同一个口径**，否则「界面说还有额度、
实际进不去」（或反过来）又是一处静默不一致。

```python
def add_if_room(self, job: Job, limit: int) -> bool:
    """**同一把锁内**检查并发额度并插入；返回 False 表示已满（未插入）。"""
    with self._lock:
        if sum(1 for j in self._jobs.values() if j.state in BUSY_STATES) >= limit:
            return False
        self._jobs[job.id] = job
        return True
```

`running_count()` 保留但降级为**仅供展示**，docstring 里明确写了
「不要用它做『够不够再开一个』的判断再另行 `add()`」。

**另一处顺带修正：`start_generate` 的语句顺序**

原顺序是「先查额度 → 再 `Pack()`」。修完后必须调整，因为占额度需要先有 `Job` 对象：

```python
pack = Pack(self.root, req.pack)       # ① 先验包
jid = new_job_id(); job = Job(...)
if not self.registry.add_if_room(job, MAX_CONCURRENT_JOBS):   # ② 再原子占额度
    raise StateConflict(...)
```

两个理由：
1. **反过来会留一条永久漏额度的路径** —— 若先占额度再验包，`PackError` 抛出去时
   那条 `queued` 作业已进注册表、没人回收，额度被永久吃掉一个。
   这正是 P0-3 的形态，不能刚修完又自己引入一个。
2. 包不存在是**永久性**错误（404），比可重试的 409 更该先说。

行为差异只在「包不存在 **且** 额度已满」这一种组合下出现（409 → 404），
已用 `test_bad_pack_reported_before_quota` 把这个**刻意的**顺序钉住。

**新增断言** `tests/test_concurrency_quota.py`（8 条）

| 组 | 断言 |
|---|---|
| 原语原子性 | `test_add_if_room_is_atomic_under_threads`（40 线程抢 4 位 → 恰好 4） |
| 失败无副作用 | `test_add_if_room_full_leaves_registry_untouched` |
| 口径一致 | `test_add_if_room_ignores_paused_jobs` |
| 端到端并发 | `test_start_generate_never_exceeds_limit`（20 线程 → 恰好 4 个 job_id，其余全是 `StateConflict`） |
| **变异捕手** | `test_start_generate_does_not_check_then_act` |
| 额度可回收 | `test_limit_is_reusable_after_slot_frees` |
| 顺序 | `test_pack_error_does_not_consume_a_slot` / `test_bad_pack_reported_before_quota` |

**「确定性」是怎么做到的**（这决定了断言有没有用）

并发 bug 光靠 Barrier 起跑测不准 —— 老实现**可能**碰巧不超额。所以加了
`_slow_running_count` 夹具，把「读计数」这一步人为拉长 50ms：

- 新实现 `add_if_room` **内联算计数、不调用** `running_count()` → 夹具完全无影响 → 恰好 4；
- 退回 check-then-act → 20 个线程全读到同一个旧值 → 全部插入 → 必现报红。

夹具里先抓住原始函数（`_ORIG_RUNNING_COUNT = JobRegistry.running_count`）再 monkeypatch，
否则 `super()` 会自我递归。注释里明确标注「**这是夹具，不是被测代码**」，
免得后人误以为它在改行为。

**变异检验**

| 变异 | 报红 |
|---|---|
| F `start_generate` 退回 `running_count()` + `add()` | `test_start_generate_does_not_check_then_act` |
| G `add_if_room` 退回锁外读计数 | `test_add_if_room_is_atomic_under_threads` + 变异捕手 |

> **一条值得记的观察**：变异 F 只被**插桩那条**测试抓到，
> 不插桩的 `test_start_generate_never_exceeds_limit` 反而全绿 ——
> 20 个线程的 Barrier 不足以稳定踩中那个窗口。
> 这正好印证了「并发测试不加插桩就是碰运气」，也说明两条测试都有存在价值：
> 一条守语义（超额数必须恰好为 0），一条守机制（不许走 check-then-act）。

**实测**：

```
$ pytest tests/test_concurrency_quota.py -q
........                                                   # 8 passed
$ pytest -q                                                # 全量
137 passed                                                 # 129 → 137
$ node _verify/verify.js
114/114 通过
```

---

### P1-5 `banwords.yaml` 读坏 → **合规校验静默全过** ✅ 实测

> **来源**：修 P1-2 时补扫同类形态发现的（原报告漏了）。与 P1-2 是**同一个根因**
> —— `knowledge.read_yaml_cached()` 里那句无日志的 `except yaml.YAMLError: return {}`。

**位置**：`app/knowledge.py:62-68`（`read_yaml_cached`）→
`Pack.banwords_data()`（`:288`）→ `pipeline.py:293` / `:477` 的 `Banwords(...)`

`read_yaml_cached` 是 `pack.yaml` / `banwords.yaml` / `skill.yaml` /
`private/*.yaml` **共用**的读取器，YAML 解析失败一律返回 `{}`、不记日志。
对 `banwords.yaml` 来说，`{}` 意味着 `Banwords` 拿到**空词表**。

**实测**（同一段文案，只把 `banwords.yaml` 写坏）：

```
正常   -> {'hard': [{'word': '全网最低价', 'count': 1}, {'word': '包过检', 'count': 1},
                    {'word': '绝对安全', 'count': 1}, {'word': '零事故', 'count': 1}],
           'soft': [{'word': '绝对', 'count': 1}], 'dropped_short': ['最']}
损坏后 -> {'hard': [], 'soft': [], 'dropped_short': []}
```

**影响**：`绝对安全，零事故，全网最低价，包过检。` 这句话在正常包下是
**4 处硬伤**，词表读坏后是 **0 处** —— 合规红线被静默关闭，文案直接出厂。
而 `dropped_short` 也是空的，意味着连「词表没完全生效」这个**已有的可见性机制**
一起失效了。

包作者改词表打错一个字符（本项目明确支持手改 YAML）就会踩到，
而且**任何地方都不会提示**：不报错、不降级、日志里没有、界面上没有。

**为什么这条比 P1-2 更严重**：P1-2 影响的是「我填的配置没生效」，
用户迟早会从行为差异上察觉；这条影响的是**校验结论本身**，
而校验结论是用户唯一的「这条文案安全吗」的依据 —— 它错了不会有人发现，
直到文案被平台下架。

**建议**：`read_yaml_cached` 区分「文件不存在」（正常，静默）与
「文件存在但读不出来」（异常，必须留痕），后者记 WARNING；
并把错误沿 `Pack` → `pipeline` 传到 `check` 结果里，让界面能摊一条
「禁用词表读取失败，本次只做了基础检查」。

---

### P1-6 `pack.yaml` 读坏 → 参数条空掉、包名退成 slug、**`param_audit` 一起哑掉** ✅ 实测

**位置**：同 P1-5 的 `read_yaml_cached`；消费方 `pack_info()`（`:87`）与
`Pack.__init__`（`:199`）

**实测**（`pack.yaml` 只多缩进一个空格）：

```
正常      : params 数 = 7 | display_name = 电梯   | param_audit = {...}
坏 pack.yaml: params 数 = 0 | display_name = elevator | param_audit = {}
仍在列表吗 = True
```

四个后果，每一个单独看都不致命，合起来是「包被悄悄掏空」：

1. `params` 7 → **0**：参数条整个空掉，用户没有任何可选参数；
2. `display_name` 「电梯」→ **`elevator`**：slug 直接显示在界面上（MEMORY 里
   记过「不要吐 slug」这条，这里是同一个现象的另一条路径）；
3. **`param_audit = {}`** —— 这是最值得记的一条。`param_audit` 是**专门用来防
   静默降级**的机制（「用户能选、但本包没定制」会标警示色），但它自己依赖
   `pack.yaml` 解析成功：**审计机制依赖它要审计的那份数据**。数据没了，
   审计一起哑掉 —— 防线和数据同生共死，等于没有防线。
4. 包**仍在列表里**、仍可选、仍能生成 —— 用户没有任何理由怀疑它坏了。

**建议**：与 P1-5 同源同修；另外给 `PackInfo` 加一个 `yaml_error: str` 下发，
让行业包选择器能标出「这个包读坏了」。**不要**让 `param_audit` 去兜这个
（它兜不了，见上）。

#### 修复记录（2026-09-15，P1-5 与 P1-6 一起修）

**根因只有一处**：`read_yaml_cached()` 把「文件写坏了」与「本来就没这个文件」
压成同一个 `{}`。所以修在根上，四个消费者各自决定怎么办。

**改动**

| 文件 | 改动 |
|---|---|
| `app/fileio.py` | 新增 `read_yaml_file() -> (dict, str)` 与 `yaml_error_brief()`（**从 config 上移**，去掉下划线）。全项目读 YAML 只此一份口径；一律走**文件对象**，`str(e)` 进不了对外消息 |
| `app/config.py` | `read_config_file` 改为委托 `read_yaml_file`，只补一条 WARNING |
| `app/knowledge.py` | `read_yaml_cached` 返回 `(data, err)`，**err 一起进缓存**；新增 `PackBrokenError(PackError)`；`pack_info` 带 `pack_error`；`Pack.__init__` / `banwords_data` / `skill` / `private_facts` 四处拒绝降级 |
| `app/schemas.py` | `PackInfo.pack_error: str = ""` |
| `app/server.py` | `PackBrokenError` → **409**（`PackError` 仍是 404）；导出路由先捕子类 |
| `desktop/renderer/js/ui.js` | 包下拉标「（损坏）」；参数条上方一条 warn 色说明 |
| `desktop/renderer/index.html` | 加 `#pack-err` |

**返回值改成元组是故意的**：不是「顺手记个日志」，而是让**每个调用点都被迫
显式决定**「读不出来怎么办」。原来那 6 个调用点全都在用 `return {}` 的默认语义，
改签名之后编译器/测试会盯着它们，不靠自觉。

**两层检查，各守各的（不是重复机制，别删其中一层）**

1. **构造期 `Pack.__init__` → 快速失败**。坏包不该被接受进队列：
   `banwords.yaml` 是在写稿+回炉阶段才读的，等那时才发现，
   `select` 阶段那一次真模型调用**已经付过费了**。
2. **出口 `banwords_data` / `skill` / `private_facts` → 不变式**。构造期检查只
   代表「那一刻是好的」；一个作业要跑几十秒，期间包被改坏完全可能。

两条各有一条独立的证伪测试 —— 这是刻意的：如果只有构造期那一条，
「出口守卫」就是不可证伪的死代码；只有出口那一条，就丢了快速失败。

**验收**

```
pytest                        147 → 170   （新增 tests/test_pack_yaml_integrity.py 23 条）
node _verify/verify.js        116 → 121/121（新增 5 条：12e 组）
```

**变异检验**（每一项都单独验过，且都能报红）

| 变异 | 内容 | 报红 |
|---|---|---|
| M1 | `read_yaml_cached` 丢掉 err（`err = ""`） | **14 条** |
| M2 | `Pack.__init__` 去掉 `pack_error` 检查 | **3 条** |
| M3 | `banwords_data()` 去掉出口守卫 | **1 条**（专为它写的那条） |
| M4 | `pack_info` 的 `pack_error` 改回 `""` | **6 条** |
| U1 | 下拉去掉「（损坏）」标记 | **1 条** |
| U2 | 参数条上方的说明改成空串 | **1 条** |

⚠ **M5 单独不成立，别把它当证伪依据**（实测）：把 `read_yaml_file` 的 `open(p)`
换成 `p.read_text()` → 本组 **0 红**。因为 `yaml_error_brief` 根本不用 `str(e)`，
只取 `context` / `problem` / `problem_mark`。**两条防线各自都够**，
要同时去掉才会泄漏：M5 + M6（把 brief 改成 `return str(e)`）→ **19 条红**。
写下来是为了别把「改一层没红」误判成「这条断言是假的」。

**顺带修掉的两处**

- `param_audit` 里 `except Exception: rules = set()` 删了 —— `read_yaml_cached`
  不再抛，那个裸 except 只是把「词表坏了」伪装成「本平台没配词表」。
  现在坏词表会让**每个**平台选项都标上「整个禁用词表都失效」。
- `skill.yaml` 读坏原先会被报成「行业包缺少 skill.yaml」—— 把「坏了」说成
  「缺了」，用户会去翻目录，而文件明明就在那儿。现在报真实原因。

**踩到的坑（写进测试注释，别踩第二次）**

同一条坏 YAML 在两次探针里分别报「第 2 行第 6 列」和「第 3 行第 3 列」——
查下去发现是 **shell heredoc 会把 `\n` 转义弄坏**（写进文件的是字面 `/n`），
我第一版探针拿到的是被改过的内容。教训两条：
① 探针别用 heredoc 传含反斜杠的字符串；
② **断言里不要硬编码行列号**，用 `第 \d+ 行第 \d+ 列` 的正则。

---

## P2 —— 可修（规范 / 一致性 / 卫生）

### P2-1 前端与后端的 temperature 取值域不一致 📖 读码

- `desktop/renderer/js/settings.js:127`：`num < 0 || num > 1.5` → 提示「0 ~ 1.5」
- `app/server.py:470`：`("temperature", 0.0, 2.0)` → 允许到 2.0

前端更严，1.5~2.0 这段在界面上永远填不进去。两处应同一口径
（建议前端放开到 2.0；若产品上确实只支持到 1.5，则后端也该收到 1.5，
而不是留一个只有 curl 能用的区间）。

#### ✅ 已修（2026-09-15）

**取「前端放开到 2.0」**。理由：0~2 是主流 OpenAI 兼容接口的通用区间，
后端已经接受；把它收窄到 1.5 会让「后端能力」与「界面能力」永久错位，
而且没有产品理由 —— 本项目面向的是自填接口地址的用户，模型可以是任意一家。

**改动 1 · 后端把区间提成模块常量**（`app/server.py:127-143`）

```python
NUMERIC_BOUNDS: dict[str, tuple[float, float]] = {
    "retries": (0, 10), "timeout": (5, 1800),
    "max_tokens": (256, 200000), "temperature": (0.0, 2.0),
}
```

原来它是 `set_config` 里内联的元组序列，**不可 import、不可 grep**。
提成常量后测试才能读到它去和前端比对。

**改动 2 · 前端也收成一张表**（`settings.js:19-51`）

```js
export const NUMERIC_BOUNDS = {
  temperature: [0, 2], retries: [0, 10],
  timeout: [5, 1800], max_tokens: [256, 200000],
};
```

`temperature` 原来是一个**单独的 `if`**、区间写死 1.5，而其余三项走循环 ——
两套写法正是「只改一处」的温床。现在四项同表，校验与 `min`/`max` 都从它取：

- `saveSettings()` 用同一张表做前端预校验（保留「留空 = 不改」语义）；
- 新增 `applyNumericBounds()`，在 `bindSettings()` 里把区间写进输入框的
  `min`/`max`。HTML 里那对属性降级为**初始值**，以 JS 为准。
- `index.html` 的 `st-temperature` 同步改成 `max="2"`。

**新增断言**

`tests/test_numeric_bounds_consistency.py`（10 条）——**读两个文件比对**，
因为前后端是两种语言、没法共享代码，只能靠断言钉住：

| 断言 | 守住什么 |
|---|---|
| `test_js_bounds_match_backend` | 逐项相等（核心） |
| `test_backend_covers_every_js_field` | 反向：前端多写的字段也算「凭想象设限」 |
| `test_temperature_is_no_longer_narrower` | 把本次缺陷单独钉一遍 |
| `test_html_min_max_matches_backend` | HTML 初始 min/max 也不能错 |
| `test_no_orphan_numeric_input` | 有 min/max 却不在表里的输入框 = 不受约束 |
| `test_bounds_are_well_formed` | 区间非空 + 可保存的字段必须可重置 |
| `test_temperature_accepts_zero` | 0.0 在区间内（P0-2 的配套） |

解析用正则，因此带**非空守卫**：`assert m`（常量改名会明确报错）+ `assert out`
（正则失配导致解析为空时直接红，不让比对空转通过）。

`_verify/verify.js` 新增 2 条，驱动**真实行为**：把 `#st-temperature` 填 1.8、
点保存，断言请求体里确实带上了 `temperature: 1.8`。

**变异检验**

| 变异 | 报红 |
|---|---|
| H 前端 `temperature` 改回 `[0, 1.5]` | 2 条（Python 侧） |
| I 把 `export const NUMERIC_BOUNDS` 改名 | 明确报「没找到…改名了？」——**证明守卫生效**，不是静默通过 |
| J 前端 `temperature` 改回 `[0, 1.5]`（UI 侧） | 2 条，实测输出 `null` = 请求被前端拦下，真实缺陷复现 |

> **又一次「断言空转」，这次是自己抓到的**：UI 侧第一版写的是
> 「断言 `st-temperature` 的 min/max 被 JS 写对了」。写完就发现它**不可能红** ——
> HTML 里那对属性本来就已经是 `0/2`，去掉 `applyNumericBounds()` 也照样绿。
> 于是弃用，改成驱动真实行为（保存请求体里有没有 1.8）。
> 变异 J 证明新写法确实吃劲。
>
> **规律**：**断言「A 导致 B」时，先确认「没有 A 时 B 是假的」。**
> 如果 B 本来就成立，这条断言只是在复述现状。

**实测**：

```
$ pytest tests/test_numeric_bounds_consistency.py -q
..........                                    # 10 passed
$ pytest -q                                   # 全量
147 passed                                    # 137 → 147
$ node _verify/verify.js
116/116 通过                                   # 114 → 116
```

### P2-2 `bindXxx` 的幂等守卫只有 `bindShell` 有 ✅ 已修（2026-09-15）

`ui.js:389` 有 `if (bindShell._bound) return;`，而 `bindSettings()` / `bindOverlays()`
是**裸的**（`bindSessionList` 对 `list`/`box` 各自加了 `_bound`，属第三种写法）。

`ui.js` 顶部注释明写：「修复前『新建行业包完成』会二次 `boot()`，导致 Ctrl+\ 连翻两次」——
说明二次 boot 真实发生过。守卫只补了一处，另两处一旦再遇到二次 boot 就会重复挂监听，
表现为一次点击触发两次保存、`appConfirm` 的 Promise resolve 两次。

#### 实测：为什么只有一部分是真问题

| 绑定函数 | 用什么挂 | 重复调用的后果 |
|---|---|---|
| `bindSettings` | 7 × `addEventListener("input")` | **真重复挂** |
| `T.bindScrollPin` | 1 × `addEventListener("scroll")` | **真重复挂** |
| `bindSessionList` | 2 × `addEventListener`（内部 `bindSearch` 另 2 个） | **真重复挂** |
| `bindShell` | 只有 `onclick =`（+ store 订阅 `on`） | 无害（后写的覆盖前写的） |
| `bindOverlays` | 只有 `onclick =` | 无害 |

所以危害集中在**用 `addEventListener` 的那三个**。但修复不是只补那三个 ——
统一成一条规则，将来谁在 `bindShell` 里加一个 `addEventListener` 也不用再想一遍。

#### 修复：三种写法收成一处

```js
// util.js
export function bindOnce(fn) {
  const wrapped = function (...args) {
    if (wrapped._bound) return undefined;
    wrapped._bound = true;
    return fn.apply(this, args);
  };
  return wrapped;
}
```

五个绑定函数统一改成 `export const bindXxx = bindOnce(function bindXxx() { … });`。
顺带**删掉了两处已经变成死代码的旧守卫**：`bindSearch` 的 `box._bound` 与
`bindSessionList` 的 `list._bound` —— 它们的唯一调用者现在被 `bindOnce` 包住了，
那两行永远不会为真。**留着「看起来在保护」的死代码比没有更糟。**

#### 验证入口：`__ts.rebind()`

`main.js` 的自动化接口上加了一个方法，把五个绑定函数**再跑一遍**：

```js
rebind: () => {
  bindShell(); bindSettings(); bindOverlays(); bindSessionList(); T.bindScrollPin();
},
```

`_verify/verify.js` 的桩在 `<head>` 里包了 `EventTarget.prototype.addEventListener`
计数（桩是阻塞脚本，早于 main.js 执行，计数从页面第一刻就开始）。断言两条：

1. **正对照**：手工挂一个监听器 → 计数必须 +1（证明计数器是活的）
2. **主断言**：`__ts.rebind()` 前后计数**必须一个都不多**

> 这一条断言**同时覆盖五个绑定函数**：任何一个漏了 `bindOnce` 包装都会多出监听器。

#### 变异检验

| 变异 | 去掉哪一处的包装 | 报红 | 多出的监听器 |
|---|---|---|---|
| M1 | `bindSettings` | **1** | +7（正好是那 7 个 input） |
| M2 | `T.bindScrollPin` | **1** | +1（scroll） |
| M4 | `bindSessionList` | **1** | +4（`bindSearch` 2 + 自己 2） |
| M3 | `bindOverlays` | **0** | — |

三点值得记：

1. **M3 是 0 红，而且这是对的。** `bindOverlays` 只用 `onclick =`，重复挂无害，
   所以「监听器数量」这个观测量**看不到它**。它的 `bindOnce` 是**统一规则 +
   将来防呆**，不是当前可观测的缺陷 —— 这条如实记在这里，别让后来者以为
   「M3 没红说明断言有问题」。
2. **M1/M2/M4 同时证明了 `__ts.rebind()` 真的调到了绑定函数** ——
   如果 `rebind` 是个空函数，去掉包装也不会多出监听器。
   所以「主断言会不会空转」这个疑问，由变异检验回答，不需要再加运行时正对照。
3. **M4 的 +4 证明收成 `bindOnce` 没有丢掉 `bindSearch` 原有的保护** ——
   它以前有自己的一份守卫（`box._bound`），删掉之后靠外层包住，行为不变。

### P2-3 `packs/elevator` 与 `agent-skills/elevator` 双份并存且已漂移 ✅ 已修（2026-09-15）

```
相同: banwords.yaml
相同: pack.yaml
相同: knowledge/topics.md
不同: skill.yaml      ← 已漂移
```

`agent-skills/` 已进 `.gitignore`，注释也写明了「是生成物，不是源码…之前被跟踪进仓库，
于是它与 packs/elevator 逐渐漂移」。判断是对的，但**磁盘上这份漂移副本仍在**，
后来者容易把它当真源。

#### 删除前的核对（不是「顺手 rm」）

先逐文件比对，确认删掉不会丢东西：

| 类别 | 文件 | 结论 |
|---|---|---|
| 两边都有 | **21 个，全部逐字节相同**（含 `private/` 下全部 4 个 yaml） | 源包 `packs/elevator` 是完整的 |
| 只有导出副本有 | `SKILL.md` / `tools/check.py` | 导出器从模板生成，不是用户内容 |
| 两边都有但不同 | `skill.yaml` | 就是漂移本身，源包那份才是对的 |

**特别注意 `private/`**：里面有 `products.yaml` / `service.yaml` / `cases.yaml` /
`faq.yaml`，是用户填的商业资料。确认它们在源包里逐字节相同之后才动手 ——
否则删掉的可能是唯一副本。

删完实测导出功能仍正常：`export_agent_skill()` 落盘 18 个文件、`private/` 默认排除。

### P2-4 磁盘卫生 ✅ 大部分已修（2026-09-15）

#### `.gitignore`：从「按扩展名列举」改成一把覆盖

原来是：

```
_verify/_*.txt
_verify/_*.log
_verify/_*.js
_verify/_app.log
_verify/_start.bat
```

**每冒出一类新临时件就要回来补一条** —— `_net_probe.py` / `_scan_trace.py`
两个临时 py 就是这么漏掉的。按扩展名列举永远追不上新写法。改成：

```
_verify/_*
```

（下划线开头 + 任意扩展名。`_verify/` 下没有任何已跟踪文件以下划线开头，
`git check-ignore` 逐个核对过不会误伤 `verify.js` / `csp-probe.js` / `lib/` / `legacy/`。）

> 报告原文提到 `_net_probe.py` / `_scan_trace.py` 仍在磁盘上 —— 实测**已经没有了**
> （之前某轮清掉了），但 `.gitignore` 的漏洞还在，所以规则照补。
> **规则的价值在「下次不再漏」，不在「这次有几个文件」。**

#### 清理结果

| 类别 | 数量 | 结果 |
|---|---|---|
| `_verify/_*.png` / `_*.log` / `_*.bat` | 36 个 | ✅ 已送回收站 |
| `_verify/_cprof`（临时 Chrome profile） | 11 MB | ✅ 已送回收站 |
| `desktop/dist2` … `dist14`（残缺打包产物） | 13 个 | ❌ **没删成**，见下 |

保留：`_verify/legacy/`（有意保留）、`_verify/_*.js`（探针脚本）、
不带下划线的正式产物与工具（`verify.js` / `main-*.png` / `result-*.png` 等）。

#### 13 个 `dist*` 为什么没删成

每个 `desktop/distN` 里只有一个文件：`win-unpacked/resources/app.asar`（约 40 KB）。
后来又试了一轮（跑完 `npm run dist` 之后，确认没有进程占用），把行为摸清了：

| 操作 | 结果 |
|---|---|
| `fs.rmSync`（走本机的 safe-delete 垫片 → 回收站） | `[safe-delete] 操作失败: … Some operations were aborted` |
| `fs.renameSync`（改 `.asar` 文件名） | `EBUSY` |
| `fs.renameSync`（改**父目录**名） | `EPERM` |
| **`fs.writeFileSync(asar, '')`（截断）** | **成功**（文件可写！） |
| 截断之后再 `fs.rmSync` | 仍然失败 |
| PowerShell 的删除命令 | 静默无效果 |

**文件可写、却删不掉也改不了名** —— 这不是文件锁，是环境对 `.asar` 的
**保护性拦截**（`.asar` 是 Electron 应用包格式；本机的沙箱显然把它当成
「不许动」的目标，免得某个进程篡改已安装的应用）。
`Remove-Item` 被静默吞掉也符合这个解释。

**结论：这是环境的既定限制，不是可以绕过去的 bug。**
共约 1 MB（另外 `dist15` 是 193 MB 的旧打包产物，同样是这个原因）。
`desktop/dist*/` 已在 `.gitignore` 里，不影响仓库。
**用户在资源管理器里直接删即可**（沙箱之外没有这层拦截）。

> 我没有为了这 1 MB 去绕过回收站强删 —— 那条底线不值得为可再生成的产物破掉。
> 现在磁盘上多了一个**正经命名**的 `desktop/dist/`（本次真打包的产物，含 96 MB 安装包），
> 那 14 个编号目录是历史遗留，删掉它们只是让 `ls` 清静些。

### P2-5 渲染层缺少 CSP（纵深防御）✅ 已修（2026-09-15）

`desktop/renderer/index.html` 没有 `Content-Security-Policy`。
`contextIsolation: true` / `sandbox: true` / `nodeIntegration: false` 都对，
`preload.js` 只暴露 `isElectron` / `platform`（收敛得很好）。

**转义方面做了全量排查：12 个模块的所有 `innerHTML` 写入点，动态值都过了
`esc()` / `fmtText()` / `textContent`，没有发现可利用的注入路径。**
所以这**不是**一个现成漏洞。但渲染层要展示**模型生成的内容**，且同源加载自
`http://127.0.0.1:<port>` —— 一旦将来某个 `innerHTML` 漏掉转义，没有 CSP 就没有
第二道防线。

#### 修复：收紧到不带 `'unsafe-inline'`

报告原本建议 `style-src 'self' 'unsafe-inline'`。实际做的时候把它收紧了：
渲染层里只有 **5 处**内联 `style=`，全部改成 class 是零风险的：

| 位置 | 原来 | 现在 |
|---|---|---|
| `result.js` 分镜表「时间」列 | `style="width:86px"` | `.col-time` |
| `result.js` 合规表「检查项」列 | `style="width:160px"` | `.col-item` |
| `settings.js` 文件表「大小」列 | `style='width:90px'` | `.col-size` |
| `settings.js` 文件表「说明」列 | `style='width:110px'` | `.col-role` |
| `settings.js` 文件路径单元格 | `style="font-family:var(--mono);font-size:12px"` | `.cell-mono` |

> 顺带一个教训：第一次 grep 只查了 `style="`（双引号），漏掉了 `settings.js` 里
> 两处用**单引号**写的 `style='width:90px'`。查这类「模板串里的 HTML 属性」
> 必须两种引号都查。

`el.style.setProperty(...)` 走 CSSOM，**不受 `style-src` 管辖** —— 界面里那些
动态样式（`card.style.setProperty("--i", i)` 等）照常工作，这是实测确认的（见下）。

最终指令集：

```
default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:;
font-src 'self'; connect-src 'self'; object-src 'none'; base-uri 'none';
form-action 'none'; frame-ancestors 'none'
```

- `img-src ... data:` 是必须的：`styles.css` 里下拉箭头是 `data:image/svg+xml`。
- `script-src 'self'` 够用：`index.html` 没有内联 `<script>`，全项目也没有
  内联事件处理器（`onclick=` 之类 0 处）、没有 `eval` / `new Function`、
  没有动态 `createElement('script')`、没有 `<iframe>` / Worker。
- `connect-src 'self'` 够用：所有请求都走 `api.js` 的 `fetch(path)`，路径全是相对。

下发方式：**挂在中间件上给所有响应加**，不是只挂 `index.html`。
CSP 由**文档**响应决定，逐个挂没有意义；挂全量只是多几十字节，换来的是
「不会有人将来漏挂某一类响应」。

#### 实测：CSP 到底拦得住哪几种注入

新增 `_verify/csp-probe.js`（零依赖探针，自带无头 Chrome）把这件事固化成可复现的：

| 注入向量 | 结果 | 违规指令 |
|---|---|---|
| `setAttribute('style', …)` | **拦** | `style-src-attr` |
| `innerHTML` 带 `style` 属性 | **拦** | `style-src-attr` |
| `insertAdjacentHTML` 带 `style` | **拦** | `style-src-attr` |
| `createElement('style')` + textContent | **拦** | `style-src-elem` |
| `createElement('script')` + textContent | **拦** | `script-src-elem` |
| **CSSOM `el.style.setProperty`** | **放行** | — |

最后一行是承重的：界面里所有动态样式全靠它。

**两个踩到的坑（探针第一版就栽在第一个上）：**

1. **`securitypolicyviolation` 是异步派发的。** 注入完立刻读计数会得到 0 ——
   第一版探针就是这么写的，6 个向量全报「没拦」，而实际拦了 14 次。
   每个向量之间必须等一拍。
2. **一次违规派发 2 条事件**（Chromium 重复上报）。所以断言只能写「增加了」，
   不能写「恰好 +1」。

#### 测试侧的坑：桩服务不发 CSP → 断言全空转

`_verify/verify.js` 的桩引擎原本把桩脚本**内联**进 `index.html`，而且**不发 CSP 头**。
如果就这么加断言，页面会在「没有 CSP」的环境里跑完全部断言 —— 全绿，什么也没验证。

两处改动：

1. **桩脚本改成同源外部脚本 `/_stub.js`**，注入点从 `</body>` 前挪到 `<head>` 开头。
   内联脚本在 `script-src 'self'` 下会被直接拦掉，而「为了让它跑起来给测试环境
   放宽 CSP」正是断言空转的温床。改成外部脚本后桩自己也在生产 CSP 下运行，
   顺带证明了「同源外部脚本能加载」。放在 `<head>` 是**阻塞执行**，先于 body 解析 ——
   连初始 HTML 里的内联样式都逃不掉。
   （⚠ 改成外部脚本时忘了剥掉 `stubScript()` 里的 `<script>` 包装 → 整个文件以
   字面量 `<script>` 开头 → 语法错误 → 桩完全没跑起来。这个坑踩过一次。）
2. **`readCsp()` 从 `app/server.py` 里读出真实的 CSP 常量**，由桩服务原样下发；
   解析失败**直接抛**，绝不允许「解析不出来 → 静默跳过 CSP 断言」。

#### 验收

- `tests/test_server_hardening.py::test_renderer_sends_csp`（新增 1 条）：
  头在、10 条指令齐全、**没有 `unsafe-inline` / `unsafe-eval`**、子资源也带。
- `_verify/verify.js` 新增 5 条：
  - 桩服务下发的 CSP 与生产**逐字相同**（测试环境不宽松于生产）
  - `style-src 'self'` 且无 `unsafe-inline`
  - **正对照**：故意写一次内联 `style`，必须被拦下并记到
  - 真实点击「导出 SRT」覆盖 `blob:` 下载路径
  - 全流程（6 次 `Page.navigate` + 生成全流程 + 下载）**零违规**

```
pytest    202 → 203
verify.js 126 → 131 / 131
```

#### 变异检验

| 变异 | 改回什么 | pytest | UI |
|---|---|---|---|
| M1 | 中间件不下发 CSP 头 | **1** | 0（保持绿） |
| M2 | `style-src` 加回 `'unsafe-inline'` | **1** | **2** |
| M3 | 把一处 class 改回内联 `style="width:86px"` | 0 | **1** |
| M4 | 桩服务不下发 CSP（测试环境比生产宽松） | 0 | **3** |

三条值得记：

1. **M1 里 UI 侧保持绿是正常的，但要说清为什么。** 桩服务的 CSP 来自
   `readCsp()` 读的常量，不是来自真实引擎的中间件 —— 所以「引擎忘了下发」
   这件事 UI 测不到，那是后端断言的职责。**两组断言各守一件事**：
   后端守「头真的下发了」，UI 守「下发之后不拦坏东西」。
2. **M3 只红了 1 条，而且布局断言全绿。** 这正是重点：内联样式被 CSP 静默忽略后，
   列宽失效但**不破坏任何几何断言** —— 没有这条哨兵，这个回归会一路静默过去。
3. **M2 里「正对照」也跟着红了**（`before:0, after:0`）：开了 `'unsafe-inline'`
   之后内联样式不再被拦，正对照自然抓不到违规。这说明**正对照本身有分辨力**，
   不是一条永远为真的装饰。

#### 顺带说明：这条修法为什么不是「为了安全牺牲体验」

5 处内联样式改 class 之后，列宽行为**完全不变**（值逐字照搬）。
而 `style-src` 收紧带来的是：谁以后再往模板里写 `style="..."`，
**那条断言会立刻报红**，而不是等到用户发现某列宽度不对。

#### 可能的后继（本次没做）

`X-Content-Type-Options: nosniff` 是同一类纵深防御，一行就能加。本次刻意没带 ——
P2-5 是 CSP，把无关指令塞进来会让变异检验的分辨力变差（哪条红了说不清是谁的功劳）。
需要的话单独开一项。

### P2-6 工作区有两处未提交的真实安全修复 ⚠️

`git status` 显示 `app/server.py` 与 `tests/test_server_hardening.py` 已修改未提交，内容是：

| 修复 | 内容 |
|---|---|
| `get_pack` 补 `_safe_name` | 此前 `GET /api/packs/%2e%2e` 会命中 `Pack(root, "..")`，回 200 并附带 `base.rglob("*")` 的**整棵目录树清单**（含 `config.yaml` 与各包 `private/` 的文件名与体积） |
| `temperature` 区间校验 | 999 / -5 曾能存进 `config.yaml`，之后**每次生成**都带着越界值请求模型 |

两处都是对的、注释也写清了理由。但它们悬在工作区，**任何一次 `git checkout` / 切分支都会丢**。
建议立即提交。（注：这两处正是 P0-2 要接着修的地方 —— temperature 的区间放开到 `0.0`
却读不回来，提交时最好把 `config.py` 一起改掉。）

### P2-7 `list_packs` 吞 `Exception` 且不记日志（包静默消失）✅ 已修

**位置**：`app/knowledge.py` 的 `list_packs` / `pack_info`

```python
try:
    out.append(pack_info(d))
except Exception:               # noqa: BLE001
    continue
```

「单个包写坏不该让整个列表崩掉」这个取舍是对的，但代价被藏起来了：
包会**从列表里静默消失**，用户看到的是「我的包不见了」，而不是「我的包坏了」。

#### 实测：比「少一条日志」严重得多（2026-09-15）

修 P1-5/P1-6 时以为这片代码已经安全了 —— **不对**。P1-5/P1-6 修的是
「YAML 语法错」，而这里有**另一类坏法：YAML 语法完全正确、结构不符合约定**，
它不触发 `YAMLError`，所以 `err` 是空的，P1-5/P1-6 的修复**盖不住**：

```
pack.yaml 里 version: v2（写成字符串）
  → pack_info 抛 ValueError: invalid literal for int() with base 10: 'v2'
  → 被 `except Exception: continue` 咽掉
  → list_packs 返回 []            ← 包从界面上消失，用户以为包丢了
  → 而 /api/generate 那边 Pack() 抛的是**裸 ValueError** → HTTP 500
    （既不是 404 也不是 409，前端只能显示「服务器错误」）

pack.yaml 里 params.segment: 家用电梯（字符串当映射用）
  → 同上：list_packs 返回 [] + Pack() 抛裸 AttributeError → 500
```

注意 `list_packs` 返回 `[]` 与「这个 root 下真没有包」**长得一模一样** ——
这正是「静默」的定义。

#### 修复

| 改动 | 说明 |
|---|---|
| `pack_info` 变成**全函数**（保证不抛） | 兜住「内容不符合约定」这类：记一条带 traceback 的 WARNING，返回带 `pack_error` 的降级 `PackInfo`（`name`/`display_name` 退成目录名） |
| `list_packs` 的裸 `except Exception: continue` 收窄成 `except OSError` | 只兜「目录本身读不了」（权限 / 被占用），且**必须 `log.warning`** |
| 顺带 | `Pack()` 对这类包现在抛 `PackBrokenError` → **409**，不再是 500 |

`pack_info` 的异常分支**刻意不缓存**（与 `read_yaml_cached` 不同）：它只由
`/api/meta` 与 `Pack()` 调用，不是每次生成读十几遍，一条 WARNING 不算刷屏；
而缓存反而会让「改好了」之后仍显示旧错误。

界面文案跟着改：「这个行业包读不出来」→「这个行业包**不可用**」——
两类坏法（语法错 / 结构不对）它都要说得准确。

#### 验收

```
pytest                        170 → 184   （新增第 7 组 14 条）
node _verify/verify.js        121/121
```

变异：M7（`pack_info` 把异常放出去）**12 红** /
M8（`list_packs` 目录失败不记日志）**1 红** /
M9（`pack_info` 的内容错误不记日志，但仍返回 `pack_error`）**5 红**。

M9 的分辨力值得记一笔：它**只**让「日志断言」变红，「包还在列表里」那几条仍绿 ——
证明这两组断言各自守着不同的性质（前者守「不静默」，后者守「不消失」），
而不是互相重复。

### P2-8 `quota_degraded` 只写不读（降级信号没人消费）✅ 已修（2026-09-15）

**原位置**：`app/pipeline.py:261` 写入；`desktop/` 全目录搜不到任何读取

```bash
$ grep -rn "quota_degraded" desktop/ --include=*.js --include=*.html --include=*.css
(空)
```

`quota_degraded` 是「行业包没配 `quota_table`，字数配额退化成通用值」的信号 ——
正是本项目一直在强调的那类「必须可见」的降级。它被算出来、写进 `params`、
然后**没有任何地方读它**。

#### 实测：比「只写不读」严重得多 —— 它压根没写出去

用一个**没配 `quota_table`** 的电梯包跑一次真实（mock）生成：

```python
① 内存 job.params 有 quota_degraded 吗: True -> True
   quota: {'total': 256, 'hook': 38, 'body': 166, 'cta': 51}

② result.json 存在: True
   正文里出现 quota_degraded 字样: False        # ← 一个字都没有
   result.params 的键: ['audience','cta','duration','format','mode','persona',
                        'platform','rate','segment','style','topic','voice']
   result.quota: {'total': 256, 'hook': 38, 'body': 166, 'cta': 51}
```

全库 67 份历史产物里，出现 `quota_degraded` 的：**0 份**。

原因有三重，每重都能单独把信号吃掉：

| # | 位置 | 发生了什么 |
|---|---|---|
| 1 | `pipeline._finalize` | `raw["params"]` 是一份**手写白名单**（12 个键），`quota_degraded` 不在里面 → 落盘时被滤掉 |
| 2 | `schemas.ScriptResult` | 契约里根本没有这个字段。而 `model_validate` 对**缺失**字段只取默认值、**不报错** → 契约层一声不吭 |
| 3 | `desktop/` | 就算前两重都通了，也没有任何地方读它 |

第 1 重是「白名单漏了」，第 2 重是「Pydantic 对缺失字段沉默」，
第 3 重才是报告原本写的那一条。**只修第 3 重（前端加个横幅）什么也修不到 ——
因为前端拿到的数据里就没有这个字段。**

而它造成的后果是这样的（降级包 vs 正常包）：

```
降级包 quota: {'total': 256, 'hook': 38, 'body': 166, 'cta': 51}
正常包 quota: {'total': 261, 'hook': 40, 'body': 171, 'cta': 50}
```

两份都是**合法、看着完全正常**的配额。界面照常显示「261 字」、
每段卡片照常显示「12/85 字」—— 用户和包作者都看不出这份配额
不是为本行业定制的。这就是「静默」的定义。

#### 修复

| # | 改动 | 说明 |
|---|---|---|
| 1 | `schemas.ScriptResult` 加 `quota_degraded: bool = False` | 默认 `False` 是给**老产物**用的（它们没这个键），新产物一律显式给值 |
| 2 | `pipeline._finalize` 的 `raw` 带上它，且用 `p["quota_degraded"]` 不用 `.get()` | 键缺失时这里必须炸；「缺了就当成没降级」正是要整治的静默 |
| 3 | `pipeline` 抽出三张表 `PERSISTED_PARAMS` / `PARAMS_ELSEWHERE` / `PARAMS_DROPPED` | 把「`_normalize` 的每个键去哪」写成**显式声明**，不再是一份手写白名单 |
| 4 | **新增** `pipeline.check_contract_complete(raw)` | 契约守卫，见下 |
| 5 | `desktop/renderer/js/result.js` | 与 `dropped_short` 同一套 `<details class="banner info">`：折叠行说「配额是估的、不是本行业配的」+ 具体数字，展开后给 `quota_table` / `pack.yaml` 的补救办法 |
| 6 | `store.render_script_md` | 导出的 `脚本.md` 也带一行降级说明 —— 只在界面提示、导出后看不出，降级又变回不可见 |
| 7 | `knowledge.param_audit` | **生成前**就能看出这个包没配额表，见下 |

#### 顺带修掉：审计本身在说错话

`param_audit` 已经会标「时长参数没定制」，但**整表缺失**时它写的是
「字数配额按相邻时长插值」—— 而实际路径是「按 时长×语速 估一个通用值」。
包作者照这句话去补相邻档位，**补完仍然是降级**。审计把人引到了错的方向。

顺带发现第三种情况也没说对：`quota_table` 只有一档时，`Quota._row` 的行为是
「任何时长都取这一档」（180 秒拿到 60 秒的配额），说成「插值」同样误导。

现在三条路径各自说清：

| 情形 | 真实行为 | 说明文案 |
|---|---|---|
| `quota_table` 整个缺失 | 按 时长×语速×0.95 估算 | 整个 quota_table 缺失：字数配额按 时长×语速 估算 |
| 只有一档 | 任何时长都取该档 | quota_table 只有 60 秒一档：任何时长都取这一档，配额不随时长变化 |
| 有表、缺该档 | 按相邻档位插值 | 字数配额按相邻时长插值 |

#### 新增守卫：`check_contract_complete`

修 P2-8 时意识到，`model_validate` **只卡得住「类型对不对」，卡不住「字段有没有」**：

- **少**给一个字段 → 静默取默认值（`quota_degraded` 就是这么丢的）
- **多**给一个字段 → 静默丢弃（Pydantic 默认忽略未声明的键）

两头都是「信号掉了但没人知道」。所以 `_finalize` 在 `model_validate` **之前**
显式要求 `raw` 与契约字段集合**完全一致**。

> ⚠ 这条守卫是被**变异检验逼出来的**：第一版测试只写了「产物键集合 == 契约字段集合」，
> 而 Pydantic 会**先**把多余键丢掉，产物键集合照样等于契约 —— 往 `raw` 里塞一个
> 假键，15 条断言**全绿**。等于断言空转。守卫必须做在 `model_validate` 之前才有效。

用显式比对而不是 `ConfigDict(extra="forbid")`：后者只堵「多」这一头，
而且会一并改变 `_run_rewrite_segment` 里那次「读旧产物再校验」的语义 ——
升级路径上的宽容度不该被顺手收掉。

#### 验收

新增 `tests/test_quota_degraded_signal.py` **18 条**；`_verify/verify.js` 新增 12f 组
**5 条**（含一条对照：正常包不出现该提示）。

```
pytest   184 → 202
verify.js 121 → 126 / 126
```

关键断言：

- `test_degraded_signal_reaches_disk` —— 内存里对、磁盘上也要对
- `test_degraded_signal_is_not_always_true` —— 正常包必须为 `False`（防「恒为 True」的假绿）
- `test_degraded_quota_is_indistinguishable_without_the_flag` —— 两份产物的 quota 数字
  结构与量级都正常，**只有 flag 能区分**（把「为什么需要它」钉在测试里）
- `test_contract_guard_rejects_a_stray_key` / `..._a_missing_key` —— 守卫两个方向
- `test_contract_guard_accepts_the_real_thing` —— 前提守卫，防「测一个永远会炸的函数」
- `test_normalize_keys_are_all_accounted_for` —— `_normalize` 的每个键都要表态去哪
- `test_everything_in_elsewhere_really_lands_in_the_result` —— 声明了落点就得真的找到
- `test_segment_rewrite_keeps_the_signal` —— 单段重写会重建整份 result，别把信号洗掉
- `test_healthy_pack_audits_clean` / `test_param_audit_still_says_interpolation_when_the_table_exists`
  —— 前提守卫：防「三条路径的断言在测空气」

UI 侧（12f）：横幅是可展开的 `<details>`、折叠行含「估算 / 不是本行业 / 261」、
展开后有 `pack.yaml` + `quota_table` 的补救办法，以及那条证据断言 ——
**降级后的每段卡片照样显示「12/85 字」，与正常配额在界面上无法区分**。

#### 变异检验

| 变异 | 改回什么 | 报红 |
|---|---|---|
| M1 | `_finalize` 的 `raw` 去掉 `quota_degraded` | **5** |
| M2 | 契约字段改名（`raw` 留着） | **7** |
| M3 | `result.js` 的横幅块短路掉 | **3**（UI） |
| M4 | 往 `raw` 塞一个没表态的键 | **13**（生成直接失败，守卫炸） |
| M5 | `param_audit` 三条路径合并回「插值」 | **2** |
| M6 | `render_script_md` 的降级说明去掉 | **1** |
| M7 | 重写路径把 `quota_degraded` 过滤掉 | **1** |

两点值得记：

1. **M2 没让「产物键集合 == 契约」那条报红** —— 因为两边一起改名时它本来就该绿。
   它守的是「一致」，守「字段存在」的是 `test_contract_declares_the_signal`。
   **一条变异打红一片时该怀疑有搭便车的；反过来，一条变异没打红某条，也要问清
   它守的到底是不是这件事。**
2. **M3 第一次跑时把验证脚本弄崩了**（横幅缺失 → `qdeg.chips` 是 `undefined`
   → 断言里取 `.length` 抛异常 → **后面所有断言（含布局组）都不执行**）。
   红是红了，但红得毫无信息量，还掩盖了后续回归。已改成「横幅缺失也把 chips 带回去」，
   现在 M3 是干净的 123/126。**验证脚本自身的健壮性也是断言质量的一部分。**

#### 与 P2-7 是同一族问题的两面

- P1-6：**信号依赖的数据**坏了 → 信号失效；
- P2-7：`except: 继续` 只堵住看得见的那类异常 → 包静默消失；
- P2-8：数据好、信号也算对了 → **信号没人接**（而且先被落盘白名单吃掉）。

三种写法都让「防静默降级」的机制形同虚设。合起来看，「静默降级」在本项目里
已经见过**四种形态**：①信号依赖它要审计的数据；②信号算对了但没人接；
③同一个兜底口径抄了两份；④`except: 继续` 只堵住看得见的那类异常。

---

## 已确认正常的部分（避免重复排查 / 避免误判）

审查里有几处**看起来像问题、核实后是好的**，记在这里省得下次再查一遍：

| 疑点 | 核实结论 |
|---|---|
| 前端 `innerHTML` 是否 XSS | ✅ 12 个模块全量排查，动态值一律过 `esc` / `fmtText` / `textContent` |
| 本地 API 是否可被任意网页访问 | ✅ 三层到位：Host 白名单（挡 DNS rebinding）+ Origin 同源 + `/api/*` 一次性令牌 + `hmac.compare_digest` |
| 路径穿越（`%2e%2e` / `%2f`） | ✅ `pack_file` 用 `resolve()` + `is_relative_to`；`export_skill` 的 `_frontmatter_name` 会把 `..` 规整成 `-`；`_safe_jid` 的正则同时挡了 glob 通配 |
| `write_atomic` 的并发安全 | ✅ 临时文件名带 pid + uuid4（避开同进程两线程撞名），`fsync` 后才 `replace`，有限重试覆盖 Windows 杀毒软件占用 |
| 状态机并发（TOCTOU） | ✅ `transition()` 检查与置位同锁，`update()` 显式拒绝 `state` 键；并发额度曾漏网（P1-4），已由 `add_if_room()` 补齐 |
| 「停止」会不会变成「失败」 | ✅ `_fail()` 显式跳过已取消作业；`_delta_handler` 在收包过程中就检查取消 |
| 落盘顺序不变量 | ✅ `_finalize` 与 `_run_rewrite_segment` 都守住了「先落盘、后置 done」/「先迁移、后落盘」 |
| 前端会话列表频繁重画导致点击丢失 | ✅ 已改成增量复用 DOM 节点（同一条记录复用同一节点） |
| 空内容诊断（推理模型思考吃光 token） | ✅ `_ensure_content` 在流式与非流式两个分支都生效 |

**特别值得保留的设计**：`knowledge.param_audit()` 把「用户能选、但本包没定制」的参数值
算成一句人话降级说明下发前端。这是把「静默降级」摊开给人看的正面例子 ——
建议 P0-2 / P1-1 / P1-2 的修法都参照它：**不要只是修好，要让不可见的失效变得可见**。

---

## 不建议动的部分

沿用 `_verify/verify.js` 的既有约定，这几处改之前请先想清楚：

- **`.sess-del` 常驻 `opacity:.5`**（改回 hover 会「删除要点好几次」）；
  `.sess-topic` 截断 187px（给删除按钮留 31px）；会话行高 49px。
- **左栏六段间距统一 16px**，不要引入第二种节奏；项间距用容器 `gap` 不用子项 `margin`。
- **`#session-list` 不许贴底**（不许 `margin-top:auto`）。
- **设置页不要再加搜索框**（它是唯一会隐藏导航项的入口）。
- **`_verify/` 命名约定**：下划线开头 = 临时件；`main-*.png` / `result-*.png` /
  `settings-*.png` 是回归引用的正式产物，不能删。

---

## 建议的动手顺序

> **执行进度（2026-09-15）**：**17 项全部处理完毕并验证** ——
> P0-1 / P0-2 / P0-3 / P1-1 / P1-2 / P1-4 / P1-5 / P1-6 /
> P2-1 / P2-2 / P2-3 / P2-4 / P2-5 / P2-7 / P2-8 已修；
> P1-3 已落地并**真跑过 `npm run dist`**（安装包 96 MB，app 启动命中出厂运行时、
> `/api/health` 回 200）；P2-6 已随主提交入库。
> 回归：pytest **212**、`node --test desktop/` **11/11**、
> `_verify/runtime-smoke.js` **10/10**（构建树与打包产物各跑一遍）、`verify.js` **133/133**。
> 唯一遗留：14 个 `desktop/dist*` 旧目录删不掉 —— 环境对 `.asar` 的保护性拦截，
> 需在沙箱外手动删（不影响仓库，已 gitignore）。

1. **提交工作区那两处修复**（P2-6）—— 零成本，先止血。
2. **P0-2**（temperature）—— 改 3 行，且与刚提交的改动同源，趁热一起改 + 补断言。
3. **P0-1**（索引口径）—— 改 1 个函数，收益最大（生成期间每 3 秒省一次全量解析）。
4. **P0-3**（并发额度）—— 改 `running_count` 一行，或加超时。
5. **P1-1 / P1-2** —— 各改一处出口 + 补日志。
6. **P2 系列** —— 一致性收口，可合并成一次「规范化」提交。

修完之后建议给前四项各补一条断言（现有 65 条测试没有覆盖这些边界）。

---

## 附：本轮沉淀的通用规律

这几条是修的过程中反复踩到才写下来的，比单个 bug 更值得留：

1. **`or 默认值` 只在「0/空是非法值」时才安全。** `temperature` 的合法区间含 0，
   于是 `x or 0.7` 把它吃掉了。判「键缺失」要用 `is None`，不要用真值判断。（P0-2）
2. **写探针/测试数据一律用 `yaml.safe_dump` 写盘，不要手拼 YAML 文本。**
   手拼的 `\n` 没转义 → 解析失败被静默吞掉 → 把 1 个 bug 误判成 2 个。
   **这个假阳性本身就是 P1-2 的现场演示。**
3. **「先筛 / 先构造，再断言」必须单独验证筛选或构造没空**（`assert out`）。
   踩过两次：`querySelectorAll` 过滤后列表为空 → 断言空转；
   用**字符串**输入构造「YAML 异常带正文」场景，而生产路径走文件流 → 脱敏断言空转。
4. **断言「A 导致 B」时，先确认「没有 A 时 B 是假的」。**
   UI 侧曾想断言「min/max 被 JS 写对了」，但 HTML 属性本来就对 → 去掉实现也全绿。
   改成驱动真实行为（保存请求体里有没有 1.8）才有意义。（P2-1）
5. **并发测试不加插桩就是碰运气。** 光靠 `threading.Barrier` 起跑，
   老实现可能碰巧不超额（实测：变异后不插桩那条测试全绿，插桩的才抓到）。
   夹具里要先抓住原始函数再 monkeypatch，否则自我递归。（P1-4）
6. **「防静默降级」的机制自己也会静默失效，已经见过四种形态**：
   - **信号依赖它要审计的数据** → 数据坏了，审计一起哑掉（P1-6 的 `param_audit`）；
   - **信号算对了但没人接** → 只写不读（P2-8 的 `quota_degraded`）；
   - **兜底口径抄了两份** → 下次只修一半（P1-2 / P1-5 的 `_yaml_error_brief`）；
   - **`except: 继续` 只堵住看得见的那类异常** → 包静默消失（P2-7）。

   加一个「可见性」机制时，要同时问三句：
   **它依赖的东西坏了怎么办** / **谁在消费它** / **它到得了消费者手里吗**。
   第三句是 P2-8 补的 —— 信号算了、也算对了，但在落盘那一步被白名单滤掉，
   前两句都答对了也没用。
7. **静默降级有三种写法，都要盯**：无日志的裸 `except`；
   把非法值当成「没填」；**兜底口径散在多处**（`checker` 有、`pipeline` 没有 → P1-1）。
8. **「同一个兜底口径抄了两份」就是下一次只修一半的原因。**
   `read_config_file` 与 `save_config` 各抄了一份 try/except，两处都静默（P1-2）；
   `_yaml_error_brief` 修好之后，`knowledge` 那边还是 `read_text()`（P1-5/6）。
   对策不是「记得都改」，而是**把口径收成一个函数**——这次把它上移到 `fileio`，
   读 YAML 全项目只走 `read_yaml_file`。
9. **让调用点「必须做选择」比「记得做选择」可靠。** `read_yaml_cached` 的返回
   从 `dict` 改成 `(dict, str)` 之后，6 个调用点全都被迫显式表态；
   如果只是「顺手加一条 WARNING」，下一个新增的调用点照样会静默。
10. **安全性质不要取决于「用了哪种写法」。** `open(p)` 与 `p.read_text()`
    只差一行，但只有前者不泄漏 YAML 出错行的正文（P1-2 的 `str(e)` 实测）。
    所以脱敏做在**出口函数**里，而不是指望每个调用点选对读法。
11. **两层防线要么各有一条独立的证伪测试，要么就别留两层。**
    这次构造期检查与出口守卫并存，一开始 M3（去掉出口守卫）**只红 1 条** ——
    差点因为「构造期已经查过了」把它当成冗余删掉。
    补了 `test_banwords_guard_survives_a_change_after_construction`
    （构造后再改坏文件）之后，两层各自都可证伪，才是真的两层。
12. **变异检验要能证伪自己，也要能识别「改一层不该红」。**
    M5（把 `open(p)` 换成 `read_text()`）**0 红**，但这不是断言失效 ——
    是 `yaml_error_brief` 本来就不用 `str(e)`，两条防线各自都够。
    实测 M5+M6 组合才 19 红。**把「单层不红」的原因写进注释**，
    否则下次有人会误判成「这条断言是假的」然后删掉它。
13. **shell heredoc 会弄坏反斜杠转义。** 探针里 `"...\n..."` 写进文件变成了
    字面 `/n`，于是同一条坏 YAML 在两次探针里报出不同的行列号，
    白查了一轮。**探针别用 heredoc 传含反斜杠的字符串**（用 Write 落盘）；
    **断言里不要硬编码行列号**，用 `第 \d+ 行第 \d+ 列` 正则。
14. **「语法错」和「结构错」是两类坏法，修一类盖不住另一类。**
    P1-5/P1-6 修完 `YAMLError` 之后，`version: v2`（YAML 合法、结构不对）
    照样能让包**从列表里消失**，而且 `Pack()` 抛的是**裸异常 → HTTP 500**。
    **凡是 `try: ... except: 继续` 的地方，都要问一句「这里的异常到底有几种来源」** ——
    只堵住看得见的那一种，剩下的会在你以为修完的时候继续静默。
15. **「不崩」与「说清楚」是两条要求，别只满足前一条。**
    `except Exception: continue` 满足了「不崩」，代价是把「包丢了」变成了
    用户唯一能看到的信息。而且 `list_packs` 返回 `[]` 与「真的没有包」
    **长得一模一样** —— 这正是「静默」的定义。
16. **同一段代码里的两条断言若守的是不同性质，变异检验要能把它们分开。**
    M9（内容错误不记日志，但仍返回 `pack_error`）**只**让日志断言变红、
    「包还在列表里」那几条保持绿 —— 这才证明两组断言不是互相重复。
    **如果一条变异把所有相关断言一起打红，就该怀疑其中有些是搭便车的。**
    反过来也成立：**一条变异没打红某条断言时，要问清那条断言守的到底是不是这件事。**
    P2-8 的 M2（契约字段改名）没让「产物键集合 == 契约」报红 —— 因为两边一起
    改名时它本来就该绿。它守的是「一致」，守「字段存在」的是另一条。
17. **契约的沉默有两头，守卫要两个方向都堵。**
    `model_validate` 卡的是「类型对不对」，不是「字段有没有」：
    **少**给一个字段 → 静默取默认值（`quota_degraded` 就是这么丢的）；
    **多**给一个字段 → 静默丢弃。两头都是「信号掉了但没人知道」。
    所以守卫要做在 `model_validate` **之前**，显式比对字段集合。
18. **「挑字段落盘」的白名单是静默降级的温床。**
    `{k: p[k] for k in (12 个硬编码键)}` 这种写法，新增字段时不会报错、
    也不会有人记得改它 —— 信号就无声地掉了。改成**三张显式声明的表**
    （进 params / 另有落点 / 明确不要且附理由）+ 一条断言
    （`set(_normalize 的产出) == 三张表的并集`），新增键时**必须表态**。
    这和第 9 条是同一个手法：让调用点「必须做选择」。
19. **变异检验不只验证「断言会不会红」，还会暴露「断言红了之后有没有把后续断言带走」。**
    P2-8 的 M3 第一次跑时把验证脚本弄崩了：横幅缺失 → 取 `.length` 抛异常 →
    **后面所有断言（含布局组）都不执行**。红是红了，但毫无信息量，
    还掩盖了后续回归。**验证脚本自身的健壮性也是断言质量的一部分** ——
    断言里凡是对「可能不存在的东西」取属性，都要先兜住。
20. **审计/提示机制说错话，比不说更糟。**
    `param_audit` 在整表缺失时说「按相邻时长插值」，而真实路径是
    「按 时长×语速 估算」—— 包作者照这句话去补相邻档位，**补完仍然是降级**。
    提示的价值在于「照它做就能修好」；说错方向等于把人往坑里带。
    **加提示时要把「用户照它做了会发生什么」走一遍。**
21. **「测试环境比生产宽松」是断言空转的头号温床。**
    加 CSP 时，桩引擎既不发 CSP 头、又把桩脚本内联进 HTML —— 页面于是在
    「没有 CSP」的环境里跑完全部断言：全绿，但什么也没验证。
    对策有两条，缺一不可：
    **① 测试用的安全配置必须从生产源读出来**（`readCsp()` 解析 `app/server.py`），
    解析失败就**抛**，不许「解析不出来 → 静默跳过」；
    **② 断言「测试下发的 == 生产那份」**，让宽松本身变成一条会红的断言。
    这条对任何「配置类」断言都成立（CSP / 超时 / 上限 / 白名单）。
22. **浏览器事件是异步派发的，读状态前要等一拍。**
    `securitypolicyviolation` 注入后立刻读计数会得到 0 —— 探针第一版 6 个向量
    全报「没拦」，而实际拦了 14 次。同类还有 `MutationObserver`、`transitionend`。
    另外**同一次违规可能派发多条事件**，所以断言写「增加了」，不写「恰好 +1」。
23. **「零 X」必须有正对照，否则它可能只是「X 根本没被测量」。**
    「全流程零 CSP 违规」听起来很强，但监听器没装上时它**同样全绿**。
    所以要先故意造一次违规、断言被抓到 —— 正对照本身也要有分辨力：
    M2（开了 `'unsafe-inline'`）时它跟着红了，证明它不是一条永远为真的装饰。
24. **查「模板串里的 HTML 属性」要两种引号都查。**
    第一次 grep 只写了 `style="`，漏掉 `settings.js` 里两处 `style='width:90px'`。
    模板串里单双引号都可能出现，**只查一种等于没查**。
    同理：查 `onclick=` 也要连 `onclick='` 一起。
25. **忽略规则别按「枚举」写，按「类别」写。**
    `.gitignore` 原来一条条列 `_verify/_*.txt` / `_*.log` / `_*.js` / `_app.log` /
    `_start.bat` —— 每冒出一类新临时件就要回来补一条，`_*.py` 就是这么漏的。
    改成 `_verify/_*`（下划线开头 + 任意扩展名）之后不用再维护。
    **判据**：一条规则如果「新增同类东西时需要改它」，那它写错了粒度。
26. **删之前先核对「这是不是唯一副本」，而不是「它看起来像生成物」。**
    `agent-skills/` 明显是导出产物、也在 `.gitignore` 里 —— 但它里面有
    `private/products.yaml` 这类用户填的商业资料。**逐文件比对确认源包里
    21 个共同文件逐字节相同**之后才动手。凭「它应该是生成的」就删，是在赌。
27. **变异 0 红要区分两种原因：「断言失效」与「这个观测量本来就看不到它」。**
    P2-2 的 M3（去掉 `bindOverlays` 的包装）0 红 —— 因为它只用 `onclick =`，
    重复挂无害，而断言观测的是「监听器数量」。这不是断言有问题，
    是这条修复本身**当前不可观测**（价值在统一规则 + 将来防呆）。
    **把这种区分写进注释**，否则下一个人会误判成「断言是假的」然后删掉它。
28. **工具链做不到的事，如实报告，不要为了小收益破底线。**
    14 个 `dist*` 删不掉（里面的 `app.asar` 既进不了回收站、也改不了名）。
    查清后发现是**环境对 `.asar` 的保护性拦截**：文件**可写**（能截断成 0 字节）
    却删不掉、改不了名 —— 不是文件锁。`.asar` 是 Electron 应用包格式，
    沙箱显然把它当成「不许动」的目标。
    合计约 1 MB、且是可重新生成的残缺产物 —— **列清单、说清原因、交给用户**，
    而不是绕过回收站强删。**「查清为什么做不到」和「做到」一样有价值**：
    前者能让人判断该不该换个环境做。
29. **「跨版本安装」是静默失败的高发区，必须有一条功能性自检兜底。**
    构建内嵌 Python 时，宿主是 3.14 而运行时钉 3.13 —— `pip install --target`
    不指定 `--python-version` 就按**宿主** ABI 挑 wheel，装进去 `cp314` 的扩展。
    **脚本报「完成」，目录 38.7 MB 看着完全正常**，只有真去 import 才炸。
    对策不是「记得加参数」，而是**在构建末尾用产物自己的解释器 import 一遍**：
    这类问题的共同点是「构建期一切正常、运行期才炸」，只有真跑一次才拦得住。
30. **「顺序」这种性质，读码验不了，得把它变成可执行的东西。**
    P1-3 的核心是「出厂运行时排在 `engine.exe` 之前」。写在 `main.js` 里就只能靠
    读 `if` 的先后 —— 看漏和改错都太容易。把纯逻辑从框架壳里拆出来
    （`engine-path.js`，注入 `exists`），`node --test` 就能把每条路径与先后钉住。
    **判据**：一段逻辑如果「对了看不出来、错了也看不出来」，它就该能单测。
31. **忽略/暂存目录的「已存在就跳过」是个陷阱。**
    自检要临时铺一份 `app/` 到构建目录。第一版写的是「已存在就跳过」——
    上一次跑崩留下的残留让这次直接跳过，而跳过之后 `staged` 是空的、
    清理也不会删它，残留一直攒着。**改成「总是先删再铺」**：
    暂存目录的语义是「临时件」，不该有「保留上一次」的余地。
32. **把「从哪条路起的」打进日志。** `resolveEngine` 命中哪一条以前只存在于
    `if` 的顺序里，引擎起不来时第一件要问的恰恰是这个。加一个 `via` 字段
    既进了日志、又成了断言的抓手。后来真启动打包产物时，正是这行日志
    （`engine (engine/py（出厂运行时）): …`）一次证明了整条链路走对了。
33. **「静态断言 + 构建树冒烟」不等于「产物能用」，最后一定要拿产物本身跑一遍。**
    P1-3 前面已经有 20 条断言（9 条读配置 + 11 条测顺序），但
    「electron-builder 真的会调那个钩子吗」「产物里真的有解释器吗」仍是未知。
    真跑 `npm run dist` 只花 1m46s，一次消掉三个未知；再给冒烟工具加个 `--root`
    让它能对着产物跑，最后启动 app 抓日志 —— 到这一步才敢说「装完能用」。
    **验证手段要能指向交付物，不能只指向源码。**
34. **单一防线挡不住「嫌麻烦的人」：两层要错开位置。**
    字节码那条修了之后加了两道守卫，变异出来的结果很有意思：
    M7 只去掉 `--no-compile` → 构建**直接失败**（校验还在）；
    M8 把编译和校验**一起**删 → 构建**「成功」**，退出码 0、产物看着完全正常。
    真正兜住 M8 的是**产物级断言**（扫交付物里的 `.pyc` 标签）。
    构建期校验**早**、但可以被一并删掉；产物级断言**晚**、但要让它失效得同时改两处。
    **判据**：一道防线如果「删掉它自己也跟着消失」，它守不住将来那个嫌麻烦的人。
35. **性能数字取最小值，体积数字要同口径 —— 两个都踩过。**
    启动耗时单次测出来「1.15s vs 0.67s」，差点写成「省 0.5 秒」；
    改成多次取最小值后差异 ~0.03s，在噪声内（那是磁盘冷缓存的假象）。
    死重体积最初记「1.8 MB」，是把同一份 `site-packages` 分别交给 3.14 / 3.13
    编译复测后才发现是 **6.76 MB vs 5.77 MB** —— 少报了近 4 倍。
    **一个让我多报收益，一个让我少报问题，方向相反，病因相同：量法没站稳。**
36. **同一个结论抄在两处，必有一处先过期。**
    报告里已经把「0.5 秒」证伪了，构建脚本的注释却还写着
    「用户每次启动都要付这 0.5 秒」—— 下一个读代码的人会拿到过期的结论。
    **改结论时，全局搜一遍它的其他副本**（注释 / docstring / 报告 / 记忆）。
    > **这条我自己连踩两次**：第一次改了两处、漏了 `installDeps()` 上方那段
    > 「纯 1.8 MB 死重」；更尴尬的是**我搜到了 5 条结果，扫了 3 条就以为完事**。
    > 所以操作口径要写死：**grep 出全部 → 逐条点开改 → 重跑同一条 grep 复查**。
    > 别信「我看过了」这种自我感觉，只信复查的 0 条输出。

    **但上面这套只是「把这一处改对」，不是修复。** 第二次补完之后我数了一下：
    新的数字（6.76 MB / 5.77 MB / ~0.03s）**照样抄在 3 个文件 8 行里** ——
    我只是把一批过期数字换成了另一批**将来同样会过期**的数字，病根一点没动。
    更要命的是同一个量复测两次就不一样（6.76 / 6.78 MB；耗时那次甚至出现
    **有缓存比无缓存还慢**，方向都不稳）。**会漂移的东西就不该有副本。**

    所以真正的修法按教训 30 的判据来 —— **把数字变成可执行的**：

    | 措施 | 作用 |
    |---|---|
    | `_verify/pyc-cost.js` | 一跑就出数（18 秒）：错版本/对版本缓存的体积与数量、启动耗时**自动取 5 次最小值**并判定「是否噪声」。数字的唯一来源。 |
    | 注释里**只指路、不抄值** | 「要量跑 `pyc-cost.js`」「看 `du -sm desktop/vendor/py`」，连「38 MB」这种量级也删了 |
    | pytest 哨兵 | 扫三个文件的**注释 / docstring 行**，命中「数字 + MB/s/秒」直接报红，并指出文件与行号 |

    没有数字，就没有「同步」这回事。三个方向的变异各报红一次：

    | 变异 | 改回什么 | 结果 |
    |---|---|---|
    | M9 | 注释里写回 `6.76 MB` | 报红，指出 `build-python-runtime.mjs:291` |
    | M10 | 注释里写回 `0.5 秒` | 报红，指出 `runtime-smoke.js:138` |
    | M11 | 把旧数字写进**哨兵自己所在的工具**的注释 | 报红，指出 `pyc-cost.js:8` |

    M11 值得单独说：哨兵第一版是**枚举 3 个文件名**，而 `pyc-cost.js`
    （就是那个用来消灭数字的工具）**自己头部注释里就躺着「1.8 MB」「6.76 MB」** ——
    它不在枚举清单里，于是一直没被拦。按教训 25 的判据（「新增同类东西时要改它」
    就是写错了粒度），改成 glob：`desktop/scripts/*.mjs` + `_verify/[!_]*.js`
    （后者排除下划线开头的临时件）。改完 M11 立刻报红。
    **写防线的人最容易忘的，就是给防线自己上锁。**

    > 一个意外的收获：哨兵**连我自己都拦了** —— 我在测试的 docstring 里
    > 讲「这个坑的经过」时不得不引用「1.8 MB」这种旧数字，于是它也报红了。
    > 这反而定下了一条边界：**代码注释只写「现在该怎么做」，历史经过归报告**。
    > 讲述过去要引用旧值，而旧值正是被禁止的东西 —— 两者天然冲突，别硬塞在一处。
37. **改了「产品能力」，面向用户的文档要一起改 —— README 比注释更容易过期。**
    P1-3 让打包版自带 Python 运行时之后，README 的打包章节仍写着
    「运行打包版需要目标机器装有 Python + 依赖」，已知边界里也还留着
    「打包版依赖目标机器 Python 环境」—— **与实现直接矛盾**。
    代码注释过期误导的是读代码的人，**README 过期误导的是使用者和决策者**。
    **判据**：实现里凡是「能力从无到有 / 从有到无」的改动，
    都要反问一句「README / 文档里有没有反着写的话」。

