# 修复记录 · 2026-09-26 · 五路审查全量修复 + 复审

> **上游**：`审查报告-20260926-五路全方位审查.md`（P1×5、P2×11、P3×~28、两条红灯基线）。
> **本轮**：按报告 §7 顺序逐条修复；每条行为级修复带能红的守卫，P1/P2 级登记进
> `_verify/mutate.py` 的 **s9 组**（9 条全部实测报红 OK）。
> **修后基线**：verify.js **379/379** · pytest **零 F** · desktop node --test **116/116**（三连跑稳定）· mutate -k s9 **9/9 红**。

---

## 1. 修复清单（与上游报告逐条对应）

### 红灯基线（§2）
| # | 修复 | 验证 |
|---|---|---|
| §2.1 usage 守卫假红 | `tests/test_usage_keys_cross_layer.py` 重写为 `_merge_usage` 的三个不变式（dict 递归分支在、累加非覆盖、两条 chat 路径各接一次） | 变异：删 dict 分支 → 红；还原 → 绿 |
| §2.2 errorpath 夹具竞态 | `desktop/main.errorpath.test.js`：cleanup 等日志流 fd 释放再删（`rmDirWhenReleasable`）+ 建 `desktop/engine` 引擎桩让 spawn 真成功 | node --test 三连跑 116/116 |

### P1（5/5 全修）
| # | 修复 | 守卫与变异 |
|---|---|---|
| P1-1 删模型静默启用 | `server.py` 判据加 `active and`（空 active≠悬空） | `test_deleting_while_all_deactivated_stays_deactivated`；s9-1 红 |
| P1-2 换包静默降级复发 | `ui.js` 新增 `applyPackSwitch()` 唯一收口：change 监听、`fillPackSelect`（值变化才调）、`settings.selectPack`（值变化才调）三处同源；时序自查：首屏填充 prev 为空 → reset 无副作用，meta 事件同值 → 保住用户参数 | verify.js 379 全过（覆盖参数条渲染链） |
| P1-3 瞬时 OSError 钉缓存 | `knowledge.py` OSError 分支 `return ""` 不落缓存（与 `read_yaml_cached` retryable 口径对齐） | msvcrt 真占用测试：占用中返回空、解除后读回；s9-2 红 |
| P1-4 情报落点键两本账 | **身份统一到目录 slug**：`pack_info` 的 `PackInfo.name = dir_name`（yaml `name` 键退为标签）、`fetch_pack`/`select_for_prompt`/Job params 全部传 `pack.dir.name`、`_intel_sources` 按 slug 匹配；前端零改动（`p.name` 本来就是唯一身份键） | 3 条测试（身份/读写对称/作业落点）；s9-3 红 |
| P1-5 packseed 先删后拷 | `_copy_pack` 改「先拷进 `.seed-new` → 旧包挪 `.seed-old` 备份 → rename 换入 → 还原」；`list_packs` 跳过点开头目录；失败如实说「旧包完整保留在备份」 | 5 条测试（同步保 private+传删除 / 失败原样+下次自愈 / 还原 / 双重失败保备份 / 点目录不冒充包）；s9-4 红 |

### P2（11/11 全修）
P2-1 建包取消统一回收（transition 被拒路径也 discard，两条相邻注释统一口径）→ `test_cancel_landing_after_the_last_check_also_reclaims`（s9-5 族之外单独验证变异红）；
P2-2 嵌套结构校验（`pack_shape_error`/`skill_shape_error`，pack_info try 内 ValueError 统一出口 → 409+点名键）→ 3 条形状测试 + s9-5 红；
P2-3 `_swap_in` 跨盘清半截 dst + 如实报错 → `test_...partial_dst`（s9-6 红；⚠ 此条首版测试假绿，见 §3 教训）；
P2-4 模板包两处 `$alt_guide` → 契约测试（s9-7 红）；
P2-5 主进程 exit 处理器身份校验；P2-6 初始 loadURL 接 catch（日志 `[load]` + 兜底弹窗，与 did-fail-load 共享一次性标记）；
P2-7 重写 chip 用 `c.value` 定位卡片；P2-8 高级配置先校验后存模型；P2-9 `ZWSP="\u200B"`（转义序列防再被剥）；
P2-10 `DELETE /api/packs/{name}` 路由测试；P2-11 `_prune_history` 两条删除语义测试。

### P3（批量清）
- **后端**：`running_count` docstring 去重；「连续 3 天沉底」三处声称改如实（intel.py/server.py/需求方案）；`_section_has_body` 死守卫删除；`_str_list` 防字符串拆单字；zip 保留设备名（con/aux/nul/com1-9/lpt1-9）白名单层点名拒绝；`load_latest` 元素形状防御（非对象剔除+score 归 None）；`today()` 透出忽略记录读坏；`is_new/prev_keys` 统一 `item_key`；`ai_tells` 非字符串给说明；`config._num` 登记非法回退进 `llm_defaulted`；`request_cancel` 锁内拒终态（TOCTOU 收口）。各配守卫测试并逐条变异验证红。
- **渲染层**：`awaitJob` 状态改写 `#topics-note`（不再污染全局 `#rh-state`）；`onCancelled` 保留主题；`testModel` 死函数删除；导入/卸载/转正 catch 拆分（成功后刷新失败如实说）；四处死导入删除；`s.type`/`x.type` 过 esc；collectParams 缺元素发 null；`packName()` 去硬编码 elevator；`api.js` 成功后 clearTimeout；`#btn-packinfo` 只绑一处；`lastCreatedPack` 收编进 store；死三元化简。
- **主进程**：did-fail-load 过滤 isMainFrame/-3；startEngine 清 stderrBuf；second-instance 无窗留痕；publish-update.mjs sha512 重算对账 + HTTP 整体超时；`#upd-status` 默认文案清空（七态口径唯一化）；`result===null` 注释按 electron-updater 6.8.9 实况改写。
- **文档**：README 调用次数按 draft 合并路径分档（默认包 2~4、老包 3~5，最坏 24/32/30）、发布三分支对齐实现、符号锚点；UI规范 13→10；知识库内容规范 draft 注入口径；自动更新方案 28px/四通道/补传分支/锚点。

## 2. 修后基线（全部实跑）

```
node _verify/verify.js              → 379/379 通过（GUI 实跑）
.venv/Scripts/python.exe -m pytest  → 零 F（进度行无 F/E）
desktop: node --test                → 116/116（三连跑稳定）
_verify/mutate.py -k s9             → 9/9 报红 OK（文件还原校验通过）
```

期间发现并修掉的三处「修复自身的回归」：
1. README 账目改档后 `test_readme_worst_case_request_count_is_the_real_one`（旧账 5×3=30）红 → 守卫改为对齐四格新账（4×2=24、4×3=32、5×2=30，README 补粗体）；
2. `#upd-status` 清空默认文案后 verify.js 关于面板断言（要求状态行非空）红 → 断言改「元素存在」（Chrome 无 IPC 环境空串是正确形态）；
3. publish-update.mjs 注释带实测数字撞 `test_no_hardcoded_measurement_numbers_in_comments` → 改「隔几十秒挤一个字节」。

## 3. 本轮踩坑（记给下一轮）

- **变异检验抓住了测试自己的假绿（s9-6）**：`half_move` 里 `dst2` 是 **str**（`_swap_in` 传 `str(tmp_pack)/str(dst)`），`dst2 / "pack.yaml"` 直接 TypeError、半截文件根本没落地 → 回滚照常发生 → 变异后测试照样绿。修法：`dst2 = Path(dst2)`。教训：**mock 的入参形态要先量再写**，`shutil.move` 的 dst 不一定是 Path。
- **`git checkout <file>` 会把未提交的历史修复一并抹掉**：调试时 checkout packimport.py，把上一轮的 `_ledger_broken` 一起回退了；靠 `/tmp` 备份恢复。教训：调试性回退一律用「读原文→写回」，不碰 git。
- **agent 基础设施不稳**：本轮 6 次派发 3 次死于 captcha/并发限额，但死前的已完成改动可用（按特征码探测 + 测试套件验收）；文档代理完整交付。

## 4. 未修（明确记录，非遗漏）

| 项 | 理由 |
|---|---|
| `/api/history` 幽灵失败条目（P3） | 需要产品决策：左栏保留但标注「记录不可用」vs 不显示；改动横跨前后端，留待拍板 |
| `/api/intel/today` 对不存在 slug 返回空结构（P3） | docstring 明示 B3「空或坏返回空结构，别顺手统一」；slug 统一后「不存在」与「没抓过」合流是可接受语义 |
| `#btn-ignored` 无 onclick（P3） | 需要产品决策（忽略列表 UI 不存在）；index.html 本轮未在任何修复分区内 |
| `panes/navs` 硬刻度（P3） | 核实与当前 DOM 相符，属受维护不变量 |
| `.gitignore` 声明忽略的 12 个被跟踪 png | 需 `git rm --cached`（提交级操作，留给本人决定） |
| mutate.py 变异分支不核对「是不是该条断言红」、`_verify/` 一次性探针、NSIS 真机安装 | 上轮遗留的长期欠账，本轮未扩大范围 |

## 5. 复审结论

复审按「实现正确性 / 副作用 / 兄弟路径遗漏」三问逐条过了一遍（agent 派发持续失败，由主线完成）：
- **P1-4 身份变更波及面**：`pack.name` 全部消费点逐一核对——情报链（fetch/inject/today/ignore/refresh）与 Job params 已统一 slug；`Pack.name` 只活在报错文案；前端 `p.name` 天然跟随；`_NAME_RE` 不含点号，备份暂存目录无法被 API 直呼。
- **applyPackSwitch 时序**：四个调用点（boot 首屏 / meta 事件 / onPackDone prefer / 面板点选）逐一推演——同值保参数、丢包回默认才 reset、首屏 reset 无副作用。
- **守卫对账**：修复触发的三条守卫（README 账目、关于面板状态行、注释实测数字）已对齐并回绿。
- **无未收敛项**：三基线 + 变异套件全绿；每条行为级修复都有能红的守卫且登记或实测过变异。

## 6. 收尾轮（2026-09-26 晚，"还有吗"）

| # | 事项 | 结果 |
|---|---|---|
| 1 | 配置指南两处 ideas.md 阶段枚举（上轮登记项）| 补全「select 与 draft 两个阶段都注」 |
| 2 | **既有变异条目失效体检**：本轮重构（pack_info 重排、dedup 收进 item_key）+ 上轮 working-tree 改动（package.json files 追加 updater-core）使 3 条老条目的旧串失效 → 会静默 SKIP = 守卫已死 | 逐条更新匹配串并实测报红 OK；体检复跑 **75/75 旧串在位** |
| 3 | 最终全量 pytest（覆盖收尾阶段的全部小改：作业参数 slug、README 粗体、batch9/runtime_requirements 守卫对齐、publish 注释）| **零 F**（进度行 100% 无 F/E） |
| 4 | 记忆库更新（验收惯例 +4 条教训；未修清单改为当前状态） | 完成 |

> 收尾后的最终态：verify.js 379/379 · pytest 零 F · desktop 116/116（×3）· 变异网 75/75 条目完好（s9 组 9/9 红 + 修复的 3 条老条目红）。

---

*2026-09-26 · 修复轮：P1 5/5、P2 11/11、P3 批量清（3 项明确跳过并记录）、红灯基线 2/2；s9 变异组 9/9 实测报红。*

## 7. 提交（2026-09-26 晚，develop 分支，按主题 4 个 commit）

| commit | 内容 |
|---|---|
| `9ecf00a` | 存档审查报告与修复记录；解除 _verify png 跟踪；探针归档 legacy |
| `64042c7` | 自动更新整条线 + 主进程/渲染层五路审查修复 |
| `5e88065` | 五路审查修复轮（后端）：P1 五条 + P2 后端批 + s9 变异组 9/9 红 |
| `61ac635` | 文档对齐：调用次数分档、发布三分支、ideas.md 注入口径 |

提交前最终态：verify.js 379/379 · pytest 零 F（全量复跑）· desktop 116/116（×3）·
mutate s9 9/9 红（含归属核对）· 变异网 75/75 条目完好 · `npm run dist` +
verify:package 认证通过（TalkScript 0.2.2.exe，包内引擎与源码逐文件一致）。
