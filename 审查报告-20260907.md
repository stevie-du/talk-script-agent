# TalkScript 项目审查报告

审查日期：2026-09-07　审查范围：全仓库（app/ · desktop/ · packs/ · agent-skills/ · docs/ · tests/ · 配置与仓库卫生）
审查方式：全量代码通读 + 冒烟/重试测试实测 + git 与依赖盘查

---

## 一、总体结论

**架构是站得住的，主要风险集中在「导出链路」和「本地服务安全边界」两处，不是主流程。**

设计上最值得肯定的三点：

1. **代码守下限、模型拼上限**落地得彻底——字数/时长/禁用词全部由 `checker.py` 确定性计算，模型不做算术；不合格自动回炉（≤2 轮），回炉反馈是结构化的具体问题而不是"重写一遍"。
2. **行业包与引擎解耦干净**——引擎只认 `pack.yaml` 结构、不认任何具体行业；提示词/注入文件/回炉上限外置到 `skill.yaml`，换行业不动代码。
3. **防编造意识贯穿**——packgen 明确不生成任何标准/法规编号，只吐"待核实清单"；私有资料未填一律 `{{待补：xxx}}` 占位。这对合规敏感行业（电梯）是对的做法。

### 实测结果（两项均通过）

| 测试 | 结果 |
|---|---|
| `tests/smoke_mock.py` | ✅ 一键直通+回炉闭环（第1轮命中"政府补贴"→回炉→终版 240 字，偏差 -7.8%）/ 人味 off / 仅口播 / 分步确认+选题编辑 / 单段重写+落盘同步 / packgen / 技能导出（24 文件，独立校验器可跑） |
| `tests/test_llm_retry.py` | ✅ 网络错误重试 / 401 不重试 / 429 重试 / 重试耗尽报错 / 配置合并保存（空 Key 不清空） |

mock 夹具故意首轮写违规词来验收回炉闭环，这个做法很专业。

---

## 二、问题清单（按严重度）

### P0 — 建议尽快修

**1. 导出的 Agent 技能硬编码了电梯行业红线（跨行业导出即错误）**
`app/export_skill.py:164-171`，SKILL.md 的"硬约束"段落是写死的：

> 3. 禁止危险行为演示（扒门、攀爬轿顶、短接门锁等，包括反面演示）
> 4. 救援口径唯一：按警铃或对讲 → 拨打当地电梯应急救援服务电话 → 原地等待

无论导出哪个行业包（如"全屋定制"），产出的技能都会带上电梯救援口径。应从 `compliance/industry.md` + `pack.yaml` 动态取，或至少由 pack 提供 `redlines_for_skill` 字段。

**2. 私有商业资料会流进 git**
`export_skill.py` 的 `COPY_DIRS` 含 `private`，导出目录默认是**仓库内** `agent-skills/<name>/`。而 `agent-skills/elevator/private/{products,service,cases,faq}.yaml` **已在版本库中被跟踪**。用户一旦填入真实型号、报价、服务承诺，导出 + commit 就直接外泄。
建议：导出默认排除 `private/`（技能自带"分享时删除 private"提示，导出环节却主动带上，逻辑自相矛盾），或把 `agent-skills/` 加进 `.gitignore`。

**3. 本地引擎端口对任意网页敞开**
`app/server.py:36` `allow_origins=["*"]`，且全站无任何鉴权。任意网页（用户在浏览器里打开的第三方站点）都能 fetch `127.0.0.1:8765`：读取 `generated/` 里全部历史脚本（含私有资料生成的成品）、`POST /api/config` 改 api_key、`DELETE /api/history/{jid}` 删记录、创包。属于典型的本地服务 CSRF/DNS-rebinding 面。
建议：origin 收敛为 `null`（file:// 场景）与本机构造的约定值，或加一个启动时生成的随机 token 由 preload 注入。

**4. `config.yaml` 在版本库里，且 API Key 明文回写**
`config.yaml` 已被 git 跟踪，`save_config()` 会把用户在界面填的 Key 明文写回该文件 → `git status` 变脏 → 极易随其他改动一起提交。
建议：新增 `config.local.yaml`（gitignore）优先加载，或至少把 `config.yaml` 加入 `.gitignore` 并提供 `config.example.yaml`。

---

### P1 — 影响使用体验或正确性

**5. 建包接口用的是启动时的陈旧配置（确认是 bug）**
`server.py:32` 的 `cfg = load_config(root)` 在 `create_app` 时求值一次。`/api/packs/create`（130-138 行）用它做 Key 校验，`/api/health`、`/api/meta` 也用它回值。而 `/api/generate` 用的是 `load_config(root)` 重新加载。
**后果：用户在界面填完 Key 后，生成能用，但「＋新建行业包」仍然报"未配置模型 API Key"，且顶部 meta 的模型名/Has-Key 状态不刷新——必须重启应用。**

**6. 已导出技能与源包发生漂移**
`diff` 实测：`agent-skills/elevator/skill.yaml` 比 `packs/elevator/skill.yaml` 少了后来新增的 `$growth_block` 占位符和 `files.growth` 映射 → 导出版实际不注入 `patterns/growth.md`；`tools/check.py` 也只在重新导出时才同步。
根因：**把生成产物提交进仓库**。建议 `agent-skills/` 改为 gitignore + 一键导出脚本，或加 CI 校验"导出产物 == 源包 + SKILL.md"。

**7. 打包后配置与产物写在安装目录**
`main.js:111` 打包态 `rootDir = resources/engine`，而 `config.yaml`、`generated/` 都在这个 root 下。装到 `C:\Program Files\` 后，用户改设置、生成脚本都会因权限不足**静默失败**（`_persist` 还 `except: pass` 吞掉了）。
建议：打包态把 `--root` 指向 `app.getPath('userData')`，engine 代码/行业包仍从 resources 只读加载。

**8. 参数未做白名单，存在路径穿越**
- `Pack(root, name)` 直接拼接 `root/packs/<name>`，`name` 可为 `..%2f..`：GET `/api/packs/{name}` 会 `rglob` 并列出任意目录文件；`POST /api/packs/{name}/undraft` 可越权改写同级已存在的 `pack.yaml`。
- `/api/history/{jid}`、`DELETE /api/history/{jid}` 用 `root.glob(f"generated/*/{jid}")`，`jid=*` 会命中任意记录。
本地应用风险有限，但成本极低：`name`/`jid` 加 `^[A-Za-z0-9_-]+$` 校验即可。

---

### P2 — 代码整洁与健壮性

| # | 问题 | 位置 |
|---|---|---|
| 9 | 死代码：`Pipeline._persist_result` 已无调用；`ScriptResult`/`SceneItem` 模型定义了但 `_build_scenes` 返回裸 dict，契约模型实际未参与校验；`Pack.files_text` 未使用 | `pipeline.py:482`、`schemas.py:107,120`、`knowledge.py:71` |
| 10 | 文档漂移：`pack.yaml` 的 `files.select/write/compliance/facts` 已不再被读取（引擎改读 `skill.yaml` 的 `stages.*.files`，只有 `files.private` 还活着），但 README 与 `docs/` 仍按旧结构描述 | `packs/elevator/pack.yaml:87-92` |
| 11 | `Quota._row` 在 `quota_table` 只有 1 条时取 `keys[1]` 抛 IndexError | `checker.py:107-109` |
| 12 | `Pipeline.jobs` 无上限不清理，长跑内存只增不减；`writing/rewriting` 状态无法真正取消（只能取消排队/待确认）；并发重写同一 job 有竞态（`_step` 对 `job.steps.append` 未加锁） | `pipeline.py:76,121,461` |
| 13 | `export_skill.py:194` 用 `assert` 做产物校验，`python -O` 下失效；`smoke_mock.py:145` 断言输出含 "244" 这类魔法数字，字库一改测试就红 | — |
| 14 | `ConfigIn` 未暴露 `retries`/`timeout`（config.yaml 里有配置项，界面改不了）；`v not in ("", None)` 会把 `temperature=0` 当空值忽略 | `server.py:202-211` |
| 15 | 仓库卫生：根目录 `_shot2.js`（临时截图脚本，未跟踪）；`desktop/dist2 ~ dist15` 共 **14 个打包目录 ≈ 5.9GB**（.gitignore 只挡了提交，挡不住磁盘）；`package.json` 输出目录写死 `dist2`，与 README 说的 `dist/` 不一致 | — |
| 16 | 缺 `LICENSE`（package.json 声明 MIT）；无 CI；测试是独立脚本、无统一入口（建议 pytest 化） | — |

---

## 三、建议修复顺序

| 优先级 | 动作 | 预估改动 |
|---|---|---|
| 1 | 导出技能：硬约束动态化 + 默认排除 `private/` + `agent-skills/` 移出版本库 | 小 |
| 2 | 建包/meta/health 改为每次 `load_config()`，与 generate 一致 | 极小 |
| 3 | 本地服务收敛 CORS origin 或加随机 token；`name`/`jid` 加正则白名单 | 小 |
| 4 | `config.yaml` 拆出 local 配置（不入库） | 小 |
| 5 | 打包态 root 指向 `userData`，避免写安装目录 | 中 |
| 6 | 清理 `dist2~dist15`、`_shot2.js`；`package.json` 输出目录统一 | 极小 |
| 7 | 清死代码、修 `Quota._row`、补 jobs 上限与 `_step` 加锁 | 小 |

前 4 项都是低风险小改动，建议一次性做完。

---

## 四、做得好的地方（别改坏了）

- 渲染层 29 处 `innerHTML` 全部经过 `esc()` 转义，无 XSS 面——这个自觉性在很多项目里没有。
- LLM 客户端两层重试（网络层指数退避 + 解析层带错误信息重试）区分了可重试/不可重试状态码，401 不白重试。
- 知识文件按 `topics_map`/`audience_map` 切片注入，不全量塞上下文。
- 新包 `draft: true` + 校对清单 + 角标，把"模型生成必须人工校对"做成了机制而不是口头约定。
- `checker.py` 带独立 CLI，且导出时打补丁让技能目录可自持运行（`CHECKER_PATCH` 还在源码变化时主动报错，防补丁失效）。
