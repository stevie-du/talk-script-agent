# 全方位代码审查报告 · 2026-09-13（第二次）

范围：`app/*.py`（引擎 12 个模块，含 `checker/llm/pipeline/server/packgen/export_skill/knowledge/config/fileio/schemas`）、
`desktop/main.js`、`desktop/preload.js`、`desktop/renderer/*`（app.js 1609 行 + styles.css 932 行 + index.html）、
`desktop/package.json`、`tests/*`、`_verify/*`、`packs/elevator` 配置。

方法：全文精读 + **只读探针实测**（`TestClient` + 真实 pack.yaml，未改动任何项目文件）。
凡标注「实测」的结论都有可复现的探针输出，不是静态推断。已剔除静态推断中不成立的项（见文末）。

结论：**4 个 P0 / 14 个 P1 / 19 个 P2 / 11 个 P3**。

> 与上一版（`审查报告-20260913.md`）的关系：那一版的 3 个 P0 + 8 个 P1 已全部落地并逐条复核通过
> （见文末「已修复项」）。本报告是**新一层的深挖**，焦点从「用户能感知的坏状态」转到
> **安全边界、并发正确性、数据一致性、工程化**。

---

## P0 · 安全边界与数据一致性（建议立刻处理）

| # | 位置 | 问题 | 触发与后果 | 修法 |
|---|---|---|---|---|
| 1 | `desktop/package.json:31` | **明文 API Key 被打进安装包**：`extraResources` 里有 `{ "from": "../config.yaml", "to": "engine/config.yaml" }`，而 `config.yaml` 当前存着真实 Key（`sk_pxjS…0zkp8`，明文） | `npm run dist` → NSIS 安装包 / portable exe 内含 `resources/engine/config.yaml` → **每一个拿到安装包的人都能解出开发者的 Key**。打包版 `main.js:112` 的 `rootDir` 正是 `resources/engine`，引擎启动即读它 | ① `extraResources` 删掉这一条，只打 `config.example.yaml`；② 首次启动时由引擎从模板生成 `config.yaml`；③ **轮换这个 Key**（它已长期明文落盘于工作区） |
| 2 | `app/server.py:76-95` + 全站无鉴权 | **本机任意网页 / 浏览器扩展可完全接管引擎**：`_origin_allowed` 主动放行 `null`、`file://`、`app://`、`chrome-extension://` | Chromium 对 `file://` 文档统一上报 `Origin: null` → **用户下载并打开的任意 .html 都能**：`GET /api/history` 读走全部脚本、`DELETE /api/history/{id}` 删记录、`POST /api/config` 把 `base_url` 改到攻击站点（下次请求即外送 `Bearer <api_key>`）、`POST /api/generate` 烧额度。引擎被 Electron 拉起后常驻，不是理论风险 | 启动时生成随机 token，经 `main.js` 已有的 query 通道传给渲染层（现在只传了 `port`），所有 `/api/*` 校验 `X-TalkScript-Token`；同时从放行集合移除 `null`/`file://`/`chrome-extension://`。**注意**：`tests/test_server_hardening.py:105-111` 目前**断言这些 Origin 必须被放行**，改动需同步更新该用例 |
| 3 | `app/pipeline.py:176-189`、`191-204` | **状态检查与迁移非原子（TOCTOU）**：`confirm` / `rewrite_segment` 都是「先无锁读 `job.state` 判断，再 `job.update(state=…)`」 | 连点两次「继续」：两个请求都读到 `paused_awaiting_confirmation` → 都通过 → 起两个 `_continue_write` 线程 → **双倍 token、steps 重复、result 互相覆盖**。`rewrite_segment` 同理：两个线程同时改 `sections[index]` 并各自写 `result.json`。上一版只补了「入口置中间态」，但**检查与置位之间仍有窗口** | 给 `Job` 加 `transition(from_states, to_state, **kw) -> bool`，在 `self._lock` 内完成「校验 + 置位」，失败返回 409。这也是 P3-3 的修法 |
| 4 | `app/pipeline.py:622` vs `481` | **同一作业的落盘目录按两个时钟算**：`_persist` 用 `datetime.now()`，`_write_output_files` 用 `result["created_at"]` | 作业 23:59:50 暂停、00:00:10 完成 → `job.json` 落在 `generated/20260913/`、`result.json` 落在 `generated/20260914/`。而 `server.py:325-327` 的 `history_delete` **在第一个 glob 命中后就 `return`** → 另一个目录成孤儿，其 `result.json` 会被 `history()` 重新扫出来，**删除的记录复活** | 作业创建时算一次 `job.dir` 存进 `Job`，所有落盘复用；`history_delete` 改成删除**全部**命中项 |

---

## P1 · 功能错误与逻辑缺陷

| # | 位置 | 问题 | 后果 | 修法 |
|---|---|---|---|---|
| 5 | `checker.py:149-150` + `pipeline.py:401-403` vs `app.js:801,813` | **每段配额从不除以要点数**：`SEG_FIELD` 把 `point` 映射到 `body` 后直接取 `target["body"]`（整个正文配额），而前端除以了 `nPoints` | **实测**：60s@4.5 → `body=171`。一个 134 字的要点段：后端阈值 `171×1.3=222` → **判定「未超配额」**，于是 `_violation_feedback` 的「段落超配额」分支**实际是死代码**；前端阈值 `171/2=85` → **卡片标红**。同一段后端说合格、前端说超了 | `check_script` 增加 `points` 参数（或直接把 `body` 除以要点数）再写进 `segments[].quota`；顺带把字数统计口径统一（见 #22） |
| 6 | `app/llm.py:122-124` | **流式分支绕过了空 content 诊断**：`if on_delta: return self._stream_once(...)` 直接返回，跳过了 153-159 那段「模型返回空内容 → 请调大 max_tokens」的诊断；而 pipeline **全程都传 `on_delta`** | 推理型模型把输出预算全用在思考上时，用户看到的是误导性的「模型输出无法解析为 ScriptDraft」，而项目特意写好的那条最有用的排障提示**在真实使用中永远不会出现** | 流式返回后同样判空，复用 153-159 的异常文案 |
| 7 | `app/checker.py:83` | **禁用词按子串计数 → 重叠词重复计数**：`text.count(w)` | **实测**：`包过检` → `包过`×1 + `包过检`×1 = **2 处**；`加微信` → 2 处；`全网最低价` → 2 处。硬伤数虚高、`_violation_feedback` 把重复词喂给模型，可能**触发不必要的回炉（多花钱）** | 长词优先匹配，命中后从文本中扣除；或改用 Aho-Corasick / 正则交替分组 |
| 8 | `app/checker.py:66` + `banwords.yaml` | **soft 表里有单字词「最」** | **实测**：`最近` / `最后` / `最好` / `最终` 全部命中 → `soft_hits` 噪声极大，界面「待确认 N 词」长期无意义。若将来把单字词提到 hard，会直接误判必改 | 词表禁止单字条目；或引入白名单/负向上下文规则 |
| 9 | `app/server.py:192-196` | `/api/generate` 传不存在的行业包 → **HTTP 500「Internal Server Error」**（`PackError` 未被捕获） | **实测**。用户误传包名只得到一句 500，无法自查 | 捕获 `PackError` → 404；`GenerateRequest.pack` 加存在性校验 |
| 10 | `app/pipeline.py:320` + `checker.py:104-120` | **缺 `quota_table` 的包会让生成以 `list index out of range` 失败**：`_normalize` 无条件调 `quota.target()`，而空表的 `_row()` 会访问 `keys[0]`；单行表访问 `keys[1]` 同样崩 | **实测**：缺 `quota_table` 的包 → 作业 `failed`，`error=list index out of range`（且已白烧一次 select 调用）。单行 `quota_table` 对任何非该行的 duration 也崩 | `Quota.target` 对空表返回 `{}`、单行表直接取该行；`_normalize` 在缺表时抛可读的包配置错误 |
| 11 | `app/schemas.py:8-23` | **`duration` / `topic` / `facts` 无上界** | **实测**：`topic` 20 万字符 → 200 接受并进提示词；`duration=100000` → 200 接受，配额被**线性外推**到 14076 字；`duration=0` 甚至算出**负配额**（`total=-4`） | `duration: Field(gt=0, le=600)`（或限定为 pack 的 `options`）、`topic: max_length=200`、`facts: max_length=20000`；`Quota._row` 对超出表范围的值钳制到端点 |
| 12 | `app.js:872-894` | **导出 SRT / MD 未 `revokeObjectURL`** | 每次导出泄漏一个 Blob（两个泄漏点） | `const u = URL.createObjectURL(...); a.href = u; …; setTimeout(() => URL.revokeObjectURL(u), 0)` |
| 13 | `app.js:1204-1219` | `openSession` **不重置 `genParamsSnapshot`** | 打开历史记录后改任意参数 → 弹「参数已修改，当前展示的是旧参数结果」，但展示的其实是**历史记录**，提示语与实际不符（反之也可能该提示时不提示） | `openSession` 里 `genParamsSnapshot = null`，或存该记录的 `params` |
| 14 | `app.js:969-985` | `waitDone` **无空值守卫、不识别 `cancelled`** | 重写途中切走 → `activeMsg` 为 null → `body.innerHTML` 抛 TypeError，被外层 catch 成误导性的「重写失败」；`currentJob` 为 null 时 `currentJob.id` 同样抛错。若期间点了停止（状态 `cancelled`），循环不认 → **空转 180 秒后才报「超时」** | 每轮先判 `currentJob?.id` 与 `activeMsg`；把 `cancelled` 当终止态立即返回 |
| 15 | `app.js:1278-1286` | 每次 `openSettings` 都无条件 `preloadSettings()` | **复现**：设置里改完 base_url → `Ctrl+,` 关闭 → `Ctrl+,` 打开 → 输入被服务端旧值覆盖，未保存的编辑丢失 | 仅在输入框为空或 `dirty` 标记为假时回填 |
| 16 | `app/pipeline.py:449-477` | **cancel 与 `_finalize` 之间没有检查点**：最后一个 `_abort_if_cancelled` 在回炉循环开头（:344），而 `_finalize` → `_write_output_files` 之间无检查 | 用户在「模型刚写完、正在校验/组装」这一窗口点停止：`request_cancel` 已把状态冻结为 `cancelled`，但 `_write_output_files` 照样把 `result.json` 写下去 → `history()` 扫到它并标为 **`done`**，**历史里凭空多一条「已完成」记录**（`tests/test_cancel_concurrency.py:96` 只覆盖了流式窗口，没覆盖这个） | `_finalize` 入口加 `_abort_if_cancelled`；`_write_output_files` 前再判一次；或改为「状态迁移成功后才落 result.json」 |
| 17 | `desktop/main.js:13-22, 115-116` | **端口 TOCTOU + 引擎崩溃不重启**：`findFreePort` 拿到端口后关闭监听，Python 稍后才绑定 | 中间被别的进程抢占 → 引擎绑定失败 → 只表现为「健康检查超时」；引擎中途崩溃后 `engineProc=null`，界面永远停在「无法连接本地引擎」，只能重启应用 | 让引擎自己绑定 `0` 端口并把真实端口写到 stdout/端口文件，主进程读取；加崩溃重启 + 指数退避 |
| 18 | `app/server.py:246-252` | `/api/packs/{name}/export-skill` 是**唯一没有 `_safe_name` 守卫的包路由**（`get_pack`/`undraft` 都有） | **实测**：`POST /api/packs/%2e%2e/export-skill` 被解码成 `..` 并一路走到 `Pack(root, "..")`（返回 500「行业包不存在: ..」）。当前仅因目标目录没有 `pack.yaml` 而未形成穿越写入，属**纵深防御缺口**，补一行即可 | 加 `_safe_name(name)` |

---

## P2 · 健壮性、性能与细节

**性能**

- **#19 `server.py:267-313`**：`history()` 每次全量 glob + **完整解析所有 `result.json`**。前端生成期间每 3 秒调一次（`app.js:1029`）→ 100 条记录 × 数十 KB 就是每 3 秒几 MB 的解析量；`[:100]` 的截断还发生在**全部构造完之后**。建议落盘时同步写一份精简索引，`history()` 只读索引。
- **#20 `llm.py:125,173`**：每次请求都 `httpx.post` / `httpx.stream` 新建连接，**无连接池复用**。一次生成 2~4 次调用、外加重试，每次都要重新 TCP+TLS 握手。`httpx.Client` 本身线程安全，可长期复用。
- **#21 `pipeline.py:347-363`**：**每一轮回炉都重新读全部知识文件**并重新 `yaml.safe_load` 私有资料（`Pack.file_text` / `private_facts` 无缓存）。按 `(pack, mtime)` 做进程内缓存即可。
- **#22 `pipeline.py:91-106`**：`snapshot()` 在**运行期也把整个 `result` 一起返回**，而前端轮询只需要 `state/steps/stream/error`。结果体（sections+scenes+logs+check）每 0.9 秒被 JSON 序列化一次。非终态时省略 `result`，改动面很小（`poll()` 只在 `done` 分支读 `snap.result`）。

**错误处理**

- **#23 `pipeline.py:619-634`**：两处 `except Exception: pass` 静默吞掉落盘失败。磁盘满/权限问题**无任何痕迹**。至少 `logging.warning`，并把失败反映到 `job.error`。
- **#24 `server.py:315-320`**：`history_item` 的 `json.loads` **没有 try/except** → 坏文件直接 500；而 `history()` 里同样的解析是 `except: continue`（记录凭空消失）。**同一份数据的容错策略不一致**，应统一。
- **#25 `server.py:289-296`**：**失败作业只在内存里**——`history()` 只扫 `result.json`，而 `job.json` 写了却**从未被任何代码读取**。重启后失败记录从历史中彻底消失，用户既看不到也不知道失败过。
- **#26 `packgen.py:173-180`**：`out.segments[0]` / `audiences[0]` / `personas[0]` 无空数组保护 → 模型返回空数组即 IndexError → 500。且 `create_pack` 中途失败会**残留半成品目录**（`d.exists()` 的守卫在最前面，但没有失败清理）→ 重试被 409「行业包已存在」挡死。
- **#27 `packgen.py:128-220`**：生成的知识文件全用裸 `write_text`，**没走项目自己的 `write_atomic`**（`config.yaml`/`result.json`/`pack.yaml` 都走了）。口径不一致。
- **#28 `pipeline.py:637-645`**：`wait_job`（测试辅助）只认 `done`/`failed`，作业被取消时会一直等到超时。
- **#29 `llm.py:146` / `pipeline.py:240,254`**：上游响应体 `resp.text[:300]` 被原样塞进 `error`，并**落盘进 job.json + 展示在界面**。若上游回显请求上下文，可能带出敏感信息。

**配置与耦合**

- **#30 `packgen.py:76`**：`create_pack` **硬编码 `Pack(root, "elevator")` 作为模板**；`checker._default_pack()`、`DEFAULT_CONFIG["default_pack"]` 也硬编码 `elevator`。该包被改名/删除 → 建包功能整体失效。建议抽 `packs/_template/` 或把模板包名写进 config。
- **#31 `export_skill.py:23,54-61,198-204`**：导出会**把 `private/`（产品型号/服务承诺/案例/异议应答等商业资料）一起复制出去**，只在返回的 `hints` 里提醒用户「分享时自己删」。用户直接打包分享 = 商业信息外泄。建议默认排除 `private/`，加 `include_private` 显式开关。
- **#32 `export_skill.py:63-70`**：用「字符串包含 + replace」给导出的 `checker.py` 打补丁，源码结构一变就 RuntimeError。至少是 fail-fast，但更稳的做法是让 `checker.py` 从参数/环境变量取包目录，而不是改写源码。
- **#33 `config.py:70-73`**：环境变量覆盖只支持 `api_key`/`base_url`/`model`，`retries`/`timeout`/`max_tokens`/`temperature` 无法用环境变量配置（打包版尤其需要）。
- **#34 `server.py:350-355` + `app.js:1303-1312`**：`set_config` 过滤空串 → `base_url`/`model` **永远无法清空回默认**；前端也不传 `temperature` → 设置页显示的温度实际不可改。
- **#35 `server.py` 全局**：无速率限制，`_spawn` 无上限地为每个作业开线程，`POST /api/generate` 可被本机脚本无限调用 → 线程与 token 双爆。建议限制并发作业数（如 3），超出排队。

**前端细节**

- **#36 `app.js:732-734` vs `checker.py:32-37`**：`countCN` 与 `count_chars` 是**两份实现且口径不同**。**实测**同一句话：后端 13 字 / 前端 20 字，差 5~7 字（中文标点被前端计入、数字串未折叠）。→ 卡片「N/quota 字」与顶部「字数」永远对不上，卡片会莫名标红。建议把字数统计下移到后端（结果里给每段 `chars`），前端只展示。
- **#37 `app.js:15`**：`esc()` 不转义单引号。当前所有动态值都进文本节点或双引号属性，**未找到可利用路径**；但 `pack.yaml` 的 `label` 等是用户可编辑内容，将来一旦用于单引号属性就会破。补 `'` 是一行成本。

---

## P3 · 架构与可维护性

- **#38 上帝类**：`app/pipeline.py` 646 行，`Pipeline` 同时承担配置加载、LLM 构建、作业注册表、状态机、提示词渲染、知识注入、产物落盘、输出格式化。建议拆 `JobRegistry` / `PromptRenderer` / `ArtifactStore` + 纯函数式的阶段实现。
- **#39 契约模型定义了却没用**：`schemas.ScriptResult` / `SceneItem` / `SceneVisual` **从未被实例化**，`_build_scenes` 手搓 dict，`docs/场景序列契约.md` 的契约没有代码强制。建议在 `_finalize` 出口 `ScriptResult.model_validate(result)` 卡一道，让契约真的生效。
- **#40 状态机散落**：合法迁移规则分布在 `confirm` / `rewrite_segment` / `cancel` / `_write_with_recheck` / `TERMINAL_STATES`，没有单一真相。用一张显式迁移表（同时修掉 P0-3）。
- **#41 死代码**：`Pipeline._persist_result`（定义于 :628，**从未调用**）、`preload.js` 暴露的 `window.talkscript.isElectron`（渲染层从未使用）、`Pack.files_text`、`SceneItem`/`ScriptResult`。
- **#42 测试不可被标准 runner 发现**：`tests/test_*.py` 里的函数叫 `case_*` → **`pytest` 会收集到 0 个用例**，只能手工 `python tests/xxx.py`。`requirements.txt` 不含测试依赖，无 CI 配置，无 lint/format 配置（ruff/black/eslint/prettier 一个都没有）。→ 改函数名，或加 `pytest.ini: python_functions = case_*`。
- **#43 巨型单文件**：`app.js` 1609 行、`styles.css` 932 行、`_verify/verify.js` 6.4 万字符单文件。建议按职责拆（渲染 / 轮询 / 会话列表 / 设置 / 导出）。
- **#44 版本号两处**：`server.py:33 VERSION` 与 `desktop/package.json` 的 `version` 各自维护，无单一来源。
- **#45 依赖漂移**：`requirements.txt` 全是 `>=` 下限、无 lockfile。实测已出现 `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated` → 依赖已在漂移。
- **#46 仓库外垃圾**：`desktop/dist2`…`dist15`（**14 个过期构建目录**）、`_verify/_cprof/`（一份 **241 文件的 Chrome Profile**，含 .pb/.tflite）、`_verify/*.png`。虽都在 `.gitignore` 内，但会持续膨胀并拖慢 glob 与打包。
- **#47 静态资源耦合**：`server.py:139-153` 把 `desktop/renderer` 直接暴露给引擎，`index()` 不检查文件存在。建议把 renderer 作为引擎的 `static/` 资源，或至少加存在性检查。
- **#48 文档与行为不一致**：README「已知边界」未提「失败作业重启后消失」「历史仅保留 100 条」等实际行为。

---

## 值得保留的设计（不要在这次重构里改掉）

- **分层是清晰的**：Electron 壳 / FastAPI 引擎 / 自包含行业包。引擎**只认 `pack.yaml` 的结构、不认行业**，扩展新行业无需改代码——这是全项目最好的一个决策。
- **「知识库管写什么、技能管怎么写」**：把提示词从代码里搬进 `skill.yaml` 随包分发，方向正确，也是这套东西能横向复制到其他行业的前提。
- **代码守下限、模型拼上限**：字数/时长/禁用词由 `checker` 用确定性代码卡死，不合格自动回炉 ≤2 轮。定位很清醒，没有把可验证的事交给模型。
- **注释质量极高**：几乎每处非显然的实现都写了「为什么」（`write_atomic` 为什么必须同目录 rename、CORS 为什么放行 `file://`、`Job.update` 为什么冻结 `state`）。这类注释比代码本身更值钱，请保持这个习惯。
- **前端两处正解**：`paintSessions` 的按 key 增量更新（解决「删除按钮要点好几次」）、`bindSessionList` 的事件委托（解决「行被重绘后绑定错位」），都是踩过坑之后的正确做法。

---

## 建议的修复批次

| 批次 | 内容 | 风险 |
|---|---|---|
| **A（P0）** | 安装包剥离 `config.yaml` + **轮换已泄露的 Key**；引擎加一次性 token 并收紧 origin 放行集；`Job.transition()` 原子化状态迁移；作业目录单一化 | 中（token 方案要同步改前端与 CORS 用例） |
| **B（P1 后端逻辑）** | 每段配额除以要点数；流式空内容诊断；禁用词去重 + 单字词治理；`duration/topic/facts` 边界；`PackError`→404；`Quota` 空表/单行表兜底；`export-skill` 补 `_safe_name`；`_finalize` 取消检查点 | 低（都有明确单点改法） |
| **C（前端）** | 导出 `revokeObjectURL`；`openSession` 重置快照；`waitDone` 守卫与 `cancelled`；设置页回填策略；**字数口径统一到后端** | 低 |
| **D（工程化）** | `history` 索引化 + 快照瘦身；`httpx` 连接复用；`packgen` 失败清理与空数组兜底；pytest/CI/lint；清理 `dist*` 与 `_cprof`；拆 `pipeline.py` 与 `app.js` | 低，但工作量大 |

每批次改完跑 `_verify/verify.js` 与 `python tests/*.py`，并**为每条修复补一条断言**——
本轮发现的问题里，有 6 条（#5 #7 #10 #11 #16 #18）都是「有测试但没覆盖到那个分支」，说明
测试的**分支覆盖**比用例数量更值得投入。

---

## 已修复项（上一版报告，本轮逐条复核通过）

- P0-1 轮询加作业令牌 → `app.js:542-544` 已实现，`.catch` 里也做了同样的令牌校验（:599）
- P0-2 停止不再是空操作 → `Job.cancel_event` + `request_cancel` + `_delta_handler` 流式中断 + `update` 冻结 `state`，`tests/test_cancel_concurrency.py` 覆盖
- P0-3 作业字典加锁 → `_jobs_lock` + `add_job/get_job/snapshot_jobs` 三个入口
- P1 `.dot` 三态样式（`styles.css:393-400`）、`--ok-soft`（:60）、`.overlay` z-index 70（:868）、`.rw` 默认 `opacity:.45`（:704）、`.sess-sub` `padding-right:32px`（:411）——**全部到位**
- P1 重写静默失效 → 改为禁用态 + 悬停说明 + `toast`（`app.js:804,823,955`）
- P1 快捷条改参数不提示过期 → `change` 监听改为全局（`app.js:1501`）
- P1 二次 `boot()` 重复绑定 → `bindStatic._bound` 守卫（:1468）+ `pg-done` 改为只刷新下拉（:1578）
- P1 确认浮层随场景关闭 → `closeOverlays()` 统一收口（:1299），`clearChat`/`openSession`/`openSettings`/`attachJob` 均调用
- P1 后端中间态 → `confirm`/`rewrite_segment` 入口置 `writing`/`rewriting`（仍有 TOCTOU 窗口，见本报告 P0-3）
- P1 配置闭包陈旧 → `_cfg()` 每次现读；CORS 收紧 → `_origin_allowed`（放行集过宽，见 P0-2）；落盘原子写 → `fileio.write_atomic` 全覆盖

## 静态推断但不成立（已排除）

- 「`_origin_allowed` 的 hostname 解析有绕过」：`urlparse("http://127.0.0.1@evil.com")` 的 hostname 是 `evil.com`，`_local_host` 正确拒绝。
- 「`history_delete` 有路径穿越」：`_safe_jid` 的 `^\d{8}-\d{6}-[0-9a-f]{6}$` 已封死。
- 「`esc` 不转义单引号导致 XSS」：逐处核对所有 `esc()` 的输出位置，均在文本节点或双引号属性内，**无可利用路径**（保留为 #37 的纵深防御建议）。
- 「`main.js` 的 `engineProc.kill()` 残留孙进程」：该引擎不起子进程，不成立。
- 「`config.yaml` 的 Key 已随 git 历史泄露」：`git log -p -- config.yaml` 显示入库的两个版本 `api_key` 都是空串，**历史干净**；泄露路径只有 P0-1 的打包。

---

*本报告的所有代码引用行号均已回读核对；标「实测」的结论可用 `TestClient` + 真实 `pack.yaml` 复现。*
