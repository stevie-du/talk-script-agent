# 全量代码审查报告 · 2026-09-13

范围：`desktop/renderer/*`（渲染层 1523 行 + 样式 880 行 + HTML）、`desktop/main.js`、`app/*.py`（引擎 9 个模块）、`_verify/*`（回归网）。
方法：三路并行审查 + 逐条回读代码核对行号（已剔除 1 条误报，见文末）。

结论：**3 个 P0 / 8 个 P1 / 8 个 P2**。P0 都是"用户能明确感知的坏状态"，建议一次性修掉。

---

## P0 · 会卡死、丢数据或行为明显错误

| # | 位置 | 问题 | 触发与后果 | 修法 |
|---|---|---|---|---|
| 1 | `app.js:522-527, 578` | **轮询没有作业令牌**：`poll()` 只判断 `currentJob` 是否存在，响应回来后直接写 `currentJob.state = snap.state`，不校验这个响应属于哪个作业 | 生成中点「新建对话」或点左栏另一条生成中记录、再发一条 —— 旧请求在飞行中，`clearTimeout` 掐不掉它。旧响应回来会把**旧状态写进新作业**；若旧快照是 `failed`，会执行 `currentJob=null; setBusy(false)`，新作业还在跑却已解锁，气泡永久转圈 | `poll(jobId)`：请求前记 `const id = currentJob.id`，`.then` 里先 `if (!currentJob || currentJob.id !== id) return;` |
| 2 | `app.js:584-596` + `pipeline.py:151-160` | **「停止」对进行中的作业是空操作**：前端 `abortGeneration` 调 `/cancel` 并 `catch(_){}` 静默吞错；后端 `cancel` 只允许 `queued`/`paused_awaiting_confirmation` | 生成中点「停止」→ 界面显示"已放弃本次生成"（作业已解锁），但后端线程**照跑完并落盘 result.json** → 历史里凭空多出一条"已完成"记录，用户以为停了其实 token 照烧 | 后端：Job 加 `cancel_event`，各阶段入口检查；前端：把错误 toast 出来，别静默 |
| 3 | `server.py:234` + `pipeline.py:119` | **无锁并发迭代字典**：`for job in pipeline.jobs.values()` 与工作线程 `self.jobs[jid] = job` 无同步 | 前端 1s 轮询 `/api/history`，新建作业插入键的瞬间 → `RuntimeError: dictionary changed size during iteration` → 500，列表闪空 | `Pipeline` 加 `threading.Lock`；`history` 内先 `list(self.jobs.values())` 拷贝 |

---

## P1 · 功能错误 / 状态不一致 / 点不到

| # | 位置 | 问题 | 后果 | 修法 |
|---|---|---|---|---|
| 4 | `styles.css`（全文无 `.dot`） | **状态圆点没有任何样式**：`app.js:1045` 写 `class="dot"` / `dot no`，但 CSS 里 0 条 `.dot` 规则 → 空 span 宽高为 0 | 会话列表左侧的"合格/不合格"标记**完全不可见** | 补 `.dot{width:6px;height:6px;border-radius:50%;background:var(--text-3)} .dot.no{background:var(--bad)}` |
| 5 | `styles.css:842` vs `485` | **层级倒挂**：`.overlay` z-index 50 < `.screen` 60 | 确认弹层（删除确认/选题确认）打开时按 `Ctrl+,` 打开设置页，整窗设置页盖住弹层 → 弹层消失且点不到，只能 Esc | `.overlay` 提到 70（低于 select-menu 80、toast 99），或在弹层打开时拦截 `Ctrl+,` |
| 6 | `styles.css:682` | **「✎ 重写」默认 `opacity:0`**，与刚修完的 `.sess-del` 完全同源 | 用户不知道段落可以重写（只有 hover 卡片才显形） | 照抄 `.sess-del`：默认 `opacity:.45`，hover 到 1 |
| 7 | `styles.css:394` | `.sess-sub` 缺 `padding-right:32px`（`.sess-top` 有） | 删除按钮绝对定位盖在行右侧 28px，副标题偏长时**看着是文字、点下去是删除** | `.sess-sub{padding-right:32px}` |
| 8 | `styles.css:189` | `--ok-soft` **未定义**（`:root` 只定义了 warn/bad） | 该声明无效 → 「✓ 合格」绿字无底色，与 warn/bad 胶囊视觉不一致 | `:root` 补 `--ok-soft` |
| 9 | `app.js:889` | 历史回看时点「重写」**静默失效**：`if (!currentJob …) return`，而 `openSession` 会把 `currentJob` 置空 | 打开一条历史记录后点 ✎ 或回车，毫无反应、无提示 | 校验 `currentJob?.id === currentResult.id`，否则 toast「已结束的记录不支持单段重写，请用『换一版』」 |
| 10 | `app.js:1416-1418` | 全局 `change` 只处理 `#settings-screen` 内的元素 | 快捷条（主界面）改时长/风格 → 不触发 `updateStale()`，「参数已修改，当前展示的是旧参数结果」横幅不出现 | 条件改为 `if (e.target.closest("#settings-screen, #quick-params, #composer"))` |
| 11 | `app.js:1497 → 1416/1423` | **二次 `boot()` 导致全局监听重复注册**：生成行业包后 `pg-done` 调 `await boot()`，而 `bindStatic()` 里的 `document.addEventListener("change"/"keydown")` 没有守卫 | 建完行业包后：按 `Ctrl+,` 打开设置**立刻被第二个 handler 关掉**；`Ctrl+\` 折叠侧栏失效；change 处理两遍 | `bindStatic` 加 `_bound` 守卫；或 `pg-done` 改为只 `refreshPacks()` |
| 12 | `app.js:1150/1133/1101` | 待确认浮层不随场景关闭：新建对话 / 切历史 / attachJob 都只 `closeSettings()` | `#confirm-overlay` 仍盖在最上层，此时点「继续」→ `currentJob.id` 为 null → 弹「确认失败」 | `abandonRunningJob()` 内一并 `$("confirm-overlay").classList.add("hidden")` |
| 13 | `pipeline.py:123/137` | `confirm` / `rewrite_segment` **不置中间态就起线程**，状态检查形同虚设 | 连点两次「继续」→ 起两个 `_continue_write`（双倍 token、steps 重复、result 互相覆盖）；重写全程 `state` 恒为 `done`，可并发发起多次并同时写盘 | 入口即 `update(state="writing"/"rewriting")` 并二次校验 |
| 14 | `server.py:70` | `cfg = load_config(root)` **只在启动时加载一次** | 用户在设置里保存 Key 后，`/api/meta` 仍报 `has_api_key=false`、`/api/packs/create` 仍报"未配置 API Key"，而 `/api/generate` 已重新加载 —— 同一功能两种结果 | 改为每次 `load_config(root)` |
| 15 | `server.py:75` | CORS `allow_origins=["*"]` 且无任何鉴权 | 本机任意网页可读写/删除历史；更可 `POST /api/config` 把 `base_url` 改到攻击站点，下次请求把 `Bearer api_key` 送出去 | 限定 origin、或加一次性 token |

---

## P2 · 健壮性与细节

- `app.js:908` `waitDone` 里 `const body = activeMsg` 未判空 —— 重写途中切走 → 抛错并弹误导性的「重写失败」
- `app.js:578` 轮询 `catch` 无退避无上限：引擎挂掉后每 1.5s 空转、界面一直转圈且无任何提示
- `app.js:811/828` 导出未 `revokeObjectURL`，每次导出泄漏一个 Blob
- `app.js:1093` 删除当前正在查看的记录后，只置空 `currentResult`，消息流与头部标题仍显示已删内容
- `app.js:1196/1210` 每次打开设置都 `preloadSettings()`，会覆盖未保存的 base_url / model
- `pipeline.py:417/454、533-548`、`config.py:97` 落盘全部非原子：写一半崩 → 坏文件；坏 `result.json` 被 `/api/history` 的 except 静默跳过（记录凭空消失），而 `/api/history/{id}` 无 try/except → 500
- `pipeline.py:536 vs 416` job.json 用 `now()` 取日期、result.json 用 `created_at`，跨零点落在两个目录，`delete` 只删其一
- `styles.css:675` `.card-text` 有 `pre-wrap` 无 `word-break`：长 URL 会撑出横向滚动条
- `styles.css:213` `button:focus-visible` 的焦点环被后面的 `.ghost/.primary/.send` 的 `box-shadow` 冲掉 → 键盘聚焦零反馈
- `styles.css:825` `.quick-bar.locked` 只改光标，未 `pointer-events:none` → 看着禁用仍能点开改参数（改了也不生效）
- `llm.py:124` 流式分支直接 return，绕过 153-159 的空 content 诊断（而 pipeline 全程走流式）
- `packgen.py:99 vs 172` 先建目录后校验，失败残留目录导致重试被 `FileExistsError` 挡成 409

---

## 已排除（审查中提出但核对后不成立）

- ~~「`bindStatic()` 被重复调用」~~ —— 只有 `boot()` 一处调用（L252）。**但**真实成因是 `pg-done` 二次 `boot()`（见 P1-11），症状相同、根因不同。
- `main.js` 的 `engineProc.kill()` 是否残留孙进程 —— 该引擎不起子进程，不成立。
- XSS：`esc` 未转义单引号，但目前所有用户输入都进文本节点，未找到可利用路径。

---

## 建议的修复批次

- **批次 A（P0，3 条）**：轮询令牌 / 停止语义 / 后端加锁 —— 直接消灭"卡死 + 幽灵记录"
- **批次 B（P1 样式，5 条）**：`.dot` / `.overlay` 层级 / 重写按钮可见 / `.sess-sub` 安全区 / `--ok-soft` —— 纯 CSS，风险极低
- **批次 C（P1 逻辑，5 条）**：重写静默失效 / 快捷条过期提示 / 二次 boot 守卫 / 确认浮层随场景关闭 / 后端中间态
- **批次 D（稳健与安全）**：配置闭包、CORS 收紧、落盘原子写

每批次改完跑 `_verify/verify.js`（当前 77/77）并补对应断言。
