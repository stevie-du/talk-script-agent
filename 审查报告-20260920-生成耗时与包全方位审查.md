# TalkScript 审查报告 · 2026-09-20 —— 生成耗时 · 生成参数 · 行业包 · 技能包

> 范围：`app/`（llm / pipeline / checker / prompts / knowledge / config / jobs / server / packgen）· `packs/elevator/` 全部 22 个文件 · `desktop/renderer/js/`（jobs / progress / settings / ui）· `config.yaml` · 87 份 `generated/*/*/result.json` 与 job.json 离线解析
> 方法：**真实模型调用实测**（两轮不同模型 / 两个端点）+ 在进程内 monkeypatch 计时（不落盘、不改文件）+ 静态全量审读 + 上一轮报告逐项核对
> 基线：`develop @ 660b715`，工作区含**用户自己未提交的 6 个文件改动**（`_verify/verify.js`、`desktop/renderer/js/{jobs,progress,settings,ui}.js`、`styles.css`）—— 本次审查全程只读，未触碰
> 测试基线：`.venv/Scripts/python.exe -m pytest` → **288 通过**（18 个测试文件）。⚠ 全绿而本文 P0 全部存在，说明这些分支没有被覆盖

---

## 一、实测结果（全部真跑，不是引用）

### 1.1 端到端真实生成（当前模型 `deepseek-v4-flash-0731` @ `:9200`）

走完整 `Pipeline.start_generate` → `wait_job`，参数：`elevator` / 60s / 抖音 / 家用 / 业主乘客 / voice=strong / format=both。产物落在 `generated/20260920/20260920-153453-940cb7/`。

```
select  74.3s  ✓ 成功（plan 已产出）
write  129.4s  ✗ EmptyContentError（HTTP 200，content 为空）
────────────────────────────────────────────
总 203.8s → 作业 failed，零产出，全程未触发任何一次重试
```

### 1.2 同一 write 提示词（渲染后 8269 字符）的重复测量

| 变体 | 耗时 | 正文字符 | 思考字符 | finish_reason |
|---|---|---|---|---|
| 含分镜（**现状**）×5 | 82.8 / 93.5 / 102.9 / **110.2** / **127.3** | 1511~2268 | 1686~**18913** | stop |
| **去掉 storyboard** ×2 | **12.1 / 22.6** | 688~721 | **718~2414** | stop |
| 含分镜 + `max_tokens=6000` ×2 | 52.4 / **61.7** | 1722 / **0** | 6382 / **9449** | stop / **length** |
| 含分镜 + 非流式 ×2 | 22.2 / 86.9 | 完整 | 616 tok | stop |
| 含分镜 + **无** `response_format` ×1 | **125.2** | 1660 | **21336** | stop |

### 1.3 上一轮模型 `hy4-preview` @ `:9177`（09-20 上午，已被用户换下）

| 测量 | 结果 |
|---|---|
| 成功作业 `20260920-101043` | **select 173s + write 352s = 525s**，校验 0s，回炉 0 次 |
| 失败作业 ×2 | `12:18:47`、`12:25:20`，均为 write 空内容 failed |
| write 原始调用 | 190.9s，`reasoning_tokens=15148`，`finish=length`，正文截到 1633 字符 → JSON 不完整 |
| `max_tokens` 对照 | mt=800 → 思考 800；mt=2000 → 思考 2000；mt=16000 → 思考 5464 + 正文 650 |
| `enable_thinking:false` / `chat_template_kwargs` | **均被端点忽略**（思考照旧） |

### 1.4 提示词体积（实测渲染结果，非估算）

| 阶段 | system | user | 主要构成 |
|---|---|---|---|
| select | 97~407 | 3307~5110 | `hooks` 2052 + `topics_slice` 540 + `audience_slice` **364（命中）/ 2167（未命中→整份）** |
| write | 407 | 8099~8320 | `growth` 2328 + `voice_block` 2521 + `facts_block` 2404 + `plan_json` 108~372 |

> 静态部分占 write 的 90.1%。**但提示词不是慢的主因**：write 提示词只约 2~4.8k token，而思考烧到 6k~19k。慢在输出侧的推理量。

---

## 二、根因（三条，每条都有上面表格里可对上的数字）

### R1 `max_tokens` 在**反向**决定思考长度

两个模型上思考量都随 `max_tokens` 单调上升：

- `hy4-preview`：800→800、2000→2000、16000→5464
- `deepseek-v4-flash`：6000→6382~9449、16000→17440~18913

即 `max_tokens` 事实上是**思考上限**。当前 `config.yaml:5` 的 `16000` 字面上就是在命令模型"往死里想"。

**由此推翻两件事**：
1. `app/llm.py:226` 的错误文案"请调大 config.yaml 里的 llm.max_tokens"**方向是错的** —— 调大只会更慢，并更容易想满预算导致 content=0。本文 3 条失败作业（12:18 / 12:25 / 15:34）全是这一形态。
2. 但**单纯调小也不对**：`mt=6000` 两次里有一次 `finish=length / content=0`。稳态必须靠"降低思考需求 + 让预算可分阶段 + 让空内容可补救"三件一起，见第六节。

### R2 一次调用塞了两件异质的活

`skill.yaml:74-99` 要求 write 同一轮产出 `sections`（5 段口播）**+ storyboard（8~11 个分镜）**，而分镜要从口播文案反推时间轴 —— 这是把规划复杂度推到最高的一脚。

实测：**砍掉 storyboard，同一提示词从 110~127s 降到 12~23s（5~10 倍），思考字符从 1.7 万降到 7 百。**

而分镜从头到尾**没有被校验过**：`app/checker.py:234` 的判据是 `full_text = "\n".join(s.get("text","") for s in sections)`，只看 sections 的 text。也就是说，占了绝大部分推理量的那一半输出，校验层一个字都不消费。

### R3 尾部失败零补救，且代码在结构上看不见"被截断"

三处叠加：

1. `app/llm.py:130` 的重试只 catch `(ValueError, ValidationError)`。content 为空时 `_ensure_content`（`llm.py:223`）抛的 `EmptyContentError` 从 `_complete` 直接逃逸出 `chat_json` → **不重试，整条作业 failed**。用户等 130~200s 拿到"生成失败"。
2. `app/llm.py:180` 流式分支 `self._ensure_content(content, None, streamed=True)` —— **finish_reason 写死成字面量 `None`**；`_stream_once`（`llm.py:248-269`）只读 `delta`，从不读 `choices[].finish_reason`。唯一读它的 `llm.py:210` 在非流式分支，而 pipeline 全量传 `on_delta` → **代码没有任何一处知道"这次是被截断的"**。
3. 于是 `chat_json` 的解析重试（`llm.py:125-137`）对截断是**用完全相同的 payload 再打一遍**（同 `max_tokens`、同 `temperature`，只多塞一句"只输出合法 JSON"）→ 注定复现，净多烧一次 110~130s。

---

## 三、对照成熟口播智能体的通行流程

成熟链路（度加 / 蝉妈妈这类，以及 Self-Refine 这类公开方法）的稳定形态是：
**选题/调研 → 结构大纲 → 成稿 → 局部润色 → 合规扫描**，其中两条与本项目当前实现相反：

| 通行做法 | 本项目现状 | 代价（实测） |
|---|---|---|
| **中间产物可复用**：选题一次、成稿多次 | `select` 的 `plan` 全项目**零下游消费方**（`grep plan` 在 `desktop/renderer/js/*.js`、`app/server.py` 均无命中）。它存在的理由"分步确认"已于 2026-09-19 整条删除（`app/jobs.py:19-24`，删除理由写的正是"成本收益不划算"）—— **收益删了，开销留下** | 74~173s／次 |
| **修订是把上一版喂回去改**（Self-Refine 的定义即如此） | `app/pipeline.py:338` 每轮回炉重发 9270 字符提示词、**不含上一版正文**，`_violation_feedback` 只给统计数字 → 是"重新掷骰"不是"改骰子" | 每轮 12~130s，且换出一篇别的稿子 |
| **违规走局部改写** | 段落级问题（超配额、命中禁用词）本可路由到已有的 `rewrite_segment`（`pipeline.py:494`，不注入 growth/voice/facts，请求体小一个数量级），现在一律走全文回炉 | 13 条真实作业 6 条走到 r2（46%） |

**结论：当前"两段式 + 全文回炉"的规划本身不合理**，不是模型慢那么简单。慢 = 规划把一次能做完的活拆成 2~5 次重推理 × 每次的推理量被 `max_tokens` 和"分镜"推高。

---

## 四、问题清单

标注：✅ = 本报告作者亲自复跑/复读验证过（不是转抄子 agent 结论）。

### 生成参数

**P0-1** ✅ `app/llm.py:180` 流式分支 finish_reason 写死 `None`；`_stream_once`（`:248-269`）从不读 `choices[].finish_reason`；唯一读它的 `:210` 在 pipeline 永不经过的非流式分支。
触发：任何一次真实生成。后果：截断不可识别 → 重试参数不变 → 白烧一次完整调用（R3）。
修：`_stream_once` 累计并回传 `finish_reason`（改返回 `(text, finish)`），`:180` 透传。

**P0-2** ✅ `app/llm.py:130` `chat_json` 的重试只 catch `(ValueError, ValidationError)`，`EmptyContentError` 逃逸 → 作业直接 failed、不重试。
触发：思考吃满预算（今天 3 次）。修：把空内容纳入可重试，且**重试必须升级参数**（提预算，或退回"只出 sections"的轻结构），不能原样重发。

**P0-3** ✅ `app/config.py:38` / `config.yaml:5` `max_tokens: 16000` 全阶段一刀切（`app/llm.py:169`），选题只需 400~650 字正文却与撰写同预算。
修：分阶段预算。**管道现成** —— `_complete` 已有 `max_tokens` 形参，目前只有 `ping()`（`llm.py:150`）在用；给 `_select` 传 4000、`rewrite_segment` 传 3000、write 保持。

**P0-4** ✅ `desktop/renderer/js/settings.js:56-59` 的 `NUM_FIELDS` 只剩 `retries` / `timeout`（`max_tokens`、`temperature` 已于 09-17 移出设置页）。而 P0-2/P0-3 的错误文案让用户去调 `llm.max_tokens` —— 打包版用户在界面上**找不到这个键**，只能手改 `%APPDATA%\TalkScript\config.yaml`，且提示方向还是反的。
修：`max_tokens`/`temperature` 加回设置页；`_ensure_content` 文案改用**本次真实预算**并把方向改成"该模型思考吃预算，建议降 recheck_rounds 或换非推理档"。

**P1-5** ✅ `config.yaml:3-4` `timeout: 180 / retries: 3`。httpx 流式下 180 是**两次 read 之间的最大间隔**，上游思考期不 flush 就在 180s 抛 `ReadTimeout`；`generated/20260912/20260912-152825/job.json` 两条 retry 间隔正好 **180s / 181s**、note 全是 `网络异常（ReadTimeout）`。
调用上界 = `(1+1+recheck_rounds) × (1+chat_json.max_retries) × (1+llm.retries)` = `4×2×4 = 32` 个 HTTP 请求 ≈ **98 分钟**，全程无"整作业预算"。`README.md:28` 写的"只有 2~4 次"只算了正常路径。
另：`get_client()` 里 `httpx.Timeout(180.0, connect=20.0)`（`llm.py:57`）形同虚设 —— `:181/:240` 传 float 会把 connect 一起改成 180。

**P1-6** ✅ `app/server.py:486` `pipeline.reload_llm()` 注释写"不影响正在跑的作业"，但 `pipeline.py:321/344` 每阶段每轮**现取** `self.llm` → 作业 A 跑到 r2 时 B 点生成，A 会中途换模型/换预算。这正是 `pipeline.py:152-155` 一段注释声称已修掉的场景。`rewrite_segment` 入口（`server.py:497-504`）又不 reload，两条入口口径不一。
修：作业开始时把 client 抓成局部变量一路传参，或给 `Job` 挂 `client` 字段。

**P1-7** ✅ `app/jobs.py:141-147` + `app/pipeline.py:587-589`：`begin_stream` 只在**收到第一个增量时**才触发。实测 select 首字节前静默 **74s**，这期间快照没有 `stream` 键 → `progress.js:155-158` 把思考块 `hidden`，界面只剩"已用 N 秒"。这是"以为卡死了"的那一段。
修：阶段一开始就 `begin_stream`，思考块显示"等待模型首个 token…"。

**P1-8** ✅ `app/pipeline.py:321-323` 选题调用不传 `temperature`，`reroll` 的提温只在 `:341-343` 作用于 write → 「换一版」花 74~173s 重掷出一个几乎一样的 plan。
修：见第六节方案 10。

**P2-9** ✅ `desktop/renderer/js/jobs.js:22` `REWRITE_TIMEOUT_MS = 180000`，而同一模型实测 82~130s 起步；`waitRewriteDone`（`jobs.js:457-475`）**全程不调 `renderProgress`**，唯一反馈是 `jobs.js:440` 一句 2.2s 就消失的 toast。后端还在跑并会写回 result.json（`pipeline.py:536`），前端已抛"重写超时"→ 用户看到"重写失败"、历史里却是改好的内容，**两边相反**。另 `api.job(jid, true)` 每 700ms 序列化整份产物（正是 `jobs.py:171-176` 注释要避免的事）。

**P2-10** ✅ `app/llm.py:149-150` `ping()` 用 `max_tokens=16` → 推理模型必然空 → 设置页显示"连接失败……请调大 llm.max_tokens（**当前 16000**）"。本轮实测就截到了这句：数字用的是 16，提示说的是 16000。

**P2-11** ✅ `app/config.py:231-238` `_env_num` 不卡数值区间（`server.py:630-638` 的 `NUMERIC_BOUNDS` 在这条路上不生效），而 `CONFIG_TEMPLATE`（`config.py:146-150`）把环境变量列为"推荐"方式。`TALKSCRIPT_MAX_TOKENS=100` → 每次生成必然空内容；`TALKSCRIPT_RETRIES=999` → `attempts=1000`。

**P2-12** ✅ `app/llm.py:35,271-274` 429 与 5xx 同一档退避（封顶 8s）、不读 `Retry-After`。`generated/20260912/20260912-105341` 日志里 3 次尝试在约 3 秒内全部撞 429 耗尽。

**P2-13** ✅ `app/pipeline.py:573-578` 文档字符串称"每进入一个阶段（含每次重试）都会清空缓冲"，但 `on_delta=self._delta_handler(...)` 每**轮**只求值一次，`state["started"]` 一旦 True 就不再 `begin_stream` → 被丢弃的尝试也累进 `stream_reasoning_len`（`jobs.py:166`），界面"思考 N 字"可能是两遍之和。

**P3-14** ✅ `app/pipeline.py:101-104` `PERSISTED_PARAMS` 不含 model / max_tokens / temperature → 产物里查不到当时用的是什么配置，事后无法解释"这条为什么 525s"。本轮排查只能靠当前 `config.yaml` 倒推，而它今天已被改过两次。

**P3-15** ✅ `app/llm.py:258-260` 的 `if not choices: continue` 恰好把 OpenAI 兼容带 `usage` 的最后一个 chunk 丢掉 —— 上游本来就在回 `completion_tokens_details.reasoning_tokens`（本文所有思考 token 数字都是从原始响应里拿到的）。接上 usage 记进 step 日志，这类问题下次不用抓包。

**P3-16** ✅ `README.md:28`"单次生成的模型调用只有 2~4 次"与 P1-5 的公式对不上；`app/server.py:521-522` 注释"前端每 3 秒调一次"实际是 `jobs.js:20` 的 900ms。

### 行业包 `packs/elevator/`

**P0-17** ✅ **脚手架假事实已污染真实产物。** `private/cases.yaml:12-14` 的 `year: 2025 / units: 12`、`private/products.yaml:29-30` 的 `source: "2026版产品手册" / verified: "2026-09-06"`（伪造溯源章）经 `prompts.py:111-113` 以 **【私有知识库（优先作为事实来源）】** 标题注入，**每个回炉轮重发一次**。
在 `generated/20260920/20260920-101043-82b9e9/result.json` 第 3 个 point 里查到了原句 **"去年那12台"** —— 模型在讲"检验标志怎么看"的段落里把模板占位台量写成了交付业绩，且因 `public: true` 连匿名化都不触发。**这是合规风险，不只是性能问题。**
根因：`app/knowledge.py:508-511` 的 `strip_empty` 只对**叶子字符串**判 `示例：`，**条目级示例剔不掉**（`products.yaml:7` 的注释说明包作者知道，但机制不支持）。
修：① 立刻清空三个私有文件的示例数字与 `source/verified`；② `strip_empty` 增加条目级判定（dict 任一 string 含"示例："→ 整条 None），并改用显式 `_example: true` 标记而不是猜字符串；③ `source/verified` 为空的条目注入时降级为"【未核实，只能用于 {{待补}} 占位】"。

**P0-18** ✅ **51.3% 的知识文件从未进过提示词，而漏掉的恰好是最不可替代的部分。**
`pack.yaml:87-92` 声明了 `files.select / write / compliance / facts`，但引擎唯一消费的是 `files.private`（`app/knowledge.py:471`）；阶段注入实际来自 `skill.yaml` 的 `stages.<阶段>.files`（`app/prompts.py:60`）。实测永不被注入：

| 文件 | 字符 | 内容 |
|---|---|---|
| `compliance/industry.md` | 2047 | 统一救援 5 步口径（"唯一标准答案，不得增删"）、反谣言库 7 条、危险行为禁演清单、`不要写死 96333`、`119 不作首选`、术语规范、禁止输出操作方法 |
| `knowledge/standards.md` | 2195 | 16 项标准编号 +"现行是 GB/T 7588.1-2020，不是 GB 7588-2003"、16 条术语→口语对照 |
| `knowledge/ideas.md` | 2615 | 60 条已验证选题 |
| `compliance/platform.md` | 2001 | 平台审核特点、**AI 生成/虚构演绎强制标注要求** |
| `compliance/ad-law.md` | 1535 | 绝对化词族（词表的上游依据） |
| `rules/duration.md` / `rules/output-template.md` | 842 / 2440 | 见 P2-24 |
| **合计** | **13675 / 26647 = 51.3%** | |

后果可证：`private/faq.yaml:39` 在注入文本里写"统一口径见 `compliance/industry.md` 反谣言库"—— 模型手里没这个文件，最要命的救援话术现在**自由发挥**；`banwords.yaml` 对 `96333`、`打119`、法律定性词**零覆盖**，这条防线目前是 0 层。

**P0-19** ✅ **死结构会复制给每一个新包。** `app/packgen.py:253-264` 逐字写同一份孤儿 `files.*`，`:215` 还专门生成一份永不注入的 `compliance/industry.md`，`:210` 生成一份"不注入"的 standards.md。

**P1-20** ✅ **`cta` 是界面摆设，从未进入任何提示词。** 把 `skill.yaml` 的占位符全列出来：`$anti_ai_rule $audience $audience_slice $context $duration $facts_block $feedback_block $growth $hooks $industry $persona $plan_json $platform $points $quota_body_per $quota_cta $quota_hook $quota_total $rate $seg_* $segment $style $topic $topics_slice $voice_block` —— **没有 `$cta`**（`$quota_cta` 是字数配额），`app/prompts.py:78-89` 的 `base_ctx` 也不提供。
而 `app/pipeline.py:101-104` 把 `cta` 写进 `result.json.params` → **产物声称"结尾引导：私信"，脚本里根本没按它写**。
更糟：`banwords.yaml:89-90` 把 `私信我/私聊/加我` 列为 hard、`:180` 小红书 extra_hard `私信领取` —— 用户在界面上选「私信」这个动作，语义上等于要求模型写硬禁词。

**P1-21** ✅ **词表"该拦的零覆盖、拦住的 100% 误伤"。**
- `soft` 里的 `第一` 命中 **169/175 轮**（"第一件事/第一步"—— `knowledge/ideas.md:11`、`audience.md:16` 自己就在推荐这些说法）
- `ad-law.md` 列了而表里没有的至少 30 个：`最低价 最便宜 最好 最佳 最优 全国第一 销量第一 行业领先 顶尖 彻底 百分百见效 NO.1 TOP1 保证通过验收`… —— **"最X"这一整类广告法最高频违规目前零覆盖**（表里只有被 `MIN_WORD_LEN=2` 丢弃的单字 `最`）
- ✅ 复跑确认跨表重复计数：`"这个方案绝对安全，永久免维护，官方唯一授权，100%安全"` → `hard` 4 项 **加** `soft` 4 项（`绝对/永久/唯一/100%`），同一处违规报两遍。`checker.py:146` 注释声称"长词优先已保证不重叠" —— **只在同表成立**
- 真实运行（非夹具）只有 2 条作业，`hard_hits` 均为 0，其中一次回炉原因是 **−54.4% 写太短**

修：① `scan()` 先按 hard 命中建位置掩码再跑 soft（一处修掉 4 个假 soft）；② `soft` 删 `第一`、补"最X"词族；③ 加一条测试让 `ad-law.md` 词族表与 `banwords.yaml` 对账（`:10-11` 注释自己承认要手工同步，已漂 30 词）。

**P1-22** ✅ **切片未命中→整份注入，本轮实测踩中。** `app/knowledge.py:401-405`：传 `audience="业主"`（合法选项是 `业主/乘客`、`物业业委会`、`维保公司`、`经销商/总包`、`设计师/家装`）→ 返回 **2167 字符 = 整份 audience.md**，其中 5 个别的受众章节会把模型注意力拉向"物业/开发商"话术。正常切片是 181~392。
两条降级还不对称：未知 `segment` 退到「通用」章节（355 字符，体积正常），未知 `audience` 退到全文（6 倍）。
`param_audit`（`knowledge.py:237-242`）只遍历 `params.audience.options`，5 个 option 全部映射正常 → **结构上不可能报出这条降级**（实跑返回 `{}`）。`schemas.py:27` 对 audience 是不校验选项的自由串。

**P1-23** ✅ **86/87 份产物是 mock 夹具且没有 `mock` 标记**（`app/mock_fixtures.py:39` 故意首轮写"政府补贴"）。直接后果：**任何"回炉到底因为什么"的统计都无法回答** —— `hard_hits` 的 79 次命中绝大多数来自夹具。
修：`ScriptResult` 加 `mock: bool`，夹具产物落 `mock: true`。

**P2-24** ✅ `knowledge/voice.md` **在同一份文件里自我否定**（每轮 write 注入 1696 字符）：`:10,21-24` 明列 ❌`撂挑子`/`整栋楼骂你`，`:43,52` 又要求"加一句带态度的：整栋楼骂你"、范例直接用 `撂挑子`；`:34`"必须含 ≥1 个具体数字"与 `topics.md:3`"禁止估算填充"冲突 —— 这正是 P0-17 的动因。
术语口径相反且生效的是错的那份：`voice.md:40` 教"制动器→刹车"（注入），`compliance/industry.md:61-74` 把"电梯刹车"列为禁用错误口语（不注入）。真实产物 `20260920-145654` 写的正是"关键查三样：刹车、门锁、速度保护"。
**矛盾比长度更贵**：模型每轮要静默仲裁一次，仲裁结果还随温度漂移 → 直接表现为回炉。

**P2-25** ✅ 信噪比：`patterns/growth.md` 2328 字符里行业专属约 250（值得留的只有 `:56-68` 高客单信任表、`:86` 电梯 CTA、`:99-101` 本地化时效），其余与 `skill.yaml:64-71`、`anti-ai-smell.md` 三处重复；`hooks.md:37-95` 5 套风格模板每轮只用 1 套（约 1040 字符噪声），`:84-95` 与 write.system 逐条重复。
私有资料实测剔除率：`products` 2144→645、`service` 1583→**74**（95% 是空模板）、`cases` 817→173、`faq` 2172→1378。

**P2-26** ✅ 配额自相矛盾：`pack.yaml:60-65` 给 90s→3 点、180s→4 点，配 `quota_table` 得每点 100/150 字 = **22~33 秒一个信息点**，而 `growth.md:11` 要求"每 10~15 秒一个新信息点"（超 2.2 倍）。`rules/duration.md:41` 又写"60s 及以上保留 3 个要点"（与 180s→4 冲突，该文件不注入所以是纯文档腐）；`:7` 的"快节奏 5.5 字/秒"在 `rate_by_style` 里无映射。
15 秒档 `pack.yaml:53`：hook15+body40+cta15 → **43% 字数在正文之外**，15 秒里花 3 秒念 CTA。

**P2-27** ✅ ±10% 时长门槛硬编码在 `app/checker.py:259`，不可配。60s/4.5 下通过窗口只有 234~288 字（**±27 字容错**），实测 46% 掉出去。15 秒档合格区间 56~70 字（±7 字），同时要塞进 10 类钩子 + 具体数字 + open loop + 反 AI 清单 —— **物理装不下**（实测 124~131 字的三次尝试全为 +90%/+101%）。

**P3-28** ✅ 悬空引用 4 处（模型被告知去查它没有的文档）：`hooks.md:71`、`growth.md:97`（→ ad-law.md 未注入）、`growth.md:6`（→ hooks.md 不在 write 阶段）、`private/faq.yaml:39`（→ industry.md 未注入）、`anti-ai-smell.md:27`（→ standard 档不注入 voice.md）。
`banwords.yaml:6` 宣传 `promote` 机制而全包零处使用。`audience.md:152-176`（交叉表 + 同题异受众反例，611 字符，价值高）在任何 `##` 之后，**切片永远够不到**。

**P3-29** ✅ `schemas.py:55` `TopicPlan.points` 无长度上限，`select` 提示词写"恰好 $points 条"，模型给 5 条时 `write` 仍被告知 3 条 —— 真实作业 `101043` 的主题写"5 个坑"而只有 3 个 point，就是这条。

### 技能包

**P1-30** ✅ `format=voice`（仅口播）**不减一次生成**：`app/pipeline.py:433-434` 只是在校验通过、组装产物前把 `storyboard = []` 丢掉，`skill.yaml:82-99` 无条件要求输出分镜并"覆盖整条片子的时间线"。实测分镜数 8 / 9 / 11 飘忽（无上限约束），而 `_build_scenes`（`pipeline.py:405-407`）只取前 `len(sections)` 个。结合 R2，这一项单独就值 5~10 倍。

**P2-31** ✅ 输出结构三套口径：`rules/output-template.md:3-11`（①-⑤ markdown）+ `:120-151`（含 `meta/quota/compliance/publish` 的 JSON）vs `skill.yaml:85-93`（只有 `sections/storyboard`）vs `app/schemas.py:62-73`。目前因该文件不注入所以没炸；一旦按 P0-18 接上 `rules/*`，模型会在两种结构间摇摆（且 `ScriptDraft` 会静默丢掉多给的键）。
另：`_build_scenes` 读 `shot.get("bgm")/("transition")/("shot_type")/("style")`（`pipeline.py:416-425`），而 `StoryboardShot` 只有 `time/shot/subtitle/sfx/note/voiceover` → **`scenes[].audio.bgm` 恒为空**，`skill.yaml:92` 让模型写的"音效/BGM"进不了场景序列。

**P2-32** ✅ `packs/elevator/skill.yaml` 全文 `grep` 仍留有 `$growth_block`（在 `:11-13` 的注释里，无害），但 `prompts.py:43-56` 的 `unfilled()` 只检查模板原文，而 `_render_stage`（`pipeline.py:309-315`）把它记进作业日志 —— 界面上不显眼。建议包详情/日志页签把"未填充占位符"提为一级信号。

**P3-33** ✅ `private/README.md:19-26` 承诺的"丢文件到 `raw/` 说处理一下"在 `app/` 里**没有任何实现**（只有只读 `GET /api/packs/{name}/file`，`server.py:397`），`app/export_skill.py:112` 重复同一承诺。这正是示例数据常年留在包里的原因。

---

## 五、与上一轮报告（`全方位审查-20260917.md`）的核对

| 20260917 条目 | 现状 | 证据 |
|---|---|---|
| §六 `pack.yaml` 契约核对第 148 行断言"**无包配了引擎不理的孤儿字段**" | ❌ **该结论本身是错的** | `files.select/write/compliance/facts` 四组共 10 个路径全是孤儿（本文 P0-18/P0-19）。同类问题最早在 `审查报告-20260907.md:81` 口径正确，被 20260917 改判成"无问题" |
| P1-1 `param-front` 与 `quick-params` 同 id select | ✅ 已修（提交 `3970e94`） | `settings.js`/`ui.js:253-259` 改用 `dataset.key`，真值收进 `state.genParams` |
| P1-2 生成期间同屏双状态无断言 | ✅ 已补（这一行的 ⬜ 与 §14.6 矛盾，是表格自己过时了） | `verify.js:1235` 与 `:1347` 两条：生成中左栏**不许出现重复 data-id**、收尾后仍无重复且**本次作业最多并入一条**（20260917 遗留的那一格，批次 9 补的） |
| P2-1 `chat_json` 解析重试共享 `messages` | ✅ 已随 P0-2 一起收口（第 6 轮复核复验） | 升级预算那一支换了 `max_tokens`、截断那一支还追加一对「上一版 + 请完整输出」消息；`tests/test_batch9_audit_findings.py:61-62` 直接断言两次请求的 payload 不许逐字节相同，而"到顶且没什么可补"的那一支现在**根本不发**（`llm.py:256-265`） |
| P2-3 `PAUSED_KEEP=20` 丢弃无信号 | ✅ 随分步确认整体删除而消亡 | `jobs.py:22-25,275-283`，提交 `8c57059` |
| P2-4 `fmtText` 与 `count_chars` 两份剥离口径 | ✅ 已消（这一行原先写 ⬜，是表格自己过时了） | 界面不再自己数字数：字数与配额一律读后端 `check.segments[].chars`（`result.js:35,159-164`），`fmtText` 只剩"渲染正文 + 打可跳转标记"一个职责（`result.js:172`）——同一件数字只有一个权威源 |
| P2-5 环境变量误配静默回落 | ✅ 已收口（本文 P2-11 那一半真的落地了） | `config.py:256` 越界先 `log.warning("环境变量 %s=%r 超出合法区间 […]，已夹逼为 …")` 再夹紧；复验 `TALKSCRIPT_MAX_TOKENS=100` → 256 + WARNING |
| P2-6 `export_skill.py` 死代码去留 | ⬜ 未拍板 | 235 行仍在，`api.js:78-80` 仍写"UI 入口已删" |
| §五 README 测试条数陈旧 | ✅ 已修 | README「测试」一节已去掉具体数字 |

**没有发现"修坏了"的项**；新增两处**注释与实现相反**（`server.py:486` → P1-6；`pipeline.py:576-578` → P2-13），都不是行为缺陷，是"照着注释写新代码会被误导"的缺陷。

---

## 六、优化方案

### 第 1 层 · 改配置即可，不动代码

| # | 动作 | 依据 |
|---|---|---|
| 1 | `skill.yaml:29` `recheck_rounds: 2 → 1` | 最坏 chat_json 4 → 2 |
| 2 | `config.yaml` `retries: 3 → 1` | 最坏 HTTP 32 → 8 |
| 3 | `config.yaml` `max_tokens: 16000 → 12000` | 别照搬"调大"（R1）；也**不能一刀切小**（mt=6000 实测 1/2 概率空内容），稳态靠第 5/6 条 |
| 4 | **清空 `private/products.yaml`、`cases.yaml`、`service.yaml` 的示例数字与 `source/verified`** | P0-17，这是**合规风险不是性能问题**，优先级最高 |

### 第 2 层 · 小改，收益最大

5. **把 storyboard 拆出 write 阶段**：sections 校验通过后再单独生成一次分镜。依据实测 **110~127s → 12~23s（5~10 倍）**；同时修掉 P1-30（`format=voice` 真正少生成一次）与 P2-26/P2-31 的分镜字段问题，并让回炉不再重做与校验无关的分镜（`checker.py:234` 只看 sections）。
   ⚠ 需拍板：会改变产物时序，界面多一个"分镜生成中"阶段。
6. **修 R3 三件套**（`app/llm.py` 约 25 行）：流式接上 `finish_reason`（P0-1）；空内容/`length` 纳入可重试且**重试必须升级参数**（P0-2）；`_ensure_content` 文案按本次真实预算给、方向改对（P0-4/P2-10）。
7. **per-stage 预算**：`_select` 4000、`rewrite_segment` 3000、write 保持（P0-3，`_complete` 的形参现成）。
8. **`max_tokens`/`temperature` 加回设置页**（P0-4），否则所有相关提示对打包版都是空话。
9. **记 `usage` 进 step 日志**（P3-15），界面显示"思考 N token / 正文 M 字"。

### 第 3 层 · 结构调整（对齐第三节通行流程）

10. **合并 select+write 为一次调用**（plan 作为输出前几个字段），或轻量版：`GenerateRequest` 加 `base_plan`，让「换一版」与失败重试复用已成功的 plan。省 74~173s（实测 select 占总耗时 17%~33%），且今天 3 条失败里已拿到手的选题被整段丢弃过。
11. **回炉带上一版正文**、只改违规段（Self-Refine 的正解）；段落级违规路由到 `rewrite_segment`（请求体小一个数量级）。
12. **把该注入的行业资产接上、把通用常识砍掉**：新增 `stages.write.files.redlines`（industry.md §2/3/4/6/7 精简 ~600 字）+ `stages.write.files.terms`（standards.md §5 ~400 字）；同时 `growth.md` 砍到 ~700、`hooks.md` 按 style 切片（复用现成 `slice_heading`）。**净提示词变小而行业覆盖变强**。`pack.yaml` 的 `files.*` 二选一：删键，或让引擎真消费（并同步改 `packgen.py:253-264`）。
13. **补三类守卫测试**（当前 288 项一条都没覆盖到）：① `packs/**/*.md` 每个文件必须被引用或在显式人读白名单里；② `param_audit` 增加"该参数值未被任何 `$占位符` 消费"这一维（同时逮住 P1-20 的 cta 与 P1-22 的整份回退）；③ `ad-law.md` 词族与 `banwords.yaml` 对账。
14. **短时长容差**：`checker.py:259` 改成 `abs(dev) <= max(10%, 3.0/duration*100)`（15s→±20%、60s→±10%），并把阈值提到包配置（P2-27）。

### 预期效果

| 路径 | 现状 | 做完第 1+2 层 | 再做第 3 层 |
|---|---|---|---|
| 正常成功 | 2 次调用，156~525s | 约 **30~60s**（write 拆掉分镜 + 分阶段预算） | 约 **15~25s**（单次调用） |
| 尾部（想满预算） | 130~200s 后**零产出** | 自动补救，不再零产出 | 同左 |
| 回炉 | 全文重生成 ×2 | 局部改写 | 局部改写 |
| 最坏上界 | ≈98 分钟 | ≈10 分钟 | ≈6 分钟 |

---

## 七、建议动手顺序与待拍板

**建议顺序**：第 2 层的 6/7/8/9（纯健壮性 + 可观测，不改产出内容）→ 第 2 层的 5（拆 storyboard）→ 第 1 层顺手改 → 第 3 层单独开一轮。

**需要拍板的两点**：
1. 方案 5 会改变产物时序（分镜在正文定稿后才出现，界面多一个"分镜生成中"阶段）。这是唯一能拿到 5~10 倍的一手，可接受吗？
2. 方案 10 要合并 select+write，动 `TopicPlan`/`ScriptDraft` 契约。做完整版，还是只做"复用 plan"的轻量版？

---

## 附录 A · 复现方法

```bash
# 1) 提示词体积（离线渲染，不发请求）
.venv/Scripts/python.exe -c "
import sys; sys.path.insert(0,'.')
from pathlib import Path
from app.knowledge import Pack; from app.prompts import PromptRenderer
pk=Pack(Path('.'),'elevator'); pr=PromptRenderer(pk)
# p 见本报告 1.4 节参数
"

# 2) 端到端真实生成（会向 generated/ 落一份产物）
.venv/Scripts/python.exe -c "
import sys,time; sys.path.insert(0,'.')
from pathlib import Path
from app.config import load_config
from app.pipeline import Pipeline, wait_job
from app.schemas import GenerateRequest
root=Path('.').resolve(); cfg=load_config(root,root)
pl=Pipeline(root,cfg)
jid=pl.start_generate(GenerateRequest(pack='elevator', topic='家用电梯怎么选，避开哪几个坑',
  segment='家用', audience='业主/乘客', duration=60, style='亲和接地气', platform='抖音',
  persona='维保工程师', cta='关注', facts='', voice='strong', format='both', reroll=False))
print(wait_job(pl,jid,timeout=1500)['state'])
"

# 3) json_mode × 流式 对照（1.2 节的 A/B/C/D）：同一渲染后提示词，
#    分别开关 response_format 与 stream，读 usage.completion_tokens_details.reasoning_tokens

# 4) 词表跨表重复计数
.venv/Scripts/python.exe -c "
import sys,yaml; sys.path.insert(0,'.')
from app.checker import Banwords
d=yaml.safe_load(open('packs/elevator/banwords.yaml',encoding='utf-8'))
print(Banwords(d).scan('这个方案绝对安全，永久免维护，官方唯一授权，100%安全','抖音'))
"

# 5) 切片整份注入
.venv/Scripts/python.exe -c "
import sys; sys.path.insert(0,'.')
from pathlib import Path
from app.knowledge import Pack
pk=Pack(Path('.'),'elevator')
for a in pk.param_options('audience')+['业主']:
    print(a, len(pk.audience_slice(a)))
"
```

## 附录 B · 本次审查留下的文件

- 新增本文档。
- `_verify/write_raw.txt`（未跟踪的临时件）被我的 write 阶段测试覆盖了 —— 里面现在是模型原始返回，需要的话可以删掉。
- **未修改任何被 git 跟踪的文件**；工作区那 6 个 `M` 是用户自己的未提交 UI 改动。
- 新增一份真实产物：`generated/20260920/20260920-153453-940cb7/`（failed 作业的 job.json，含完整 plan，是 P1-6/R3 的证据）。

---

## 八、二轮补充审查（2026-09-20 复核）

> 复核方式：逐文件亲读 `app/` 全部引擎代码与 `packs/elevator/` 全部 26 个文件、实测产物 `generated/20260920/20260920-101043-82b9e9/result.json`（含"12台"、无 mock 键）；三路并行调研（成熟口播智能体 / 开源 LLM 工具最佳实践 / 包内容深读）。
> 标注：✅ = 复核者亲自读原文件 / 实跑验证过。

### 8.1 对原报告的修正（3 处，不影响原结论，动手前须知）

1. **「plan 零下游消费方」表述过重**：`grep plan` 在前端与 server 零命中属实，但 `pipeline.py:338` 把 `plan.model_dump()` 经 `prompts.py:104` 的 `$plan_json` 喂进**每一轮 write 提示词**。plan 不是零消费，是"只剩 write 上下文这一个消费方"。方案 10（合并 select+write）成立的前提是**保住这段上下文**，不是删掉它。
2. **P0-3 的"管道现成"不准确**：`_complete`（`llm.py:156`）有 `max_tokens` 形参，但 pipeline 唯一入口 `chat_json`（`llm.py:109-117`）**没有**该形参。分阶段预算要穿透 `chat_json`，不止 `_complete`。
3. **P1-21 举例微调**：`顶级/第一/唯一/绝对` 已在 soft（`banwords.yaml:112-114,121`）。实测缺的是：`最佳 最优 最好 最大 最小 最高 最低 最新 最有效 最便宜 最低价 最划算 最受欢迎 巅峰 顶尖`（15）+ `全网第一 全国第一 销量第一 行业第一 NO.1 TOP1 仅此一款`（7）+ `顶尖工艺 行业领先 领导者 销量冠军`（4）+ `彻底 无瑕 永不反弹 百分百见效 永久售后 百分百赔付`（6）≈ **32 词族**。对账测试依旧必要。

### 8.2 新增问题（原报告未覆盖）

**P1-34** ✅ `knowledge/ideas.md` 永不注入（`skill.yaml:58-59` select.files 仅 hooks；`prompts.py:58-62` 只读 skill.yaml）**且格式不可消费**：钩子类型含 `清单钩子/权威科普/严肃警示`（hooks.md 十类之外，其中 `权威科普` 甚至是 `pack.yaml:28` 的 style 选项名——"风格"与"钩子类型"两个维度混在一个字段）；受众 `业主/物业/维保/业委会/行业` 非 `params.audience` options；无 cta/points 字段。修：按 select 提示词契约（angle/hook_type/hook_line/points/cta）规范化后挂 `select.files.ideas`。

**P1-35** ✅ `knowledge/voice.md` 第二个自我矛盾：`:23` 把"修一次大几万"列为**禁编的浮夸细节**，`:34` 又把同一句话列为**必须含的具体数字**示范（`:23` 有"没依据的"限定、文件给了一半自洽规则，但逐字注入时模型看到的是"必须含"与"禁编"并列）。修：`:34` 示例改为有依据的写法或加限定。

**P2-36** ✅ 语速三处对不上：`compliance/platform.md:96` 抖音 5.0~5.5 字/秒 vs `rules/duration.md:7` 快节奏 5.5 vs `pack.yaml:44-49` 封顶 5.0；`duration.md:24` "科普/警示类按 4.5 字/秒换算 × 0.9" 与 `:9` 科普 4.5、`:10` 严肃警示 4.2 自相矛盾（`pack.yaml:46/48` 权威科普 4.5 / 严肃警示 4.2 与 :9-10 一致、与 :24 不一致）。校验/配额口径漂移，是 P2-27 之外又一处"说不清为什么掉出容差"的来源。

**P2-37** ✅ 维保周期口径三处重复：`compliance/industry.md:75-78`、`knowledge/standards.md:59-64`、`knowledge/topics.md:11` 同写"半月/季度/半年/年度"。三者注入状态不同（topics 注入、standards/industry 不注入）→ 改一处漏两处，且注入的是"没带 TSG 依据的那份"。修：收敛到 standards.md 单点，其余只留引用。

**P3-38** ✅ 悬空引用第 5 处（原报告 P3-28 只查到 4 处）：`knowledge/topics.md:184` "与反谣言库'轿厢非密闭、有通风口'冲突"——反谣言库在未注入的 `compliance/industry.md:31-45`。select 切片注入时同样把悬空引用带进提示词。

**P2-39** ✅ `banwords.yaml:6-8` 宣称 promote/demote 机制，实测平台分级仅 4 平台约 11 条（抖音 demote×1、B站 demote×5、小红书 extra_hard×9、视频号 extra_hard×6），**promote 全包零使用**，且与 `platform.md` 的高危词清单无对账测试（如 `platform.md:47` 小红书"私聊/二维码/进群"靠基础 hard 兜底，纯靠人工记得）。修：promote 要么真启用（做跨平台状态机）要么删文档，并加词表-文档对账测试。

### 8.3 引擎层新增（对接开源最佳实践）

**P1-40** ✅ **回炉轮间前缀缓存整段失效（本轮最大降本项）**。实测 write 提示词 8099~8320 字符、90.1% 静态，而 `$feedback_block` 恰好排在 user_template 最前（`skill.yaml:76`）——每轮回炉 user 开头就变，DeepSeek 前缀缓存（官方：自动开启、命中比 miss 便宜约 50 倍）**整段打不中**。修：`$feedback_block` 挪到 user_template 末尾（system+user 静态约 8k 字符前缀稳定），并接 `usage.prompt_cache_hit_tokens` 验证（对接 P3-15）。一行模板调整。

**P2-41** ✅ `_violation_feedback`（`pipeline.py:366-383`）只给统计数字（"命中硬禁用词「绝对安全」×1"），不给**违规句原文摘录与替换建议**。对照 Self-Refine 的标准形态（critique 输出可执行修改清单），这是"重新掷骰"的直接成因。修：反馈带上违规句（含段落序号），代价 100~300 字符。

**P3-42** ✅ 「换一版」是顺序重掷（`jobs.js:234-235` reroll=true → 整条重新 generate），对照度加/元器类产品"一次出 3 版让用户挑"的公开工作流，成本相同、体验差一档。若做方案 10 合并 select+write，可顺手支持 reroll 并行出 2~3 版。

### 8.4 二次复核强化（P0-17 要补的第二源头）

**A. P0-17 有第二个注入源，只清 private 不够。** ✅ 实测 `generated/20260920/20260920-101043-82b9e9/result.json` 含"12台"（无 mock 键，另实测 88 份产物 0 份带 mock 标记）；而 `patterns/growth.md:62`（**每轮 write 注入**）的信任素材表原文就是"`去年我们在XX小区换了 12 台`"。真实产物里的"去年那12台"既可能来自 `cases.yaml:14`，也可能来自这份每轮注射的范例。修 P0-17 必须两头都清：`private/*.yaml` 示例数字 + `growth.md:62` 这类"教模型编具体案例数字"的注入模板（改为 `{{待补：小区}}/{{待补：台量}}` 占位或只留规则）。

**B. ideas.md 混维（P1-34 补充）**：`权威科普` 是 style 选项名不是钩子类型，选题库把两个维度塞进 `标题 — 受众 · 类型` 同一槽位，注入前必须先拆分。

**C. output-template.md 内部自相矛盾（P2-31 补充）** ✅：分镜 markdown 表（`:65-69`）只有"音效/BGM"一列，结构化 JSON（`:139-141`）却拆成 `audio` 与 `sfx` 两个字段，`:120` 写"如需二次加工"暗示可选。一旦按 P0-18 接上 `rules/*`，模型会在两套结构间摇摆。

### 8.5 最终动手顺序

1. **P0-17 双源清理**（合规风险）：`products.yaml:13-30` / `cases.yaml:12-14,20` + `growth.md:62`；`strip_empty` 加条目级判定。
2. **P0-1/2/3 生成层三件套**（`llm.py` 约 30 行 + `chat_json` 加 max_tokens 形参）。
3. **P1-40 模板一行挪位** + 接 usage 验证缓存命中。
4. **P1-34/35 + 注入连通**：ideas.md 双维拆分后挂 select；voice.md 自洽；write 增 `files.redlines`（industry.md §2/3/4/6/7 精简 ~600 字）+ `files.terms`（standards.md §5 ~400 字）。
5. **P1-21/2-39 词表**：补"最X"族约 32 词 + 词表-文档对账测试 + promote 二选一。
6. **P2-36/37 口径收敛**，最后做方案 5/10 结构调整。

---

## 九、新建行业包（packgen）专项审查（2026-09-20 同日）

> 范围：`desktop/renderer/js/settings.js` packgen 段（入口/跳转/生成流程）、`index.html #pane-packgen`、`styles.css` packgen 两段、`app/server.py:428-445`、`app/packgen.py` 全部、`api.js:76`。
> 对照：Dify/Coze/GPT Builder 的"保存即校验"与分阶段生成、generate 侧已有的 Job/流式/取消基建。

### 9.1 做得对的部分（避免误伤）

双入口 + 取消回来源（`settings.js:614-632` packgenFrom）、`NAV_OF_PANE` 导航高亮、布局已对齐三列设置面板（`styles.css:2206-2212`）；失败 UX 保留表单值 + 常驻错误横幅（`settings.js:869-881`）；空数组兜底与半成品清理（`packgen.py:122-149`）；原子写；draft + 校对清单 + undraft 人工闭环。

### 9.2 问题清单

**P1-43** ✅ **packgen 是全产品唯一不走 Job 管道的长请求**：`server.py:428-435` 同步 POST，无状态/无流式/无取消/无并发额度；`settings.js:843-882` 只有计时器。且它承载全代码库**最大单次输出**（`PackGenOut`：segments+audiences+personas+全部 topics+audience_details+≥30 ideas+redlines+词表+verify_list）——在主报告 R1/R3（`max_tokens` 被思考吃光、`chat_json` 原参重发）下是最可能 length/空内容的那次调用。generate 侧全套 Job 基建（`pipeline.py`）一条没复用。修：走 Job（或后端进度+取消），空内容纳入可重试且升级参数。

**P1-44** ✅ **生成结果零机器校验**：`create_pack`（`packgen.py:99-157`）写盘即返回 ok，不加载 `Pack(slug)` 冒烟、不跑 `param_audit`、不查占位符。裸奔点：① segments 与 `topics[].heading` 不一致 → topics_map 指向空章节 → select 整份注入（`packgen.py:227` identity 映射但模型未必逐字对齐）；② `banwords_extra` 单字词被 `MIN_WORD_LEN=2` 吞掉（`checker.py:44`）；③ 章节标题与 `slice_heading` 关键词不匹配。成熟智能体通行"保存即校验 + 错误喂回重试"。修：`Pack(slug)` 加载 + `param_audit()` 全量 + `unfilled()` 占位符检查，体检结果并进 `_checklist`（`packgen.py:272-283`）。

**P1-45** ✅ **packgen 是注入架构缺陷的工厂**：`_materialize` 复制电梯包同款孤儿结构——`:253-264` 写永不消费的 `files.select/write/compliance/facts`、`:203-204` 写永不注入的 ideas.md、`:210` 写永不注入的 standards.md、`:212-215` 写永不注入的 compliance/industry.md。**每个新包真正到达模型的只有 topics.md/audience.md（经 maps 走 select）**，价值最高的行业红线/核实清单/选题库全部死胎。与 P0-18/P0-19 同源：架构修一次，`_materialize` 必须同步。

**P2-46** ✅ **并发无上限 + 同名 TOCTOU**：`create_pack` 不占 `MAX_CONCURRENT_JOBS` 额度（`pipeline.py:85` 只管 generate）；`packgen.py:133` 的 `if d.exists()` 检查-写入两段，同 slug 双请求可双双通过、后者原子覆盖前者。修：slug 级 create 锁或并入 BUSY 额度。

**P2-47** ✅ **「完成」与「取消」去向不对称**：取消回 `packgenFrom`（`settings.js:632`），完成永远 `setPane("gen")`（`settings.js:885`）。从 packinfo 进来的人完成被带走。修：`packgenFrom==="packinfo"` 时完成回 packinfo 并刷新列表。

**P3-48** ✅ **生成期间「取消」可点 → 共享 DOM 竞态**：`runPackgen` 只 disabled `pg-run`（`settings.js:852`）；取消后再进再点生成 → 两个 createPack 并发，后完成者覆盖前者的 `pg-form/pg-result/pg-checklist`。修：生成期间禁 `pg-close` 或请求带序号守卫。

**P3-49** ✅ **关闭设置窗口不刷新 meta**：`onPackDone` 是唯一刷新路径（`settings.js:884-893`），`closeSettings`（`settings.js:85-87`）只隐藏 → 结果页直接点 X 关掉后 composer 行业包下拉无新包。修：`lastCreatedPack` 非空时 `closeSettings` 也刷新 meta。

**P3-50** ✅ checklist 用 `<pre>` 纯文本（`index.html:379`），markdown 不渲染，条目一多即文本墙。低优先。

### 9.3 对照成熟智能体（可落地项）

| 通行做法 | 现状 | 落地动作 |
|---|---|---|
| 保存即校验 + 校验失败喂回重试（Dify/Coze/GPT Builder） | 零机器校验 | P1-44 |
| 分阶段生成、进度可视（度加/元器；generate 侧已有 select/write/check 步骤） | 单次巨型 JSON + 计时器 | P1-43：至少拆"骨架→内容"两段 |
| 任务化 + 可取消（generate 侧 Job 基建现成） | 同步 POST | P1-43 复用 |
| 并发/额度守卫 | 无 | P2-46 |
| 生成后可迭代（GPT Builder 对话式调整） | 完成即回 gen | 可后置 |

### 9.4 建议顺序

P1-45（随 P0-18/19 架构修复）→ P1-43（复用 Job）→ P1-44（冒烟 + 校验回炉）→ P2-46/47 → P3-48/49/50。

### 9.5 结果页优化已实施（2026-09-20 同日，用户确认范围）

落地内容：
1. **产物摘要**：`create_pack` 返回扩展（`packgen.py:154-168` 追加 segments/audiences/personas/topic_count/ideas/redlines/banwords_extra_*），结果页渲染"已生成「X」行业包（草稿）+ 统计 + 列表 + 选题示例"（`settings.js` 新增 `renderPackgenSummary`，**全部 textContent，杜绝模型内容 innerHTML 注入**）。
2. **双出口**：结果页「返回」（回 `packgenFrom`，与取消同语义）+「完成」（去工作台用新包）（`index.html:379-384`、`settings.js:634`）。
3. **生成中反馈**：`pg-close` 生成期间禁用（杜绝 P3-48 双请求竞态）+ 固定说明文案"模型正在生成行业结构…通常 1~2 分钟"（`settings.js:852-855`、`index.html`）。
4. 样式全部走刻度 token（`styles.css` .pg-summary/.pg-more/.pg-working，间距 --s1..--s8、字阶 --f-sm/--f-md、字重 600、行高 1.35/1.6，箭头画法对齐 .st-adv/.think-stream）。

验证：pytest **284 通过**；verify.js **237/237**（新增 3 条断言：结果页摘要渲染、双出口齐备、返回回来源面板；桩补了 `/api/packs/create` 夹具，未弱化既有断言）；mock 模式真实浏览器实测全流程通过（截图 `_verify/_packgen-result.png`）。

**P2-51** ✅ **实施实测新发现：packgen slug 基于模型返回的 `display_name`，与用户输入脱节**。`packgen.py:131` `slug = slugify(out.display_name or industry)`——用户输入"门窗定制"，模型若返回 display_name"全屋定制/装修"，包目录就叫 `全屋定制-装修`，用户找不到自己刚建的包；且同名 slug 重试必撞 `FileExistsError`（mock 夹具下实测复现）。修：slug 优先用用户输入的 industry（模型 display_name 只做展示），或用"industry + 时间戳"消歧；同时 mock 夹具的 display_name 应随输入变化，否则无法重复测建包。

---

## 十、三批修复记录（2026-09-20 同日，全部落地并验证）

### 批次 1 · 生成层健壮性（`llm.py` + `pipeline.py`）

| 条目 | 修复 | 验证 |
|---|---|---|
| P0-1 | `_stream_once` 返回 `(text, finish)`，累计读 `choices[].finish_reason`；`_complete` 流式分支透传——截断从此可识别 | pytest 13/13（test_llm_retry） |
| P0-2 | `chat_json` catch `EmptyContentError` 并升级预算（×1.5、封顶 `RETRY_UPGRADE_CAP=20000`）重试；重试耗尽仍抛 `EmptyContentError`（保持类型，调用方能识别"预算问题"） | 同上 |
| P0-3 | `chat_json` 加 `max_tokens` 形参；`_select`→4000、`rewrite_segment`→3000（write 保持）——分阶段预算落地 | 全量 285 |
| P2-10 | `ping` 用 512 且空内容视为"已连通"（推理模型思考吃光不算失败）；`_ensure_content` 文案改用**本次实际预算** | 13/13 |
| 测试适配 | `test_streaming_empty_content`（预算升级后数字断言改正则）、两处假 LLM 签名补 `max_tokens=None`（`test_engine_api` / `test_cancel_concurrency`） | — |

### 批次 2 · 词表合规（P1-21 三件套）

- **①跨表去重**：`checker.scan()` 建 hard 命中**位置掩码**，soft 与硬命中重叠即跳过——`绝对安全`(hard)+`绝对`(soft) 不再双计。对应测试 `test_pack_yaml_integrity` 断言 soft 1→0（该 1 正是跨表双计的证据）。
- **②词表修订**（`banwords.yaml`）：soft 删裸 `第一`（169/175 轮假命中，"第一件事/第一步"是正常用法）；补"最X"族 15 词（最低价/最便宜/最好/最有效/最划算/最受欢迎/最佳/最优/最大/最小/最高/最低/最新/巅峰/顶尖）+ "X第一"带限定词 7 词（全网第一/全国第一/销量第一/行业第一/NO.1/TOP1/仅此一款）+ 广告法 §2 剩余 4 词（彻底/无瑕/永不反弹/领导者）。
- **③对账测试**：新增 `tests/test_banwords_alignment.py`——ad-law.md 六个词族行逐词与 banwords（hard∪soft）子串互含对账，缺失即红灯，杜绝手工同步漂移。

### 批次 3 · 包数据与前端

| 条目 | 修复 |
|---|---|
| P0-17 | `products.yaml` 全文件清空示例数值（含不带"示例："标记的 home/freight/escalator 固定值——它们同样会被注入）；`cases.yaml` 清 year/units、public 默认 false；`growth.md:62` "去年…12台"改 `{{待补：小区}}/{{待补：台量}}` 占位。 |
| P1-20 | `prompts.py base_ctx` 加 `cta`；skill.yaml select 要求"用户指定的动作是「$cta」不得自换"、write 加【结尾引导动作】$cta 行。 |
| P2-51 | `packgen.py` slug 改用 `slugify(industry)`（用户输入优先，display_name 只做展示）。 |
| P3-49 | `closeSettings` 在 `lastCreatedPack` 非空时刷新 meta——**只刷新不切包**（切包是「完成」的职责；实测 prefer 切包会污染当前选中，已按此修正）。 |

**验证**：pytest **285 passed**；verify.js **237/237**（含批次前的 packgen 三条新断言；修复过程中一度因 closeSettings 切包导致 236/237，已定位并修正）。

### 批次 4 · 行业包生成逻辑 + 引擎行为（用户授权自主决策，合理性优先）

**行业包生成逻辑（用户点名重点）**：
- **P0-18/19 注入孤儿打通（最小版）**：skill.yaml `write.files` 加 `redlines: compliance/industry.md`，write 模板加【行业红线】段 —— 统一救援口径 / 禁演清单 / 术语规范从"从未进过提示词"变成每轮撰写都带（这曾是 P0-18 指出的"最不可替代却永不注入"部分）；`pack.yaml files.*` 删孤儿（select/write/compliance/facts 是引擎不消费的死键），`packgen._materialize` 同步只留 `private`。
- **P1-44 生成自体检**：`create_pack` 末尾 `Pack(slug)` 加载（结构坏即报）+ `param_audit` 全量（切片/配额/词表降级逐项），体检结果并进校对清单 —— 生成出来就能用，不再等到第一次生成才发现（那时已付费）。
- **P2-46 slug 并发锁**：`_creating` 集合 + lock，同名 slug 并发创建直接 409，消除 exists()-写入 TOCTOU。

**引擎行为**：
- **P1-6 client 作业级贯穿**：`_run_generate` / `rewrite_segment` 入口一次抓 `self.llm` 为局部变量传到底 —— B 作业 reload_llm 不再让 A 作业中途换模型/预算。
- **P1-7 阶段开始即有流式状态**：`_delta_handler` 构造即 `begin_stream` + progress.js 有 stream 键即显示"等待模型首个 token…"（不再静默几十秒）。
- **P1-8 select 同提温**：「换一版」的 +0.25 温度现在作用于 select（原来只作用于 write，重掷出的 plan 几乎一样）。
- **P2-9 重写等待补进度**：`waitRewriteDone` 改轻量轮询（`api.job(jid)` 不含 result）+ 每轮 `renderProgress` + 超时 180s→240s（同模型实测 write 82~130s 起步）。
- **P3-14 params 落盘模型配置**：PERSISTED_PARAMS 加 `model/max_tokens/temperature`（新声明 `RUNTIME_PARAMS`，`_normalize` 保持不读 self.llm；`test_quota_degraded_signal` 断言同步）。
- **P3-29 TopicPlan.points 上限**：`Field(max_length=6)`（180s→4 点的 1.5 倍余量），杜绝"主题写 5 个坑却给 8 条 point"的口径漂移。

**测试适配**：假 LLM 补 `cfg.model`（`test_engine_api` ×4 / `test_cancel_concurrency`）；会话行断言随用户单行制改版由用户工作区同步（verify.js 现 245 条断言）。

**验证**：pytest **285 passed**；verify.js **245/245**。未处理（明确后置）：P1-43 packgen 走 Job、P1-30 拆 storyboard、方案 10 合并 select+write（产品时序决策）、P3-15 usage 记录（穿透面大、收益小）。

### 批次 5 · 剩余低风险清理（2026-09-20）

- **P2-24** voice.md 自洽：与"过度口语化负面清单"冲突的三处示例统一（"整栋楼骂你/厂家自己都脸红"的态度示例换中性词、范例的"撂挑子"换行业真实表述、删"修一次大几万"的具体数字示例、制动器按行业规范改"抱闸/闸"并注明"不说刹车"）。
- **P3-28** 悬空引用清理：hooks.md:71 / growth.md:6,97 / anti-ai-smell.md:27 / faq.yaml:4 引用未注入文档处改为语义描述（faq.yaml:39 与 topics.md:184 引用的 industry.md 已随 P0-18 注入落地，不再悬空）。
- **P3-33** raw/ 空承诺：private/README.md 明示"引擎当前未实现自动解析"，给两条可用的替代路径；export_skill.py 删除"有未处理文件时先提示用户"的不实承诺。
- **P3-16** 过期文档：README「2~4 次调用」改为"正常 2~3 次 + 最坏 32 个 HTTP 请求"（对齐 P1-5 公式）；server.py 注释"每 3 秒"改 900ms（对齐 jobs.js POLL_MS）。
- **P1-22** audience 切片未知值**不注入整份文件**（原 2167 字符 => ""，宁缺毋滥；param_audit 已报降级）。
- **P2-27** ±10% 硬编码 → 时长自适应 `max(10, 3.0/duration*100)`（15s→±20%），并支持 `tolerance` 参数显式覆盖。
- **P2-11** 环境变量数值夹逼（`_ENV_NUM_BOUNDS`：temperature/retries/timeout/max_tokens，越界夹逼 + WARNING；与 server 侧 NUMERIC_BOUNDS 同口径）。
- **P1-5** 流式 read timeout 放大一倍（`STREAM_READ_TIMEOUT_MULT=2.0`）——思考期不 flush 不再被 180s 误杀。
- **P1-23** 产物落盘 `mock` 标记（ScriptResult.mock + _finalize raw.mock）——夹具产物可辨识，统计不再被污染的 86/87 份污染。
- **P0-4 不实施**：max_tokens/temperature 加回设置页**违背用户 2026-09-17 的明确决策**（index.html 注释：「高级设置去掉 token 限制吧」）。该条的核心痛点已被化解：P2-10 让错误文案显示**本次实际预算**、P0-2 空内容自动升级重试、P0-3 分阶段预算（空内容出现率大幅下降）、config.yaml 模板与 README 均写明该键位置。

**验证**：pytest **296 passed**（11 条增量：用户会话期间的测试 + 本批覆盖）。verify.js 未涉及（本批无前端 JS 改动）。

---

## 十一、批次 6 · 1️⃣ 拆分镜阶段 + 4️⃣ 包内容结构（2026-09-20 深夜 → 09-21）

用户按 1️⃣→4️⃣ → 2️⃣ → 3️⃣ 的顺序拍板，本批做前两项。

### 1️⃣ P1-30 · 分镜拆成独立阶段

| 位置 | 改动 |
|---|---|
| `packs/elevator/skill.yaml` | write 的 JSON 契约只留 `sections`；新增 `stages.storyboard`（`$segments_with_time`、"N 段 = N 个分镜"、危险操作用示意）；头部占位符表与 description 同步 |
| `app/pipeline.py` | 新增 `StoryboardDraft` 与 `_storyboard()`（时间轴由 `_compute_timings` 算好喂给模型，模型不排时间）；`_continue_write` 只在**校验通过且 format≠voice** 时调用；`_finalize(…, storyboard, …)` 不再从 draft 取分镜；删掉 `ScriptDraft.storyboard` 字段（没有任何读取方，旧产物走 `ScriptResult`） |
| 老包兼容 | 缺 `storyboard` 阶段的包**跳过并留痕**（日志 `storyboard_skip`），而不是让一条已经写完、已经过校验的正文以 `KeyError` 收场 |
| `app/jobs.py` | 新增状态 `storyboarding`：进 `BUSY_STATES`（占并发额度），迁移边 `checking→storyboarding`、`rewriting→storyboarding`、`storyboarding→done/failed/cancelled` |
| `desktop/renderer/js/progress.js` | `STATE_LABEL.storyboarding = "分镜生成中"` + `BUSY_STATES` 同步（由 `test_job_state_vocabulary_consistency` 守前后端同源） |
| 单段重写 | 改写通过后重画分镜（`rewriting→storyboarding`）；**重画失败保留旧分镜并留痕**，不报废这次改写 |
| 预算 | 分镜阶段 `max_tokens=4000`（P0-3 分阶段预算同口径，不占 write 的 16000） |

**实测**：拆分前同一提示词 write 110~127s，拆分后 12~23s（主报告 §六 第 5 条）。
mock 活体链路 steps = `select → write_r1 → check_r1 → write_r2 → check_r2 → storyboard`，
5 段 / 5 分镜 / 5 scenes，`check.estimated_seconds=55.3` 与 `timings[-1].end=55.3` 相等。

### 4️⃣ P2-25 / P2-26 / P2-31 / P2-36 · 包内容结构与口径

- **`growth.md` 2371 → 774 字**：只留电梯特有且规则里没有的三块（高客单信任素材排序、
  一段≠一个信息点的节奏口径、CTA 的三个约束）。通用短视频常识与 `write.system` /
  `anti-ai-smell.md` 重复的部分删除；"每 10~15 秒一个新信息"改写成与
  `points_by_duration` 不打架的说法（一个要点 63~150 字 / 14~33 秒，段内每 10~15 秒落一个新信息）。
- **`hooks.md` 按风格切片**：新增 `Pack.hooks_slice(style)`（2062 → 1056 字/轮），
  结构约定 `## 一、钩子库…` + `### <风格名>`；认不出结构时退回整份。
  `select.files` 不再声明 hooks（引擎负责切）。
- **`files` 支持 `路径#章节`**：新增 `Pack.file_slice()`，
  新增 `write.files.terms = knowledge/standards.md#核心术语`（427 字 vs 整份 2195 字）。
  `#章节` 找不到时返回**空串**而不是整份 —— 新建包的 standards.md 只有一张待核实清单，
  退回整份等于每轮往提示词里灌编号清单；代价（章节改名→静默不注入）由新增的
  `test_declared_stage_files_are_actually_found` 静态对账兜住，不靠运行期日志。
- **净提示词**：select −1006 字；write 每轮 8751 字（本批前约 9921，且这轮还多带了一张术语表）。
- **`rules/duration.md` 重写**：5.5 字/秒标为"本包未采用"；`×0.9` 特例改成通用折算式
  `目标字数 = 表值 × 语速 ÷ 5.0`；删掉代码里并不存在的"强调停顿另计时"；
  ±10% 换成批次 5 的真实口径 `±max(10%, 3秒÷目标时长)`；要点数注明以 `points_by_duration` 为准。
- **`rules/output-template.md`**：顶部标注"本文件是成品版式与导出技能的输出约定，
  模型输出的结构契约在 `skill.yaml` + `app/schemas.py`"；删掉第三份 JSON schema
  （它与分镜 markdown 表自相矛盾：`audio`+`sfx` 两个字段 vs 契约里只有 `sfx`），
  改为列出 列↔字段 的对应关系；要点三条示例注明只是 60s 档。
- **`compliance/platform.md`**：平台语速行与 `rate_by_style`（封顶 5.0）对齐，并注明真正取值在 pack.yaml。
- **时间轴与校验同口径**：`_compute_timings` 原来把 0.5 秒停顿加在**首段**、段落之间不加，
  而 `estimate_seconds` 按"(段数−1) 处各加 0.5 秒" —— 5 段片子两处差 1.5 秒，
  "校验说合格"与"界面时间轴更短"同时成立。改为段间计停顿，新增 `test_timeline_matches_checker_estimate`。
- **新建包侧同步**：`packgen._materialize` 生成的 standards.md 预留 `## 核心术语` 空节 ——
  否则 `write.files.terms` 在新包里永远注入不到东西，而作者不知道该写在哪。
- **文档**：`docs/知识库与技能配置指南.md`（阶段表加 storyboard、`路径#章节` 语法、
  hooks 切片约定、消费关系按现状改准）；`docs/场景序列契约.md`（分镜从哪来、`time` 不进 scenes、
  哪些字段目前是契约里的空位）；`README.md`（编排链路与调用次数：正常 3~4 次、最坏 30 个 HTTP 请求）。

**验证**：pytest **298 passed**（新增 2 条用例：时间轴对账、files 声明对账；
冒烟里新增分镜阶段 4 组断言 + 老包缺阶段 1 例）；verify.js **268/268**；mock 活体 1 条完整链路 + 1 次新建包切片检查。

**仍后置**：方案 3 轻量选题复用（3️⃣）、
P1-34 `ideas.md` 规范化后注入 select、`scenes` 里 `visual.transition/shot_type/style/audio.bgm`
四个契约空位、P3-15 usage 记录。（P1-43 见批次 7。）

---

## 十二、批次 7 · 2️⃣ P1-43 建包改走后台作业（2026-09-21）

**病灶回顾**：`POST /api/packs/create` 是全项目唯一一个把 1~2 分钟模型调用挂在同步
HTTP 请求上的操作。三个洞：断连即失明（客户端不知道包到底建没建出来）、
「取消」只是丢弃返回值（钱与目录照旧发生）、不占并发额度。

| 位置 | 改动 |
|---|---|
| `app/jobs.py` | 新增状态 `packing`：进 `BUSY_STATES`（占额度），`queued→packing→done/failed/cancelled` |
| `app/pipeline.py` | `start_packgen()` / `_run_packgen()`：同步挡下"名称为空 / 同名包已存在"（不花钱的结论不该等 1~2 分钟）、`add_if_room` 与生成共用 `MAX_CONCURRENT_JOBS`、client 作业级抓一次（P1-6 同口径）、进度用现成的 `_delta_handler` / `_retry_logger` |
| `app/pipeline.py::_persist` | `work_dir is None` 直接跳过 —— 建包没有产物目录，硬落盘会在 `generated/` 里建出一个点不开的"会话" |
| `app/packgen.py` | `create_pack(..., on_retry=, on_delta=, should_abort=)`：取消检查点落在**模型调用返回之后、写盘之前**（token 收不回，目录可以不落）；新增 `preview_slug()` 供入口做同步预检 |
| `app/server.py` | `/api/packs/create` 返回 `{job_id}`；`/api/history` 过滤 `kind != "generate"`（左栏会话列表不混入建包作业） |
| `desktop/renderer/js/settings.js` | `runPackgen` 改「起作业 + 每 0.9s 轮询」；`packgenJob` 是模块状态 → 切面板、关设置页再回来进度都还在；说明行搬作业上的真实进度（思考字数 / 接口重试），秒表不再冲掉它；「取消」在生成期间**可用且真取消**（P3-48 的禁用理由随同步请求一起消失），并给"取消中…"的乐观反馈 |
| `desktop/renderer/js/progress.js` | `STATE_LABEL.packing = "行业包生成中"` + `BUSY_STATES` 同步（`test_job_state_vocabulary_consistency` 守） |

**测试**（`test_engine_api.py` / `test_server_hardening.py` / `test_smoke_mock.py`）：
- 原 `test_packs_create_returns_502_on_llm_error` 的 502 路径**按新契约改指作业**：
  模型失败 → 作业 `failed`，理由原文进 `job.error`；另加"不写产物目录""不进历史列表"。
  这条断言没有降级：它原来要守的是"令牌过期必须让用户看见"，现在仍在守同一件事，
  只是位置从 HTTP body 换到了作业字段。
- 新增：同名包同步 409（不起必然失败的作业）、额度满 409、
  `should_abort` 为真时不留下半个目录、新包 standards.md 必须留 `## 核心术语` 这一节。
- 硬化的 `_require_model` 测试原来靠 patch `srv.create_pack` 过活，函数搬走后那个名字
  不存在了 → 改 patch `Pipeline.start_packgen` 并**加一条 `started == []`**：
  原来只断"HTTP 400"，现在还能断"没配 Key 时作业根本没被起起来"。

**验证**：pytest **300 passed**；verify.js **281/281**（新增 4 条建包作业断言：
生成中按钮与结果页的先后、取消可用、说明行显示真实进度、取消真的发出 cancel）；
mock 活体走真引擎：`POST → job_id → done(kind=packgen, result 摘要) → 结果页渲染 → 完成`，
`/api/history` 不含该作业，重复建同名包 409，控制台零报错。

---

## 十三、批次 8 · 3️⃣ 轻量选题复用（2026-09-21）

**要省的是什么**：`select` 一次 4000 token、实测 10~30s。用户的常见动作是
「这个主题再来一版措辞」—— 参数一个没改，模型却把同一个角度与结构重算一遍。

**做法**（`app/pipeline.py::_select`）：进程内缓存 `TopicPlan`，
**指纹 = 模型名 + 渲染后的 system + user**。

- 为什么指纹不枚举参数：`{topic, segment, audience, duration, style, platform, persona, cta}`
  这张表一旦漏一项（或者漏掉"被注入的知识文件变了"），就是**静默复用了一个不匹配的选题**。
  渲染结果天然包含参数、skill.yaml 模板与全部切片 —— 任何影响选题的输入变了，指纹自己变。
  反向也一样准：改 `hooks.md` 里**不进提示词**的章节不会白白重跑（测试里钉了这条）。
- **「换一版」（reroll）永不复用**：那个按钮的语义就是要一个新角度。
- **必须可见**：复用时作业日志那一步写作 `select_reuse`「复用上次选题（本次未调用模型）」，
  并**占掉**「选题策划」那一步的位置 —— 照常记一步「选题策划」等于告诉用户"它又想了一遍"。
- 只在内存（`Pipeline` 实例生命周期 = 引擎进程），上界 32 条先进先出；
  跨重启的缓存要处理失效与隐私，而产物里本来就留着 plan，收益不值那个复杂度。

**测试**：新增 `tests/test_plan_reuse.py` 5 条 —— 复用命中、改主题/细分/风格/时长各不复用、
reroll 不复用、改切片内知识必重跑 + 改切片外知识不重跑、缓存容量与淘汰顺序。

**验证**：pytest **305 passed**。README 的调用次数与流程行同步。

---

## 十四、批次 9 · 复核批次 1~8 后把死代码补齐（2026-09-21 → 09-22）

> 触发：批次 1~8 的「已修」里有 **3 条是死代码**（P0-2 空内容重试 / P2-11 环境变量夹逼 / P1-7 首字节前的思考块），
> 而 pytest 全程绿。本批先把这些声明**逐条跑一遍**再补，然后按同一口径把剩余项清掉。
> 分工：主 agent 管引擎与 `_verify/verify.js`；三条并行 lane 分别管 包内容与 packgen / 桌面端安全与交付 / 渲染层。
> 两条 lane 中途撞到自身轮次上限，其产物由主 agent 逐条复验并补齐（下面标注了归属）。

### 14.1 复验：本批真跑过的关键结论

| 结论 | 怎么验的 | 结果 |
|---|---|---|
| P0-1/2/3 尾部失败可补救 | MockTransport 记录每次请求的 `max_tokens` | **3 次请求，预算 1000→1500→2250**，流式与非流式同形；`finish=length` 且正文非空也走同一条升级分支（原来只有空内容进得到） |
| P2-11 环境变量夹逼 | `TALKSCRIPT_MAX_TOKENS=100 / RETRIES=999 / TIMEOUT=99999 / TEMPERATURE=9` 真跑 | 256 / 10 / 1800 / 2.0 + 四条 WARNING（区间与 `server.NUMERIC_BOUNDS` 逐项一致，`tests/test_env_bounds.py` 钉住） |
| P1-7 静默窗口 | 查 `Job.snapshot()` 的 `stream` 键门禁 | 门禁已从「两个计数器非 0」改成 `stream_phase` —— 首字节前就在下发，`progress.js` 那句提示终于可达 |
| P0-17 双源 | 渲染 write 提示词后 grep | 注入的 `growth.md` 只剩 `{{待补：小区}}/{{待补：台量}}`；私有资料示例值已清 |
| 新建包不再串行业 | 真建一个「宠物医院」包 + 渲染两阶段提示词 | write 4972 字 / select 2295 字里「电梯」**0 次**（修复前 11 次 + 1673 字电梯私有 FAQ） |
| 前缀缓存（P1-40） | 渲染回炉第 1、2 轮 user，量公共前缀 | **7148 / 7188 = 99.4%**（`$feedback_block` 挪到模板末尾之后；修复前每轮从头就变 → 0%） |
| 提示词净体积 | 同上 | write 每轮 8751 → **7580** 字，且这轮多带一张术语表（`standards.md#核心术语` 460 字）与红线切片（`industry.md` 的 838 字，而不是整份 2848） |
| 令牌不经 argv | 起真引擎 + `Get-CimInstance Win32_Process` 读命令行 | 命令行只有 `--port/--root/--data-dir`；无令牌时 `/api/meta` **401**、env 给的令牌 **200** |
| `/docs` 无鉴权 | 真引擎 curl | `/docs` `/redoc` `/openapi.json` 全 **404** |
| private 文件出口 | 真引擎 + 6 种路径变体 | 带令牌也 **403**（`pack.yaml` 仍 200，`../../config.yaml` 404） |
| `undraft` 吃注释 | 复制真包 → 转正 → 逐字节 diff | **完全相同**（12 行整行注释 + 行尾注释全在） |

### 14.2 本批新修（引擎）

| 条目 | 改动 | 落点 |
|---|---|---|
| P2-13 | `chat_json` / `_complete` 加 `on_attempt`，**每次 HTTP 尝试前**清空流式缓冲 —— 被丢弃那次不再累进「思考 N 字」 | `app/llm.py`、`pipeline._stream_reset` |
| P1-5 后半 | `JOB_BUDGET_SECONDS = 1200` + `Job.overdue()/reset_budget()`；`_abort_if_cancelled` → `_stop_check`，超预算抛 `JobBudget`（与 `JobCancelled` 分开：取消不是失败）；单段重写重新计时（否则昨天的记录一开就"超时"） | `app/jobs.py`、`app/pipeline.py` |
| P2-41 / 方案 11 | `checker.locate_hits` 给每条命中补 `at: [{segment, line}]`，报告导出 `tolerance_pct`；`_violation_feedback` 改为「点名第几段 + 原句 + **上一版全文**」，容差取真值不再写死 10%（15 秒档 ±20% 时不再出现「报告说合格、反馈催你改长度」） | `app/checker.py`、`app/pipeline.py` |
| P2-31 后半 | `StoryboardShot` 补 `bgm/transition/shot_type/style` 四个契约槽，`skill.yaml` 分镜阶段的 JSON 契约点名要 **bgm / transition / shot_type** 三个（`style` **刻意不问模型** —— 它就是本次生成的风格参数，让模型每镜重述只多一份开销与漂移）；`scenes[].style` 的取法：模型给了用模型的，没给回落风格参数 | 夹具与单测各验一支（`test_engine_budget_and_streams` / `test_batch9_audit_findings`） |
| P1-23 | 产物 `mock` 取**真正用了哪条通道**（`LLMClient.mock`，含 `api_key == "MOCK"`），不再只看 `Pipeline.mock` | `pipeline._finalize` |
| 人味检测接线 | `app/ai_tells.py`（264 行、原来**无人 import**）→ `Pack.ai_tells_data()` → `_load_tells` → `check_script(tells=…)` → `check.ai_tells`；**只出分数与命中，不进 `passed`**；新建 `packs/elevator/ai_tells.yaml`。该模块的 `unknown` 守卫另有优先级 bug（`a | b - ALL` 把每个合法名都报成未知）已修 | `app/{knowledge,checker,pipeline,ai_tells}.py`、`tests/test_ai_tells.py` |
| 选题缓存 | 加 single-flight（`_plan_flight`）：同指纹并发生成只打一次模型；**先入缓存再放行等待者**（顺序反了等待者查不到、自己再打一次，等于没做） | `pipeline._select_once` |
| 错误文案 | `_ensure_content` 分清预算来源：只有显式传了 `max_tokens` 的阶段（选题/分镜/重写）才说「改 llm.max_tokens 不影响本次」，write 用的就是 config 的值 | `app/llm.py` |
| `limits.recheck_rounds` 写错 | 0/负数/非数字不再让回炉循环一次不跑并以「行业包配置缺少字段：'sections'」收尾（真因在 limits，且已白烧一次 select）；夹到 ≥1 并留 `limits_bad` 痕迹 | `pipeline._write_with_recheck` |

### 14.3 包内容与 packgen（lane + 主 agent 复验）

- `packs/_template` 建起来了（原来不存在 → `TEMPLATE_PACK` 静默回落到电梯包，每个新行业包继承电梯的 `growth.md/voice.md/hooks.md`）；模板是中性的骨架，`_materialize` 不再复制行业事实。
- 注入打通：`select.files.redlines/ideas`、`write.files.redlines`（`industry.md` 的 §2/3/4/6/7 切片 838 字）、`write.files.terms`（`standards.md#核心术语` 460 字）；`hooks.md` 按风格切片。
- 钩子范例自检：`就是在违法` / `七万三千次` / `九成的人` 这类**教模型违规**的写法已清（`patterns/hooks.md`）。
- 结构写错即失效的一类：`validate_banwords`（标量 `hard:` → 命中归零 + 谎报「N 个单字被忽略」）、`quota_table_errors`（`total: "约290"` → 静默丢键，提示词渲染成「总计≈ 字」）都改成 **加载即报错并摊到 `pack_error` / `param_audit`**（`tests/test_checker_structure.py`、`tests/test_pack_injection.py`）。

- **P2-37 口径漂移守卫**（本批补）：同一件事实写在多处不是可以一删了之的问题 —— 维保四类
  同时出现在 `topics.md`（进 select）、`industry.md` 两处（进 write 红线）与 `standards.md`
  （不注入），把注入那两处改成"见 standards.md"就会造出一条**悬空引用**（P3-28 那一类）。
  所以收的是"不许漂"：`tests/test_pack_consistency.py` 三条 —— 四类枚举必须同集合同顺序、
  禁令行本身引用反例不算违规（第一版就在这里假红过）、量化周期（`N 天一次`）一律不许出现、
  文档里的语速数字必须能在 `rate_by_style` 找到（P2-36 的同类）。已做变异检验：注入
  「半月、季度、年度」与「5.6 字/秒」两条断言立刻红，还原即绿。
- **P2-24 复核（不是改，是验）**：`voice.md:40` 现在写的是「制动器 → 抱闸或"闸"（**不说"刹车"** ——
  行业禁用错误口语，见合规红线）」，与 `industry.md:9` / `:77` 同一口径。write 提示词里
  「刹车」出现 3 次，逐条读下来**全是禁令引用**（"不说刹车"、对照表左列），「抱闸」5 次 ——
  两份文件不再互相否定，模型不必每轮静默仲裁一次。
- **P2-26 短档引导占比**：15 秒档原 `hook 15 / body 40 / cta 15` —— 正文只占 57%，
  而 cta 15 字 ≈ 3.2 秒，一条 15 秒的片子里五分之一在念引导。改成
  `hook 15 / body 47 / cta 8`（总量 70 不变，长档不动），并加两条守卫：
  `rules/duration.md` 那张抄件必须与 `quota_table` 逐格相等（P2-37 同一形态的漂移），
  以及任何档的 cta 占比 ≤25% 且短档不许高于长档。两条都做了变异检验：
  把抄件改回旧数字 → 第一条红；把 cta 改成 25 → 两条都红。
- **P2-39**：`banwords.yaml` 头不再宣传没用上的 `promote`（现写明"引擎支持，本包一条都没用"），
  平台差异化覆盖不足由 `param_audit` 摊到平台选项上；词表-文档对账测试见 `test_banwords_alignment.py`。

### 14.4 桌面端安全与交付（lane）

argv 令牌 → env（`TALKSCRIPT_TOKEN`）；日志脱敏 + `<userData>/logs/engine.log`（>2MB 轮转，写前掩码令牌/`Bearer`/`sk-`）；`engineProc.on('error')` 给真人话并让健康检查立刻出局；单实例锁；`undraft` 行级改写保注释；private 文件 HTTP 403；`base_url` 拒 `file://`/`javascript:`/内嵌凭据/空格，远端明文 http **警告而不拒**（不破坏本地/内网 LLM 用法）；**可写包根搬到 `%APPDATA%/TalkScript/packs` + `app/packseed.py` 内容指纹播种**（覆盖安装不再吞掉用户自建包；出厂未变→一字节都不写，用户改过→绝不覆盖并留 WARNING）。
渲染层三个"后端给了没人读"的口子本批补上并加了断言：`base_url_warnings`/`warnings` 常驻在模型页（`.cfg-err` 同族色）、`/api/meta.packs_dir` 摊在包详情、点 private 文件卡显示「不经界面浏览」而不是「读取失败」。

### 14.5 渲染层（lane + 主 agent 补齐）

停止撞上完成不再谎报（读取消返回值：`它已经跑完了 —— 产物已保存`）；失败/取消记录重启后真渲染「这次失败了·重来」卡；**刷新后重挂正在跑的作业**；后台作业可见 + `停止全部后台生成（N）`（我把它从首页的输入卡下方挪走 —— 那里有「空着不占位」与 24/16 节奏两条硬不变量，实测漏出时会把 chipsGap 从 16 顶到 44；现在首页不写这行、会话态写且只占一行）；「添加模型」草稿不再被切面板吞掉；冷启动 placeholder 给默认值；`modelSetupGap(meta)` 用**字段**判「没配模型」（原来靠正则读服务端人话，三条文案里只有一条匹配得上）；Esc 优先级（收菜单 → 关浮层 → 关设置 → 最后才是停止）；左栏 roving tabindex（100 条不再 200+ 停靠点）、`for=` 12 处、toast `aria-live`、自绘下拉 `aria-expanded`；重写等待超时不再宣称失败（「仍在进行，继续为你盯着」）。

### 14.6 顺手修掉的两个"守卫本身在说谎"

- `verify.js` 崩溃时只给一句 `Cannot read properties of undefined (reading 'rebind')`，页面侧真实异常全程不露面 → 现在崩溃路径一起打「页面侧异常」（本批靠它 30 秒定位到 `settings.js` 里一处重复声明）。
- 20260917 遗留的 P1-2「生成期间同屏双状态无断言」补上：左栏会话列表在**生成中与收尾后都不许出现重复 id**，且本次作业最多并入一行。

### 14.7 收尾三条（同批，主 agent）

| 条目 | 改动 | 证据 |
|---|---|---|
| P2-46 后半 | 同名建包的占位从"`create_pack` 内、模型调用之后"提到 **`start_packgen` 里花钱之前**（`claim_slug`/`release_slug`）；占不到就同步 409。三条出口都归还：完成/失败/**额度满 409**（额度检查现在排在占位之后，这条是新增路径，第一版就漏了 —— `tests/test_packgen_claim.py::test_quota_rejection_also_gives_the_name_back` 钉住） | 同名并发：模型只被打 **1 次**、注册表只有 **1 条**建包作业、第二条同步拿到「正在创建中」 |
| P2-27 后半 | 时长容差**终于能按包配**：`pack.yaml` 的 `duration_tolerance_pct`（`Pack.duration_tolerance()` → pipeline 两个校验调用点）。取值写错在加载时就判死成 `pack_error`（0 / 字符串 / 布尔 / 越界都不放行进生成）；生成链路用 mock 活体产物验到 `check.tolerance_pct == 35.0` | `tests/test_duration_tolerance.py` 20 条 |
| 人味词表随包出生 | `packs/_template/ai_tells.yaml`（行业无关的起点词表）+ `packgen.STYLE_FILES` 复制；`REQUIRED_IN_TEMPLATE` 一并要求 | 真建「宠物医院」包 → `_load_tells` 拿到 **10 条 tell、零 config_warnings**（不补这一步 = 新包的人味检测静默关闭） |
| 安装包重打 | `npm run dist`（exit 0，未签名 = 没配证书，符合预期） | `dist/win-unpacked/resources/engine/packs` 下 **private 文件 0 个**、`app.asar` 内 `packs/**/private` **0 命中**、`_template` 与 `elevator` 都在、`ai_tells.yaml` 已随包 |

### 14.8 门与体积

- `.venv/Scripts/python.exe -m pytest -q` → **437 passed**（本批新增 `test_refine_feedback` / `test_engine_budget_and_streams` / `test_ai_tells` / `test_packgen_claim` / `test_pack_consistency` / `test_duration_tolerance` / `test_batch9_audit_findings` / `test_unverified_facts` / `test_checker_structure` / `test_env_bounds` / `test_pack_injection` / `test_template_pack` + `test_plan_reuse` 的 3 条并发用例）。
- `node _verify/verify.js` → **296 / 296**（桩 fetch，界面回归）。
- `node _verify/e2e-live.js` → **10 / 10**（真引擎 + 真页面 + 真落盘：健康检查、mock 作业跑到 done、
  5 张卡片渲染、全流程无 JS 异常、产物与 `index.json` 落在 `--data-dir`、左栏出现该记录、
  SRT/MD 导出不抛错）。这条门本批此前没人跑过 —— 状态机加了 `storyboarding` / `packing`
  两个状态之后必须再验一次真进程链路。
- 一条断言按新措辞重指（不是削弱）：`test_segment_quota_divided_by_points` 原来认「段落超配额」这个字面短语，现在认「第 2 段 + 134 字 + 配额」——要守的东西一个字没松。
- 提示词实测（同一组参数）：write 每轮 **7580** 字（本批前 8751）、回炉两轮 user 公共前缀 **99.4%**、`industry.md` 注入 838 字（整份 2848）、`standards.md#核心术语` 460 字（整份 2195）。

### 14.9 仍然开着（不假装完）

1. **P3-15 usage / reasoning_tokens 不读**：批次 4 起就明确后置（穿透面大、收益小）。本批的重试与预算已经能被观察，这一项的紧迫性下降。
2. **方案 10 合并 select+write / P3-42 一次出 2~3 版**：改产物时序，是产品决定不是 bug。
3. **§六 第 1 层的两处降档**（`recheck_rounds 2→1`、`config.yaml retries 3→1`）：**没做**。前者会削弱合规回炉、后者是用户自己那份未跟踪的 `config.yaml`，都不该由修 bug 顺手改掉。
4. **干净机器的 NSIS 安装验证仍缺**：安装包已重打（见 14.7），但"覆盖安装后用户自建包健在"这条只由 `tests/test_server_hardening.py` 的 `--packs-dir` 用例守着，没在真装机上走过一遍。
5. 界面还没有**人味分数**的落点：字段进了 `result.json` 与回炉反馈，但产物卡不显示（展示口径要先定，不擅自加）。
6. 既有 UI 尾差（都在 `styles.css`，而用户在该文件里有在飞改动，不该由修 bug 顺手改）：
   6 处状态标记对比度 3.95~4.41 低于 AA；`#composer-body` 半径用 `--r-pop` 而规范说卡片是 12；
   `.qp-more` 字形 14 对 §4 的 16；`.chev` 11×8；约 21 处 ≤2px 视觉补偿缺"补的是什么"的注释。
   （`progress.js` 的 `stepKind()` 死类已在本批删掉，见 §14.13。）
7. `app/export_skill.py` 去留（P2-6）：没有界面入口，235 行；文档已不再承诺按钮，但代码还在。

### 14.10 独立复核（另一条 lane 逐条反证 §14.1~14.2）与它查出的五处残留

复核方式：MockTransport 逐次记录 `max_tokens` 与**整份 payload**、真 `Job` 驱动流式回调、
真跑 mock 生成读 steps、改 `jobs.JOB_BUDGET_SECONDS` 看文案。结论：1~9 条判「真修好」，
第 10 条判「半修」。**五处残留全部落码修掉，各有一条用例钉住**
（`tests/test_batch9_audit_findings.py`）：

| # | 复核实测到的残留 | 修法 |
|---|---|---|
| 1 | **预算升到 `RETRY_UPGRADE_CAP` 之后，第三次请求与第二次逐字节相同** —— 空内容这一支不往 `messages` 里加任何东西，于是 R3.3 批评的"原样重发"在封顶后复活 | `app/llm.py`：算出 `nxt <= cur` 且不是截断支 → **不再发**，异常里写明"已到上限、不再原样重发"。反向也钉住：截断那一支带着「请完整输出」的新消息，payload 变了，到顶照样再试（不许把唯一还有救的那一支一起砍掉） |
| 2 | 触发读 `jobs.JOB_BUDGET_SECONDS` 的实时值、文案读本文件的 from-import 副本 → 改值时报的分钟数是旧的（两本账） | `_stop_check` 改读 `_jobs.JOB_BUDGET_SECONDS`；用例把值改成 60 秒，断言文案说「1 分钟」 |
| 3 | 建包的 `should_abort=job.is_cancelled` 只认取消、不认预算闸 | 改成 `lambda: job.is_cancelled() or job.overdue()`；用例断言超预算时不写盘、不留下半个目录 |
| 4 | `limits.recheck_rounds: 0` **没有任何痕迹**（审计原话：「§14.2 那句对 0 不成立」）；`2.9` 被 `int()` 截成 2 也无痕 | 新增 `limits_zero` 步骤（"本包配置为不回炉"）与非整数截断的 `limits_bad`；`0 / 2.9 / abc / -1` 四档各有留痕，`2` 一档确认无噪声 |
| 5 | mock 夹具的 `_STORYBOARD` 四栏全空 → 新契约槽只在单测里成立，mock 产物恒空 | 夹具按真实契约填 `bgm/transition/shot_type`，**最后一镜的 `style` 故意留空**，让"回落风格参数"那一支也有活样本 |

另外两处复核指出但不算缺陷的口径，记录在此免得下一轮又被当 bug 找出来：
「3 次请求 / 1000→1500→2250」是**探针参数**（`max_retries=2`）下的形态，生产调用点都用默认
`max_retries=1`，真实形态是 **2 次请求 / 16000→20000**；`app/llm.py` 的 `on_attempt`
只在流式分支调用（非流式没有缓冲可清），所以"每次 HTTP 尝试前清零"这句话严格来说只对流式成立。

复核也没能证的部分（下一轮别再当已验）：真实上游会不会回 `finish_reason: "length"`、
思考是否真吃掉 16000 预算 —— 全部结论出自 MockTransport，本地 `config.yaml` 没有可用模型。

### 14.12 P1-44 第三条腿，以及它顺出来的一个新包级 P0

补 `_pack_audit` 缺的第三条腿（`Pack()` 加载 + `param_audit` + **占位符对账**）时，
刻意复用生产函数（`_normalize` + `*_ctx` + `unfilled`）而不是在 packgen 里再抄一份
"引擎提供哪些占位符"的表。验证：把模板里 `$hooks` 全局改成 `$hooksXX` →
体检点名它；干净的新包 → 零说明。`tests/test_packgen_selfcheck.py` 里还有一条
反向守卫：对账函数里不许写死任何占位符名（写死 = 引擎加新的就误报）。

**顺出来的真缺陷（新包级 P0，P0-18 在同一套代码里重演）**：为了把这条消息改准
（`param_audit` 原来写"将注入整份文件"，而代码早已改成不注入），把生成包的体检
结果念了一遍 —— 发现新造的包里 **每个 segment / audience 的切片都是空的**：
`topics_map` / `audience_map` 是 identity 映射（选项 → 同名章节），
而 `_materialize` 的章节标题用的是**模型给的** `topics[].heading` /
`audience_details[].name`，提示词"要求"它们与选项一致，但那只是要求。
对不上时的后果与 P0-18 一模一样：选题与撰写阶段**一个字都不注入**这个行业的
细分知识与受众话术，而包看起来完全正常（列表、参数、文件都在）。
`tests/test_template_pack.py` 早就断言"新包的切片必须解析得出"，
只是原来那句错误的消息里不含"切片为空"，测试的过滤器抓不到它 ——
**改注释把真缺陷暴露给已有测试**，这是本轮最划算的一次。

修法：章节标题一律按 `pack.yaml` 的选项写，模型给的内容用归一化名称匹配挂上去
（先逐字相等、再互为包含，命中即从池子里扣掉，防两个选项抢同一条）；
挂不上的**仍写出空骨架章节**并标「待补」，让"标题不存在"与"标题在、内容待补"
两种缺在切片层可分辨；模型多给的段落挪到末尾「附」区等人工并回，不静默丢弃。

同批还修了两处：
- **P2-47「完成」的去向**与「返回 / 取消」统一（从行业包面板进来的，完成后留在行业包，
  不再被扔回工作台），verify.js 补一条断言；
- 桩的 private-403 分支原来只认 `elevator` 包 —— 前面有用例建出第二个包之后，
  请求落到通用清单分支，界面拿到没有 size/text 的响应，断言红得像是实现的错。
  改成与包名无关（与真实后端一致）。

**自己造的一处进程级污染（记下来防复发）**：新加的测试在**模块顶层**
`os.environ.setdefault("TALKSCRIPT_MOCK", "1")`，而 `load_config` 每次都读环境变量
—— 于是后面所有「没配模型必须被拒」的用例统统放行，四条毫不相干的测试
在全量跑时红、单跑全绿（顺序依赖）。已改成只用 `cfg.mock = True`，并加了一条
AST 守卫：测试模块**顶层**不许写 `os.environ`（函数内改、`finally` 还原是正当用法，
`test_env_bounds` / `test_llm_retry` 就是这么做的）。守卫本身做过变异检验。

门：**pytest 443 passed · verify.js 297/297 · e2e-live 10/10**。

### 14.13 桌面端 lane 的声明，用**它自己写的测试之外**的方式复验

那条 lane 的报告很详细，但本项目的教训是"lane 自己的绿测试不算证据"。
另起一次真引擎（`--root . --data-dir <临时> --packs-dir <临时>/packs`，令牌经 env 给）逐条打：

| 声明 | 独立探针 | 结果 |
|---|---|---|
| 令牌只进 env | `Get-CimInstance Win32_Process` 读命令行 + 无/错/对令牌各打一次 `/api/meta` | argv 里没有令牌；401 / 401 / 200 |
| 文档端点关闭 | `/docs` `/redoc` `/openapi.json` | 404 / 404 / 404 |
| private 不经 HTTP | 6 种写法：`private/`、`Private/`、`private\x`、`knowledge/../private/`、`..%2fprivate/`、`private%2f` | 全部 403（最后一种 404），对照 `pack.yaml` 仍 200 |
| `base_url` 拒/警分工 | 7 种非法（`file://`、`javascript:`、`ftp://`、内嵌凭据、含空格、裸 `https://`、缺协议）+ 3 种合法 | 非法全 400；远端明文 http 200 且 warnings=1；`127.0.0.1`/`localhost` 200 且**无**警告（用户的本地/内网 LLM 没被改坏） |
| `undraft` 不吃注释 | 复制真包 → 置 draft → 走 API 转正 → 逐字节比对 | 24 行注释全在；除 `draft:` 那一行外**逐字节相同** |
| 可写包根 + 播种 | `--packs-dir` 起引擎 → 手改 `banwords.yaml` → 读回 | `/api/meta.packs_dir` 暴露；用户内容未被覆盖 |

同批：`P2-47`「完成」的去向已与「返回/取消」统一（从行业包进来就回行业包，
verify.js 补断言）；`progress.js` 的 `stepKind()` 连同它产出的 `k-*` 类一起删掉
—— `styles.css` 对这些类**一条规则都没有**，而它除了拼 class 之外没有任何用处
（`grep -c "k-select\|k-write" styles.css` = 0），verify.js 也不依赖它们。

门（本条复验后）：**pytest 443 · verify.js 297/297 · e2e-live 10/10**。

### 14.14 最后两条未验项（P2-12 / P3-28）与守卫顺出的两处模板级悬空引用

**P2-12（429 与 Retry-After）**——独立探针（MockTransport，第一次回 429 + `Retry-After: 2`）：
相邻两次请求间隔实测 **2.06 秒**，作业日志留下「上游限流(429)，2s 后重试」。
限流不再是"三秒内烧完三次"（报告引的 `generated/20260912-105341` 形态）。

**P3-28 / P3-38（悬空引用）**——批次 5 说"faq.yaml 引用的 industry.md 已随 P0-18 注入，不再悬空"，
这半句是真的、半句是错的：注入的是 `compliance/industry.md#红线速查` 那一节
（808 字，实测确实含 救援口径 / 反谣言 / 96333 / 119 / 抱闸），
但**文本里仍然写着"统一口径见 compliance/industry.md 反谣言库"** —— 模型手里没有一个叫这个名字的文件。
新增 `tests/test_dangling_refs.py`：把 select / write 在**每个 segment / audience / style / platform 选项**
下各渲染一遍，要求文本里不出现任何 `*.md` 路径；对电梯包与**新建包**都查。

这条守卫立刻抓出两处**写在模板里、会复制给每一个新包**的同类指路：
`_template/patterns/hooks.md:33`「口径见 `compliance/industry.md`」与
`_template/knowledge/voice.md:43`「术语表见 `knowledge/standards.md` 的「核心术语」」。
都改成指向提示词里**实际存在**的那段（红线段的真实标题是 【行业红线】；
术语那份在提示词里是「核心术语（规范说法 → 口语解释）」这张表）。
中途我自己差点再造一条假指路（写成【术语口径】段 —— 那个标题不存在），
是把渲染出来的段标题列出来核对才发现的：**改指路之前必须先看渲染文本里到底有什么**。

门（本条之后）：**pytest 446 · verify.js 297/297**。

### 14.15 P0-17 的第三条修法（清数据之外，注入层要自己认账）

批次 3 只做了修 ①（清空 `private/*` 的示例数字与假溯源章）与一半的修 ②，
**修 ③「`source`/`verified` 为空的条目注入时降级」一直没做** ——
`private/README.md` 甚至明写着"引擎不校验、也不因为空着而降级"。
清空只解决"仓库里不许有假数字"，解决不了正常用法里的那一条：
**用户填了真数字但没写出处**，那段照样顶着
【私有知识库（优先作为事实来源）】的标题进提示词，模型照念成既成事实。

现在 `knowledge.strip_empty` 给这类条目就地打 `_未核实` + 一句
「只能写成 `{{待补：…}}` 占位，不许当既成事实播报，也不要替它编一个来源」。
三条口径由 `tests/test_unverified_facts.py`（6 条）钉住：
标记**贴在条目上**而不是整份文件末尾的统称（否则等于没标）；
写了出处的条目**不被连坐**；只有空出处键、别的全空的条目**整条消失**（不凭空多一条标记）。
本包实况是精确的：`products.yaml` 清空后不再注入，只有 `service.yaml`
（有内容、没出处）被标记，`faq.yaml` / `cases.yaml` 本来就没有这两个键、不受影响。
`private/README.md` 那段"不降级"的说法同步改掉，并把结论翻过来：
**编一个溯源章比留空更糟**，因为留空会被标出来。

## 十五、批次 10 · 三条审计 lane 反证批次 9，以及两条 lane 中途阵亡后的收尾（2026-09-22）

审计方式：三条独立 lane 并行反证批次 1~9 的"已修"（引擎生成链路 / 包与 packgen / 桌面端与交付），
每条都要求"跑出来的证据 + P0~P3 + file:line"。返回 **3 个 P1（引擎）+ 3 个 P1（包）+ 2 个 P1（桌面）**
共 40 余条，其中绝大多数由主 agent 逐条复验后落码。
**两条修复 lane 在 150 轮上限处阵亡**，包层与桌面层各留下"实现已落、收尾未落"的半截状态，
由主 agent 接完（见 15.3 / 15.4）—— 这条教训写进记忆：**修复 lane 的分工要小到能在预算内闭环**。

### 15.1 引擎（主 agent 直接修，`tests/test_engine_audit10.py` 23 条）

| # | 实测到的缺陷 | 修法 |
|---|---|---|
| P1-45 | **一条"活着但一个字都不给"的流停不掉**：取消/预算闸只挂在 `on_delta` 上，而只发心跳、只推空 delta 的流永远不触发它；httpx 读超时在这种流上也不生效（字节一直在到）。实测作业状态早已 cancelled、线程还在收包，收满 800 行才报"空内容" | `llm._stream_once` 逐行查 `should_abort()`（+ 每次尝试前查一次，覆盖退避等待）；`pipeline._abort_gate(job)` 把 `_stop_check` 的两种异常原样送进网络层。修后第 5 行就断 |
| P1-46 | **作业停在忙态 = 永久吃掉一个并发额度**（`prune()` 只回收终态）。三条路径同时存在：建包超预算被 `should_abort` 谓词报成 `JobCancelled("已取消")` → 分支 `pass` → 永远停在 `packing`；`_spawn` 起不来；`store.job_dir` 抛错。实测失败三次（上限 4）之后，引擎在**一个都没在跑**的情况下一直回 409 | `_spawn` 起不来就 `_fail` 再抛；`start_generate` 的建目录失败同样落终态；新增 `_settle_cancel()`：不是真取消的 `JobCancelled` 一律按 `JobBudget` 收尾；建包的 `should_abort` 改传**会抛异常的闸门**而不是布尔谓词 |
| P1-47 | **空产物以 done 收场**：`{"sections": []}` 一路走到落盘，`check_script([])` 在容差够宽时返回 `passed=True` → 状态 done、`result.json` 是空的、历史字数 0 | `_finalize` 开头拒绝空正文（抛 `EmptyContentError`，文案指到推理预算）；单段重写同理：**空 `new.text` 不许覆盖已经合格的那一段** |
| P2 | 预算"升级"成降级：`max_tokens` 可配到 200000 而升级上限写死 20000 → `min(90000, 20000)` 把截断重发降到 20000（注定再截断），日志还写着"已提高到 20000" | `cap = max(RETRY_UPGRADE_CAP, cur)`，永不降档；到顶那一支的文案改成"已到升级上限、只能原额再发"，并且反向守住截断支仍会重发 |
| P3 | 选题缓存指纹只算 model 名 → 同名模型换服务商仍复用上一家的选题；`limits.recheck_rounds` 的 `"0" / "2.9" / true` 三种写法一条痕都不留；`Job.snapshot()` 交出 `steps/params` 的活动引用（锁外序列化时被工作线程改掉）；`choices[0]` 非对象冒成英文 `TypeError`；`usage` 被 `if not choices: continue` 连同末尾 chunk 丢掉（P3-15，顺手接上：select/write 步骤里现在有 `usage`） | 各一条用例钉住；`_plan_key` 加 `base_url`；recheck_rounds 先按数值理解再决定留哪种痕；snapshot 交副本；结构异常统一 `LLMError` 中文文案 |
| P3 | `_violation_feedback` 里写死『政府补贴』→『财政补助』——电梯行业的口径混在通用回炉文案里，会给猫咖也上一课"可申请财政补助" | 改成行业中立的一句（并明说"换同义词躲过检查不算改"） |

### 15.2 顺出来的一条门禁用法纠正

`pytest -q` 在本项目**看不到测试总数**：`pytest.ini` 的 `addopts` 已经带 `-q`，命令行再加一个
就是 verbosity −2，整段结尾报告（含 `FAILURES`）都不打印，只剩点号 —— 之前几轮"用 `--collect-only` 数数"
的绕路是误诊。门禁的正确跑法是**不加 `-q`**：`489 passed in 68.84s`。

### 15.3 包层（lane 阵亡后由主 agent 接完）

lane 把实现落全了（`knowledge.name_matches` 的"逐字相等 → 包含取对称差最小 → 并列都不给"、
`param_audit` 的空切片/坏 tell 词表降级、`_section_has_body`、重复选项在载入时判死），
但**最后一步没接上**：`packgen.py` 少了 `name_matches` 的 import（`NameError` 让建包全红），
`_materialize` 仍声明 `-> None` 且两处 `_match_by_name` 没传 `notes`（`create_pack` 直接
`TypeError: can only concatenate list (not "NoneType")`）。补 import、把匹配账穿进去并返回，
"模型压根没给对应段落"这一支原本只有文档承诺、没有代码，也补上。
它的测试文件引用了三个不存在的符号（`TopicSpec` / `build_pack` / `generate_pack`）且带一段
`if False else None` 的死代码 —— 重写为 14 条真用例（`tests/test_pack_layer_audit10.py`）。
过程中三条用例被主 agent 自己的错预期写错（把"非连续子串"当成包含候选、把重复选项的**判死**当成告警、
`Banwords.scan` 的参数顺序），逐条跑真值后改准，而不是改实现去迁就用例。

### 15.4 桌面层（lane 阵亡后由主 agent 接完）

落地并复验：409 不再一锅端（服务端给 `quota_exceeded` / `pack_broken` 机器码，坏包那句带行列号的原话上屏）、
被遗弃的单段重写不再解锁别人的作业、「重新连接」把 DOM 一起接回、`共 ` 空读数、
令牌不再进错误弹窗（`maskSecrets` 抽成 `engine-dialogs.js` 并有测试）、编辑框 Esc 不再顺手停掉作业、
设置模态的焦点陷阱与 `role=dialog`、占位符「待补：待补：」双前缀、定位首处跳到字数胶囊、
建包取消失败后按钮死锁。**lane 自己留下的两处坏**由主 agent 修：
`verify.js` 里 `querySelectorAll('.fail-actions [data-act="retry"]))` 少了收尾单引号
（整份 UI 门禁直接崩在 `SyntaxError`，而它红得像是渲染层的错），
以及**桩把 `created_at` 每次轮询现算**——于是「已用 N 秒」被钉死成常数，
两条计时器断言红的是桩不是实现；改成按作业 id 记账后 322/322 全绿。
README 两处漂移（verify.js「秒级」、picker「glm-4.7（默认）」）随 P3-11 一起改。

### 15.5 门（批次 10 收口时实跑）

**pytest 495 passed · verify.js 325/325 · e2e-live 10/10 · node --test desktop/ 26/26**。
新增测试 47 条（引擎 29 + 包层 14 + 桌面/壳若干），其中 8 条是守卫（假客户端签名、假 cfg 属性、
`os.environ` 顶层写入、忙态到终态的迁移表、测量数字不进注释）。

### 15.7 收口之后的最后四条（lane 阵亡后剩下的尾巴）

1. **悬空引用守卫扩到 `*.yaml`**（批次 10 的 P3-9 只做了一半：`test_dangling_refs.py` 的正则只认 `.md`）。
   扩开立刻抓出六处模型读不到的指路：`patterns/growth.md`（模板与电梯各两处）指向 `skill.yaml`、
   `patterns/hooks.md` 两处指向 `banwords.yaml`、`knowledge/voice.md` 一处、`knowledge/ideas.md` 一处、
   `private/faq.yaml` 一处指向另一个私有文件。全部改成指向**提示词里真的存在**的那段
   （「口语化硬规则」与【字数配额（硬性）】两条是逐字核过渲染文本才写的 —— 见 §14.14 的教训）。
2. **引擎自己也在往提示词里写路径**：私有资料注入段的段名原来是 `=== private/service.yaml ===`。
   改成 `=== 私有资料 · service ===`（来源信息保留、路径形态去掉），
   `tests/test_unverified_facts.py` 的断言同步改口径（它测的是"标记贴在条目上而不是整份末尾"，
   这条语义没动）。
3. **纯符号的行业名共用一个目录名**：`slugify("？？？")` 回落成 `"custom"`，于是第二个纯符号名字
   永远建不出来，而报错写着「行业包已存在」。改成：`slugify` 如实返回空串、
   `start_packgen` 在花钱之前拒掉并说清"要含中文/字母/数字"，
   同时删掉 `create_pack` 里"取模型的 display_name 兜底"那一支（那正是 P2-51 的原始形态）。
   `preview_slug` 那段"预演不出来交给正式判定"的注释是错的，一并改准。
4. **运行期的"这一节是空的"以前只在行业包卡片上说一句，作业里毫无痕迹**（批次 10 的 P1-3 只做了一半）。
   `_run_generate` 现在除了 `_unmatched_options`（选了包里没有的值）之外，还走一遍
   `param_audit` 问"**真正选中的**那一节有没有正文"，有降级就记一步 `knowledge_empty`。
   刻意复用 `param_audit` 而不是在流水线里再判一次"标题在不在" —— 第二本账迟早又不一致。
   用例：`tests/test_engine_audit10.py::test_empty_section_for_a_valid_option_is_said_during_the_job`
   （正向：清空某一节正文 → 有这一步；反向：健康的包不产生噪声）。

### 15.6 仍然开着（批次 10 之后）

1. §14.9 的 1~3 条不变：方案 10 / P3-42 是产品时序决定；`recheck_rounds 2→1` 与用户自己那份
   `config.yaml retries` 不由修 bug 顺手改。P3-15 的 `usage` 本轮已**记进作业步骤**，
   但界面还没有消费方（显示"思考 N token"仍待做）。
2. 干净机器的 NSIS 安装验证仍缺。**包内容本轮从当前工作树重打并逐项复验**：
   `npm run dist` exit 0 → `resources/engine/packs` 30 个文件、`private` 命中 **0**、
   `_template/ai_tells.yaml` 与 `elevator/ai_tells.yaml` 都在（新建的包开箱就有人味检测）、
   无 `config.yaml` / `generated/`、`engine/app` 下 `.pyc` 为 **0**（45 个 `__pycache__` 全在
   解释器 `py/` 里，是自带字节码），engine 共 41 MB。
   仍缺的只有"覆盖安装后用户自建包健在"这一条真装机动作 —— 它目前只由
   `tests/test_server_hardening.py` 的 `--packs-dir` 用例守着。
3. `styles.css` 的既有 UI 尾差（用户在该文件有在飞改动）：6 处状态标记对比度低于 AA、
   `#composer-body` 半径档位、`.qp-more` 字形、约 21 处 ≤2px 视觉补偿缺注释。
   另加一条本轮留下的：**`.pg-more pre { margin-top: var(--s2) }` 现在是死规则** ——
   校对清单从 `<pre>` 改成行级结构（P3-50），该选择器不再匹配任何东西；删掉它或在
   `.pg-more > div` 上补回这 8px 的缝，由 styles.css 的主人一起处理。
4. `app/export_skill.py` 去留（P2-6）：没有界面入口；本轮另实测到"选项里含 `: ` 会让
   frontmatter 抛 `ScannerError`"，等去留决定一起做，不单独修。
5. 人味分数的界面落点属另一条产品 track（`docs/需求方案-去AI味与热点情报.md`，8 条待确认）。

### 15.8 收口复核（第 12 节 lane）：反证 §15 的 12 条声明，查出五处真缺陷

复核方式：一条只读 lane 逐条"跑出来反证"，探针在 %TEMP%，不改仓库。
它的结论：**5 条声称成立**，**5 处证伪成功**（另附一条不变量被破）。全部落码，各有用例：

| # | 复核实测 | 修法 |
|---|---|---|
| P1 | `rewrite_segment` 里 `client = self.llm` 仍在 `transition_if_room` **之后、try 之外** → 第四条漏槽路径（实测作业停在 `rewriting`，`running_count()` 一直是 1） | 取值与 `_spawn` 一起进 try，抛错先 `_fail` 再抛 |
| P2 | `Job.snapshot()` 仍交活动引用：`snap["result"] is job.result` 为真、`steps[0]["data"]` 跨快照同一对象、`result["logs"] is job.steps` 为真（快照会随活列表继续长） | 快照交 `dict(result)` + 每条步骤连同 `data` 一起复制；两处 `"logs": job.steps` 改成 `list(job.steps)` |
| P2 | 取消请求失败后收回「取消中…」的那句 `if (packgenTick) packgenTick()` 是**死代码** —— `packgenTick` 从来没有被赋值 | `runPackgen` 里 `packgenTick = tick;`，`finish()` 清空；闸门断言由红转绿 |
| P2-48 | 一条"永远不吐一行"的流：逐行检查点无从触发，读超时 `180×2` 再乘重试，实测最坏 ≈**48 分钟**，是整作业预算（1200s）的 2.4 倍 | `Job.deadline()` → `chat_json/_complete/_stream_once` 的 `deadline` → `_capped_read()` 把读超时压到剩余预算（下限 1s）；`create_pack` 也一并透传 `should_abort`，建包流现在同样可中断 |
| P3 | 「空稿不 done」只挡了 `text.strip()`：四段全是 `{{待补：…}}`（含全角空白）照样判 done | 新增 `_readable()`：剥掉占位、空白与标点后什么都没有才算空稿 |
| P1（不变量） | `result.json 存在 ⟺ done` 被破：`write_result` 先落 result.json，随后建索引/渲染 md 抛 `KeyError` → 状态 failed 而盘上有完整产物 | `write_result` 改成**先算完全部内容再动磁盘**；`_finalize` 落盘抛错时顺带清掉半成品目录 |

**顺带把桩的三处"替实现撒谎"改忠实**（批次 10 的门禁口径）：
`/api/packs/<name>` 详情与 `/undraft` 原来无论问哪个包都回电梯的数据，
现在按请求里的包名回显、逐包记草稿位；`POST /api/packs/create` 补上
`claim_slug` 的 409（原来恒成功，"界面对并发提交做了什么"永远量不出来）；
建包结果页的 `checklist` 从一行假文本换成与 `packgen._checklist` 同形状的真 markdown。
**桩一改忠实，立刻证出一个真缺陷**：`undraftPack()` 读的是下拉 `#pack` 的值而不是右列正在看的包 ——
用户看着电梯包点的按钮会去转正另一个包（记 **P3-51**）。
修法：`openPackInfo` 记下 `piName`，转正按它发请求、事后也只刷新这个包；
断言用「列表点开 A、下拉漂到 B、点转正」的真实漂移场景钉住，
并做过变异检验（把 `piName` 去掉，这条立刻红）。

### 15.9 第四轮复核（专打 §15.8 这批新代码）：又查出四处，全部落码

复核方式：一条只读 lane 专门反证"最近两小时写的代码"（探针在 %TEMP%，不改仓库）。
它证明了三处干净（`_readable` 不误伤单字正文、`param_audit` 每次生成 21 次 stat / 0.78ms、
`renderChecklist` 全程 textContent 无注入面），并查出四处：

| # | 实测 | 修法 |
|---|---|---|
| P2 | §15.8 那条"落盘失败要清掉半成品"的修法是**坏的**：`store.delete()` 会立墓碑，随后 `_fail` 的 `_persist` 被墓碑拦住，于是这条失败记录只活在内存里，**引擎一重启就查无此事**（探针：`write_job returned False / job.json on disk: False`） | 撤销该删除；半个产物的回滚下沉到 `write_result` 自己（两个文件分开原子写，第二个失败就把已写的撤走，索引还没动所以不用回滚）。现在实测：`result.json: False`、`job.json: True` |
| P2 | 到点之后被主动截断的读超时，在协议层与"网络断了"长得一模一样 → 报成「模型接口连接失败（已重试 N 次）」，用户去查网线 | `except httpx.RequestError` 分支先走一次 `should_abort()`：预算/取消优先于"网络"归因 |
| P3 | 快照只复制了一层：`result["sections"][0]["text"]`、`result["check"]` 仍是活引用（实测能观察到已发出快照被改） | 带上产物时改 `copy.deepcopy`；量过成本：`include_result=False` 4.29µs、`=True` 7.54µs（30 步），相对 0.9s 轮询可忽略 |
| P3 | 桩的两处新Shortcut：`PG_CLAIMED` 只在 done/cancel 两条路上清（作业没走到就是漏），且详情分支把**所有**非 elevator 的包都冒充 fitment | 占位改成集合、按名字归还；详情按请求名回显（未登记的包名原样返回） |

另外实测到并顺手修掉的一条**可达崩溃**（原在 §15.6 第 4 条里等去留决定）：
`export_skill.py` 用 f-string 把行业名 / 细分列表直插 frontmatter，选项里出现 `": "`
就是非法 YAML → 导出抛 `ScannerError` → 界面一个 500，而技能目录已经写了一半在盘上。
改成 `yaml.safe_dump`（该引就引）+ 自检按**前两道 fence** 取 frontmatter（`split("---")[1]`
会被正文里的水平线切错）+ 失败说人话（不再用裸 assert 炸给用户）。
去留仍是产品决定，但一条能崩的路径不该因为"可能要删"就留着。

**门（批次 10 全部收口后实跑）：pytest 508 passed · verify.js 325/325 · e2e-live 10/10 · node --test desktop/ 26/26。**
四道门都跑过两遍以上，且 §15.8 / §15.9 的每条修复都做过变异检验（撤掉修复 → 对应用例立刻红）。

**本轮复核方式留下一条待办**：`_verify/` 下还留着几条 lane/probe 写的探针脚本
（`_live.js`、`_proto-audit.js`、`_proto-census.js`、`_chip-probe.js`、`_probe_*.py`、`_tsdata/`，
全部命中 `.gitignore` 的 `_verify/_*`，不进仓库）。它们与更早会话留下的同名系列混在一起，
分不清哪几条是别人的在飞产物，因此**没有删** —— 由会话主人自己清理。

### 15.10 第五轮复核（只打"最新写的代码"）：§15.8 的修法里又有一处是我自己造的坑

同一套办法再用一次：一条只读 lane 专攻最近两小时写的那五处改动。
它先纠正了自己一次误判（首次 `Read` 拿到的是改前的旧内容，`inspect.getsource` 才是真的），
然后查出四件事，全部落码：

| # | 实测 | 修法 |
|---|---|---|
| P2 | §15.8 那条"半个产物回滚"是**数据丢失**：`write_result` 不只服务新建，单段重写与"取消后回滚到重写前"都是在一份已存在的合格产物上**覆盖**；第二笔写失败时把第一笔删掉，用户从"有一版好的"变成"什么都没有" | 覆盖前先把旧内容留在手里，失败就**还原**（原本不存在的才删除）。两条用例分别钉住：覆盖失败 → 旧产物逐字节不变；新建失败 → 不留半成品 |
| P2-49 | 退避不认预算：`_capped_read` 只压读超时，`Retry-After: 60` 配只剩 2 秒的作业照样睡满 60s（实测 60.00s），`retries=3` 时最坏 ~3×60s | 等待按剩余预算夹紧；顺带把它做成**可打断**的（用户在退避期间点停止，原来要等睡完才生效，期间还占着额度）。两条用例：预算到点前就收工 <6s；退避中途取消 <3s |
| P3 | 预算闸把真因盖掉了：一开始就连不上（坏 URL）→ 作业超时抛出 `JobBudget` 时不带原因，界面上只剩"超过 20 分钟…建议降低 retries"，用户按建议改了配置还是在原地失败 | `_readable_error` 读一眼异常链：`（上一步：Connection refused）`。用例断言结论与真因**同时**在场 |
| P4 | `_TOMBSTONE_CAP=512` 的淘汰是真洞：再删 512 条之后，一条被删记录若在飞，其 `write_result` 会把它整个复活 | 上限提到 8192（约 250 KB 常驻），并把"这仍然只是把洞推到实际不可达、真正闭合要落盘前问注册表"写在注释里，不假装修干净 |

另外两处顺手：模型给的 `display_name` 折成一行（换行是往 pack.yaml / 校对清单 / 导出 SKILL.md
**正文**里注入 `---` fence 与 `# 标题` 的入口，复验实测 `\n---\nname: evil` 能原样进去），
以及 lane 报的"导出失败仍是 500"保持原样 —— `export_skill.py` 的去留是 §15.6 第 4 条那个待决决定，
不在决定之前给它加新语义。

**判干净的**（各自跑过）：`write_result` 的墓碑短路 / RLock 无重入死锁 / 临时文件不污染
`_count_on_disk`；快照 deepcopy 在真产物上 0.26ms、200 条 done 作业共 29ms（其中 18.3ms 是 JSON 序列化）；
`_finalize` 换成 `RuntimeError` 后没有改变任何 except 分支的走向，job.json 里原因原样保留，
`server.py` 走 `read_job` 回退所以记录仍打得开。

### 15.11 收尾自查（不再开新 lane，主 agent 自己复跑）

补两处 §15.10 修法自身的毛病，各一条用例：

1. **归因补了重复话**：外层消息常常已经把原文塞进去（`_brief(str(e))`），再补一句
   「上一步：…」就是同一件事说两遍。现在只在真能补信息时才追加
   （原文与异常类型名都已在消息里 → 不追加）。
2. **退避夹紧之后还要能被打断**：只夹 `deadline` 不够 —— 没到预算但用户点了停止时，
   原来仍要睡完才响应。`_interruptible_sleep` 以 0.5s 为段问一次闸门；
   **没有闸门可问时保持一次性 sleep**（第一版无条件切片，把四条测退避时长的
   `test_llm_retry` 用例改成了数 0.5s 片段的次数，那是测试被实现绑架，已回退实现）。

**批次 10 的最终门（四道全在当前工作树上实跑，非引用旧数）**：
`pytest` **508 passed**（74.5s）· `node _verify/verify.js` **325/325**（约 2 分钟）·
`node _verify/e2e-live.js` **10/10**（真引擎 + 真页面 + 真落盘）· `node --test desktop/` **26/26**。
安装包另从重打后的 `dist/win-unpacked` 复验：`packs` 30 个文件、`private` 命中 0、
两个 `ai_tells.yaml` 都在、`engine/app` 下无 `.pyc`、无 `config.yaml`/`generated/`。

**这一轮的方法学结论**（写下来免得下一轮又用错办法）：修复 lane 的规模要能在它自己的
轮次预算内闭环 —— 本轮两条修复 lane 都在 150 轮上限处断掉，各自留下"实现已落、收尾未落"的
状态（包层少一个 import、`_materialize` 没返回匹配账；桌面层在 `verify.js` 里留了一个
未闭合的字符串），如果主 agent 没接住，这些会直接以红门或崩溃的形式回到用户手上。
下一轮改成：**多条窄任务**（每条 ≤15 个改动点）而不是一条宽任务。

### 15.12 第六轮复核（打"上一轮的修法只落了一半"这类问题）：P3-50 的第二半

复核方式：一条只读 lane 反证 §15.8~15.11 的六处新代码；主 agent 同时扫两条它不碰的面。
主 agent 这边查到两处：

1. **P3-50 只改了一个调用点**（判据：同一份数据在两个界面画成两种东西）。
   校对清单在这个应用里有**两个落点**：建包结果页（`#pg-checklist`，本轮改成行级）与
   **行业包详情页**（`#pi-checklist`）—— 后者一直是 `$("pi-checklist").textContent = p.checklist`，
   而 `p.checklist` 正是盘上 `校对清单.md` 的**原文**（`app/server.py:645-649` 整个文件读出来）。
   于是同一份二十来条的清单：结果页是行级清单，点开包详情又是一面 markdown 墙。
   修：`index.html` 的 `<pre id="pi-checklist">` → `<div>`，`settings.js` 复用同一个
   `renderChecklist()`（**不新增 CSS 档**：行级样式沿用结果摘要已有的 `.pg-summary-*`；
   `#pi-checklist-wrap` 是 flex 容器但只按直接子级排 gap，标签换 div 不动它）。
2. **`verify.js` 的包详情桩在替实现撒谎，所以这一条量不出来**（两处不忠实）：
   详情分支的 `checklist` 恒为 `'1. 核对参数 / 2. 核对禁用词'` —— 既不是 `packgen._checklist`
   的形状（H1 / 说明 / `- [ ]` / `## ` / 缩进续行），也**从不为空**。真实后端是按
   "盘上有没有 `校对清单.md`" 回的：只有 packgen 建出来的包有，手写包（仓库里的 elevator）没有。
   也就是说"这个包没有清单"那一支界面代码在整个门禁里**从没执行过**。
   修：桩改成 `HAS_CL` 按包名判定 + 与 `_checklist` 同形状的 markdown（转正不删文件，所以不随 draft 变）。
   门：新增 2 条断言（草稿包详情画成 8 行、其中 3 条 □、没有任何行以 `- [` 或 `## ` 开头；
   手写包整块区域收起）。**变异检验**：把 `renderChecklist` 换回 `textContent = p.checklist`
   → 第一条立刻红（诊断里直接印出 `text: "# 新行业包校对清单\n\n行业…"`）。
   ⚠ 判据只看**行首**：清单正文里合法地引用了「红线速查」这类字样，全文 `indexOf('## ')` 会误判。

3. **README 的最坏请求数报价没有机器账**（顺带补的守卫，不是缺陷）：`README.md:45-49` 用
   `逻辑调用数 × 2 × (llm.retries + 1)` 报价 30/40 次请求，那个 2 就是 `chat_json` 默认
   `max_retries=1` 的尝试数。实测（MockTransport，`retries=0`，生产默认参数）：
   空内容与截断两条分支都是 **2 次请求、预算 1000→1500**；而"3 次 / 2250"那种数字来自
   显式传 `max_retries=2` 的探测用例（`tests/test_batch9_audit_findings.py:83`），不是生产口径。
   新增 `test_readme_worst_case_request_count_is_the_real_one`：默认值一改、或有调用点抬高
   `max_retries`、或 README 的倍数与 30/40 没跟着改，都会红。四种变异各跑一遍（基线绿 + 三种改坏全红）。

顺带排掉一条假缺陷：探针输出里 `'??????'` 这种乱码是**控制台代码页**替换了不可编码字符，
不是引擎写盘编码问题 —— 全仓 `app/*.py` 的读写都显式 `encoding="utf-8"`
（`fileio.write_atomic` / `read_yaml_file` / `knowledge.read_text_cached`），
`_reveal_in_explorer` 在 Windows 走 `os.startfile`、其余平台用列表参数 `Popen`（无 shell 拼接）。

**本轮四道门（当前工作树实跑）**：`pytest` **509 passed** · `node _verify/verify.js` **327/327** ·
`node _verify/e2e-live.js` **10/10** · `node --test desktop/` **26/26**。
**编号项对账（"是否还有遗漏"的机器口径）**：全文 66 个 `P*-N` 编号，按小节类型对账 —— 每个编号要么出现在
"批次 / 复核 / 收口"型小节里，要么出现在"仍然开着 / 待办"清单里。第一轮正则粗筛报了 24 个"只在问题清单里出现"的，
其中 20 个是我把 `### 4️⃣ P2-25 / P2-26 …`、`14.14`、`15.1` 这类小节误判成了问题清单（分类器噪声，不是缺口）。
真正需要逐条落到代码上确认的是 §五 那张**与 20260917 报告对账**的表里三行还写着 ⬜ 的：

| 编号 | §五 原判定 | 现在实测（本轮回代码核） |
|---|---|---|
| P2-1 | ⬜ 未变 | 已收口：预算升级换 `max_tokens`、截断那一支追加「上一版 + 请完整输出」，且 `test_batch9_audit_findings.py:61-62` 断言**两次请求 payload 不许逐字节相同**；"到顶又无话可补"现在干脆不发 |
| P2-4 | ⬜ 未变 | 已消：界面不再自己数字数，一律读 `check.segments[].chars`（`result.js:35,159-164`），`fmtText` 只剩渲染职责 |
| P2-5 | ⬜ 未变且更严重 | 已收口：`config.py:256` 越界 `log.warning(… 超出合法区间 […], 已夹逼为 …)` 再夹紧 |

三行都在报告里改成了 ✅ 并补了证据（**这类"对账表自己过时"是同一种病**：报告说没修、其实早就修了，
下一轮复核会照着它重查一遍，白烧一轮）。另外 P1-35（`voice.md` 第二处自我矛盾）随批次 5 的 P2-24 一起没了 ——
现在"修一次大几万"只在 `:23` 以**禁例**形态出现一次，`:34` 那份"必须含具体数字"的同一例句已删；
它没被标成 P1-35，属记账口径问题，这里补一句归属。

**注入覆盖率（P1-45 的收尾判据）——本轮实跑，结论是"没有新洞"**：真跑一次 mock 建包 + 一次生成，
把 `skill.yaml stages.*.files` 每一条声明都过 `Pack.file_slice` 量一遍：
电梯包 `红线速查 807 / 选题库 2292 / growth 775 / 核心术语 426`，新建的口腔诊所包
`53 / 58 / 941 / 113` —— **两边都没有一条空切片**（新包数字小是 mock fixture 只出骨架，真实模型出全文）。
我的判据"文件名在 `app/*.py` 里有没有被提到"启发式作废：`packgen.py` 的注释提到所有文件名，
于是每个文件都"有人消费"，量不出任何东西。结论：`rules/output-template.md`、`compliance/ad-law.md`、
`private/README.md` 这类不注入提示词的文件是**给 checker 与人看的**，与文档口径一致，不是 P1-45 复发。

### 15.13 第七轮复核（一条 lane 专打 §15.8~15.11 那六处新代码）：两处证伪 + 六条新账

lane 用 98 次工具调用、自己的探针（`%TEMP%/r6/`，不改仓库）逐条反证，结论：
`_interruptible_sleep` / `write_result` 的回滚 / 建包占位的归还**站得住**（含 `SystemExit` 这类
非 `Exception` 异常，因为 `finally` 在），而 §15.10 的两处修法被证伪。全部落码，各有用例与变异检验：

| # | 实测 | 修法 |
|---|---|---|
| P2 | **归因只走一层不够**：`RuntimeError("产物落盘失败") from OSError("") from ValueError("quota_table 第 3 行缺少 count 键")` 这条真链上，`__cause__ or __context__` 取到的是**空消息**的中间层，`if cs` 又把它当"没有原因" → 界面只剩五个字，根因一个字没露 | 沿链最多走 5 层，取**第一个有话可说的**环节；另修重复打印：外层带进来的是 `_brief` 截到 160 字的原文，用完整 `cs not in msg` 判"没说过"会把同一段印两遍（实测 160+120 字重复），判据改成比对将要打印的那 120 字 |
| P2 | `write_result` 之外还有一条**测试自己会撒谎**的路径：`tests/test_smoke_mock.py:241` 的 `subprocess.run(text=True)` 用**本机 locale** 解码子进程输出，而子进程打印中文 —— 外层一旦设 `PYTHONIOENCODING=utf-8`（Windows 上看中文输出的常规做法），`_readerthread` 抛 `UnicodeDecodeError`、`run.stdout` 静默变 None，用例以 `TypeError: NoneType is not a container` 收场。实测：带那个变量必红、不带必绿 | 两边一起钉死：`encoding="utf-8"` + 给子进程 `env` 里显式 `PYTHONIOENCODING=utf-8`（谁的环境变量都影响不到它） |
| P3 | 快照的步骤 `data` 只 copy 一层：`data["report"]` 是活引用，发出快照后再改 `hard_hits` 会串进"某一时刻"。今天树上还没有活的改写点，但 `_step` 之后继续往 `data` 里塞正是流水线常态（`usage` 就是这么加的） | 整步 `copy.deepcopy`（实测 0.08ms/次，相对 0.9s 轮询可忽略） |
| P3 | 退避通知报的是**夹逼之前**的秒数：预算只剩 3 秒、上游 `Retry-After: 60` 时，界面写「60s 后重试」而实际 3 秒就继续 —— 界面在说谎 | 抽出 `LLMClient._clamped_wait()`，通知与真正要睡的秒数**同源**（流式与非流式两处都改；`_interruptible_sleep` 改为调它，夹逼只此一处，不留第二本账） |
| P3 | `except Exception` 让 `BaseException` 把作业留在忙态（lane 实测抛 `SystemExit` 后 state 一直是 `packing`）：占位归还了、**并发额度没还**，`prune()` 只回收终态 | 修在 `_spawn` 一处：所有 worker 统一过 `_guarded()`，任何穿出线程的异常都先把作业落到终态再抛。不逐个 worker 补，那是第五次写同一件事 |
| P3 | 建包占位的归还窗口从 `self.llm` 才开始：`new_job_id()` / `Job(...)` / `add_if_room()` 任一处抛（实测强抛）就留下一个**永不归还**的 slug，那个行业名从此报「行业包正在创建中」 | `try` 起点提到 `claim_slug` 成功之后；归还点**只留一个**（原来额度分支自己 release 一次，若这期间别人抢到同名，第二次 discard 会偷走别人的锁） |
| P3 | `_TOMBSTONE_CAP` 注释里的尺寸是估的：写 250 KB，实测（tracemalloc，8192 个真形态 id）1031 KB —— 因为按纯字符串总量算，忘了 set 的槽位开销 | 注释改成实测数与算法，不改上限（上限的取舍理由仍然成立：桌面端一次会话删不满） |

**桩比后端宽容的四处**（同一批次里第三次踩到这个类别，全部改成忠实）：
`job1` 的 running 分支每拍重新取 `Date.now() - 8000`，于是"已用时 ≥ 8 秒"那条断言
**按构造恒真**（换成 `bornAt()` 记忆化之后，增长才是真量到的）；`job1-done` / `jobph` /
`jobfail` 三个快照漏 `created_at`，与桩自己 544 行的注释矛盾，补上；
`/undraft` 对非 elevator 包回 `draft:true` 而注册表里已经转正（只因界面不看响应体才没暴露）；
`/api/packs/<name>/file` 无论点哪个文件都回显 `knowledge/topics.md`。
另外 `PG_CLAIMED` 原来按**用户输入的那串字**占位，而引擎按 `slugify` 之后的目录名占 ——
「全屋定制/装修」与「全屋定制 装修」落成同一个 `packs/全屋定制-装修/`，真引擎第二次 409、
桩两次都放行。桩改成同一个 slug 口径（逐字符判，因为这段在模板字符串里不能写带反斜杠的正则）。
`calls.pgdup` 目前仍没有断言读者：要让一条 UI 断言真的量到它，得把"两种写法同一个目录名"这一对
输入送过建包表单，那与已有的同名并发 409 断言覆盖的是同一条实现路径 —— 记在这里，不当成新工作。

**我自己在这一轮里第四次踩模板字符串的反引号陷阱**：给 `/undraft` 那条忠实性注释时写了
`` `settings.js:1283` ``，`node --check` 立刻 `SyntaxError: Unexpected identifier 'settings'`。
这条陷阱的固定防线就是"改完 verify.js 先 `node --check` 再跑门禁"，它这次也如预期把它拦住了。

**第 7 轮的门（当前工作树四道全实跑）**：`pytest` **517 passed**（78s）· `node _verify/verify.js`
**327/327** · `node _verify/e2e-live.js` **10/10** · `node --test desktop/` **26/26**。
本轮 6 处修复各有一条用例，且逐条做过变异检验：撤掉链遍历 → 红；判据退回完整 `cs` → 红；
快照退回一层 copy → 红；通知去掉夹逼 → 红；worker 去掉 `_guarded` → 红；`try` 起点后移 → 红。
复原后三个源文件与改动前逐字节一致（脚本自证）。

### 15.14 交付前必须先处理的一条（四道门都看不见它）：**引擎在运行时 import 的文件有 4 处还没进版本库**

`git ls-files --others --exclude-standard` 实测（不含 `_verify/_*`、`desktop/dist`、`generated`、`__pycache__`）：

| 未跟踪路径 | 被谁要求存在（这些引用方**都已跟踪**） | 漏提交后的现象 |
|---|---|---|
| `app/ai_tells.py` | `app/pipeline.py:36`、`app/knowledge.py:232` 的 `from .ai_tells import AITells` | 引擎**起不来**（`ModuleNotFoundError`），不是降级 |
| `app/packseed.py` | `app/server.py:68` 的 `from .packseed import …, seed_bundled_packs` | 同上，服务端 import 期就炸 |
| `packs/_template/`（20 个文件） | `app/packgen.py` 的 `TEMPLATE_PACK`，且 `tests/test_template_pack.py` 整份守它 | 回到"建包回落成电梯包、把电梯的 growth/voice/hooks 复制进每一个新行业包"那个原缺陷（§14 记过的形态） |
| `packs/elevator/ai_tells.yaml` | `Pack.ai_tells_data()` | 电梯包的"人味检测"静默变 None（报告 §14.6 抓过同一类"守卫本身在说谎"） |
| `desktop/engine-dialogs.js` | `desktop/main.js:75` 的 `require('./engine-dialogs')` | Electron 主进程起不来；而且"令牌不许进对话框"这条脱敏规则整块消失 |
| `tests/` 下 **17 个**测试文件（已跟踪的是 21 个） | —— | 本轮新增的**全部**守卫（假客户端签名、假 cfg 属性、`os.environ` 顶层写、忙态迁移表、编号项对账、README 报价同源…）在别人的机器上一条都不存在，"517 passed"这句话在新克隆上根本不成立 |

⚠ 这一条之所以危险，是因为 `git commit -a` 只会带上"已跟踪且已修改"的文件，**不会**带上未跟踪文件；
四道门又全部在**当前工作树**上跑，所以本地怎么测都是绿的。修法只有一句：
提交前先 `git add` 上面这些路径（`git status --porcelain` 里以 `??` 开头、且不在
`.gitignore` 命中的那些），再跑一次门。这不是 bug 修复能代劳的事，属会话主人的提交动作，
所以我没有代提交（也从未被要求提交）。

### 15.15 安装包按第 7 轮之后的树重打并逐项复验

`npm run dist` exit 0（未签名）。解包实测 `desktop/win-unpacked/resources/`：

- **带的是新代码**：`engine/app/llm.py` 里 `_clamped_wait` 命中 4 处、`engine/app/pipeline.py` 里
  `_guarded` 2 处、`engine/app/jobs.py` 里 `copy.deepcopy(self.steps)` 1 处 —— 都是 §15.13 那六条修复；
  渲染层落在 `engine/renderer/`（由引擎同源提供），那份 `index.html` 是
  `<div id="pi-checklist">`、那份 `settings.js` 带 `renderChecklist($("pi-checklist")…)`（§15.12 那一半）。
- **排除项仍成立**：`engine/**` 下 `private/` 文件 **0** 个、`engine/app` 下 `.pyc` **0** 个、
  没有 `config.yaml`、没有 `generated/`。
- **该带的都在**：`engine/packs/` 下 `_template` 与 `elevator` 两个包齐全，且
  `_template/ai_tells.yaml` 与 `elevator/ai_tells.yaml` 两份都在（新建的包开箱就有人味检测）。

仍缺的那一条动作没有变：**干净机器上的覆盖安装**（验"重装之后用户自建包还在"）——
它需要一台干净的虚拟机，不是本地能代劳的事；目前只有
`tests/test_server_hardening.py` 的 `--packs-dir` 用例在守着这条语义。

**本轮之后还开着的（都不属"bug 没修"，列清楚免得下一轮又去找）**：
§15.6 的五条产品/环境决定（方案 10 与 P3-42 的时序、`export_skill.py` 去留、人味分数的界面落点、
`recheck_rounds`/`retries` 这类有意的配置下移、干净机 NSIS），§15.14 的未跟踪文件提交，
以及 `styles.css` 那一摞（6 处状态标记对比度低于 AA、约 21 处 ≤2px 视觉补偿缺注释、
`.pg-more pre` 死规则）—— 那个文件主人正在改，按约定我没碰。

**唯一没闭环的一条（下一轮从这里接）**：§15.13 那六处修复本身还有一条只读复核 lane 在路上
（专打 `_readable_error` 的链遍历与循环异常图、`_clamped_wait` 与通知是否处处同源、
`copy.deepcopy(steps)` 在真实步骤负载上会不会炸、`_guarded` 里 `_fail` 自己再抛怎么办、
`start_packgen` 的单一归还点在并发同名交错下会不会偷别人的锁、`pgSlug` 与 Python `slugify`
的取值域差、以及 `subprocess` 编码在四种环境变量组合下是否都过）。
它证出什么就照 §15.13 那张表的写法补 §15.16；它全判干净，这一轮才真的收口。

**接手时先看这一条已落地的部分证据**（lane 的探针日志，`%TEMP%/r7/`，非仓库文件）：
§15.13 第 2 行那条 `subprocess` 编码修法，已在**四种环境组合**下各自独立跑过
`tests/test_smoke_mock.py`：无 `PYTHONIOENCODING` / `=gbk` / `PYTHONUTF8=1` / `=utf-8:replace`
→ 四份日志都是 `1 passed`（各 2.1 秒）。也就是"外层怎么设都不再把用例弄红"这一半已经站住了；
lane 还没判完的是另外五处（链遍历的循环异常图、通知同源、`deepcopy` 的真实负载、
`_guarded` 里 `_fail` 自己再抛、同名交错的单一归还点，以及 `pgSlug` 与 `slugify` 的取值域差）。
