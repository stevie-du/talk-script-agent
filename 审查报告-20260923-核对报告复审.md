# 核对报告复审 · 2026-09-23 14:50

> **对象**：`审查报告-20260923-问题修复状态核对.md`（下称「核对报告」）。
> **问题**：它写「58 条全部闭合」——这个总结论对不对？它逐行写下的状态，现在还成立吗？
>
> **方法**：不采信核对报告的任何结论，也不采信它自己的第二版重审。基线钉到
> **当前 HEAD `df1cc48`**（12:52），全部只读探针：git 历史与时间戳、逐条 grep、
> 实跑 pytest 全量、实跑 `verify:package`、读构建产物目录。
>
> **与前两轮的区别**：前两轮核的是「58 条问题修没修」；本轮核的是
> **「那份核对报告本身还准不准」**。

---

## 〇、结论摘要

| 类别 | 条数 | 要点 |
|---|---|---|
| 🔴 报告内部自相矛盾 | **5 处** | 不查代码、只读文件本身就能判：头部与表格互相打架 |
| 🟡 对当前 HEAD 已过期 | **5 处** | 写的时候对，HEAD 前进后没有回填 |
| 🔵 报告没有的新发现 | **2 处** | 最重要的一条：**打包状态回退，且比报告写的更差**。↻15:50：两条均已闭合（重打后全账认证通过；7z 缺口由 portable 7za 对账 1112 文件闭合），见文末结案段 |
| ✅ 复审确认无误 | — | 「58 条代码缺陷全部闭合」这个**总结论站得住** |

**一句话**：核对报告的总结论（58 条闭合）经复审成立；但报告里有十几行状态
描述已经和它自己对不上——头部说 P1-40 已修、表格说「全库零引用」；§4.3
全表 ❌ 而实际 6 项全收。交付侧比报告写的**更差**：11:45 曾有一次
`verify:package` 全账通过（日志在案、报告未记），当前
`dist/win-unpacked/resources/` **整个目录不存在**。
↻15:50 补：该欠账已再次闭合——15:19 重打落地后全账认证通过（含 portable 7za
对账 Setup 载荷 1112 个文件）、runtime-smoke 12/12，详见文末结案段；
交付侧现只剩 NSIS 真机安装一项。

---

## 一、报告自身的内部矛盾（无需查代码即可判）

| # | 矛盾 | 证据 |
|---|---|---|
| 1 | **P1-40 头部说已修、表格说没修** | §〇 摘要把 P1-40 列入「已修复 58」并点名 `b2622fd`（缓存命中可见）；但 §一 主表 P1-40 行仍是 🟡「`prompt_cache_hit_tokens` 全库零引用」，§7.1、§7.2-1、§八 同样写「仍部分修复 / 只能停在代码层」。同一份文件，两个答案 |
| 2 | **verify.js 断言数三个数互不相同** | §4.1 写 **336**；§7.1 与 §八 写 **354**；§一 P3-42 行写 **356/356**。今天实测 `grep -c "check("` = 358 − 1 定义行 = **357**。四个数字，没有一个对得上现状 |
| 3 | **§4.3 与 §7.3 对同一批事实给相反状态** | §4.3「UI 尾差」六行全 ❌「仍在」；§7.3 写「`c8ed684` 已收 4 处…药丸对比度留待拍板」。§7.3 较新，但 §4.3 没有被划掉或标注过期 |
| 4 | **§4.2 verify:package ❌ 是过期结论** | §4.2 写「今天仍报红：app 内容不同 1」。但当日日志 11:45 明确记录重打后**认证通过**（app 18/18、renderer 15/15、packs 30、asar 3/3、Setup 载荷同账）——报告的最终同步提交在 **12:45**，晚于这次通过，却没有回填 |
| 5 | **§5「顺带两条仍未动」有一条已经动了** | §5 末行列 `.pg-more pre` 死规则为「仍未动」；实际 `c8ed684`（10:00，早于报告定稿）已删除。同段的 `RUNTIME_PARAMS` 那条倒是真的 |

---

## 二、对当前 HEAD（`df1cc48`）已过期的行

| # | 报告原文（位置） | 当前实测 |
|---|---|---|
| 1 | P1-40「`prompt_cache_hit_tokens` **全库零引用**」（§一主表 / §7.1 / §7.2-1 / §八） | **已闭环到「机制在、量待真请求」**：`progress.js:77-80` 真读真显示「缓存命中/未命中 N token」；`tests/test_usage_keys_cross_layer.py:112-139` 三层断言（前端真读 / 引擎不改名 / usage 全量拷 int/float）。缺的只是真实模型请求的量——这本就是环境内闭合不了的那半 |
| 2 | §4.3 UI 尾差六项全 ❌ | **六项全闭合**：`#composer-body` 圆角 `--r-card`=12（`styles.css:2211`）、`.qp-more` 16（`ui.js:307`）、`.chev` 12×8（`ui.js:335`）、状态药丸对比度 `5c3a413` 加深 token 达标 + `df1cc48` 在 `verify.js:1218-1245` 固化「动态解析 token 比值 ≥4.5」守卫 |
| 3 | §4.3「`.pg-more pre` 死规则仍在 `styles.css:1466` / `:2333`」 | 两个 `.pg-more pre` 确已删；**但 `pre#pg-checklist`（`styles.css:2356`）仍是死规则**——`index.html:467` 的元素早已是 `<div id="pg-checklist">`，该选择器匹配不到任何节点。报告把两个选择器塞进同一行，读者会以为整行都清了 |
| 4 | §7.2-2 / §5「`RUNTIME_PARAMS` 死常量，`pipeline.py:161`」 | 结论仍成立（生产零消费、仅测试引用），**行号已漂到 173**。低危，但注释「由 `_run_generate` 附加」依旧与实现不符 |
| 5 | §〇「🔵 仍开着 2 类：NSIS 真机安装、UI 尾差」 | UI 尾差已闭。真正仍开着的是：NSIS 真机安装、Setup 载荷内容核对（本机无 7z）、**打包回退（见 §三-C1，报告完全没有）** |

---

## 三、报告没有的新发现

### C1 🔴 打包状态回退：`resources/` 没了，且比报告写的两种状态都差

时间线（全部有证据）：

| 时刻 | 事件 | 证据 |
|---|---|---|
| 11:28 | `npm run dist` 产出两份 exe + win-unpacked | `dist/` mtime |
| 11:45 | `verify:package` **全账通过**：app 18/18、renderer 15/15、packs 30、asar 3/3、Setup 载荷与 win-unpacked 同账；`runtime-smoke.js` 12/12 | 当日日志 |
| 12:29 / 12:30 | `b2622fd`（P1-40）/ `65d73f8`（P3-42）改动引擎与渲染层 → 包重新变旧 | git log |
| 12:50 | 发起重打，日志记「重打中…完成后需复跑 `verify-package.mjs`」——**此后再无完成记录** | 当日日志 |
| 现在 | `dist/` 内无 12:00 之后的任何文件变动；`win-unpacked/resources/` **整个目录不存在**；实跑 `verify:package`：**app 0/18、renderer 0/15、packs 30 个只在源码、缺 `resources/app.asar`**、两份 exe（11:28）早于最新源码（12:52） | 本轮实测 |

**能断言的**：12:50 那次重打没有完成（或被环境打断）；11:28 那次的
`resources/` 在其后消失，原因不明（`dist2…dist15` 是 **09-11** 的旧截断残留，
72–76K，与今天无关）。报告 §4.2 的两行——「安装包内容 ✅ 与报告一致」和
「verify:package ❌ app 内容不同 1」——**描述的都是已经不存在的状态**。

**不能断言的**：Setup.exe / portable exe 本身是 92MB 有效 PE（核对脚本确认），
载荷里有没有 app 无法确认——本机无 7z，脚本明说跳过这项。所以不能说
「装出来是空的」，能说的是「**对账基准没了，且没人知道重打为什么没完成**」。

---

**↻15:45 结案：重打已完成，全账认证通过（含 Setup 载荷逐文件对账）。**

- `npm run dist` 于 15:19 落地产物（两份 exe + win-unpacked，`resources/` 含
  `app.asar` + `engine/` 1039 文件——**环境并未持续拦截 .asar**）。
- `verify-package.mjs` **认证通过**：app 18/18、renderer 15/15、packs 30、
  asar 3/3 按路径取字节。
- `runtime-smoke.js --root …/resources/engine` **12/12**：.pyc 全 cpython-313、
  `/api/health` 通、提交生成作业 → done → sections=5 quota=261——包里真能跑。
- **12:50 那次为何没完成，已有（并发会话的）解释**：重打卡死约 2.5 小时
  （疑撞当时并发会话的 git repack），留下残缺 `win-unpacked`；清掉后 1 分 31 秒
  即重打成功。与本轮 15:05 的重打（14 分钟，含 win-unpacked 全量拷贝）是两次
  独立构建，最终状态由本复审带 `SEVENZ` 独立复跑确认。
- **「缺 7z」缺口已由并发会话闭合**：官方 portable 7za（`SEVENZ=E:/tmp/7za/
  extracted/7za.exe`），Setup 载荷与 win-unpacked **逐文件对账 1112 个**，
  本节 14:50 写的「载荷有没有 app 无法确认」不再成立。
- 剩余唯一交付欠账：干净机器上的安装-升级-卸载（NSIS 真机）。
- ⚠ **工具坑**：`npm run dist` 产物齐后 npm 进程挂了 28 分钟不退出
  （管道被孙进程占住）——**看 dist mtime + 跑校验，别干等进程退出**。

### C2 🟡 「缺 7z」这个缺口有现成的补法，报告没写

`verify-package.mjs` 支持 `SEVENZ` 环境变量指定 7z 路径。日志 11:35 记了
「需用户装 7-Zip 或设 SEVENZ」；报告只写「7z 缺失那条自记跳过」——
缺一个**可执行动作**，读者不知道这条靠自己就能补上。

---

## 四、复审确认无误的部分

- **代码基础未动**：`c8ed684`（报告第二版基线）之后全仓只有 7 笔提交，其中触及
  引擎/渲染层的仅 `b2622fd`、`65d73f8`、`5c3a413`、`df1cc48` 四笔，且工作区干净。
  其余 55 条「已修复」的代码证据自第二版重审后未被触碰——**「58 条全部闭合」的
  总结论不需要翻案**。
- **抽查四条**（防「报告说修了就信」）：P0-1/P0-2（`llm.py` finish_reason=length
  识别 + `EmptyContentError` 预算升级重试）、P3-42（`schemas.py:201`
  `alternatives`）、P1-34（`skill.yaml` ideas/growth/redlines 真声明真注入）、
  P1-40（见 §二-1）。全部与报告一致。
- **门禁**：pytest 全量实跑到 100%、进度行零 F（收集总数 **668**，报告记 667；
  汇总行被沙箱的 pytest 临时目录清理拦截截断，故以收集数 + 零 F 为准）；
  verify.js 静态计数 **357** 条断言。
- **NSIS 真机安装**：确实仍欠账，本机闭合不了——报告这条没错。

---

## 五、建议（按性价比排序）

1. **报告头部加一行基线声明**：「本报告状态截至 `c8ed684`（11:00）；其后 7 笔提交
   （`b2622fd`/`65d73f8`/`5c3a413`/`df1cc48` 等）未回填。」——本轮绝大多数过期行
   的总根因，一行解决大半。
2. **§4.2 补两笔**：① 11:45 认证通过（「安装包与源码不同步」曾闭合）；② 当前
   `resources/` 缺失 + 12:50 重打未完成——后者比原先的「exe 早于源码」更重，
   需要重打并查清重打为何没完成。
3. **删 `pre#pg-checklist` 死规则**（`styles.css:2356` 一行）——P3-50 清尾漏的一条。
4. **`RUNTIME_PARAMS`**（`pipeline.py:173`）：让注释如实（生产零消费）或删常量，
   二选一，别让注释继续宣称不存在的行为。
5. **verify.js 断言计数别写死**：报告里已出现 336/354/356 三个数，实际 357——
   按报告自己的说法「以当天 grep 计数为准」，那就每次回填或改为不写。

---

## 六、↻17:20 58 条从头复量（不再继承前两轮结论）

应用户「没有遗漏的bug了吧」的要求，把 58 条编号问题**逐条用一手证据复量**
（grep 文件:行 + 读关键区段），不引用前两轮任何结论。**58 条全部命中，
无一漏网**。证据按复审批次分组：

**§一 生成层（21 条全在）**
P0-1 `llm.py:8/263/287`（finish_reason=length 识别透传）· P0-2 `llm.py:174/286-322`
（EmptyContentError + RETRY_UPGRADE_CAP=20000 预算升级）· P0-3 `llm.py:231`
（chat_json max_tokens 形参）+ `pipeline.py:798/1146/1323`（select/分镜 4000、
重写 3000，write 不传）· P0-4 `llm.py:345-355`（指 config.yaml + 明说不在设置页 +
不影响本次调用，saw_truncated 可达）· P1-5 `llm.py:63/639` + `jobs.py:52`
（STREAM_READ_TIMEOUT_MULT=2.0 / CONNECT_TIMEOUT / JOB_BUDGET_SECONDS=1200）·
P1-6 `pipeline.py:367/396/461`（作业级抓 client）· P1-7 `pipeline.py:1495/1522` +
`progress.js:206`（构造即 begin_stream + 「等待模型首个 token…」）· P1-8
`pipeline.py:762/921/1007`（三条路径 +0.25）· P2-9 `jobs.js:33/229`
（REWRITE_TIMEOUT_MS=1200000 + 轻量轮询）· P2-10 `llm.py:372`（ping 512 + 空判连通）·
P2-11 `config.py:254/268`（_ENV_NUM_BOUNDS 夹逼）· P2-12 `llm.py:48-55`
（429 独立退避 + Retry-After 封顶 60）· P2-13 `pipeline.py:1505-1523`
（_stream_reset 经 on_attempt 每次尝试前清零）· P3-14（今日 f40af9a 已强化）·
P3-15 `progress.js:64-71/126`（思考/正文 token 徽章）· P3-16 `README.md:36` +
`server.py:917`（3~5 次 / 900ms）· P1-40 `skill.yaml:184`（feedback_block 在末尾）
+ `progress.js:77-80`（缓存命中可见）· P2-41 `pipeline.py:990-1050`（反馈带违规
处数 + 上一版全文）· P2-48 `pipeline.py:550-551/795-796/929-930/1016-1017`
（deadline + should_abort 四路透传）· 方案10 `skill.yaml:34` + `pipeline.py:468`
（stages.draft 声明 + 分支 + 老包照旧）· P3-42（今日已强化）· P2-1 `llm.py:311-330`
（重试支追加新消息）· P2-3 `jobs.py:474`（PAUSED_KEEP 仅存历史注释，不适用成立）。

**§二 包内容（23 条全在）**
P0-17 `knowledge.py:1143/1158/1183` + private yaml 示例已清空（products.yaml:11
注释在案）· P0-18 `pack.yaml:112-113`（files 只剩 private）+ `test_pack_orphan_files.py:39`
（HUMAN_ONLY 登记）· P0-19 `packgen.py:835`（_materialize 只留 private）· P1-20
`prompts.py:134` + skill.yaml `$cta`×4 · P1-21 `checker.py:288-301`（covered 区间
去重）+ `test_banwords_alignment.py:30`（「国家级、」前缀）· P1-22 `knowledge.py:565`
（audience 缺→空串不注入）· P1-23 `schemas.py:178-181`（mock 标记）· P2-24
`voice.md:40`（抱闸、不说刹车）· P2-25 growth.md 776 字符（≈报告 686 字，口径差）+
`knowledge.py:945`（hooks_slice）· P2-26 `duration.md:10-11`（单表封顶 5.0）+
`growth.md:20`（「这一段里每 10~15 秒」）· P2-27 `checker.py:512`
（max(10.0, 3.0/duration*100)）· P3-28/38 `topics.md:184`（语义描述，无悬空）·
P3-29 `schemas.py:111`（points max_length=12）· P1-30 `pipeline.py:501-513`
（final_passed 才画分镜 + storyboard_skip）· P2-31 `schemas.py:125-137`
（bgm/transition/shot_type/style）· P2-32 `progress.js:124-125`（note→.step-note
徽标）· P3-33 private/README.md:20（raw/ 不自动解析，两份都在）· P1-34
`test_pack_injection.py:108`（ideas 契约测试）· P1-35 `voice.md:23`（仅负面示例
一处）· P2-36 `platform.md:96-103`（4.5-5.0 + 声明以 pack.yaml 为准）· P2-37
`standards.md:59`（单点）+ `industry.md:87-90`（唯一出处）+ service.yaml:33
（cycle 空）+ `test_pack_consistency.py:41-45`（_texts 纳 yaml）· P2-39
`test_banwords_alignment.py:112` + banwords.yaml:111/116（6 词在表）。

**§三 packgen 与渲染（18 条全在）**
P1-43 `jobs.py:14/62/81` + `server.py:824`（packing 状态 + job_id）· P1-44
`packgen.py:487/563/574/613`（_pack_audit 三腿 + _placeholder_audit 复用 unfilled）·
P1-45 `packgen.py:624/707-711`（_match_by_name + 选项标题 + 核心术语空节）·
P1-44新 `packgen.py:624`（配不上留空骨架）· P2-46 `packgen.py:44/53/61`
（_creating + lock + claim_slug）· P2-47 `settings.js:28/807-809`（packgenFrom）·
P3-48 `verify.js:2215-2222`（closeDisabled===false）· P3-49 `settings.js:159/1156`
（lastCreatedPack 只刷新）· P3-50（今日 f40af9a 已补死规则清除）· P2-51
`packgen.py:169/188`（slugify 同口径）· P3-51 `settings.js:1274-1277`（piName）·
P2-6 export_skill.py 不存在 ✓ · P2-4 `result.js:274/370`（字数走后端）· P1-1
`ui.js:216-232`（data-key + state.genParams）· P1-2 `verify.js:1850-1862`
（双状态 data-id 断言）· P2-1 同 §一 · P2-3 同 §一。

**§四 交付（今日已全部闭合）**：verify:package 全账 + 7za 1112 对账、
runtime-smoke 12/12、UI 尾差 6 项全闭、人味分面板在；仅 NSIS 真机待真机。

**复量中发现的报告描述漂移 2 处**（均非代码退步）：
1. P0-3「draft=4000」——实际 draft 路径**刻意不设** 4000、用 write 全额预算，
   `_draft_once` docstring（`pipeline.py:908-913`）明说这是三处有意设计之一；
2. P2-25「growth 686 字」——实测 776 **字符**（≈686 汉字），口径差非错误。

---

*复审人：WorkBuddy（第三只眼，只读探针 + 全量 pytest 实跑 + 58 条从头复量）*
*基线：HEAD `df1cc48`（2026-09-23 12:52），工作区干净*
