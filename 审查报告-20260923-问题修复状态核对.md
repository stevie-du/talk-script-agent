# 问题修复状态核对 · 2026-09-23

> **范围**：核对 `审查报告-20260920-生成耗时与包全方位审查.md`（2629 行，§四~§十五）里
> 提出的全部编号问题（P0-1 … P3-51，共 **58 条** + 若干结构性条目），逐条判定今天的状态。
>
> **方法**：**不采信报告自己的「已修」结论**，直接读当前代码 + 只读探针 + 在
> HEAD 的干净检出上实跑 pytest。四条只读 lane 分别核验「生成层 / 包内容与注入 /
> packgen 与渲染层 / 交付与门禁」，主 agent 对其中 9 条争议项逐条复量。
>
> **核对基线**：HEAD = `f711dbe`（`A线收口：A-1 满分通道 + A-6 误伤收敛 + A-3 误改率回归集`）。
> ⚠ 核对期间仓库里有**另一条并发会话在飞改动**（7 个文件未提交），下文单独记。

---

## 〇、结论摘要

| 状态 | 条数 | 条目 |
|---|---|---|
| ✅ 已修复 | **53** | 见下文逐条表（含本轮收尾的 P0-4、P1-21、P0-17、P2-37、P2-39） |
| 🟡 部分修复 | **4** | P1-30、P1-34、P1-40、P2-32 |
| ❌ 未修复 | **1** | P3-42 |
| ⚪ 不适用 | **1** | P2-3 |
| 🔵 仍开着（非代码缺陷） | 3 类 | NSIS 真机安装、UI 尾差、人味分数界面落点 |

> **2026-09-23 后续**：核对报告写完之后，六条「可收的尾巴」已落码：
> `f21b652`（P0-4 文案、P1-21 词表对账前缀、P0-17 文档）、`52b0626`（P0-17 条目级
> 示例判定）、`89b2ab6`（P2-37 维保周期口径收敛）、`4d814d6`（P2-39 platform.md
> 词表对账）。见 §五。

**一句话**：报告里点名的 58 条**代码缺陷已全部收口**（除 P3-42 属产品决策）。
剩下 4 条部分修复是「决定不做」或「信号算了但没人接」：P1-40 缓存命中验证、
P2-32 占位符信号界面呈现、P1-30 不合格稿仍烧分镜、P1-34 ideas 格式。
交付侧：真机安装没验过、安装包与源码不同步、UI 尾差。

⚠ **最重要的一条前置事实**：核对进行中，工作区被并发会话改成了**非全绿**状态
（见 §四）。**HEAD 是绿的，工作区不是。**

---

## 一、生成层（llm / pipeline / jobs / config）

| 编号 | 状态 | 判断依据 | 残留风险 |
|---|---|---|---|
| P0-1 流式 `finish_reason` 写死 None | ✅ 已修复 | `llm.py` `_stream_once` 累计读 `choices[].finish_reason` 并返回 `(text, finish)`，`_complete` 流式分支透传；截断从此可识别 | — |
| P0-2 空内容逃逸不重试 | ✅ 已修复 | `llm.py` catch `EmptyContentError` → 预算 ×1.5（封顶 20000）重试；**到顶且非截断支 `break` 不再原样重发**（这条正是第 9 批复核补的洞）；耗尽仍抛原类型 | 默认 `max_retries=1`，只升一档 |
| P0-3 `max_tokens` 全阶段一刀切 | ✅ 已修复 | `chat_json` 真加了 `max_tokens` 形参并穿透到 payload；调用点 `pipeline.py` select/draft=4000、`rewrite_segment`=3000、storyboard=4000，write 不传走 cfg | — |
| P0-4 `max_tokens` 不在设置页但文案让用户去调 | ✅ **已修复**（`f21b652` 收尾） | **加回设置页仍按决定不做**（违背用户 2026-09-17 明确决策）。改动为：错误文案不再指「在设置里调大输出预算」，改为指 `config.yaml` 的 `llm.max_tokens` 并明说**不在设置页**；引擎写死的阶段预算（select/分镜 4000、重写 3000）则明说「改 `llm.max_tokens` 不影响本次调用」。见 §五 | — |
| P1-5 流式 180s 读超时误杀思考期 | ✅ 已修复 | `llm.py` `STREAM_READ_TIMEOUT_MULT=2.0`；`connect=20` 不再被 float 覆盖；另加整作业预算 `jobs.JOB_BUDGET_SECONDS` | — |
| P1-6 `reload_llm` 让在跑作业中途换模型 | ✅ 已修复 | `pipeline.py` `_run_generate` / `rewrite_segment` 入口一次抓 `client` 局部变量传到底 | `reload_llm` 仍在（只被 `server.py` 一处调用） |
| P1-7 首字节前静默几十秒 | ✅ 已修复 | `pipeline.py` `_delta_handler` 构造即 `begin_stream`；`jobs.py` 看 `stream_phase`；`progress.js` 显示「等待模型首个 token…」 | — |
| P1-8 reroll 提温只作用于 write | ✅ 已修复 | select / draft / write 三条路径都 +0.25 | — |
| P2-9 前端重写超时误报 + 无进度 | ✅ 已修复 | `jobs.js` `waitRewriteDone` 改轻量轮询（`api.job(jid)` 不含 result）+ 每轮 `renderProgress`。**超时实为 `REWRITE_TIMEOUT_MS = 1200000`（20 分钟）**，报告写的「180s→240s」已过期 | 报告数字与代码不一致，以代码为准 |
| P2-10 `ping()` 用 16 token 必然空 | ✅ 已修复 | `llm.py` ping 用 512，且空内容判「已连通」；文案用本次实际预算 | — |
| P2-11 环境变量数值不卡区间 | ✅ 已修复 | `config.py` `_ENV_NUM_BOUNDS` 越界先 `log.warning` 再夹逼，与 server 侧同口径 | — |
| P2-12 429 与 5xx 同档退避 | ✅ 已修复 | 429 独立退避 + 读 `Retry-After`（封顶 60s）；`test_llm_retry.py` 三条用例 | — |
| P2-13 缓冲不随重试清空 | ✅ 已修复 | `_stream_reset` 经 `on_attempt` 在每次尝试前调用 | — |
| P3-14 产物里查不到当时用的模型/预算 | ✅ 已修复（有一处死符号） | `PERSISTED_PARAMS` 已含 `model/max_tokens/temperature`，运行期注入。**但 `RUNTIME_PARAMS` 常量全仓零生产消费**（只有测试引用），注释仍宣称「由 `_run_generate` 附加」 | 死常量会误导下一位读者 |
| P3-15 usage / reasoning_tokens 被丢 | ✅ 已修复 | 两条路径都收 usage → 入 step；`progress.js` 渲染「思考 N token · 正文 M token」；`test_usage_keys_cross_layer.py` 跨层对账 | — |
| P3-16 README 调用次数 / 注释陈旧 | ✅ 已修复 | README 改「3~5 次」；`server.py` 注释改 900ms | — |
| P1-40 回炉轮前缀缓存整段失效 | 🟡 **部分修复** | 模板已挪：`$feedback_block` 现在在 user_template **末尾**（`packs/elevator/skill.yaml:209`，`_template` 同步）。**但「接 `usage.prompt_cache_hit_tokens` 验证」全库零引用** —— 只有重排，没有验证 | 重排是否真省钱**没有任何观测手段**，与项目「让不可见的失效变得可见」原则相悖 |
| P2-41 回炉反馈只给统计数字 | ✅ 已修复 | 反馈带违规句（含段落序号）+ 上一版全文；`test_refine_feedback.py` | — |
| P2-48 永不吐行的流 → 最坏 48 分钟 | ✅ 已修复 | `Job.deadline()` → `_capped_read()` 把读超时压到剩余预算（下限 1s）；`create_pack` 也透传 `should_abort` | — |
| 方案 10 合并 select+write | ✅ 已修复 | `stages.draft` 落地，`pipeline.py` 分支判断；**elevator 真声明 `draft`**，默认包不再单独调 select（非死代码，已核实调用点） | 老包仍走 select 路径（`test_plan_reuse` 反向守护） |
| P3-42 「换一版」一次出 2~3 版 | ❌ **未修复** | 全库 grep `candidates/variants` 无命中；报告 §15.42 自认「形状已定、本轮没有写码」 | 属产品决策（动 `result.json 存在 ⟺ done` 不变式），不是 bug |

---

## 二、行业包内容 / 知识注入 / 词表

| 编号 | 状态 | 判断依据 | 残留风险 |
|---|---|---|---|
| P0-17 脚手架假事实污染真实产物 | ✅ **已修复**（`f21b652` + `52b0626`） | ① `private/{products,cases,service}.yaml` 示例数字与 `source/verified` 值**确已清空** ✓ ③ 空溯源条目注入时打 `_未核实`（`knowledge.py`）✓ ② **条目级判定已落地**：`strip_empty` 先按整条判示例（显式 `_example: true` / `example:`，或任一值以「示例：」开头），命中即整条剔除 —— `{"name": "示例：X", "units": 12}` 不再漏进来；文档那半（`_template/private/README.md`）也已如实改写。变异检验：去掉该判定 → 两条示例用例红 | 无（`_is_example_entry` 只认「任一**值**以示例：开头」，示例伪装成**键**的场景不在内，属可接受边界） |
| P0-18 51.3% 知识文件从未进提示词 | ✅ 已修复（核心） | `pack.yaml` 的孤儿 `files.select/write/compliance/facts` 已删，只剩 `files.private`；`skill.yaml` 真声明 `redlines: compliance/industry.md` / `terms: knowledge/standards.md#核心术语` / `ideas` / `growth`，且 `knowledge.py`+`prompts.py` 真消费；`hooks_slice` / `file_slice`（找不到章节返回空串）都在 | 仍有 4 份 md 从不注入：`compliance/ad-law.md`、`compliance/platform.md`、`rules/duration.md`、`rules/output-template.md`。已在 `tests/test_pack_orphan_files.py` 的 `HUMAN_ONLY` **显式登记**（清单只作减法，接错线会红）。**风险：`platform.md` 的「AI 生成强制标注要求」到不了模型** |
| P0-19 packgen 复制同一份孤儿结构 | ✅ 已修复 | `packgen.py` `_materialize` 的 `pack.yaml.files` 只写 `private` | — |
| P1-20 `cta` 是界面摆设 | ✅ 已修复 | `prompts.py` `base_ctx` 提供 `cta`；skill.yaml 的 select / draft / write 模板均含 `$cta` | — |
| P1-21 词表该拦的零覆盖、拦住的误伤 | ✅ **已修复**（`f21b652` 收尾） | ① 跨表去重掩码已实现（`checker.py`）✓ ② 裸「第一」已删、「最X」族已补齐 ✓ ③ 对账前缀错 → 已改对（`"国家级、"`）并**补了「前缀必须真命中一行」的自检断言**（防再次静默空转）✓ | `platform.md` 高危词清单仍无对账（P2-39 那半） |
| P1-22 切片未命中 → 整份注入 | ✅ 已修复 | `knowledge.py` 未知 `audience` 返回**空串**（宁缺毋滥）；`param_audit` 报降级 | — |
| P1-23 产物没有 mock 标记 | ✅ 已修复 | `schemas.py` `ScriptResult.mock`；`pipeline.py` 取 `self.mock or client.mock` | — |
| P2-24 `voice.md` 自我否定 | ✅ 已修复 | `voice.md` 改「抱闸/闸…不说刹车」，与 `industry.md` 一致 | — |
| P2-25 信噪比（growth 太长 / hooks 全量注入） | ✅ 已修复 | `growth.md` 686 字；`hooks_slice` 1084 vs 整份 2104 | — |
| P2-26 配额自相矛盾 | ✅ 已修复 | `growth.md` 已改成「**这一段里**每 10~15 秒落一个新信息」+「字数以本提示词的字数配额行为准，别在知识文件里写死数字」；`duration.md` 单表封顶 5.0 | 靠措辞收敛，无测试钉住 |
| P2-27 ±10% 硬编码 | ✅ 已修复 | `checker.py` `max(10, 3.0/duration*100)` 自适应；`tolerance` 可由包配置覆盖，报告里带 `tolerance_pct` | — |
| P3-28 悬空引用 5 处 | ✅ 已修复 | `tests/test_dangling_refs.py` 跑绿；`topics.md:184` 改语义描述 | — |
| P3-29 `TopicPlan.points` 无上限 | ✅ 已修复（值不同） | `schemas.py:97` `max_length=12`，注释说明「6 会让 8 点档每次必炸」。**报告写的 6 已过期** | — |
| P1-30 `format=voice` 不减一次生成 | 🟡 **部分修复** | 分镜已拆独立阶段 ✓（`jobs.py` 有 `storyboarding` 状态）；`format != voice` 才跑 ✓；老包缺阶段 `storyboard_skip` 留痕 ✓。**但「校验通过才跑」不成立** —— `pipeline.py:482` 注释自认「不合格也会走到这里画分镜」 | 不合格稿仍多烧一次分镜调用 |
| P2-31 输出结构三套口径 + `scenes[].audio.bgm` 恒空 | ✅ 已修复 | `StoryboardShot` 已含 `bgm/transition/shot_type/style`；`_build_scenes` 读的键全有来源；mock 夹具填齐、末镜 `style` 故意留空；`output-template.md` 标注为「成品版式」并删掉第三份 JSON schema | — |
| P2-32 未填充占位符信号不显眼 | 🟡 **部分修复** | 后端有信号（`pipeline.py:648` 记 `tpl_<stage>` 步骤）。**渲染层零消费** —— `desktop/renderer/js/` grep `tpl_` / `未填充` 无命中，信号仍埋在日志页签 | 「信号算了但没人接」，与本项目核心风险同族 |
| P3-33 `raw/` 空承诺 | ✅ 已修复 | `private/README.md` 明示「引擎当前未实现自动解析」并给替代路径 | — |
| P1-34 `ideas.md` 永不注入且格式不可消费 | 🟡 **部分修复** | ideas.md **确已注入**（`select/draft.files.ideas`，探针 2292 字），钩子类型已收进 10 类域内。**但格式不是报告声称的 `angle/hook_type/hook_line/points/cta`**，而是「标题 — 钩子类型」+ 节级「受众默认 / 要点方向 / CTA 方向」 | 与 select 提示词契约是否严丝合缝，未测 |
| P1-35 `voice.md` 第二个矛盾 | ✅ 已修复 | 全仓仅 `voice.md:23` 一处提到「修一次大几万」，且是负面示例 | — |
| P2-36 语速三处对不上 | ✅ 已修复 | `duration.md` 单表封顶 5.0、折算式自洽；`platform.md` 改 4.5~5.0 并声明以 pack.yaml 为准 | — |
| P2-37 维保周期口径重复 | ✅ **已修复**（`89b2ab6`） | 收敛为 **standards.md 单点**：industry.md 顶部的「红线速查」（会注入 write）保留四类速查；§6（不注入）写明"规范口径以 standards.md#维保周期口径 为唯一出处，两者不许漂"；topics.md（select 注入）改为指向【行业红线】段；**service.yaml 的 cycle 清空**（它经 `files.private` 注入，实测是没人守的第三本抄件）。守卫增强：`_texts()` 纳入 `private/service.yaml` + 判定改成「本行+下一行」拼接（否则 yaml 折行形态永远漏）。变异（装回错误集合）→ 精准报红 | `ideas.md:46` 的选题标题保留原样（选题库标题只能改主题、不能改事实） |
| P3-38 悬空引用第 5 处 | ✅ 已修复 | 同 P3-28，`topics.md:184` 已改 | — |
| P2-39 `promote` 机制零使用、无对账 | ✅ **已修复**（`4d814d6`） | `banwords.yaml` 已收口文档、明说本包零使用 promote ✓；**platform.md 高危词对账已补**：`test_platform_highrisk_words_are_covered_by_banwords` 抽「- **标签**：」行词与词表（含 extra_hard）对账；实测抖音 6 个高危词（清仓处理/厂家倒闭/不计成本/库存告急/点击下单/马上抢购）原全漏，已补进 hard。变异：删 6 词 → 红。有意不对账「未实际使用的推荐/编造个人经历」（行为要求，拦"推荐"误伤）与平台通用红线（行为约束） | 无 |

---

## 三、packgen（新建行业包）与渲染层

| 编号 | 状态 | 判断依据 | 残留风险 |
|---|---|---|---|
| P1-43 packgen 不走 Job 管道 | ✅ 已修复 | 新增 job 状态 `packing` 进 `BUSY_STATES`；`/api/packs/create` 返回 `{job_id}`；前端起作业 + 0.9s 轮询；「取消」真发 cancel；`/api/history` 过滤 `kind != "generate"` | 占位表仅进程内，跨引擎实例仍可双花 token |
| P1-44 生成结果零机器校验 | ✅ 已修复 | `_pack_audit` 三条腿：`Pack(slug)` 加载（读不出先按 `_RELOAD_WAITS` 重读）+ `param_audit` 全量 + `_placeholder_audit`（复用生产 ctx + `unfilled`）；结果并进 `_checklist` | 体检只报不改 |
| P1-45 packgen 是注入架构缺陷的工厂 | ✅ 已修复 | `_materialize` 只留 `private`；`standards.md` 预留 `## 核心术语` 空节；`ideas/standards/industry.md` 均被模板 skill.yaml 引用注入 | — |
| P1-44 顺出的新包级 P0（新包切片全空） | ✅ 已修复 | `_materialize` 章节标题改成按**选项**写 `## {i}. {seg}`，内容用 `_match_by_name` 挂接，配不上留空骨架 | ⚠ 包含匹配依赖标题顺序：若某选项名是更早章节名的子串（如 `[「维保合同」,「维保」]`），短名会解析到前一节 |
| P2-46 并发无上限 + 同名 TOCTOU | ✅ 已修复 | `_creating` dict + lock，`claim_slug` 在花钱前占位，占不到即 409 | 仅进程内 |
| P2-47 「完成」/「取消」去向不对称 | ✅ 已修复 | 结果页双出口；`onPackDone` 按 `packgenFrom` 决定去向 | — |
| P3-48 生成期取消 → DOM 竞态 | ✅ 已修复 | 走 Job 后取消在生成期可用且真取消；verify.js 断言 `closeDisabled===false` | — |
| P3-49 关设置窗口不刷新 meta | ✅ 已修复 | `closeSettings` 在 `lastCreatedPack` 非空时刷新 meta（**只刷新不切包**） | — |
| P3-50 checklist 用 `<pre>` 不渲染 markdown | ✅ 已修复 | `index.html` 改成 `<div id="pg-checklist">`，`renderChecklist` 行级建 DOM | ⚠ **`styles.css:1466` 的 `.pg-more pre{…}` 与 `:2333` 的 `pre#pg-checklist` 已成死规则**（无匹配元素） |
| P2-51 slug 基于模型 display_name | ✅ 已修复 | `packgen.py` `slugify(industry)`；`preview_slug` 同口径 | — |
| P3-51 `undraftPack()` 读下拉而非当前包 | ✅ 已修复 | `openPackInfo` 记 `piName`，转正按它发请求；桩已改成按请求包名回显 | — |
| P2-6 `export_skill.py` 去留 | ✅ 已修复 | `app/export_skill.py` **已不存在**、`/export-skill` 端点已删、`api.js` 注释已同步（用户拍板「去」） | — |
| P2-4 `fmtText` 与 `count_chars` 两份口径 | ✅ 已修复 | 前端无字数正则；`result.js` 读后端 `check.segments[i].chars`；`fmtText` 只剩「渲染正文 + 打可跳转标记」 | — |
| P2-5 / P2-11 环境变量误配静默回落 | ✅ 已修复 | 见上表 P2-11 | — |
| P1-1(20260917) 同 id select | ✅ 已修复 | `ui.js` 改用 `data-key`，真值收 `state.genParams` | — |
| P1-2 生成期同屏双状态无断言 | ✅ 已修复 | `verify.js` 两条断言（生成中无重复 `data-id`；收尾后仍无重复且最多并入一条） | — |
| P2-1 解析重试共享 `messages` | ✅ 已修复 | 截断支追加新消息、空内容支升预算；`test_batch9_audit_findings.py` 断言两次 payload 不许逐字节相同 | — |
| P2-3 `PAUSED_KEEP=20` 丢弃无信号 | ⚪ 不适用 | 「分步确认」整体删除后该状态不存在（`jobs.py` 只剩一条历史注释） | — |

---

## 四、⚠ 交付侧与「当前工作区不是绿的」

### 4.1 门禁

| 项 | 状态 | 依据 |
|---|---|---|
| HEAD `f711dbe` 的 pytest | ✅ 绿 | **我在 `git worktree add --detach` 的干净检出上实跑：600 passed / 0 failed** |
| `verify.js` 断言数 | ✅ 336 条，提交态 == 工作树 | `grep -c "check("` = 337（减去 `function check` 定义行）= 336；`git status` 不含 `verify.js` |
| 那 6 条「配未提交 v6 CSS」的 UI 断言 | ✅ 已闭合 | CSS 随 `133e866` 落树，与断言同树（`ddc1963` 只改 README/报告） |
| **当前工作区** | ❌ **非全绿** | **我在工作区实跑 pytest 出现 3 条 FAILED**：`test_history_keeps_failed_and_deletes_clean`、`test_failed_job_visible_after_restart`、`test_disk_failure_still_leaves_a_durable_failed_record` |

> **3 条红的归因**：核对期间另一条并发会话在改 `app/knowledge.py`(+56)、
> `app/pipeline.py`(+51/−5)、`app/schemas.py`(+5)、`jobs.js`、`ui.js`、
> `packs/*/pack.yaml`，并新增未跟踪的 `tests/test_rewrite_scope.py` ——
> 内容是一轮 **A-2「回炉改写范围三档」** 的在飞实现。
> **HEAD 干净检出是 600/600 全绿**，所以这 3 条红**属于未提交的在飞改动**，
> 不是 HEAD 的缺陷。但它意味着：**此刻的工作区不能当作可交付状态**。

### 4.2 打包与真机

| 项 | 状态 | 依据 |
|---|---|---|
| 安装包内容 | ✅ 与报告一致 | `desktop/dist/win-unpacked/resources/engine`：`packs` **30** 个文件、`private` 命中 **0**、`config.yaml` **0**、`generated/` **0**、`app/**.pyc` **0** |
| `npm run verify:package` | ❌ 今天仍报红 | 只读实跑：`app 内容不同 1`（`ai_tells.py`）、`packs 内容不同 2`、**两份 exe 早于最新源码**；7z 缺失那条自记「跳过」 |
| NSIS 真机安装/卸载/重装 | ❌ **仍欠账** | README 有「五步实机清单」，但**本机做不了**（需干净机器/虚拟机）。这是唯一一条无法在本环境闭合的交付项 |
| `desktop/dist2 … dist15` | ⚪ 环境拦截，非 git 问题 | 15 个目录、合计 908K、`.gitignore` 覆盖、`git ls-files` 命中 0；需在资源管理器里手动删 |

### 4.3 UI 尾差（本机可判、且**今天仍在**）

| 项 | 状态 | 依据 |
|---|---|---|
| 状态标记对比度 < AA | ❌ 仍在 | 实算 `--ok` on `--ok-soft` ≈ **3.98**、`--warn` on `--warn-soft` ≈ **4.17**（均 < 4.5） |
| `#composer-body` 半径档位 | ❌ 仍在 | `styles.css:2188` 用 `--r-xl` = `var(--r-pop)` = **16**，而规范说卡片是 12 |
| `.qp-more` 字形 14 ≠ 16 | ❌ 仍在 | `ui.js:306` svg `width="14"` |
| `.chev` 11×8 | ❌ 仍在 | `ui.js:334` |
| ≤2px 视觉补偿缺注释 | ❓ 未能证实 | styles.css 里「补偿」类注释只有 **6 处**，无法核「约 21 处」这个数 |
| `.pg-more pre` 死规则 | ❌ 仍在 | `styles.css:1466` / `:2333`，元素已改成行级结构 |

### 4.4 人味分数

- **后端有**：`app/ai_tells.py` 有 `score_of()`、`checker.py` 把 `ai_tells` 写进校验结果、`pipeline.py` 读它进回炉反馈。
- **渲染层没有展示点**（grep `ai_tells` / `人味分数` 在 `desktop/renderer/` 无命中）。
- `docs/需求方案-去AI味与热点情报.md` 存在，**待确认 9 条**（报告里写的是 8 条，已漂）。
- 判定：🔵 属另一条产品 track，**不是未修的 bug**。

---

## 五、需要立刻处理的三件事 —— 状态更新

| # | 条目 | 状态 | 说明 |
|---|---|---|---|
| 1 | `packs/_template/private/README.md` 与代码相反（P0-17 文档） | ✅ **已修**（`f21b652`） | 原文「这两个字段引擎不校验、也不因为空着而降级」→ 改成如实描述「声明了 `source`/`verified` 却留空会打 `_未核实` 标记」。**机制那一半（条目级判定）已随 `52b0626` 补上** —— P0-17 至此整体闭合 |
| 2 | `llm.py` 的「在设置里调大输出预算」（P0-4 残留） | ✅ **已修**（`f21b652`） | 改为指 `config.yaml` 的 `llm.max_tokens` 并明说**不在设置页**；若本次是引擎写死的阶段预算，则明说「改 `llm.max_tokens` 不影响本次调用」。⚠ 修的过程中发现那半句原计划挂在 `truncated` 上会是**死文案**（末轮若截断，`EmptyContentError` 先重抛，走不到这里），改用 `saw_truncated`（整条重试链）才真可达 |
| 3 | `test_banwords_alignment.py` 的前缀 `"级、"`（P1-21 残留） | ✅ **已修**（`f21b652`） | 前缀改对为 `"国家级、"`，并**补一条 `test_every_declared_prefix_actually_matches_a_line`**：每个声明的前缀必须真命中过至少一行 —— 否则这类「前缀写错 → 整族零覆盖而测试常年绿」还会再来一次 |

**三处的变异检验**（都精准报红）：
- 前缀改回 `"级、"` → 新断言红；
- 删掉 soft 里的 `王牌` / `世界级` → 对账红。
  ⚠ `国家级` **不能**当变异探针：它被 hard 的 `国家级检测认证` 子串覆盖，删了也不红
  （第一次变异就踩了这个，换词才验出来）；
- 把「或在设置里调大输出预算」装回去 → `test_a_structurally_bad_model_output_*
  never_quotes_internal_class_names` 红。
  ⚠ 顺带：那条测试**原来钉的就是 `"调大输出预算"` 这个子串** —— 修掉误导反而把它
  弄红，属于「钉文案 = 把缺陷钉成契约」。已改成断性质：① 至少有一条能照着做的动作；
  ② 不许再指「设置里」。

顺带两条低风险仍未动：`RUNTIME_PARAMS` 死常量（P3-14）、`.pg-more pre` 死规则（P3-50）。

---

## 六、本次核对的局限（不假装验过）

- **没发任何真实模型请求**，也没重打安装包。凡涉及上游行为（前缀缓存是否真命中、
  真实 `finish_reason` 是否回 `length`、思考是否真吃满预算）的结论，只能停在代码层。
- **`verify.js` / `e2e-live` 没有复跑**（需 GUI/Electron 环境）。「336 条断言」是
  静态计数，不是跑出来的绿。
- **工作区在核对期间被并发会话改动**，上文「工作区」相关结论按我观测到的时刻记录，
  可能已经漂移；**HEAD 相关结论以 `f711dbe` 为准**。
- 报告里的历史统计（169/175 轮假命中、2328→774 字、86/87 份夹具）在只读条件下
  **无法复现**，我只能核实「今天的机制在不在」，不能核实「当时的数字对不对」。
- UI 尾差里「约 21 处 ≤2px 补偿注释」这一条**无法证实也无法证伪**。

---

*核对人：WorkBuddy（四条只读 lane + 主 agent 复量 9 条争议项）*
*基线：HEAD `f711dbe`；工作区含并发会话未提交的 A-2 在飞改动*
