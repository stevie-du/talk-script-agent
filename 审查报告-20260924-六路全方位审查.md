# 审查报告 · 2026-09-24 · 六路全方位审查

> **审查对象**：`E:\Workspace\video\talk-script-agent`，HEAD `3ed3548` + 工作区未提交改动。
> **方法**：六路并发只读审查（后端引擎 / 主进程与打包 / 渲染层 / 自动更新新线 / 测试与验证体系 / 文档与资产），
> 主线独立复核高价值发现并实跑回归基线。**不采信任何既有报告（含 `审查报告-20260920/20260923`）的结论**，
> 全部按当前 HEAD 与工作区现状复量。

---

## 0. 本轮怎么做、边界在哪

### 0.1 起点状态（读到即记）

| 项 | 值 |
|---|---|
| HEAD | `3ed3548`（"五路审查 P0 五条：变异检验登记 + XSS 分组 + 两处工具链修复"） |
| 分支 | `develop` |
| 工作区 | **35 个未提交改动**（26 个 `M` + 9 个 `??`） |
| 未提交改动的主体 | 一整条**「自动更新」新线**（`updater-core.js` / `publish-update.mjs` / `serve-update.mjs` / `about.js` / `docs/自动更新方案.md` 43KB）+ 一批 review 修复 |

⚠ **本仓库长期有并发会话在提交**。本报告的所有行号是 2026-09-24 15:4x 的快照，几分钟内可能漂移。
判定结论前请先 `git rev-parse HEAD` 重取。

### 0.2 基线实测（本轮跑的，不是抄的）

```
node _verify/verify.js                        → 368/368 通过（4 分 01 秒，GUI 实跑）
.venv/Scripts/python.exe -m pytest            → 717 用例 / 50 个测试文件，进度行零 F（18 分 27 秒）
.venv/Scripts/python.exe -m pytest --collect-only → 717（与进度行点数吻合）
```

> ⚠ 用**仓库的 `.venv/Scripts/python.exe`**。托管 3.13.12 没装 PyYAML、系统 3.14.3 没装 pytest —— 两者都给假信号。
> ⚠ pytest 汇总行会被沙箱的临时目录清理拦截截断，以「进度行零 F + `--collect-only` 计数」为准。

> ⚠ README 第 144-149 行写「全量 367/367」，实测 **368**。README 自己声明过这个数会漂移，
> 但仍属「同一信息两份表示」，建议改成「以 `node _verify/verify.js` 输出为准」而不写死数字。

### 0.3 本轮没做的事（别把「没报」当「没问题」）

- **没跑** `_verify/mutate.py`（变异检验套件）—— 耗时且与并发会话抢锁。测试路的结论基于**静态读条目定义**，不是实跑。
- **没跑** `npm run dist`（打包）—— 产物校验（`verify:package`）本轮未复跑。
- **没做** NSIS 真机安装/卸载/重装（需干净机器，这是长期交付欠账，见 §5）。
- **没做** 自动更新的端到端冒烟（需真实更新服务器与两个版本）。

---

## 1. 结论速览

| 级别 | 条数 | 一句话 |
|---|---|---|
| **P0** | **0** | 六路均未发现崩溃 / 数据丢失 / 可利用安全漏洞。上一轮 58 条修复的成果守住了。 |
| **P1** | **7** | 1 条会静默丢用户数据，2 条是「信号算了没人接」的经典形态，3 条文档与实现实质不符，1 条验收凭证缺失。 |
| **P2** | **~40** | 集中在「静默吞错」「口径两份」「工具链少一本账」。 |
| **P3** | **~30** | 一致性、过时注释、失效行号、重复表示。 |

**整体判断**：这不是一个需要推倒重来的代码库。安全面（zip 导入、路径穿越、IPC 面、CSP、XSS）经系统性核对**站得住**；
`_verify` 这套自建断言网的质量高于同类项目（368 条断言里只有 2 处读源码文本，其余全走 CDP 量真实 DOM）。
问题集中在**「文档说做了、代码没做到」**和**「兜底口径抄了两份后悄悄漂移」**这两类 —— 恰好都是本项目最在意的病。

---

## 2. P1 清单（7 条）

### P1-1　台账损坏时，导入会静默覆盖用户改过的内置包 —— 用户改动不可逆丢失

**位置**：`app/packimport.py:175-193`（`_user_modified`）+ `:299`（调用方）

**现象**：`_user_modified` 在台账 `.packseed.json` 读失败时 `return None`（:189-190），
docstring 写着「当作"没记录"—— **packseed 同款兜底：不动用户的东西**」。
但调用方写的是 `if _user_modified(...) is True:`（:299）—— `None` 不是 `True`，于是**不拦截**，
直接走 `_swap_in`（:313）覆盖，并在 `:216` `rmtree(backup)` 删掉备份。

**为什么是问题**：同一个「没记录」语义，两处给出**相反动作**：
- `app/packseed.py:132-145`：台账坏了 → `rec_all = {}` → **不覆盖任何已有包**，且 `log.warning` 明说"本次不会覆盖任何已有包"。
- `app/packimport.py:189-190` + `:299`：台账坏了 → `None` → **覆盖**。

这同时踩中两条铁律：③「同一个兜底口径抄了两份」（且两份语义相反）、⑤「兜底值冒充用户输入」。
更要命的是 docstring **明确声称**「不动用户的东西」—— 是说谎的注释，比没有注释更坏。

**证据**：现有测试 `tests/test_pack_import.py:230/243/259` 只覆盖「台账正常 / 无记录」三条路径，
**没有「台账损坏」用例** —— 这个分支从未被验证过。

**建议**：`_user_modified` 读失败时**不要**返回 `None` 冒充"没记录"，要么抛错中止导入、
要么与 `packseed` 对齐成"视为已修改 → 拒绝覆盖"；同时补「台账损坏 + 导入同名包」的断言（含变异检验：把实现改回去必须报红）。

---

### P1-2　`/api/intel/today` 的 `errors` 前端从不消费 —— 「没抓过」与「抓了但文件坏了」长得一样

**位置**：`desktop/renderer/js/topics.js:254-268`（只取 `groups/items/fetched_at/stale/ignored`）vs `app/intel.py:668-669`、`:680`、`:682`

**现象**：后端 docstring 明写 `errors` 存在的**唯一目的**就是让界面把「没抓过」与「抓了但 `latest.json` 读不出来」分开显示
（`:668-669`：「`errors` 里带上读失败的原因，界面才能把"没抓过"与"抓了但文件坏了"分开」）。
前端**从不读**它 —— `topics.js:261` 里 `errors: {}` 只出现在本地兜底默认对象里。

**后果**：`latest.json` 损坏时，用户看到的是「今天这个池子是空的」，源表里各源仍是「已接入 0」。
后端辛苦算出的诊断信号，到不了消费者手里（铁律②）。

**建议**：`topics.js` 渲染 `errors` —— 至少把 `_read` 那一类做成源表上方的显式告警行。

---

### P1-3　`param_audit` 的 `style` / `ai_tells` / `intel_sources` 三个键算了没人接

**位置**：`app/knowledge.py:561-696`（产出）vs `desktop/renderer/js/ui.js:23`、`:297-313`、`:265-284`

**现象**：`param_audit` 产出 7 个键：`audience` / `segment` / `duration` / `platform` / `style` / `ai_tells` / `intel_sources`。
渲染层的**唯一消费点**是 `renderQuickParams`（`ui.js:297-313`），且它只遍历
`TOOLBAR_KEYS = ["segment", "audience", "duration", "platform"]`（`ui.js:23`）。
设置页那份「全量参数总账」走 `paramSelect`（`ui.js:265-284`），**完全不读 `_audit`**（`settings.js` 里 `param_audit` 零命中）。

于是 `app/knowledge.py:618`（ai_tells 结构坏）、`:626`（intel_sources 未知 id / label 重复）、
`:673`（style 的语速/钩子库降级）三处 note **永不显示**。

**为什么是问题**：这正是 `knowledge.py:561-573` docstring 自己描述的失败场景 ——
「用户加了个快手以为照常查平台红线，实际平台差异化校验整个不生效」。
`_verify/verify.js:5469` 甚至自己写着「「信号算了但没人接」是本项目最容易犯的错（param_audit 那一类）」。

**建议**：把 audit 挂到设置页参数卡（`paramSelect` 也吃 `_audit`），或明确断言这三键「有意不展示」并删掉产出。

---

### P1-4　自动更新：文档 §10 声称的变异检验**没有登记进 `_verify/mutate.py`**

**位置**：`docs/自动更新方案.md:374`（"把 `resolveFeedUrl` 的空值分支删掉 / `shouldCheckNow` 恒真 → 断言必须红"）、`:437`（路线图标"绿"）vs `_verify/mutate.py` 的 `MUTATIONS`

**现象**：grep `_verify/mutate.py`，**没有任何** `resolveFeedUrl` / `shouldCheckNow` / `classifyError` / `updater-core` 条目。

**为什么是问题**：本项目的验收口径就是「修好了必须可证伪 = 变异检验」。
`updater-core.test.js` 自身确实有会红的断言（如 `:24`），所以**不是假守卫**，而是**验收凭证缺失** ——
「绿」是自述，不是实测。文档说做了、套件里没有。

**建议**：把文档 §10 列的那两条变异点登记进 `MUTATIONS`，跑一次确认真红。

---

### P1-5　`docs/知识库内容规范.md` 说 `ideas.md` 不注入 —— 实际在注入

**位置**：`docs/知识库内容规范.md:66-69` vs `packs/elevator/skill.yaml:86`、`:166`

**现象**：文档写「⚠ 这份文件**当前不注入模型**（选题阶段只吃 `hooks.md` + `topics.md` 切片）」，
而 `skill.yaml:86`（`select.files.ideas: knowledge/ideas.md#选题库`）与 `:166`（draft.files）**都在注入**。
同仓库 `docs/知识库与技能配置指南.md:147` 还明确写「ideas.md 只注 `## 选题库` 那一节，进选题阶段」。

→ 三处口径冲突，规范文档过时。包作者按它理解会误判注入内容。

---

### P1-6　`docs/技能包系统方案.md` §1 现状盘点整表过期

**位置**：`docs/技能包系统方案.md:37-40`

**现象**：把 zip 导入 / 来源区分 / 卸载 / 第三方文档全标 ❌「待建」。
实际 `app/server.py:856`（`POST /api/packs/import`）、`:881`（`DELETE /api/packs/{name}`）、
`app/packimport.py`、`app/packcheck.py`、`docs/技能包制作指南.md` 均已存在，
`README.md:223-238` 已宣布 2026-09-23 落地。

**为什么是问题**：这是一份**方案文档的现状表**，读者会据此判断"还要做什么"。
过期状态表会让人重做已做完的事，或以为能力不存在。

---

### P1-7　`docs/知识库与技能配置指南.md` 的 pack.yaml 字段表漏列真键，且断言"不要加键"

**位置**：`docs/知识库与技能配置指南.md:103`（列"引擎真读的字段"并断言「**除此之外不要加键**」）

**现象**：漏了三个引擎**真读**的键：
- `intel_sources`（`app/intel.py:159`、`packs/elevator/pack.yaml:131`）
- `rewrite_scope`（`app/knowledge.py:537`、`pack.yaml:48`）
- `duration_tolerance_pct`（`app/checker.py:56`）

同文档 `:228` 自己又要求写 `duration_tolerance_pct` → **自相矛盾**。

**后果**：包作者照这份表做，会漏配情报源；而"不要加键"的措辞还会让人不敢用 `rewrite_scope`。

---

## 3. P2 清单（按主题分组，~40 条）

### 3.1 静默吞错 / 失败不可见（最集中的一类）

| # | 位置 | 现象 | 为什么是问题 |
|---|---|---|---|
| P2-1 | `desktop/main.js:355`、`:411-506`、`desktop/renderer/js/about.js:27-30`、`:154-163` | **idle 常驻态不可达**：`updaterStatus = {state:'idle'}` 只是初始值，`initUpdater` 启用分支**从不推送** idle；而 `refreshAbout` 一开面板就调 `u.check()` → 主进程 `runCheck` 立刻推 `checking` 并重置 6h 时钟 | 文档 §4.2/§14#12 要求面板**常驻**显示「自动更新已开启 · 每 6 小时检查一次」。实现使该文案永不出现，且每次开面板都发真实请求 + 把 6h 自动检查时钟往后推。**典型「文档说做了、代码没做」** |
| P2-2 | `desktop/renderer/js/about.js:162` | `Promise.resolve(u.check()).then(renderUpd).catch(() => {})` 空 catch | 面板停在 HTML 默认「尚未检查更新」—— 一个**看起来正常、实际没接上**的状态。同文件按钮路径（`:119-121`）刻意不吞错并 toast，两处口径不一致 |
| P2-3 | `desktop/renderer/js/sessions.js:119` | `catch (_) { /* 引擎不可达时保留上一次列表 */ }` | 刷新失败时列表停在上一次内容，界面无任何「刷新失败」信号，用户把旧列表当最新 |
| P2-4 | `desktop/renderer/js/settings.js:146` | `preloadSettings().catch(() => {})` | `/api/config` 失败时设置页显示空白表单，无错误提示（`renderConfigError` 只由 `config_error` 字段驱动，网络失败不走它） |
| P2-5 | `desktop/renderer/js/jobs.js:431` | `catch (_) { st = "cancelled"; }` 后播「已停止本次生成」 | 同函数注释（`:430`）自称「猜是不诚实的」，这里仍在猜；若后端其实还在跑，会出现「停止后又多出一条记录」 |
| P2-6 | `desktop/renderer/js/jobs.js:436` vs `:438` | 取产物失败 → `renderVersion` 只画版本条（`:297-302`）；同一路径却 toast「它已经跑完了 —— 产物已保存」 | 屏幕与承诺相反 |
| P2-7 | `app/knowledge.py:65-81` | `read_text_cached` 对 `UnicodeDecodeError` 静默返回 `""`，且**不写日志**（对比 `read_yaml_cached` 会 `log.warning`） | 中文 Windows 上知识 `.md` 存成 GBK 即触发；报出来的是"占位符未填充"，真正的根因（编码）无处可查 —— 误导性诊断 |
| P2-8 | `app/intel.py:686-695` | `load_ignored` 读坏静默返回 `{}`，无日志、无 `errors`（对比 `load_latest:678-680` 会把原因放进 `errors`）；紧接着 `add_ignored`（`:705-709`）以它为底改写整份文件 | **历史忽略记录被静默清空**，只剩新加的那一条 |

### 3.2 口径两份 / 漂移

| # | 位置 | 现象 | 为什么是问题 |
|---|---|---|---|
| P2-9 | `desktop/renderer/js/topics.js:50` vs `app/intel.py:821` vs `:408` | 前端 `itemKey = guid \|\| url \|\| title:...`，`dedup`（`:408`）同款，但 `today()` 的**忽略过滤**（`:821`）用的是 `guid \|\| title` —— **缺 url 这一档** | 只有 url（无 guid）的条目（政策库一类，`:403` 注释自己点了名）：点「忽略」后下一轮**原样回来**。前端注释还写着「必须与后端用同一个键」 |
| P2-10 | `app/jobs.py:431-432` | `transition_if_room` 硬编码 `j.state in BUSY_STATES`，**完全不按 kind 过滤**，与其 docstring「与 `add_if_room` 同款收口」（`:418`）矛盾（`add_if_room` 用 `same_quota_family`，`:411-413`） | 仍停在 `queued` 的情报作业会被算进模型额度 → 重写被误判"已达上限"返回 409，正是 `:75-77` 注释声明"不会发生"的事 |
| P2-11 | `desktop/renderer/js/result.js:426` vs `:174` | tab 用 `score ?? 100`（→"ok"绿），面板用 `Number(a.score ?? 0)`（→"0/100"红） | 同一字段两处兜底不同 |
| P2-12 | `desktop/renderer/js/result.js:625` vs `:53` | `ch.passed` 一处真值判断、一处 `?? true` | 字段缺失时头部"✓ 合格"与合规页"❌"相反 |
| P2-13 | `tests/test_env_bounds.py:24-29,60` | `SERVER_BOUNDS` 是**手抄副本**，`test_bounds_table_matches_server` 只比 `config._ENV_NUM_BOUNDS == SERVER_BOUNDS`，**从未读 `app/server.py`** | 改 `server.NUMERIC_BOUNDS` **不报红** → 环境变量夹逼与设置页夹逼可静默漂移。而测试名与 `config.py:267` 注释都声称"与 server 比对钉住"。**假守卫** |

### 3.3 工具链「少一本账」

| # | 位置 | 现象 | 为什么是问题 |
|---|---|---|---|
| P2-14 | `desktop/scripts/verify-package.mjs:72-79,170-194` | A/B/C 三账只对账 `app`/`renderer`/`packs`，**完全不含内嵌运行时 `engine/py`**；`sourceNewest()` 的目录也不含 `vendor/py` | 若 `extraResources` 里 `vendor/py` 那条被删/改名，构建仍成功、"认证通过"仍照打 —— 而它是打包版能跑的唯一依据。少看一本账 = 宣称得比证据多 |
| P2-15 | `desktop/scripts/verify-package.mjs:175-179` | 硬编码清单只列 `main.js/preload.js/engine-path.js/engine-dialogs.js`，**漏了本轮新增的 `updater-core.js`**（`build.files` 已含） | 同一信息两份表示（`asarFiles` 已改成从 `pkg.build.files` 派生，这个 `files` 数组没跟）。判据 G 对它失效 |
| P2-16 | `desktop/main.js:59-68,632-649,257-270,615` | `findFreePort()` 探测后 `close()`，到 python 真正 bind 之间有数百毫秒；`waitHealth` 只判 `/api/health` 是否 `ok`，**不验证应答者是不是我们的引擎**（该端点刻意免鉴权） | 本机进程抢到该端口并回 200 → `loadURL('http://127.0.0.1:PORT/?token=<令牌>')`，令牌进攻击者请求行。`engine-path.js:48` 大段论证的正是「令牌不能落到别的本机进程手里」，此处与设计目标相悖 |
| P2-17 | `desktop/main.js:598-601` | `setWindowOpenHandler` 只管新窗口；**全仓无 `will-navigate`** | 当前窗口导航（`location.href=` / 普通 `<a>`）可把窗口带走；CSP 的 `form-action 'none'` 拦不住 `location.assign`。页面被导航到远端源后 preload 仍注入 `window.talkscript`，用户丢失应用界面 |
| P2-18 | `desktop/updater-core.js:38-45` | `resolveFeedUrl` 只 trim + 补尾斜杠，**不校验 scheme**；`http://` 被原样接受并启用，无任何用户可见警示 | 文档 §8/§12.1 称「**HTTPS 是底线**」，代码层面对降级到 HTTP 完全沉默。README 的本地冒烟示例恰是 `http://127.0.0.1:9000/`，用户很可能带进生产 |

### 3.4 后端信号与记账

| # | 位置 | 现象 | 为什么是问题 |
|---|---|---|---|
| P2-19 | `app/knowledge.py:181-259` | `pack_info` 加载期判死 `pack.yaml`/`banwords.yaml`/`duration_tolerance_pct`/`rewrite_scope`，**不含 `skill.yaml`** | `/api/meta` 把 `skill.yaml` 写坏的包列为**无 `pack_error` 的健康包**，而 `/api/generate` 走 `Pack()` 会抛 `PackBrokenError` → 列表与生成结论相反。列表本应承担的"标出坏包"职责在此缺口失效 |
| P2-20 | `app/llm.py:494-503,601-606` + `:272-279` | `_stream_once`/`_complete` 用 `usage.update({...})`；`chat_json` 的重试循环把**同一个** `usage` 字典传给每次尝试 | 第二次尝试**覆盖**第一次。落进步骤的 usage 只反映最后一次尝试，被丢弃那次真实消耗的 token **静默不计**，用户按显示数字对账会少 |
| P2-21 | `app/intel.py:516-524,531` | `score_topics` 写 5 个字段（`fact_density`/`demand_new`/`supply_n`/`total_n`/`days_ago`）并随 `latest.json` 外发，**全仓库无消费方**（前端只用 `D`/`S`/`E`/`opportunity`） | `fact_density` 的自注释（`:514-515`）声称它是"两条线的共用信号"，实为死信号（铁律②） |

### 3.5 测试守卫薄弱

| # | 位置 | 现象 |
|---|---|---|
| P2-22 | `_verify/verify.js:1997-2013` | 小标记一族断言：`sels` 八个类里 `if (!el) return` 跳过缺失者，判据只有 `badgeFam.n === 1`，**没有「找到 ≥N 个」的前提守卫**（同页 `chipTwin` 有 `!chipTwin.missing`）。7 个类从页面消失时 `n` 仍为 1 → 绿 |
| P2-23 | `_verify/sections/topics.js:112-113` | `dashForUncomputable: /—/.test(on.textContent)` 只证明选题区文本里出现过一次破折号，**不能**证明 D/S/E 那一格是 `—` 而不是 `0`。这是该性质在整网里的唯一守卫 |
| P2-24 | `tests/test_server_hardening.py:971` | `assert body["size"] == len(body["text"].encode("utf-8")) or body["size"] > 0` —— 后半句对非空文件恒真，整条退化成「文件非空」 |
| P2-25 | `tests/test_intel.py:176` | `assert "抖音热榜" in " ".join(out["errors"].values()) or out["errors"]` —— `errors` 非空即过，前半句可永远为假 |
| P2-26 | `app/packimport.py:230-232` | `MAX_ZIP_BYTES`（zip 总大小 20MB）在 `tests/` 中**零引用**（`MAX_ENTRIES`、`MAX_FILE_BYTES` 都有）。三重限制少守一重 |
| P2-27 | `app/server.py:1338-1375`、`:1039-1050` | `POST /api/config/test`（分支多且用户可直达：`model_id` 覆盖、`check_base_url` 400、无 Key 400、warnings）、`POST /api/history/{jid}/reveal` + `store.reveal_target` 在 `tests/` 中**零引用** |
| P2-28 | `tests/test_intel.py:475,499,521` | 三处直接 `pl.start_intel_fetch(...)` 不传 `http=_fake_http()`，**发真实网络请求**（`:194` 注释自证 demand_terms 每 seed 发一次真请求）。离线/沙箱下慢或抖动，后台线程可能跨用例存活 |

### 3.6 文档与资产

| # | 位置 | 现象 |
|---|---|---|
| P2-29 | `README.md:165`、`:293` | 写产物 `0.2.0.exe` / `0.2.1.exe`，`desktop/package.json:4` 实际 `0.2.2` |
| P2-30 | `README.md:29` | 架构图列 `watchdog 父进程看门狗` 为 app 模块，**`app/watchdog.py` 不存在**（实现在 `app/server.py:1392`/`:1449`） |
| P2-31 | `README.md:577`、`:554` | 行号失效：白名单在 `server.py:621/720`（非 `:271`），`/api/packs/{name}` 在 `:753`（非 `:371-394`） |
| P2-32 | `docs/UI视觉规范.md:22` | 规定控件高度 `--h-lg 40`，`styles.css` **无 `--h-lg` 定义**（只有 `--h-sm:28`、`--h-md:32`） |
| P2-33 | `docs/UI视觉规范.md:90` vs `:24`/`:142` | hero 图标一处写 32、两处写 40；`styles.css:2167` 实际 40 → **同文档内部矛盾** |
| P2-34 | `docs/UI视觉规范.md:72-73` | 层次表给 `--e2 .04` / `--e3 .10`，`styles.css:141,146` 实际 `.03` / `.08` |
| P2-35 | `docs/UI视觉规范.md:128-131` | 称 verify.js 有「13 处 `rgb(...)` 字面量比对」，实测仅 10 处 `rgb(`、12 处 `resolvedToken`（断言已迁移）。`styles.css:40` 注释仍写 13，`:110` 注释已说改用 `resolvedToken` —— **CSS 内两条注释互斥** |
| P2-36 | `packs/_template/pack.yaml:67` | 15 秒档 `{total:70, hook:15, body:40, cta:15}`；`packs/elevator/pack.yaml:69` 已按 P2-26 修成 `{body:47, cta:8}`（`:63-67` 注释明确说 cta 15 是**错形状**）。模板包是 `packgen.GENERIC_FILES` 的骨架 → **新包会继承被点名判错的配额形状** |
| P2-37 | `packs/_template/skill.yaml:268` | 分镜输出 JSON 只有 `time/shot/subtitle/sfx/note`；`elevator` 已含 `bgm/transition/shot_type`，`app/schemas.py:136-139` 与 `docs/场景序列契约.md:80-84` 都要求这四个字段 → **新包分镜契约缺字段** |
| P2-38 | `docs/知识库与技能配置指南.md:104,197-199` | 称 skill.yaml `stages`「**四段**」，实际 **5 段**（`elevator/skill.yaml:36,92,171,254,272` 含 `draft`）。文档漏了 draft 路径 |
| P2-39 | `docs/技能包系统方案.md:89` | 白名单写「`.yaml/.md/.txt/.json`」**漏 `.yml`**；`app/packimport.py:38` 含 `.yml`，`docs/技能包制作指南.md:23` 也含 |
| P2-40 | `docs/知识库内容规范.md:135-137` vs `:81-82` | §3 给 AI 的指令让产出 `extra_soft:`，而引擎**不读** `extra_soft`（`app/checker.py:123-126` 只作 advisory）—— 同文档 `:81-82` 自己写着「引擎不读 extra_soft」 |
| P2-41 | `docs/技能包系统方案.md:52` vs `docs/知识库与技能配置指南.md:103` | 一处称 pack `version`「升级比对用」，一处称「纯声明（解析但不消费）」。实现：`knowledge.py:237` 只解析，`packseed.py:158-187` 比对的是 `app_version` + `tree_hash`，**从不比对 pack 的 version** |
| P2-42 | `config.example.yaml:20-25,43` | `base_url` / `model: glm-4.7` / `active_model: zhipu` / `default_pack: elevator` 是**未注释的真实值**；而 `app/config.py:136-198` 的 `CONFIG_TEMPLATE` 已按铁律 2 全部注释掉（`:129-135` 注释说明原因）。用户「复制该文件为 config.yaml」后，这些字段会被当成「用户配过」 |
| P2-43 | `desktop/main.js:298-316,324-329` | `updaterLogInit` 失败只 `console.error`（打包版不可见），此后 `updaterLogLine` 静默 return、状态行带 `logPath:''` → 用户失去「原文轨迹」 |

---

## 4. P3 清单（简表，~30 条）

**前端**
- `result.js:369` `<span class="seg-tag ${s.type}">`、`:253` `<i class="${x.type}...">` —— `${s.type}` 未 `esc`。被 `schemas.py:116` 的 `Literal` 约束，正常流程不可利用；但 `store.read_result`（`store.py:157-161`）读盘**不重新校验**，手改/损坏的 `result.json` 可造成属性注入
- `jobs.js:67-68` `collectParams` 的 voice/format 在元素缺失时静默发内置默认（同函数 `facts` 用 `null` 才是对的口径）
- `topics.js:45` `packName()` 硬编码兜底 `"elevator"`
- `index.html:171` `#btn-ignored` 是一颗**没有 onclick 的按钮**（`topics.js:179-183` 只写文案/`disabled`）
- `index.html:175` `#topics-note` **死元素**（JS 从不引用）
- `api.js:79-83` 成功后 `setTimeout(ac.abort, timeout)` 仍挂到超时（最长 30s）
- `jobs.js:139` `setHead(topic, "生成中", "生成中…")` 同时写 `#rh-sub` 与 `#rh-state` —— 相邻两元素说同一件事
- `ui.js:705` 与 `settings.js:823` 都设 `#btn-packinfo` 的 `onclick`（幂等但属重复表示）
- `settings.js:1191`/`:164` `state.lastCreatedPack` 是**动态挂的属性**，不在 `store.js:7-24` 的 state 定义里
- `ui.js:157` 死三元 `active ? active.id : (m.mock ? "" : "")`
- `about.js:162` 与 `_verify` 中 `panes 5/navs 4` 类硬编码刻度（改刻度要改两处）

**后端**
- `jobs.py:481-485` `running_count` docstring 有一整段重复粘贴
- `intel.py:686-695` 与 `load_latest` 口径不一致（见 P2-8）

**工具链**
- `_verify/verify.js:815-826`、`:853` 变异分支只判「有没有断言失败」，不核对「是不是该条断言失败」；基线绿保证了因果，但顺带弄红另一条会被记成「报红 OK」
- `_verify/` 混入 14 个一次性探针（`_probe_a.py`…`_probe_h.py`、`_f_ea.py`、`_store-head.py`、`ai-smell-distribution.py`、`zhique-*.py`），长期会让人分不清活工具
- `tests/test_server_hardening.py:1432,1455,1481,1509,1530` 用 `if TestClient is None: return` —— 依赖缺失时**静默通过（绿）**，与 `test_pack_yaml_integrity.py` 的 `@pytest.mark.skipif` 不一致。**skip 在报告里可见，early-return 不可见**
- `tests/test_quota_degraded_signal.py:219` 后一子句与前一子句完全等价（冗余死代码）
- `tests/test_pack_layer_audit10.py:228` `advisory == [] or all("extra_soft" in a ...)` 是过宽的逃生门
- `tests/test_dangling_refs.py:127` 前置断言用 `A or B` 两个独立条件，判据不明确
- `desktop/scripts/publish-update.mjs:273-289` 步骤 1 只比 `size`、不重算 sha512
- `desktop/main.js:603-613` `did-fail-load` 未过滤 `isMainFrame` 与 `ERR_ABORTED(-3)`
- `desktop/main.js:168-188` `startEngine` 不清 `engineStderrBuf`（`restartEngine` 清了，`whenReady` 的重试循环没清）→ 两次尝试的 stderr 串味
- `desktop/main.js:48-56` `second-instance` 在首个窗口尚未创建时**静默无操作**
- `desktop/preload.js` 多出第三条通道 `restart`，文档 §4.3 未更新（理由成立，文档该对账）
- `desktop/main.js` 关于 `logger.debug` 的注释与 electron-updater 6.8.9 源码不符（实测 `AppUpdater.js:289,551` 等**全部**有 `!= null` 保护）
- `desktop/main.js:389-395` 注释称并发检查「返回 null」，实测 `AppUpdater.js:253-275` 返回**同一 promise**；`result===null` 分支在打包+已配地址时不可达（死分支）
- `docs/自动更新方案.md` §7 标 `verify-package.mjs`「**不改**」，实际被改（版本号改为按 `pkg.version` 推导，因 `package.json` 已 bump 0.2.2）

**文档**
- `README.md:144-149` 全量数字 367 vs 实测 368
- `docs/需求方案-去AI味与热点情报.md:8` 状态行称「A-4 校准分布**未做**」，同文档 §2.11（`:664,:697,:748`）与 `README.md:538-545` 均记录**已出**并已落 `list_enumeration` 3→4
- `docs/技能包制作指南.md:19` 把「红线速查」归到 `rules/`，实际在 `compliance/industry.md#红线速查`；同文档 §1 目录树**漏了 `compliance/`**
- `docs/技能包制作指南.md:35-38` 的 `source.homepage` 被文档化，但 `packimport.py:149-152` 只读 `author`/`license`，`homepage` 被丢弃
- 界面区域命名漂移：`docs/知识库与技能配置指南.md:325`「设置 → 行业包」、`docs/技能包系统方案.md:138`「设置页「技能包」区」；实际一级菜单是「**生成偏好**」（`index.html:216,254,373-376`）
- 多处代码行号引用失效（`README.md:547`/`:555`、`docs/场景序列契约.md:73`、`docs/知识库与技能配置指南.md:210`）—— 建议改函数名锚点
- `packs/elevator/skill.yaml:266-267` 分镜要求里出现两个编号「4.」
- `.gitignore:58-59` 声明 `_verify/*.png` 忽略，但 `_verify/` 下有 **12 个 png 被 git 跟踪**（忽略规则不解除已跟踪）
- `desktop/renderer/styles.css:~290` 注释写「全局 `button{height:var(--h-btn)}`（**30px**）」，`--h-btn` 现为 `var(--h-sm)` = **28px**

---

## 5. 已复核为「非问题」的方向（下一轮别重复劳动）

这些是六路**专门查过、确认站得住**的，记下来省下一轮的时间：

**安全（全部核对通过）**
- **zip 导入**：`_clean_rel`（拒绝对路径/盘符/`..`）+ 解压后 `resolve` 复核 + `_is_symlink` + 三重大小限制 + 后缀白名单（`packimport.py:81-268`）—— zip slip / 符号链接 / 大小炸弹均挡住
- **路径穿越**：`Pack.__init__` 用 `packs_root in self.dir.resolve().parents` 做不变式（`knowledge.py:731-737`）；`_safe_name`/`_safe_jid` 用 `.fullmatch`
- **无鉴权破坏性操作**：`guard` 中间件对全部 `/api/*` 强制令牌 + 同源判定（`server.py:623-645`），仅放行 `/api/health` 与 OPTIONS；`/docs`、`/openapi.json` 已关闭
- **SSRF**：`intel.py` 抓取 URL 全硬编码，`hot_board` 的 `board` 经 `_BOARDS` 白名单（`:280-283`），无用户可控出站 URL
- **密钥泄漏**：`public_models` 剥 `api_key`（`config.py:505-522`），`pack_file` 挡 `private/` 返回 403，`_error_json` 不外泄密钥
- **前端 XSS**：24 个插入点逐一核对，**唯一未转义是 P3 的 `sections[].type`**（正常流程不可利用）。`eval`/`new Function`/`document.write`/`outerHTML`/`setTimeout(字符串)` 全仓 0 处
- **窗口安全**：`contextIsolation:true / nodeIntegration:false / sandbox:true`；`setWindowOpenHandler` 对非 http(s) 一律 deny；`shell.openExternal` 输入被 `/^https?:/` 限定
- **IPC 面**：`preload.js` 仅暴露 `isElectron/platform/updater.{check,restart,onStatus}`，与 `main.js` handler 一一对应，**无任何接受路径/命令参数的通道**
- **更新包来源**：有 sha512 完整性校验（electron-updater 内建），非「拿到就装」
- **发布脚本泄漏面**：只上传 `latest.yml` 列出的 exe/blockmap + yml，不打包源码/密钥/配置/测试
- **测试服务器**：`serve-update.mjs` 默认绑 `127.0.0.1`（非 0.0.0.0），路径穿越有双防线 + 目录外哨兵文件断言
- **仓库卫生**：无明文密钥入库（`config.yaml` 未跟踪，历史无 Key）；`packs/*/private/` 内容全是空模板

**机制（全部核对通过）**
- **子进程生命周期**：单实例锁先于 spawn、`spawn` 的 error 有监听、父进程被杀由 `--parent-pid` 看门狗自杀（`test_parent_watchdog.py` 真起进程验证）、三处回收、`quitAndInstall` 先显式 `killEngine()`
- **额度占坑不还**：`start_generate`/`start_packgen`/`start_intel_fetch`/`rewrite_segment` 的 `_spawn`/`_guarded`/`_fail` 三处兜底齐备（`pipeline.py:1550-1605`）
- **状态机**：`TRANSITIONS`（`jobs.py:99-123`）覆盖含 `failed→rewriting` 的全部真实路径，无不可达态/漏态
- **原子写与回滚**：`fileio.write_atomic`、`store.write_result`、`packimport._swap_in` 均正确
- **配置加载**：`config_error`/`llm_defaulted`/`models_defaulted` 已摊到 `/api/config`（`server.py:1119-1123`）
- **打包白名单**：逐文件对账 `files`/`extraResources` 与代码引用，**未发现任何被引用却不在白名单里的文件**（历史 P0-1 未复发）
- **打包中断语义**：`before-pack.js` 的 `assertPrivateExcluded()` 与 `build-python-runtime.mjs` 任一抛错都使 `beforePack` 失败 → 不产出半成品
- **`_verify` 不是 grep 式断言**：368 条里只有 2 处读源码文本（`readCsp()` 读生产 CSP 常量、`styles.css` 刻度扫描），其余全走 CDP 量 DOM/几何/计算样式
- **`mutate.py` 防假绿设计完备**：基线先跑并重试、分组标记校验、退出码 5 与收集失败区分、崩溃恢复日志、并发锁 PID 探活、还原后逐字节校验；逐条读 `MUTATIONS` **未发现一条有把握是假守卫的条目**
- **自动更新核心铁律代码层确实落地**：dev/portable 闸门、不配地址则零请求、失败落状态行 + 日志、地址唯一来源 = 环境变量；三个新测试文件测真行为（真 HTTP 服务器、真子进程、真 206/416/403），**断言可红**

**长期交付欠账（非代码缺陷）**
- **NSIS 真机安装/卸载/重装** —— 需干净机器或虚拟机。README「安装与卸载的行为」有 5 步实机清单。**这是唯一剩下的交付欠账。**

---

## 6. 建议的处理顺序

1. **P1-1**（`packimport` 台账损坏静默覆盖）—— 唯一会**不可逆丢用户数据**的一条，且注释在说谎。先修它。
2. **P1-2 / P1-3**（两处「信号算了没人接」）—— 与项目核心取向同源，改动小、收益直接。
3. **P1-4**（变异检验登记）—— 把自动更新这条线的验收凭证补上，否则「绿」只是自述。
4. **P1-5 ~ P1-7**（三份文档与实现不符）—— 纯文档，但会误导包作者。
5. **P2-13**（`test_env_bounds` 假守卫）—— 测试类里唯一一条「有把握是假守卫」的，优先级高于其他测试项。
6. **P2-1**（idle 态不可达）—— 自动更新这条线唯一的「文档说做了代码没做」。
7. **P2-14 / P2-15**（工具链少一本账）—— 让打包校验的"认证通过"名副其实。
8. **P2-16 / P2-17**（主进程两处安全加固）—— 本机风险，但设计目标明确要求。
9. 其余 P2/P3 按主题批量清（文档类可一次性对齐）。

> **验收口径**：以上每条修复都必须过变异检验（把实现改回去 → 断言必须报红）。
> 只改实现不补断言的，按本项目规矩等于没修完。

---

## 7. 本轮修复记录（2026-09-24 16:44 ~ 17:15）

按 §6 的建议顺序逐条修，**每条都过变异检验**（把实现改回去 → 断言必须报红）。

| # | 问题 | 改动 | 验证 |
|---|---|---|---|
| **P1-1** | 台账损坏静默覆盖用户改过的包 | `app/packimport.py`：`_user_modified` 台账坏 → 抛 `PackImportError`（新增 `_ledger_broken()` 统一出口），把「**无法判断**」与「没记录」分开 | 补 4 条测试；变异改回 `return None` → **2 条红** |
| **P1-2** | 前端不消费 intel `errors` | `topics.js`：`sourceError()` / `frameErrors()`；源表标「抓取失败」+ 原因；计数行排除失败源；框架级错误进 `#topics-note`；空态分两种 | 桩补活样本；变异 → **2 条红** |
| **P1-3** | `param_audit` 三键无落点 | `ui.js`：`paramSelect` 吃 `_audit`；新增 `#pack-audit` 装**非参数类**审计（判据 = 键不在 params 里，不写死键名） | 桩补 `style` / `ai_tells` 活样本；变异 → **2 条红** |
| **P1-4** | 自动更新变异检验未登记 | `_verify/mutate.py` 加 s8-1 / s8-2（`node:` 选择器） | 实跑 **2 条报红 OK / 0 条不合格** |
| **P1-5~7** | 三份文档与实现不符 | `知识库内容规范.md`（ideas.md 注入口径）/ `技能包系统方案.md`（§1 起点盘点）/ `知识库与技能配置指南.md`（字段表补 `intel_sources`/`rewrite_scope`/`duration_tolerance_pct`） | 逐条 grep 核实代码侧 |
| **P2-13** | `test_env_bounds` 假守卫 | 改成从 `app.server` import `NUMERIC_BOUNDS` **现读** + 键名映射 | 变异：改 server 区间 → **断言红** |
| **P2-1** | 自动更新 idle 态不可达 + 开面板即重置 6h 时钟 | `main.js` 补推 idle；新开**只读** `updater:getStatus`；`about.js` 改走它 + 失败可见；文档 §4.2 / §4.3 对账 | 变异 ×2：`getStatus` 改回 `check` → 渲染层断言红；删掉 idle 推送 → `main.errorpath.test.js` 断言红 |
| **P2-14** | `verify-package.mjs` 不校验内嵌运行时 `engine/py` | 加 `compare('py', desktop/vendor/py, engine/py)` | 新增 `packaging.test.js` 断言（判据从 `extraResources` 派生）；变异：删掉该行 → **断言红** |
| **P2-15** | `sourceNewest()` 硬编码清单漏 `updater-core.js` | 改成从 `pkg.build.files` 派生（同一信息只留一份） | 新增断言（函数体里不许再出现写死的 `.js` 文件名）；变异：改回写死 → **断言红** |
| **P2-17** | 缺 `will-navigate` 守卫（顶层导航可把窗口带走） | `main.js` 加 `will-navigate` + `isEngineUrl()`，只放行引擎同源 | `main.errorpath.test.js` 新增集成测试（假 electron 真跑 main.js）；变异：删掉守卫 → **断言红** |
| **P2-16** | 端口抢占窗口里的令牌泄漏 | `server.py` 的 `/api/health` 回显 `TALKSCRIPT_HEALTH_NONCE`；`engine-path.js` 把 nonce 与令牌走同一条 env 通道；`main.js` 的 `waitHealth` **不再只看 `r.ok`**，身份不符立刻出局 | 三层断言：`engine-path.test.js`（nonce 进 env、不进 argv）、`main.errorpath.test.js`（冒充者被拒 + 正确 nonce 被接受的正对照）、pytest（回显/不回显）；变异 ×3 全红 |
| **P2-9** | 忽略键两端口径不一致（只有 url 的条目"忽略了还在"） | `intel.py` 抽 `item_key()`：dedup / 忽略过滤 / 下发的 `item.key` 用**同一个键**；`topics.js` 读 `it.key`，不再自己拼 | 桩里给一条**前端拼不出来**的 key（否则断言空转）；变异 ×2：前端改回自己拼 → 红；后端改回 `guid or title` → pytest 红 |
| **P2-3** | `loadSessions` 空 catch，注释还与代码相反 | `sessions.js`：失败时**真的保留**上一次列表（`allItems`）并在顶部摊出原因；告警节点不带 `data-key`，下次成功的 paint 自动清掉 | verify 用 `window.__histFail` 在**同一页面内**走「成功 → 失败 → 恢复」的转移；变异：改回空 catch → 红 |
| **P2-4** | 打开设置时配置读不到被静默吞掉 | `settings.js`：`preloadSettings().catch()` 里 toast 出来 | verify 用 `window.__cfgFail`；变异：改回空 catch → 红 |
| **P2-5** | 停止时问不到作业状态却猜成 cancelled | `jobs.js`：不再猜，说清「没问到」并接回那条作业 | 桩加 `__stopNoState` + `__stopAskFail`；变异：改回猜 → 红 |
| **P2-6** | 停止时产物没读到却说「产物已保存」 | `jobs.js`：产物读到了才说「已保存」，否则说清「只有版本记录」 | 桩加 `__stopDone` + `__stopRecFail`；变异：改回无条件 → 红 |
| **P2-7** | `read_text_cached` 编码失败静默返回空串 | `knowledge.py`：`UnicodeDecodeError` / `OSError` 分开、各留一条带路径的 warning；**失败也进缓存**（否则一次生成读十几遍，同一条 warning 把日志淹掉） | 新增 pytest（GBK 文件有 warning + 重复读只说一次 + UTF-8 正对照）；变异：改回静默吞掉 → 红 |
| **P2-8** | `load_ignored` 读坏静默当空 → 一次忽略把**整个历史覆盖成一条** | `intel.py` 拆 `_load_ignored_checked`（区分「读坏」与「没有记录」）；`add_ignored` 读坏 / 写失败都抛 `IntelError`；`server.py` 的端点转成 500 + `intel_ignore_failed` | pytest ×3（坏文件原样还在 + 只读路径照常退空过滤 + 端点 500）；变异：`add_ignored` 改回 `load_ignored` → 两条红 |
| **P2-19** | `pack_info` 不读 `skill.yaml` → **列表说好、生成说坏** | `knowledge.py`：`pack_info` 补 `skill.yaml` 的读取校验（判据：凡 `Pack()` 会因此抛的，`pack_info` 都要能看见 —— "列表宽松"指不抛，不是看不见） | pytest（坏 `skill.yaml` → `pack_error` 非空，且 `Pack.skill()` 抛 `PackBrokenError`）；变异：删掉检查 → 红 |
| **P2-10** | `transition_if_room` 漏 kind 分族 → 情报抓取占掉模型名额 | `jobs.py`：计数加 `same_quota_family`（与 `add_if_room` 对齐）+ 补 `states` 参数 | pytest（情报占满时重写仍能开 + 模型族占满时拦住的反向对照）；变异：删掉分族 → 红 |

**§3.5 测试守卫薄弱（P2-22~28，7 条）** —— 这一组不修的话，前面所有「绿」都要打折扣：

| # | 病灶 | 改动 | 验证 |
|---|---|---|---|
| **P2-22** | `verify.js` 的 `badgeFam` 只有 `n === 1`，**没有"找到了几个"的前提守卫** —— 七个类消失时 `n` 仍为 1 | 加 `found >= 4` 前提守卫 + `missing` 列出来（下界是**量出来的**：实测此刻在场 5 个，另外三个属于别的面板） | 变异：把探针的 `found` 计数打掉 → 红 |
| **P2-23** | `dashForUncomputable` 只证明选题区文本里**出现过一次破折号**，不能证明 D/S/E 那格是 `—` | 改成取 `.sig-grid` 的**值格**（每行第三个兄弟）逐格判 | 变异：`fmtPct` 改回 `v \|\| 0` → 红 |
| **P2-24** | `assert size == len(text.encode()) or size > 0` —— 后半句恒真（上一行已断言 text 非空） | 改成与**磁盘上 `stat().st_size`** 比（`read_text` 在 Windows 会收 CRLF，所以不能拿 `len(text.encode())` 比） | 变异：后端 `size + 1` → 红 |
| **P2-25** | `assert "抖音热榜" in join(errors.values()) or errors` —— `errors` 非空即过 | 改成断言 `"hot_board" in errors` 且原因非空串 | 变异：`errors[spec.id] = ""` → 红 |
| **P2-26** | `MAX_ZIP_BYTES` 在 `tests/` **零引用**（另两重都有） | 补一条（monkeypatch 压到 10 字节） | 变异：删掉那重检查 → 红 |
| **P2-27** | `/api/config/test`（用户可直达，4 个分支）与 `/api/history/{jid}/reveal` + `reveal_target` 在 `tests/` **零引用** | 各补一条：前者覆盖「无 Key 400 / 非法 base_url 400 / 正常 200 / **model_id 覆盖**」，后者覆盖「非法 id 400 / 不存在 404 / 成功 / 打开失败 500」 | 变异 ×2：去掉 `model_id` 覆盖 → 红；404 改成 200 → 红 |
| **P2-28** | 三处 `start_intel_fetch(...)` 不传 `http=` → **真发网络请求** | 三处补 `http=_fake_http()`；加一条 autouse fixture：把 `_http_json` 换成离线空响应 + 收尾断言「单元测试一次都不许走到真实出口」 | 变异：去掉一处 `http=` → 收尾红（实测还顺带抓出**第四处**：`test_refresh_endpoint_starts_a_job` 也在真发） |

> ⚠ **P2-28 的 fixture 写了两版才对**：第一版让替身**直接抛** —— 而 `fetch_pack`
> 对每个源各自 try（一个源挂了不影响别的源），抛出去被**吞进 `errors`**、
> 测试照样绿（变异检验没红才发现）。判据必须放在**收尾断言**上。
> 顺带暴露了一个真竞态：`test_intel_fetch_has_its_own_limit` 原来靠"真网络慢"
> 才让作业停在 fetching，换成假 HTTP 后瞬间 done、额度当场释放 —— 现在用
> `threading.Event` 把作业**钉在 fetching**。

**修完后的基线**：`verify.js` **379/379**（368 + 本轮新增 11 条）· desktop `node --test` **108/108**
（103 + 新增 5 条）· `mutate.py -k s8` **2 条报红 OK** · `pytest` 相关文件全绿。

**P2-16 的做法**（本轮唯一需要设计的）：给「应答者是不是我们的引擎」一个**只有它知道的凭证** ——
`main.js` 生成 nonce → 与一次性令牌**走同一条 env 通道**传给子进程 → `/api/health` 回显它 →
`waitHealth` 比对。三条边界都值得记：
- nonce **只用于身份确认、不授权任何操作**，所以在这个免鉴权端点上回显它是安全的
  （不像令牌能调 `/api/*`）；
- 身份不符**立刻出局**（不等满 40 秒）—— 那个进程不会突然变成我们的引擎；
- 允许环境变量覆盖 nonce：既是集成测试的注入点，也让手工起引擎调试时两边能对齐
  （与 `TALKSCRIPT_PYTHON` / `TALKSCRIPT_UPDATE_URL` 同一个风格）。

### 收尾轮（2026-09-25，无人值守）

| # | 病灶 | 改动 | 验证 |
|---|---|---|---|
| **P2-20** | `usage.update()` 覆盖 —— 重试时第二次抹掉第一次，被丢弃那次真实消耗**静默不计** | `app/llm.py` 新增 `_merge_usage()`：**累加**（`*_tokens_details` 递归累加） | 端到端：第一次给过不了校验的正文 → 重试 → 账上 200/20 而非 100/10；变异：改回覆盖 → 红 |
| **P2-21** | `score_topics` 写 5 个**零消费方**的字段并随 `latest.json` 外发 | 删掉（含 `_FACT_RE` 这个只给它用的正则） | `test_score_has_no_dead_fields`：**允许清单**（D/S/E/opportunity/is_new），加字段必须表态；变异：加回 → 红 |
| **P2-43** | 更新日志没落成盘时只 `console.error`（打包版不可见），而 `logPath` 为空让界面那行「完整日志：」整个不显示 | `updater-core.js` 新增 `errorStatusText()`；`main.js` 记 `updaterLogError` | 单测（含"空串不算原因"的正对照）；变异：去掉原因 → 红 |
| **P2-36/37** | 新包骨架（`packs/_template`）与 elevator 不同口径：15 秒档 cta 15 字（错形状）、分镜 JSON 缺 `bgm`/`transition`/`shot_type` | 两处照 elevator 对齐 | `test_template_pack_matches_elevator_contract`；变异 ×2 全红 |
| **P2-42** | `config.example.yaml` 的 `active_model` / `default_pack` / 模型地址与 model 名是**未注释的真实值** → 复制后会被当成「用户配过」 | 全部注释掉（与 `CONFIG_TEMPLATE` 同一口径）+ 顶部说明 | `test_config_example_has_no_uncommented_real_values`；变异：去掉 `#` → 红 |
| **P2-29/30/31** | README 版本号写 0.2.0/0.2.1（实际 **0.2.2**）、架构图列不存在的 `app/watchdog.py`、两处行号失效 | 版本号改 0.2.2；watchdog 行注明"无此模块，实现在 server.py 内"；**行号换成符号名**（`PUBLIC_PATHS` / 端点名）—— 行号必然漂移 | grep 核对代码侧 |
| **P2-32~35** | UI 规范：`--h-lg` 未定义、hero 图标一处 32 一处 40、`--e2/.04` 与 `--e3/.10` 与实际不符、"13 处 rgb 字面量"已过时 | 逐项改成实测值；rgb 计数改 10（`resolvedToken` 12 处） | grep 核对 `styles.css` 与 `verify.js` |
| **P2-38~41** | stages 漏 `draft` 段、白名单漏 `.yml`、`extra_soft` 自相矛盾、pack `version` 一处说"升级比对用"一处说"纯声明" | 五段 + 补 `.yml` + 统一为"纯声明（比对用 `app_version` + `tree_hash`）"；`extra_soft` 只留"引擎不读"一种口径 | grep 核对 `packimport.py` / `packseed.py` / `checker.py` |

⚠ **本轮发现两条报告结论已过期**：P2-11/12（`result.js` 的 `passState` / `smellScore` 已存在，
旧写法只在注释里被提到）与 P2-18（`resolveFeedUrl` 已只认 http/https + `feedUrlWarning`）
**已被并发会话修掉**。教训：**动手前先看一眼现场**，别照着报告直接改 ——
也别把"报告说没修"当成事实。
（P2-40 的 `extra_soft` 那处同文档已有等价说明，本轮只做了去重。）

### P3 收尾（2026-09-25 续）

| 类别 | 已做 | 说明 |
|---|---|---|
| **文档** | README 全量数字 367 → **379**；需求方案「A-4 校准分布未做」→ 已出（剩两件）；技能包制作指南补 `compliance/` 目录 + `homepage` 注明"引擎不消费"；4 处裸行号改成**符号锚点**；`elevator/skill.yaml` 分镜要求里重复的编号 4.→5.；`styles.css` 注释 `--h-btn` 30px → 28px（= `--h-sm`） | 行号必然漂移，一律换成锚点 |
| **工具链守卫** | `test_server_hardening.py` **5 处** `if TestClient is None: return` → `pytest.skip`；`test_quota_degraded_signal.py` 的冗余子句删掉；`test_pack_layer_audit10.py` 的 `advisory == [] or all(...)` 逃生门收紧成严格断言；`test_dangling_refs.py` 的 `A or B` 判据改成单一判据 | **skip 在报告里可见，early-return 不可见** —— 依赖缺失时"静默通过"是最坏的一种绿 |

**主动跳过**（别当遗漏）：
- **界面区域命名漂移**（"设置 → 行业包" / "设置页「技能包」区" vs "生成偏好"）——
  需人工确认设置页的分区结构（`index.html` 里"生成偏好 / 行业包 / 模型接口 / 知识库 / 技能"
  看起来是并列的子区），**改错的风险大于收益**。
- **`.gitignore` 声明 `_verify/*.png` 却有 12 个被跟踪** —— 修法是
  `git rm --cached`（文件保留、只解除跟踪），属**提交级**操作，留给本人决定。

**未做**：P3 前端 11 条（`${s.type}` 未 `esc`、`#topics-note` 死元素、`#btn-ignored`
无 onclick、`collectParams` 元素缺失时静默发内置默认、`packName()` 硬编码兜底、
重复 onclick、死三元等）、后端 2 条（docstring 重复粘贴）、工具链剩余
（`_verify/` 14 个一次性探针、变异分支不核对"是不是该条断言失败"、
`did-fail-load` 未过滤 `isMainFrame`、`second-instance` 静默无操作等）。

⚠ **踩坑**：`str.replace` / `re.sub` 对 `if TestClient is None:\n        return\n`
**一处都匹配不到**（grep 明明看得到那 5 处）—— 换行/缩进层面有看不见的差异。
改用**逐行 `splitlines` 匹配**（`ln.strip() == ...`）立刻就对了。
**下次遇到「看得见却替换不掉」，直接走逐行，别再重试正则。**

**剩余**：P3 未做部分（见上）与长期欠账（NSIS 真机安装 / 卸载 / 重装需干净机器或虚拟机）。
**P2 共 43 条已全部闭合**（含被并发会话修掉、以及"不适用"的那些）。

**P2-1 的缺口已闭合**：本轮为「`main.js` 启用分支必须推 idle」补了**真起 main.js 的集成测试**
（`desktop/main.errorpath.test.js`，假 electron + mock 全局 `fetch` 绕开引擎启动）。
过程中撞到一个值得记的坑：**第一版判据看 `getStatus()` 的返回值，是假绿** ——
`updaterStatus` 的**初始值**就是 `{state:'idle'}`，"推了"与"没推"从它上面看**完全一样**；
第二版又假红（等错了信号 —— `ipcMain.handle` 在顶层立即注册，等它等于没等）。
改成「等一次推送 + 断言 `webContents.send('updater:status', …)` 里出现过 idle」才真的能红。

---

*报告生成：2026-09-24 15:5x · 六路并发只读审查 + 主线独立复核 · 基线：verify.js 368/368 · pytest 717 用例零 F*
*§7 修复记录追加于 2026-09-24 17:15 · 修后基线：verify.js 373/373*
