# 审查报告 · 2026-09-26 · 五路全方位审查

> **审查对象**：`E:\Workspace\video\talk-script-agent`，HEAD `3ed3548` + 工作区未提交改动（55 个 `M` + 9 个 `??`）。
> **方法**：五路并发只读审查（后端核心引擎 / 后端扩展模块 / Electron 主进程与更新线 / 渲染层全部页面 / 测试验证体系与文档），
> 主线独立复核高价值发现（本报告标注「✅ 已主线核实」的条目均由主线亲自读码确认）。
> **不采信任何既有报告的结论**，全部按当前磁盘现状复量；上轮（20260924 六路）已闭合的 7 条 P1 + 43 条 P2 未重复报告。

---

## 0. 基线实测（本轮实跑）

| 基线 | 结果 |
|---|---|
| `node _verify/verify.js` | **379/379 通过**（GUI 实跑） |
| `.venv/Scripts/python.exe -m pytest -q` | **1 F**（`test_usage_keys_cross_layer`），其余全绿 |
| `desktop: node --test` | **111 条中 108 过 / 3 挂**（全在 `main.errorpath.test.js`） |

**两条红灯都诊断清楚了，均不是产品代码坏**（见 §2）。但在修复之前，本工作区的「绿」是自述不是实测。

---

## 1. 结论速览

| 级别 | 条数 | 一句话 |
|---|---|---|
| **P0** | **0** | 无崩溃 / 数据丢失 / 可利用漏洞。安全面（鉴权、zip 导入、路径穿越、IPC、XSS、SSRF）复核仍然站得住。 |
| **P1** | **5** | 1 条可致意外计费；2 条是「修复自身引入的不对称」（瞬时故障钉进缓存 / 骨架包缺占位符的兄弟问题）；1 条换包静默降级在兄弟路径复发；1 条内置包先删后拷无备份。 |
| **P2** | **11** | 取消语义两份、嵌套 YAML 裸 500、跨盘回滚跳过 + 文案说谎、启动重试误报链、chip 目标错位、部分成功报失败、无障碍机制静默死亡、两条破坏性路径零测试。 |
| **P3** | **~28** | 键式口径长尾、文档数字残留、旧 P3 原样在（上轮 11 条渲染层 P3 现核实 9 条仍在、2 条已闭合）。 |

**整体判断**：主干依然健康——上一轮 50 条修复经五路复查**没有发现一条引入产品级回归**；本轮增量集中在
「边界状态组合」（全部停用 + 删除、取消 + 已落盘、重试 + 新旧进程）和「兄弟路径不同权」
（同一不变量只守了一条入口）。两条红灯基线都是测试自身的问题，但恰是本项目最在意的病：**守卫自己先坏了**。

---

## 2. 基线红灯诊断（先修这两处，绿才重新可信）

### 2.1 pytest 1F —— 假红：`_merge_usage` 重构把守卫的正则前提作废了

- 失败：`tests/test_usage_keys_cross_layer.py:93`「llm.py 里找不到 details 的点名拷贝 —— 白名单被改掉了？」。
- 根因：该守卫靠正则 `for det in \(([^)]*)\)`（`:89`）找 llm.py 里的内联白名单；修 P2-20（usage 覆盖）时，
  `llm.py:499/:604` 两处 `for det in ("completion_tokens_details", "prompt_tokens_details")` 整段替换成了 `_merge_usage(usage, u)`
  （现 `llm.py:520/:622`），正则零命中。**功能无回归**：`_merge_usage`（`llm.py:210-230`）对嵌套 dict **递归全量拷贝并累加**，
  UI 唯一读的 `completion_tokens_details`（`progress.js:67`）天然在拷贝路径上；且新行为有活守卫
  `tests/test_llm_retry.py:425`（重试累加 4+4，改回 `update()` 必红）。
- **修法**：重写守卫以匹配新不变式（断言 `_merge_usage` 含 dict 递归分支 + 两个调用点在），不是改回 llm.py。
  同文件 `:99` 的 `assert len(listed) >= 2` 一并随作废。

### 2.2 desktop 3F —— 测试夹具的 Windows 竞态，非产品缺陷

- 失败：`main.errorpath.test.js` 三条（顶层导航守卫 / idle 推送 / 健康正对照）。三条的 in-try 断言**全部通过**，
  都死在 `cleanup()`（`:187`）的 `rmSync(userData)` 上：`before-quit` → `closeEngineLog()` 对 engine.log 的
  WriteStream 调 `end()`——**关流是异步的**，同 tick 的 `rmSync` 撞上 Windows 的 delete-pending，重试窗口输掉抛 ENOTEMPTY。
  fs 探针实证：失败用例 `end()` 后 fd 未关、`rmSync` 时 `engine.log` 仍在目录里。
- 次生脆弱：测试前提「TALKSCRIPT_PYTHON 指向 node 自己 → spawn 成功」实际落空——`rootDirCached = desktop/engine`
  目录不存在，spawn 每次以 ENOENT 异步失败，三条靠 mock fetch 的微任务时序碰巧过关。
- **修法**：cleanup 等流关闭后再删（或 rmSync 带重试）；spawn 前提要么建 `desktop/engine` 桩目录、要么明说。
  附带：失败时 mkdtemp 目录残留在 %TEMP%。

---

## 3. P1 清单（5 条）

### P1-1　「全部停用模型」后删除任一条，会静默重新启用剩下的第一条 —— 可致意外计费 ✅ 已主线核实

**位置**：`app/server.py:1329-1332`（删除模型端点）

```python
if active == body.id or active not in {x["id"] for x in left}:
    active = left[0]["id"] if left else ""
```

`active` 为**空串**（用户经 `POST /api/models/activate {"id":""}` 显式关闭全部模型——合法且受支持的状态，
`_parse_models` 特意原样保留空串）时，`"" not in {剩下的 id}` **恒真**，走进本想处理「active 已悬空」的分支，
把用户明确停用的模型悄悄启用。审查代理已用 TestClient 在临时目录**实测复现**（disable all → delete m2 → active 从 `''` 变 `'m1'`）。

**后果**：`_require_model` 不再拦截，下一次生成直接用这条被停用的模型 + Key 花真钱；与 `activate_model`
docstring「关掉之后生成会被拦住」的承诺相反。现有测试只覆盖删到空 / 删当前 / 删非当前，此场景无守卫。

**修法**：悬空判据加 `active and`（一行）；补「全部停用后删除」测试 + 变异登记。

### P1-2　程序化换包不重置 genParams —— 「换包静默降级」在两条兄弟路径复发 ✅ 已主线核实

**位置**：`desktop/renderer/js/settings.js:1458-1464`（`selectPack`）、`settings.js:1296-1303`（`onPackDone` → `fillPackSelect({prefer})`）、`desktop/renderer/js/ui.js:77-81`（`fillPackSelect` 直接赋 `sel.value`）

唯一做 `resetGenParams()` 的地方是下拉的 `change` 监听（`ui.js:782`）——而它的注释把病情写得清清楚楚：
「换包 = 参数集与每项默认值全变，旧选择必须清空（否则新包沿用上一个包的取值……静默降级）」。
但两条程序化路径**直接赋值、不派发事件**，绕过了这道唯一闸门：

- 行业包面板点选包（`selectPack`：`sel.value = name`，无 dispatchEvent）→ 切回生成页直接发送；
- 新建行业包点「完成」（`onPackDone` prefer 到新包，`fillPackSelect` 同样只赋值）→ 回工作台发送。

`getParam`（`jobs.js:44-45`）第一分支优先返回 `state.genParams` 旧值 → 随请求发给新包；过期横幅救不了
（`updateStale` 只比对快照差异，pack 键没变时不弹）。

**修法**：`selectPack` / `fillPackSelect` 赋值后与 change 监听走同一收口（调用同一个「换包收尾」函数），别靠事件间接。

### P1-3　`read_text_cached` 把「瞬时读失败」钉进进程级缓存 —— 上轮 P2-7 修复自身引入的不对称 ✅ 已主线核实

**位置**：`app/knowledge.py:87-91`

```python
except OSError as e:
    log.warning("知识文件读不出来（%s）：%s —— 本次按空内容处理", path, e)
    text = ""
with _cache_lock:
    _text_cache[key] = (st.st_mtime, st.st_size, text)   # ← OSError 的空串也进缓存
```

同文件 `read_yaml_cached`（`:122-126`）特意写了 `if not getattr(err, "retryable", False)` 才缓存，
注释原话：「读失败时 (mtime, size) 都没变 —— 下次命中缓存返回的还是那条错误，**解除占用也救不回来，只能重启应用**」
（`fileio.YamlError` 记录了第 22 轮用 `msvcrt.locking` 的实测：Windows 上杀软/索引器按住刚落盘的文件是常态）。
文本这条修复时把 OSError 与 UnicodeDecodeError 同罪处理——编码错进缓存是对的（永久缺陷），**瞬时占用不该同罪**。

**后果**：生成/建包期间任一知识 `.md`（钩子库/人味范例/选题库切片）首次读取恰逢瞬时占用 → 空串入缓存，
该文件整个进程生命周期恒为空，`_audit` 只会报「切片为空」，根因早已消失却只能重启。与 yaml 口径正好相反，
是「同一个兜底口径抄了两份后语义分叉」的最新实例。

**修法**：OSError 分支不落缓存（或只对 UnicodeDecodeError 落）；补「瞬时占用恢复后重读成功」的测试。

### P1-4　情报落点键两本账：抓取写 `pack.name`，today/源表读目录 slug ✅ 已主线核实（写读两侧分别由两路代理独立定罪）

**位置**：写方 `app/pipeline.py:627`（`fetch_pack(pack.name, …)`）、`app/prompts.py:166`（`select_for_prompt(…, self.pack.name, …)`）→ `app/intel.py:390`（`data_dir/"intel"/pack.name`）；
读方 `app/server.py:1079-1091`（`_intel_sources` 用 `p.name == name` 匹配、`intel_today(data_dir, name, …)`，入参是 URL 里的**目录 slug**）。

触发：目录名与 pack.yaml `name` 不一致即可——重命名包目录不改 `name`，或改 `name` 不改目录名
（复制目录分享正是 `packs/elevator/pack.yaml` 头部注释自己宣传的用法）。

**后果**：重命名后的包「重抓」显示成功（写进 `data/intel/<旧name>/`）但选题页永远空（读 `<新slug>/`），
源分组整个消失，两处都无报错；**两个包的抓取会互相覆盖同一份 latest.json**。同一匹配对大小写也不防
（Windows 下 `Pack()` 大小写不敏感、`p.name == name` 精确比较）。

**修法**：情报落点统一用目录 slug（或统一用 name，但读写必须同一把键），并在 `pack_info` 对 name≠slug 给一条 audit 提示。

### P1-5　`packseed._copy_pack` 先删后拷、无备份无回滚，中途失败把内置包撕成半残且 UI 无自救

**位置**：`app/packseed.py:99-107`；异常在 `seed_bundled_packs:202-203` 被 `except OSError: log.warning; continue` 吞掉。

```python
for child in dst.iterdir():
    if child.name == PRIVATE_DIR_NAME: continue
    if child.is_dir(): shutil.rmtree(child)
    else: child.unlink()
shutil.copytree(src, dst, dirs_exist_ok=True)
```

先删后拷的窗口内任何失败（磁盘满/杀软锁/权限——本模块注释自己论证这些在 Windows 是常态）→ 旧内容已删、新内容半截。
此后每次启动 `stored.bundled == src_hash` 成立 → 永远判成「用户改过」（conflict 分支不修复不重播）；
pack.yaml 没拷到则包从列表静默消失，拷到了缺知识文件则生成 409；`delete_pack` 又拒删内置包
（`packimport.py:362-365`），用户只能手工文件系统操作。对照 `packimport._swap_in` 对同一件事做了备份-搬运-回滚。

**修法**：改用与 `_swap_in` 同款「备份 → 搬运 → 失败还原」；失败别 `continue` 吞掉，至少把包标记成 `pack_error`。

---

## 4. P2 清单（11 条）

| # | 位置 | 现象 | 后果 |
|---|---|---|---|
| P2-1 | `app/pipeline.py:555-567` | 建包取消的两条收尾路径**策略相反**：取消落在 `_stop_check` 上（`JobCancelled` 分支）→ `_discard_created_pack` 删掉刚建的包；取消落在 `_stop_check` 通过之后、`transition("done")` 之前的微秒窗口 → `transition` 因 cancel 置位返回 False 直接 `return`，包**留在盘上**、作业记 cancelled。`:553-554` 注释（「目录已经建好就不该假装失败」）与 `:561-564` 注释（「取消的语义是什么都没发生……只有本次真的建出来了才回收」）各执一词 | 同一情形两种结局取决于毫秒竞态；留下的包同名重建 409、应用内删不掉（无删包入口） |
| P2-2 | `app/knowledge.py:822-825`、`:783`、`:225`、`:1142-1143` | **嵌套结构写错**仍以裸异常逃出：`stages:` 写成标量/列表 → `'str' object has no attribute 'values'`；`params.<key>` 写成标量 → `(params.get(key) or {}).get(...)` AttributeError；`banwords:` 写成映射 → `pack_info` 用 `str(...)` 读成「不存在」放行、生成期拿原始值拼路径 TypeError（同一个键两处两种强转） | 坏包 500/晦涩异常而非「点名哪个文件哪一项」的 409；`pack_info`「第 2 类坏法都兜住」的承诺只对浅层字段成立 |
| P2-3 | `app/packimport.py:238-244` | `_swap_in` 跨盘（临时目录在 C:、packs 在别盘）时 `shutil.move` 退化为 copy+删，中途失败 → dst 已有半截新包 → `dst_exists` 为真 → **回滚被跳过**（旧包躺在 `.import-backup-<name>`），报错却断言「已还原原样」 | 导入失败后列表里是个加载不了的新包，旧包要手工改隐藏目录名找回；文案与事实相反。默认同盘形态走 rename 不触发，故 P2 |
| P2-4 | `packs/_template/skill.yaml`（draft/write 段无 `$alt_guide`）vs `packs/elevator/skill.yaml:159,212` | 骨架包缺「换一版」占位符。`safe_substitute` 只查「模板引用了、ctx 没有」，反向（ctx 有、模板没引用）零检查——`unfilled()` 与 `packgen._placeholder_audit` 都不报 | packgen 生成的**每个新包**「换一版」永远没有候选版、无任何日志；该功能只在 elevator 包里活着。两份 skill.yaml 已漂移 |
| P2-5 | `desktop/main.js:226-227` | `engineProc.on('exit')` 处理器无条件给模块级 `engineProc` 置 null，不校验「退出的这个是不是当前引擎」；`startEngine` 赋值后无身份绑定 | 启动两试循环里 attempt 1 被杀后旧进程的 exit 事件若晚于 attempt 2 的 spawn 落地，把**在跑的新引擎**引用清掉 → 误报「引擎启动失败」弹窗退出 + 给活着的引擎报「引擎已退出」。修法：`if (engineProc === proc)` |
| P2-6 | `desktop/main.js:699` | 初始 `win.loadURL(engineUrl())` 的 promise 被丢弃，无 `.catch`（`restartEngine` 里的同名调用在 try/catch 里，唯独首次没有） | 引擎在 waitHealth 通过后、加载完成前死亡 → unhandled rejection，Node 20 默认 throw，可能在「界面加载失败」对话框外再叠一层主进程崩溃（Electron 33 下确切语义存疑，标存疑） |
| P2-7 | `desktop/renderer/js/result.js:599-616` | 后续建议 chip「重写『要点1』」带着 `value: points[0].i`（要点段下标），但点击处理 `querySelectorAll(".script-card .rw-feedback")[0]` **恒取第一个**（钩子段），`c.value` 算了没人接 | 用户以为在重写要点1，实际重写并复检的是开场钩子，原文被静默替换 |
| P2-8 | `desktop/renderer/js/settings.js:664-683` | `saveModelDialog` 先 `await api.saveModel()`（成功）**再** `await saveAdvancedConfig()`（越界值 throw），同一 catch 统一 toast「保存失败」 | 模型已在服务端保存，界面却宣布失败、列表不刷新、dirty 不清——显示与事实相反，用户重填重存 |
| P2-9 | `desktop/renderer/js/util.js:55` | `const ZWSP = "";` ——字节级核实（od）引号内零字节，不是 U+200B。`:69` 的「重复提示 + ZWSP 强制产生一次变化」两次产生**完全相同**的文本 | aria-live 重复播报机制整体静默死亡（连按 Esc 停止、连点复制等场景读屏不重读）；疑似某次保存把不可见字符剥掉，同类字符可能在别处也被剥 |
| P2-10 | `app/server.py:893-903` | `DELETE /api/packs/{name}` 路由层在 tests/ **零引用**（只有底层 `delete_pack` 三个函数级单测）；本轮 diff 还给路由新增了 `_safe_name` + `PackImportError→400` 映射 | 卸载是用户可直达的破坏性操作，其「非法名 400 / 路由接线」回归不会红（与上轮 P2-27 同族同修法） |
| P2-11 | `app/intel.py`（工作区新增 `HISTORY_KEEP = 30` 与 `_prune_history`） | 会**删用户历史快照**的新行为在 tests/ 零引用。实现本身逐行核过是对的（不足 30 份切片为空、只动 `*.json`、OSError 只 warning），但没有断言钉住「保留最近 N 份、删最旧」 | 将来改动无报警；删除类路径恰是本项目最要求可证伪的地方 |

---

## 5. P3 清单（~28 条，简表）

**后端**
- `app/pipeline.py:410-412`　`cancel()` 的终态检查与 `request_cancel()` 之间 TOCTOU，done 产物可在内存里被翻成 cancelled（磁盘不受影响，重启自愈）
- `app/server.py:995` + `pipeline.py:283-289`　job_dir 建不出的 failed 作业不在 store 索引 → `/api/history` 列出点开即 404 的「幽灵失败条目」，驻留到重启
- `app/intel.py:513/:542/:582`　`is_new`/`prev_keys` 仍是 `guid or title`（第三套键式），item_key 统一时没跟上；只有 url 的条目标题一变就误报「本周新出」
- `app/config.py:465-466`　「写了但非法」的数值项（如 `temperature: 快`）经 `_num` 回退默认后，因值非 None/"" 不进 `llm_defaulted` → `/api/config` 把内置默认当用户配置下发
- `app/server.py:1079-1091`　`/api/intel/today` 对不存在的包名返回 200 空结构（refresh 是 404），且 `p.name == name` 匹配不防大小写；docstring 只豁免了「包坏」没豁免「包不存在」
- `app/jobs.py:490-494`　`running_count` docstring 同一段连抄两遍（旧 P3 仍在）
- `app/intel.py:737-740` + `server.py:1107-1109` + `docs/需求方案:252`　「连续 3 天被忽略才沉底」**全仓库无实现**（日期无任何消费方），三处注释/文档声称的行为不存在
- `app/intel.py:870-874`　today() 对「JSON 合法但元素形状坏」的 latest.json 不设防（`items: [1,2]` → AttributeError → 500），违背 B3「绝不抛」
- `app/packimport.py:285-289`　zip 条目用 Windows 保留设备名（`con.md`、`aux/`）→ mkdir/open 抛 OSError → 500；白名单层应拒
- `app/intel.py:229/:251/:303`　适配器 seeds/keywords 写成字符串时 `list()` 拆成**单字**搜索词，静默产生无意义抓取
- `app/intel.py:705-757`（残留半条）　`load_ignored` 只读路径读坏仍静默退空 → 用户被忽略的条目无声回来、界面「已忽略 0」与实际不符且无提示
- `app/knowledge.py:788` vs `:617/:631/:856/:1089`　options 写成 dict 列表时半支持：去重读 `.name`，其余全按 `str(dict)` 得 `"{'name': 'X'}"`
- `app/knowledge.py:318-321`　`ai_tells:` 键写成非字符串 → `str(...)` 变不存在路径 → 人味检测静默关闭，无 notes 无 audit
- `app/knowledge.py:515-519`　`_section_has_body` 包着 `try/except OSError`，而 `read_text_cached` 永不抛 OSError——死守卫

**桌面 / 更新线**
- `desktop/scripts/publish-update.mjs:181-201`　`httpRequest` 无超时，只连不答的服务器让发布脚本无限挂起（旧 P3「步骤 1 只比 size 不重算 sha512」`:273-289` 仍在）
- `README.md:339` / `docs/自动更新方案.md` §9 vs `publish-update.mjs:311-336`　文档写「线上 version ≥ 本次就拒绝」，实现是 `>` 才拒、`==` 且文件缺失走「补传」放行（测试场景 D 钉的就是补传）——两份文档都比实现少一个分支
- `desktop/renderer/index.html:672`　`#upd-status` 默认文案「尚未检查更新」与七态口径两份，且 about.js:28 明说这句「不该存在」
- 旧 P3 原样在：`main.js:687-697` did-fail-load 未过滤 isMainFrame/ERR_ABORTED；`main.js:172-259` startEngine 不清 engineStderrBuf；`main.js:50-58` second-instance 静默无操作；`main.js:425-430` `result===null` 死分支 + 注释错误
- 已确认闭合：preload `restart`/`getStatus` 通道文档已补（方案 §4.3）；logger.debug 注释已与 electron-updater 6.8.9 源码对齐

**渲染层**
- `desktop/renderer/js/topics.js:298-306`　选题页「重抓」的 awaitJob 与生成流共用 `#rh-state`：并发时每 900ms 互相覆盖、重抓落地把「生成中」整行清空
- `desktop/renderer/js/jobs.js:371`　`onCancelled` 一律把头部重置「新对话」，`onFailed` 保留主题——同一场景两种收尾口径
- `desktop/renderer/js/settings.js:507-523`　`testModel()` 死函数（三列化后无调用点）；`:1342/:1359/:1373`　导入/卸载/转正的 catch 把「成功后的后续步骤失败」误报成「导入失败/卸载失败」（与先出的「已导入」toast 自相矛盾）
- 旧 P3 现状核实（10 项）：**9 项仍在**（`result.js:395/:279` 的 `s.type`/`x.type` 未 esc、`jobs.js:67-68` collectParams 静默默认、`topics.js:67-69` packName 兜底 "elevator"、`index.html:171` #btn-ignored 无 onclick、`api.js:79-83` 成功后 abort 定时器不清、`jobs.js:139` setHead 重复、`ui.js:730` vs `settings.js:831` 重复 onclick、`settings.js:1199` lastCreatedPack 游离、`ui.js:157` 死三元）；**1 项已闭合**（#topics-note 已接框架级错误）。panes/navs 刻度与当前 DOM 相符，属受维护不变量

**测试 / 文档**
- `_verify/mutate.py`　P2-20 修复（usage 累加）的变异检验**未登记**进 MUTATIONS（上轮临时跑过，无法重放）——按本项目验收口径属凭证缺失，因 `test_llm_retry.py:425` 有活单测兜底降为 P3
- `mutate.py:714-734/:824-845`　变异判据仍只判「有没有红」，不核对「是不是该条断言红」（旧 P3 仍在）；`_verify/` 一次性探针 14+ 个仍在；`.gitignore` 声明忽略的 12 个 png 仍被跟踪
- `tests/`　`favicon.ico`（固定 204）与 `GET /` 的「渲染层文件缺失 500」分支零引用
- `README.md:36-38`　「单次生成的模型调用：正常 3~5 次」没跟上 draft 合并路径——elevator 与 _template 都声明了 `draft` 段，`pipeline.py:468-476` 走「选题与撰写一次调用」，默认正常是 **2~4 次**（`:46` 最坏公式示例 5×6=30 同源漂移，按 4 次应为 24）
- `docs/UI视觉规范.md:128 vs :131`　同段先写「10 处 rgb 字面量」（对）又写「先把那 13 处判据换成…」（上轮漏改残留）
- `docs/自动更新方案.md:308`　`--h-btn` 写 30px（实际 28px，上轮修了 styles.css 侧这份没修）；§7 preload 行写「两个通道」与 §4.3 已声明的四个不一致（§7 停在落地前快照）；§10 引 `verify.js:2182/:3335/:2949` 三处裸行号已漂移（项目自定过「行号一律换符号锚点」）
- `docs/知识库内容规范.md:66-67`　ideas.md 新口径只说「选题阶段」，`elevator/skill.yaml:166` 的 **draft 阶段也在注**——阶段枚举不全
- `README.md:146`　「verify.js 第 1198 行」已漂移（现 1270），示例数字是 9-24 快照

---

## 6. 已复核为「站得住」的方向（下一轮别重复劳动）

- **上轮 50 条修复无产品级回归**：`_merge_usage`、item_key、transition_if_room 分族、health nonce、pack_info 补 skill.yaml、台账 `_ledger_broken`、param_audit 接线、sessions/settings/jobs 的吞错修复——逐条读毕，功能都在且有活测试。
- **安全面**：guard 全 /api/* 令牌+同源；zip 三重限制 + `_clean_rel` + resolve 复核 + `_is_symlink`（仅保留设备名缺口，见 P3）；`Pack` 路径不变式；SSRF 硬编码；密钥掩码；nonce 只回显不授权且窗口内拿不到；serve-update 绑 127.0.0.1 + 双防线路径穿越；publish 只上传 latest.yml 列的文件。
- **机制**：`_merge_usage` 递归累加正确；undraft 行级改写有写前 safe_load 复检；`store.write_result` 回滚/墓碑/锁序；额度族 `_spawn/_guarded/_fail` 三处兜底；elevator 包实测 `pack_error='' / param_audit={} / warnings=[]` 全绿；渲染层 styles.css 动态 class 逐个核对无缺失；`snapshot()` 终态总带 result（轻量轮询不丢产物）。
- **文档主体**：README 版本号 0.2.2、断言数 379、watchdog 注记、配置指南五段/字段表、制作指南 compliance/、场景契约分镜四字段——均与实现逐条对上。

## 7. 建议的处理顺序

1. **§2 两条红灯**（重写 usage 守卫 + 修 errorpath 夹具竞态）——绿重新可信是后续一切验收的前提。
2. **P1-1**（一行 `active and`）+ 补测试——唯一可花钱的洞。
3. **P1-3 / P1-4 / P1-5**（缓存钉死、情报键两本账、先删后拷）——三处真实数据面，修法都已给方向。
4. **P1-2 + P2-7 + P2-8**（渲染层三处「显示与事实相反」）。
5. **P2-10 / P2-11**（两条破坏性路径补测试）+ P2-4 登记变异。
6. 其余 P2/P3 按主题批量清（文档类可一次对齐；「连续 3 天沉底」要么实现要么删声称）。

> **验收口径不变**：每条修复都要过变异检验（改回去必须红）。
> ⚠ 本仓库长期有并发会话在提交，本报告行号是 2026-09-26 的快照，动手前先重读现场——上轮就发生过「报告说没修、现场已修好」的漂移。

---

*报告生成：2026-09-26 · 五路并发只读审查 + 主线独立复核 · 基线：verify.js 379/379 · pytest 1F（假红，诊断见 §2.1）· desktop 108/111（夹具竞态，诊断见 §2.2）*
