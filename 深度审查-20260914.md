# TalkScript 全方位深度审查 · 2026-09-14

> ## ⚠️ 勘误（重要）：本审查有 4 项错判
>
> 初版审查**没有结果页截图**作为依据，凭代码推断就下了「缺失」的结论。
> 实际动手时逐条核实，发现以下 4 项是**已经实现的**，此前判断错误：
>
> | 我的错判 | 实际情况（证据） |
> |---|---|
> | #2「再生成一遍」缺失 | ❌ 已有：`重跑` + `换一版` 两个按钮（`result.js:85-86`），语义还区分了「同参数重跑」与「同主题重掷」 |
> | #3「失败重试 + 错误详情」缺失 | ❌ 已有：失败卡含 `技术细节` 可展开 + `重试` + `检查模型设置` + `复制错误`（`result.js:306-320`）；停止卡也有「再来一次/换一版」 |
> | #7「多版本对比」缺失 | ❌ 已有：Claude 式 `1 / 3` 版本导航翻页（`thread.js:1-6`、`jobs.js:190-192`） |
> | #9「校对清单默认收起」 | ❌ 没有用 `<details>`，是 `<pre>` 直接展示（`index.html:221-223`），本来就展开 |
>
> **教训**：审查带界面的东西，缺了关键界面的截图就容易把「已有」判成「缺失」。
> 已补：把 `resultSrt` / `resultMarkdown` 挂到 `window.__ts` 并补**内容**断言，
> 同时在下面「八、截图覆盖补强」里记了要补的截图清单。
>
> 下文保留原分析（便于对照），但请以本勘误表为准。
>
> **后续（2026-09-15）：六、优先级表里的 #11「设置内搜索」已实现后删除 —— 视为已关闭，不要再实现。**
> 它曾以 `filterSettingsNav()` 落地（匹配范围 = 导航项文字 + 分区面板全文，所以「重试」「超时」
> 这类字段名也能命中），随后被移除。理由不是"用不上"，而是**它是唯一会隐藏导航项的入口**：
> 过滤词残留在框里时（比如上次输的「超时」），用户下次进设置会看到一份缺项却没有任何提示的
> 导航，读起来就是个 bug。设置一共 6 个分区、一屏看全，检索的收益抵不上它占的那一行。
> 删除时连带清掉了 `.stg-search` 的 HTML/CSS、`paneHaystack()`、以及只为它服务的
> `.stg-nav-item.hidden` / `.nav-sec.hidden` 两条规则；现在有断言守着
> 「设置页搜索框已从 DOM 移除（不是只藏起来）」。

对照 Codex / Claude Code / WorkBuddy / Cursor 这类成熟 agent，从**功能模块、操作流程、布局样式、层级逻辑**四维度全方位排查。优先级 ★ 越高越值得先动。

> 本审查基于以下材料：`desktop/renderer/` 全部 12 个模块、`app/` 全部 16 个模块、
> `_verify/` 6 张真实截图、`_verify/verify.js` 56 条用例覆盖到的路径。
> 未覆盖：`desktop/main.js`（Electron 沙箱起不来，已通读）、生成后结果页（无截图）。

---

## 一、信息架构（IA）——最大缺口

**现状**：左栏会话 + 右栏工作区 + 整窗设置覆盖。三块互不重叠、不能并存。

| | WorkBuddy / Cursor / Claude Code | TalkScript 现状 | 影响 |
|---|---|---|---|
| 多栏布局 | 左：会话/文件｜中：对话｜右：Inspector/Diff/Artifact | 左：会话｜中：对话/结果。**无右侧栏** | 失去「一边看结果一边看源」的可能性 |
| 设置 | 抽屉式（不遮全屏）/ 设置中心（独立页）/ 侧边浮窗 | 整窗覆盖，回到工作区必须先关 | 看设置时无法对比工作区内容 |
| 历史检索 | 顶栏搜索框，支持语义/全文 | 按时间倒序列表，**无搜索、无筛选、无排序** | 历史一多就找不到 |
| 多会话并行 | 多个标签页 / 快速切换 | 单窗口切换 | 多任务时频繁切换上下文 |

**★ 优先建议**：左栏会话上方加 `Ctrl+K` 触发搜索框（10 行 CSS + 8 行 JS 即可），按主题/状态筛选。

---

## 二、功能模块差距

| 能力 | 成熟 agent | TalkScript | 优先级 |
|---|---|---|---|
| 会话搜索 | ✅ | ❌ | ★★★ |
| 键盘快捷键面板（`?` 弹出） | ✅ | 部分（仅 ctrl+, / ctrl+enter / esc，**未集中展示**） | ★★ |
| 命令面板（Cmd+K） | ✅ | ❌ | ★★ |
| 重生成（同一参数再来一次） | ✅ | ❌（必须重输主题） | ★★★ |
| 在结果卡片上**就地编辑** | ✅（in-place edit） | ❌（只读 + 重写） | ★★ |
| 单段复制 / 单段重写 | 部分 | ✅ 已实现 rewrite_segment | — |
| 多模态输入（图片/文件拖拽） | ✅ | ❌ | ★ |
| 主题（深色模式） | ✅ | ❌（只有亮色） | ★★ |
| 模型/配额状态条 | ✅ | 隐藏在设置页 | ★★ |
| 断点续传（中断后接上） | ✅ | ❌（取消即放弃） | ★ |
| 多版本**对比 diff** | ✅ | ❌（列表展示，无差异高亮） | ★ |
| 多窗口/分屏 | ✅ | ❌ | — |
| Pin/Star 收藏对话 | ✅ | ❌ | ★ |
| 全局状态栏（空闲/生成中/出错） | ✅ | 仅右栏右上角 rh-state 一小块 | ★★ |

---

## 三、操作流程问题

### 3.1 首启（新用户）路径

**现状**：`app.js` 注释里能看到「未配置 Key 时自动打开设置并落在「模型接口」分区」—— verify.js 也断言了这点。好。

**问题**：
- 设置页打开后，**用户没有任何「下一步该做什么」的引导**——只看到一堆空输入框。
- 输入 Key、点保存，关掉设置，回到主界面：**完全不知道这里能干什么**。示例卡有 4 张，但只在「第一次进入」时显示，之后回到工作区还是空白。
- 没有「试一下」「点这条示例就会填充到输入框」的关联提示。

**建议**：
1. 设置页底部加「先用 mock 体验」按钮（已有 mock 模式，只是不显眼）
2. 主工作区在「没有任何对话记录」时持续显示示例卡，而不是仅 NOKEY 模式

### 3.2 单次生成

**路径**：主题 → 参数条 → Enter → 步骤时间线 → 结果卡 → 导出

**顺畅之处**：底部提示带快捷键说明（`Enter 发送 · Shift+Enter 换行 · Esc 停止生成`）。

**问题**：
- **生成中想改参数**？只能在停止后才能改。Codex 等支持「生成中追加消息」。
- **生成结果**只有「采纳 / 重写单段 / 重写整篇 / 导出」——但**没有「再生成一遍同样的」**。这是最常见的诉求（换个种子而已）。
- **重写单段**会原地替换，但**没办法并排对比新旧版本**。
- 失败时只在右上角 `rh-state` 显示，**没有错误详情可点开看**——更没有「重试 / 看后端日志 / 改个模型重试」的引导。

### 3.3 多版本管理

`job.revisions` 与 `body._versions` 都在存版本，**但前端没有任何 UI 让用户挑版本**——只能看最新。`jobs.js:snapshot` 把 `revisions` 发到了后端，但前端的 `result.js` 怎么处理它的我没在截图里确认（待截图）。

**建议**：结果卡上方加一个版本时间线（横向滚动），点哪个就展示哪个版本，左右箭头切，**右键某个版本能「回到这版」**。

### 3.4 取消 / 中断

**问题**：点「停止」→ 界面立即响应（按钮变发送样式），**但没有任何「你已取消、结果不会保存」的事后提示**。用户可能疑惑「我那 30 秒的进度去哪了」。

**建议**：停止后 1~2 秒在原位显示一个简短提示「已停止（进度未保存）· [撤销取消]」，给反悔机会。

---

## 四、布局与视觉问题（基于 6 张截图）

### 4.1 截图观察到的具体问题

| # | 截图 | 问题 |
|---|---|---|
| 1 | `main-sidebar.png` | 左栏底部只有一个「设置」按钮，**大量留白**。会话多了会拥挤，少了显空旷。 |
| 2 | `main-sidebar.png` | 输入框「输入主题」上方**没有占位示例链接**——用户离开后再回来不知道自己上次干了什么。 |
| 3 | `main-sidebar.png` | 四个示例卡字号偏大，**视觉权重和标题「想聊点什么？」相当**，显得堆砌。 |
| 4 | `settings-gen.png` | 「补充资料」textarea 占了一整行，**但「行业包 / 结尾引导 / 进阶 / 输出内容 / 文案入味」用 dropdown**——五种控件混排，没有说明「哪些常用 / 哪些高级」。 |
| 5 | `settings-llm.png` | 高级项「重试次数 / 超时 / 输出预算」**视觉上没有任何「这是高级」的提示**，用户可能误以为必填。 |
| 6 | `settings-packinfo.png` | 「校对清单」用了 `<details>`，但**默认收起还是展开？未确认**——校对清单恰恰是最该默认展开的部分。 |
| 7 | 所有设置面板 | **没有任何「搜索设置」入口**。6 个分区已经不少，未来会更多。 |
| 8 | 所有设置面板 | 底部**没有任何「已保存」反馈区域**——只显示当前状态行，不显示「上次修改未保存」标记。 |

### 4.2 系统级问题

- **没有深色模式**——长时间编辑的人会累；同类应用都有。
- **没有 favicon/tab title**——`rh-title` 是「新对话」，但窗口标题栏也用这个，未与「当前会话标题」联动。
- **无障碍**：截图中没看到 `aria-label` 的「回到最新」「关闭」等按钮（HTML 里其实有，但视觉对比度不强）。
- **响应式**：固定宽度 1320×860，**拖小窗口会溢出**——verify.js 断言「无横向溢出」但只是断言测试时的分辨率。

---

## 五、层级与逻辑问题

### 5.1 「设置」作为整窗覆盖的语义负担

设置现在**独占屏幕**。这意味着：

- 看一个参数 → 关闭设置 → 切回工作区 → 忘记自己看到哪
- 设置 → 调参 → 保存 → 关闭 → 重新生成一遍才能验证

**建议**：把高频配置（生成偏好）拆成**侧滑抽屉**（400px 宽，仍能看到右栏内容）；低频（行业包导出、知识库）保留整窗覆盖。

### 5.2 「参数条」与「设置」的边界

参数条（行业包 / 受众 / 时长 / 平台 / 风格）显示在主工作区底部，但「结尾引导 / 输出内容 / 文案入味 / 补充资料」藏在设置里——**两边同一类配置被拆散**。用户在主界面调了行业包，**也得专门跑去设置看「文案入味」有没有跟着对**。

**建议**：要么把全部生成参数搬到工作区参数条（动态按包配置渲染），要么参数条只剩「最常用的 3 个」，并在 tooltip 提示「更多在设置 → 生成偏好」。

### 5.3 「历史会话」的两种状态没有区分

左栏现在按「今天 / 昨天 / 更早」分组，但每条只显示「行业包 + 主题 + 时长 + 草稿状态」——**没有区分「生成中失败」vs「已完成」vs「已采纳」vs「已删除」**。

**建议**：每条加状态点：
- 🟢 已完成 / 已采纳
- 🟡 已生成未导出
- ⚪ 中断（未完成）
- 🔴 失败（可重试）

并支持一键重试失败的记录。

### 5.4 「Pack 知识库」与「Pack 技能」两个面板的语义重叠

`packinfo` 已经有「文件清单 + 角色说明」表；`kb` 面板也有文件列表。**两者 80% 重叠**，区别只是 kb 支持点开看全文。

**建议**：
- packinfo 保留为「包概览」（徽标、简介、文件清单、导出按钮）
- kb 改为「按文件分组浏览」+ 全文查看
- 技能面板既然和 kb 几乎一样，**合并成一个面板**更清晰

### 5.5 「状态信息」分散

「当前模型 / 重试 / 超时 / 输出预算」只在设置页底部一行（`settings-llm.png` 右下「已配置 Key · 模型 glm-4.7 · 重试 2 次 / 超时 180s」）——**主工作区一个相关标识都没有**。用户在主界面连「现在在用哪个模型」都不知道。

**建议**：主工作区头部 `rh-state` 改为可点击的状态条，点开下拉显示当前模型、最近生成耗时、token 估算等。

---

## 六、优先级建议（按 ROI 排序）

| # | 改动 | 工作量 | 收益 | 优先级 |
|---|---|---|---|---|
| 1 | 左栏会话搜索（Ctrl+K） | 1h | 历史一多就找不到 → 找到 | ★★★ |
| 2 | 「再生成一遍」按钮 | 0.5h | 最常见诉求 | ★★★ |
| 3 | 失败记录一键重试 + 错误详情 | 1h | 失败体验 | ★★★ |
| 4 | 主工作区头部状态条（模型/耗时） | 1h | 可见性 | ★★ |
| 5 | 设置 → 抽屉化（高频配置） | 4h | 不再遮屏 | ★★ |
| 6 | 深色模式 | 6h | 长时间使用 | ★★ |
| 7 | 多版本对比/挑选 UI | 4h | 已埋数据但未用 | ★★ |
| 8 | 参数条合并到工作区/或仅留 3 个 | 2h | 减少认知负担 | ★★ |
| 9 | 校对清单默认展开 | 0.1h | 一点改动 | ★★ |
| 10 | kb + 技能合并为一个面板 | 0.5h | 减少冗余 | ★ |
| 11 | 设置内搜索 | 1h | 分区多了必备 | ★ |
| 12 | 主题切换 / 多模态输入 | 4h+ | 范围更大 | ★ |

---

## 七、不建议动的东西

- **「代码守下限」的核心（checker / security / jobs）**——已通过三轮审查 + 补测，重写风险大于收益。
- **三栏对话-主界面布局**——WorkBuddy / Cursor 是 3 栏，但 TalkScript 目前是 2 栏 + 设置覆盖。**强行变 3 栏反而会让本就紧凑的工作区更挤**。建议先做完上面的 1~5 再评估。
- **renderer 模块拆分**——刚做完重构、11 模块清晰稳定；没必要再动。

---

## 八、截图覆盖补强（本次审查发现的测试盲区）

`verify.js` 只在测试流程路过时拍 4 张面板，**结果页、知识库面板没有截图**——
于是：
- 我看不到生成后的界面 → 把已实现的重跑/换一版、失败重试、版本导航误判为缺失；
- `settings-kb.png` 停留在 9-13 04:15，我实现了文件查看器后**截图没更新**，
  视觉上完全看不出功能已落地。

**建议补的截图**（在 `verify.js` 加 `shot()` 调用即可）：

| 截图 | 场景 |
|---|---|
| `result-done.png` | 生成完成的结果页（含重跑/换一版/导出按钮） |
| `result-failed.png` | 失败卡（技术细节展开态） |
| `result-versions.png` | 版本导航 2/3 |
| `settings-kb.png` | 知识库（文件查看器，需重新生成） |
| `main-nohistory.png` | 无历史时的空态 |

判定原则：**新增/改动了界面，就要有对应截图**——否则「功能做了」这件事
在视觉层面不可验证。

## 九、推荐的演进路径

**第一波（修复可见性 + 找回成本）**：1、2、3、4
**第二波（优化信息架构）**：5、8、9、10、11
**第三波（体验升级）**：6、7、12

每波做完都重跑三套验证 + 生成新截图对比；优先挑能跑自动化用例的项做。